<view class="reading-progress">
  <view class="progress-bar">
    <view class="progress-fill" style="width: {{percent}}%; background: {{color}};"></view>
  </view>
  <text wx:if="{{showLabel}}" class="progress-label">{{percent}}%</text>
</view>
