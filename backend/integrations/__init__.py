# backend/integrations/
"""
外部集成层 — 第三方服务的封装

架构意图：
  将所有外部 API 调用集中在此层，与业务逻辑解耦。
  Service 层只调用集成层的接口，不直接发送 HTTP 请求。

  好处：
  1. 切换供应商只改集成层，不改 Service
  2. 可以在测试时 mock 集成层
  3. 集成层的错误处理、重试、降级策略集中管理
"""
