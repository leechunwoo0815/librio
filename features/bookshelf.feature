# language: zh-CN
功能: 书架（想读清单）
  作为一名学生
  我希望把感兴趣的书加入书架
  以便后续阅读或听读

  背景:
    假如 用户已登录小程序
    并且 用户的孩子是正式会员

  # ==================== 加入书架 ====================
  场景: 加入书架
    当 用户将图书"Charlotte's Web"加入书架
    那么 图书出现在书架列表中
    并且 书架数量加1

  场景: 加入书架不需要押金
    假如 用户未缴纳押金
    当 用户将图书"Charlotte's Web"加入书架
    那么 图书成功加入书架
    并且 不提示需要缴纳押金

  场景: 书架不限数量
    假如 书架已有50本书
    当 用户尝试将第51本书加入书架
    那么 图书成功加入书架
    并且 不显示任何数量限制提示

  场景: 重复加入同一本书
    假如 书架已有"Charlotte's Web"
    当 用户再次将"Charlotte's Web"加入书架
    那么 显示提示"该书已在书架中"

  # ==================== 移出书架 ====================
  场景: 从书架移除
    假如 书架已有"Charlotte's Web"
    当 用户将"Charlotte's Web"从书架移除
    那么 图书从书架列表消失
    并且 书架数量减1

  # ==================== 书架浏览 ====================
  场景: 查看书架列表
    假如 书架已有3本书
    当 用户打开书架页面
    那么 显示3本图书的封面和标题
    并且 每本书显示阅读进度（如有）

  # ==================== 预约借书（从书架发起） ====================
  场景: 从书架预约借阅实体书
    假如 书架已有"Charlotte's Web"
    并且 用户已缴纳押金
    并且 该书库存大于0
    当 用户点击"预约借书"
    那么 创建预约记录
    并且 库存锁定
    并且 显示"预约成功，请72小时内到店取书"
