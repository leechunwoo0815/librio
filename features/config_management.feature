# language: zh-CN
功能: 配置三级管控与运营报表（E3/E4/E6/E2）
  作为运营负责人
  我希望关键配置不被误改、活动有轻量模式、报表支撑日常决策
  以便门店安全高效运营

  背景:
    假如 用户已登录小程序

  # ==================== E3 配置三级管控 ====================
  场景: 非超管修改锁定级配置被拒绝
    假如 存在非超管管理员（config.edit权限）
    当 该管理员修改配置"price_observation"为"999"
    那么 系统返回403禁止

  场景: 警告级配置需二次确认
    假如 存在非超管管理员（config.edit权限）
    当 该管理员不带确认修改配置"borrow_limit"为"8"
    那么 系统返回409冲突
    当 该管理员带确认修改配置"borrow_limit"为"8"
    那么 配置更新成功

  场景: 自由级配置直接可改
    假如 存在非超管管理员（config.edit权限）
    当 该管理员修改配置"venue_name"为"测试馆"
    那么 配置更新成功

  场景: 配置列表带管控级别
    假如 存在非超管管理员（config.edit权限）
    当 该管理员查看配置列表
    那么 配置"price_observation"级别为"locked"
    并且 配置"borrow_limit"级别为"warning"
    并且 配置"venue_name"级别为"free"

  # ==================== E4 活动轻量模式 ====================
  场景: 轻量活动报名自动通过
    假如 管理员创建轻量活动"周末故事会"
    并且 用户的孩子是正式会员
    当 用户报名该活动
    那么 报名状态直接为"已通过"

  # ==================== E6 运营报表 ====================
  场景: 运营报表端点返回核心指标
    假如 存在非超管管理员（dashboard.view权限）
    当 该管理员查看运营报表
    那么 返回今日借还量
    并且 返回押金池总额
    并且 返回转化漏斗

  # ==================== E2 审核 SLA 巡检 ====================
  场景: 超24小时未审的退款触发超管提醒
    假如 存在一笔25小时前提交的待审核退款
    当 系统执行审核SLA巡检
    那么 生成"审核超时提醒（SLA 24h）"管理端告警
