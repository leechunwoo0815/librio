# 决策请求书 D1-D11

> **发出日期**：2026-07-20
> **发出人**：开发团队
> **接收人**：产品负责人
> **规则**：每条决策 48h 内无答复则按默认方案执行，执行后标注"待业务确认"
> **答复日期**：2026-07-20
> **答复内容**："按专家建议执行"
> **状态**：✅ 全部按专家建议执行

---

### D1：iOS 虚拟支付合规

- **背景**：500 元观察期 / 5400 元年费 / 1350 元季度 / 2700 元半年均属虚拟服务，iOS 端严禁调用 `wx.requestPayment`。当前 iOS 用户可以看到价格和支付按钮，存在被苹果审核拒风险。
- **选项**：
  - A. 商品结构改造为主：iOS 端隐藏虚拟商品价格和支付按钮，显示"因苹果规则限制，请前往线下门店或使用安卓设备办理"。后端不做改动。
  - B. 线下引导开关备用（默认关）：增加 `enable_ios_payment_guide` 配置项，控制 iOS 端是否展示引导文案，紧急时可远程关闭。
  - C. 先 A 后 B：先做 A 改造上线，后续再补 B 开关。
- **专家建议**：A 为主、B 兜底（C 方案）
- **默认方案（48h 无答复）**：C — 先完成 A 改造，同时预留 B 配置项接口但不实现 UI
- **请回复**：A / B / C 或其他指示

---

### D2："已关停"状态删除

- **背景**：代码中 `已关停` 状态零存在（grep 全库无匹配），仅全流程文档 §1.1 有"缓冲期→已关停"流转描述，属文档残留 V2.0 概念。"缓冲期超 15 天"的实际行为是 EXPIRED 状态 + 会员权益锁死（音频停、不可新借），不存在单独的"关停"状态码。
- **选项**：
  - A. 删除"已关停"概念：文档改"缓冲期超 15 天 → EXPIRED（可还书/不可新借/音频到期锁/续费解锁不重算借期）"
  - B. 保留（需新增状态码和迁移脚本）
- **专家建议**：A 删除
- **默认方案（48h 无答复）**：A — 删除概念，EXPIRED 规则补全
- **请回复**：A / B 或其他指示

---

### D3：订单状态机终态

- **背景**：当前订单终态有 PAID / REFUNDED / CLOSED，另有一"已完成"概念（无代码对应）。退款失败时订单保持 PAY_REFUNDING，用户无法继续操作。
- **选项**：
  - A. 删"已完成"终态；退款失败回退 PAY_PAID + 人工重试入口
  - B. 保持现状
- **专家建议**：A — 采纳补丁方案
- **默认方案（48h 无答复）**：A
- **请回复**：A / B 或其他指示

---

### D4：预约"已备"状态删除

- **背景**：代码枚举 `ReservationStatus` 为 PENDING(0)/FULFILLED(1)/EXPIRED(2)/CANCELLED(3)，无"已备/已预留"值。表结构文档误写为"1=已备(预留)"，实际代码用 PENDING=0 表示"待取"。代码和存量数据中均不存在"已备"值。
- **选项**：
  - A. 删除"已备"状态：表结构文档对齐代码枚举
  - B. 新增"已备=1"状态，迁移存量数据
- **专家建议**：A 删除
- **默认方案（48h 无答复）**：A
- **请回复**：A / B 或其他指示

---

### D5：favorites 表废弃

- **背景**：favorites（收藏夹）与 bookshelf（想读清单）功能高度重合。代码中 favorites 是独立表（物理删除），bookshelf 是主表。favorites 的前端入口不确定是否仍在使用。
- **选项**：
  - A. 废弃只读：先查存量数据（`SELECT COUNT(*) FROM favorites`），如已被 bookshelf 全覆盖则标记废弃，V3.9 删表
  - B. 保留双表并行
  - C. 立即合并
- **专家建议**：A 废弃
- **默认方案（48h 无答复）**：A — 先查存量再定迁移（如无独有数据，转只读+标注废弃）
- **请回复**：A / B / C 或其他指示

---

### D6：多孩押金

- **背景**：多孩家庭押金政策未在 PRD 明确定义。当前代码每个孩子独立 `deposit_record` 记录。一个家庭（3 孩）需预付押金 1200×3=3600 元，叠加会员费后一次性支出可能超 13000 元。
- **选项**：
  - A. 按孩子独立 1200 元/人
  - B. 家庭共享押金 1200 元/户
- **专家建议**：A 独立缴纳 + 附商业风险提示文案
- **默认方案（48h 无答复）**：A — PRD 明文"按孩子独立缴纳押金"
- **请回复**：A / B 或其他指示

---

### D7：多孩欠费连坐

- **背景**：一个孩子欠费（押金不足/丢书未赔）是否影响其他孩子的借阅权限？
- **选项**：
  - A. 孩子各自独立，互不影响
  - B. 家庭连坐——任一孩子欠费，全家停借
- **专家建议**：A 各自独立
- **默认方案（48h 无答复）**：A — PRD 明文"孩子独立，欠费不连坐"
- **请回复**：A / B 或其他指示

---

### D8：逾期还书

- **背景**：逾期还书（超过 21 天借期）时，系统应如何处理？阻断还书还是允许还书但罚款？
- **选项**：
  - A. 先还书，罚款挂账，不阻断还书
  - B. 必须缴清罚款才能还书
- **专家建议**：A — 先还书，罚款挂账
- **默认方案（48h 无答复）**：A
- **请回复**：A / B 或其他指示

---

### D9：亲子课人数

- **背景**：亲子课每场容量在 PRD 和代码注释中不一致。配置默认值 10 人，PRD 写 30 人。
- **选项**：
  - A. 30 人（PRD 为准），表结构注释修正 + 配置化 `activity_capacity_default`
  - B. 20 人
- **专家建议**：A — 30 人
- **默认方案（48h 无答复）**：A — 30 人，注释修正，新增配置项
- **请回复**：A / B 或其他指示

---

### D10：重考冷却

- **背景**：当前代码 60 分钟冷却从测验创建时刻起算（`timedelta(hours=1)`，硬编码）。全流程文档写 20 分钟，代码是 60 分钟。PRD 未定义冷却策略。
- **选项**：
  - A. 60 分钟可配置：代码已 60 分钟，新增 `quiz_cooldown_minutes` 配置项（默认 60），文档对齐 60 分钟
  - B. 改为 20 分钟（对齐现有文档）
  - C. 取中间值 30 分钟
- **专家建议**：A — 60 分钟可配置
- **默认方案（48h 无答复）**：A — 60 分钟，配置化，文档对齐
- **请回复**：A / B / C 或其他指示

---

### D11：退款计算规则

- **背景**：退款金额依赖 `used_days` 口径和取整方式。当前代码使用自然日差（calendar days）和 ROUND_HALF_EVEN（银行家舍入），下限无约束。
- **选项**：
  - A. 开通 24h 内退款 used_days=0（全额退）；超 24h 自然日口径、下限 1 天；同 user 同商品 30 天限 1 次全额退防刷；取整 ROUND_HALF_UP
  - B. 仅 A 中的部分规则（请说明）
  - C. 保持现状（自然日 / ROUND_HALF_EVEN / 无下限 / 无限次）
- **专家建议**：A
- **默认方案（48h 无答复）**：A — 代码 1 行改 ROUND_HALF_UP，新增下限 1 天和 30 天防刷逻辑
- **请回复**：A / B / C 或其他指示

---

*请逐条回复编号和选项（如 D1=A, D2=A, ...），或统一回复"按专家建议执行"*
