# 终极全量审查 · 第三批（D6 成长体系 / D7 to-C 体验 / D8 to-B 运营）

> 审查人：开发模型自兼专家（独立复验）｜ 日期：2026-08-05
> 范围：D6 测评/晋级/打卡/证书、D7 小程序家长端体验走查、D8 定时任务/RBAC/操作日志审计，
> 外加第一批遗留 F33/F34 闭环。

## 总判定

**D6 发现 1 个 P2（打卡 streak 自然日口径错误，已修复 + 5 测试）+ 2 个 P3（其一已修复）；D7/D8 未发现新 P0/P1；F33/F34 闭环。**

---

## D6 成长体系（测评/晋级/打卡/证书）

### 通过项（逐项亲验）

| 检查点 | 结论 | 证据 |
|---|---|---|
| 测评通过线 | ✅ | `submit_answers` 按 `quiz_pass_rate`（默认 80%）判定；C2 低龄（sort_order≤quiz_low_level_max_sort）按"3 题对 2"（quiz_low_level_questions/pass_count 配置） |
| 10 分钟重考锁 | ✅ | `start_quiz` 查近 `quiz_cooldown_minutes`（默认 10）内 COMPLETED 测验，时区应用侧 datetime.now 与 ORM 一致 |
| 同一本书重复测评不重复计分 | ✅ | `submit_answers` 通过时查"其他已通过 Quiz"（score≥本次 pass_threshold 折算口径）→ `effective_word_count=0`，word_count 只在事件源去重后累计 |
| required_books 晋级并发安全 | ✅ | `check_and_advance`/`increment_quizzes_passed`/`increment_books_read` 均 `with_for_update`；C6 低龄 required_books 收敛 |
| 证书生成幂等 | ✅ | `generate_certificate` 先查 child×level 已有，重复调用返回 already_granted |
| 排行榜去重 | ✅ | leaderboard 按 (child, book) `func.max(word_count)` 去重 + APPROVED 过滤；total 读 child 累计字段 |
| 读完提交自动 APPROVED（D4） | ✅ | `handle_quiz_passed_for_submission`：PENDING 提交 + 阅读时长≥submission_min_minutes（默认 10）→ APPROVED + ReadingBookFinishedEvent；时长不足留人工队列 |
| words 累计幂等 | ✅ | `update_reading_stats` 只收事件（QuizPassedEvent 一次 / ReadingSessionCompletedEvent 每会话一次），无重复累计路径 |

### 新发现（P2，已修复）

**P2 — 连续打卡天数（streak）自然日口径错误（`backend/events/misc_handlers.py`）**：
`handle_checkin_for_child_streak` 每次 CheckInEvent 一律 `current_streak_days + 1`，而同日 4 类型打卡
（阅读/读完/朗读/生词）各自发布事件——实测同日 4 次打卡 streak 变 4（应为 1）；且断签不重置
（周一周三打卡 streak=2，应为 1）。PRD §10.1 明示"当前连续打卡天数"，家长端 get_streak 与
观察期报告（`report/service.py:275` 读 `child.current_streak_days`）全天读到错误中间值，
对账任务（凌晨 3:45）才修正。

**修复**：handler 每次事件从今天向前重算连续段（查该孩子去重打卡日期，while 连续回溯），
结果幂等（同日多事件不叠加）；断签 current 重置为 1；longest 只升不降（与对账口径一致）。
TDD 5 用例：同日 4 类型=1 / 昨天+今天=2 / 断签重置=1 / 连续增长=4 / 断签后 longest 保留。
reading_stats.feature 10 场景复跑全绿。

### P3（其一已修复）

- **P3-a ✅ 已修复 — 进行中测验残留**：`start_quiz` 不提交的 IN_PROGRESS 测验无过期机制，
  管理端"进行中"列表累积僵尸记录。修复：再次 start 时先置旧 IN_PROGRESS→EXPIRED
  （不影响冷却判定——冷却只查 COMPLETED）。+1 测试。
- **P3-b 记录 — words_read 客户端上报**：阅读时长服务端计算（end−start），但词数/页数由
  客户端上报，无服务端独立校验——防刷有限（可谎报词数骗过 checkin_min_vocab）。
  属 To-C 信任模型，暂不改；如后续上排行榜防刷可加服务端词数统计。

---

## D7 to-C 体验走查（小程序前端，代码层面）

| 检查点 | 结论 | 证据 |
|---|---|---|
| API 层错误处理 | ✅ | `request.js`：网络状态检测（无网 toast）、401 清状态重登、403 权限提示（error_code 交调用方）、统一 detail 消息 |
| 会话契约一致性 | ✅ | `endSession(pages, words, minutes)` ↔ EndSessionRequest 字段逐字匹配；reading_minutes 冗余但 schema 显式接受 |
| 退款状态反馈 | ✅ | `refund-apply.js`：退款草稿检测恢复、金额后端计算、提交成功提示 |
| 押金状态映射 | ✅ | `deposit.js`：0-5 全状态文案（未缴/已缴/已退款/已扣除/退款中/支付中）+ iOS 实物担保豁免 |
| 多孩切换 | ✅ | child-selector 组件 + 各页面 child_id 显式传参 |
| 无新增 P0/P1/P2 | — | 前端体验无阻断项；"手感"类（动画/交互细节）不在代码走查能力范围 |

---

## D8 to-B 运营 / RBAC / 定时任务

| 检查点 | 结论 | 证据 |
|---|---|---|
| 定时任务编排 | ✅ | 凌晨 0:00-5:00 错峰 15-30 分钟无重叠（damage 确认→提醒→逾期→双对账→毕业→purge→F7）；白天 */1、*/5、*/30 高频任务职责独立；9:00 三任务（member expiry/obs reminders/weekly）均为轻量查询+消息，可接受 |
| RBAC 权限矩阵 | ✅ | 实测种子：PERMISSIONS=136、STAFF_PERMS=108、TEACHER_PERMS=29，staff/teacher 均为全量子集（无越权码）；super_admin 从 PERMISSIONS 全量派生 |
| 操作日志覆盖 | ✅ | admin_borrow_router 15+ 处 write_operation_log（借/还/押金/罚款/预约）；F11 线下建单、F13 状态变更审计均已闭环 |
| wiring 完整性 | ✅ | verify_action_wiring --strict 通过（Gate 10） |
| F34 部署依赖 | ✅ | seed_admin 创建初始超管时直接关联 super_admin 角色（不再依赖手动 migrate_admin_roles 回填）；DEPLOY_CHECKLIST 补"seed_rbac 幂等重跑"双保险 |

### P3 观察项（不阻塞）

- 9:00 三定时任务扎堆（建议错峰 9:00/9:05/9:10，低优先级）。
- 管理端预约/退款审核队列无"批量操作"（逐条处理，符合双人复核精神，暂不改）。

---

## F33/F34 闭环（第一批遗留）

- **F33 ✅**：`update_status` 审计日志 from→to 改状态名（"TRIAL → OBSERVATION"而非"0 → 1"），
  与 revive 日志口径一致；测试断言同步改状态名。9 用例全过。
- **F34 ✅**：`seed_admin.py` 创建初始超管时查 `Role(code="super_admin")` 并写入
  `admin_role_id`（此前依赖 seed_rbac.migrate_admin_roles 手动回填，漏跑则超管被锁在
  状态变更/复活功能外）；DEPLOY_CHECKLIST 补 seed_rbac 幂等重跑步骤（双保险）。

---

## 门禁（本人实测，开发机）

pytest **708 passed / 0 failed**（+6：streak×5 + IN_PROGRESS×1）· behave 211 场景 / 1369 步
（reading_stats 复跑全绿）· ruff check×2 + format --check . 全绿 · contract / 56 表 /
config 66 / wiring / integration 56/56 / alembic check 无漂移（head=d8e9f0a1b2c3）。

## 下一步

1. 甲方阻塞项维持：F41/F57/F63/F71-①/F73/F6/F9/F10/F3 + D 档五项 + C 档上线动作。
2. P3 待排期维持（F34 已闭环移除、streak 已闭环移除）：F15-F20/F23/F24/F27/F29/F30、
   user 列表 child_status 筛选摆设、存量 RETURNED 罚款回补（运营决策）、孤儿借阅人工盘点、
   words_read 防刷（观察）、9:00 任务错峰（观察）。
3. 全部八个维度（D1-D8）审查完毕，除外部阻塞项外代码侧已达"拎包入住"。
