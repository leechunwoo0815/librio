# librio 项目终态交接（防 compact 遗忘）· 2026-08-07

> 本文件是**当前唯一权威恢复卡**，取代《K3-执行中任务交接-20260726.md》（该文件头部已加
> 指针）。恢复口令："读 专家意见/项目终态交接-20260807.md，继续。"
> 写入人：接手开发模型（自兼专家）｜ 覆盖全部审查/修复/文档轮次至 2026-08-07。

---

## 🔑 Compact 恢复卡（最先读）

- **HEAD**：以 `git log --oneline -1` 为准（main，与 origin/main 同步，工作区干净）
- **一句话现状**：**代码侧与文档侧全部达到"拎包入住"**——D1-D8 八维度审查完毕、
  P0/P1 全修（F1-F14 + F35-F80 + D5 专项 600 回调 + F62 补刀）、T1 四域 steps 全真实化、
  订阅消息触达管线已实现（待模板 ID 激活）、MySQL 并发实证、全项目错误记忆库固化。
  剩余全部为**外部阻塞**（甲方口径 / C 档上线动作 / D 档输入 / 少量 P3 排期）。
- **外部专家终审（20260807）**：认可"拎包入住"，代码侧放行；保留意见透明记录
  （二/三批修复系开发自审，专家做高风险抽样复核，结果一致）。
- **立即下一步**：等甲方输入（见 §二）；若有新开发任务，开工前读 `error_list/README.md`。
- **纪律备忘**：新增迁移交付前必须开发机跑 `alembic upgrade head + check`；门禁复测禁止
  缩窄目录范围（ruff format 全仓 `.`）；十一关 + 第十二关（真实网关契约测试）；报告必须
  贴五项原始输出 + 自检闭环验证表。

---

## 〇、项目一句话

DmkWords 儿童英文阅读 OMO：线下实体书借阅 + 线上音频伴读 + 查词 + 测评晋级。
To-C 小程序 34 页 + 管理后台 39 模板（35 业务页面）+ FastAPI 28 域 56 表 24 定时任务
332 API。52 题甲方决策全落地（A1 双轨制/A2 押金 1200 退 600/A3 观察期 45 天/B7-B12
逾期损坏/E1/E2/E7 退款/F2/F4/F5 校友候补多孩快照）。

## 一、基线（本机实测，注意环境限定）

- pytest **789 collected**（开发机 789 passed / CI sqlite 708+15 skip / 无 MySQL 沙箱
  714+9 err，环境差异非缺陷）
- behave **211 scenarios / 1369 steps**（无 MySQL 沙箱 171+40 error）
- ruff check×2 + format --check . **463 files** 全绿
- API 合约 OK / 模型一致 **56 表** / wiring --strict / gen_config_doc --check **66 项** /
  integration **56/56** / alembic check 无漂移（head=`a5b6c7d8e9f0`，迁移 043-050 全落开发库）
- 版本口径：PRD **V3.23**、RBAC **136/108/29**、任务 **24**、管理端 **39 模板/35 CSS/36 JS**、
  API **332**（含 37 页面路由）、behave features **20**
- 错误记忆库：`error_list/`（README 机制 + 主记忆库 406 行，F1-F80 全覆盖）

## 二、待办清单（全部为外部阻塞，非代码缺陷）

### A · 需甲方/产品拍板（勿自行推进）

1. **F41** 库存对账口径（专家建议 available=在馆−PENDING 预约）
2. **F57** 退款中借书白名单 / **F63** 非会员预约门槛 / **F71-①** 预约是否校验押金
   （PRD 自相矛盾）/ **F73** 押金余额展示口径
3. **F6** 管理端标已付不激活会员书面确认（P3-④ 裁定前提，F4/F13 已修后成立）
4. **F9** ALUMNI 出入边（与 F3 出生日期方案联动）
5. **F10** EXPIRED 音频锁范围（硬锁全部/已借实体书放行/缓冲期是否锁）
6. **F3** birth_date 方案（送审文档已备：`专家意见/F3出生日期方案-送审-20260804.md`；
   注意迁移号以实际为准——已到 049，方案文档中旧编号需顺延）
7. **D 档五项**：亲子课前端购买页决策（补报名页 or 改 FAB 文案）、G2 门店电话/客服微信、
   C2 低龄题库配图、扫码枪采购联调、52 题最终确认
8. **新发现口径项**：DEDUCTED 押金部分扣除后剩余押金无退款通道（可退/结转/线下作废）
9. **订阅消息激活**：微信后台申请模板 ID 填入 `backend/integrations/wechat/config.py`
   SubscribeTemplate + `frontend/utils/subscribe.js` TEMPLATE_IDS，生产
   `WECHAT_SUBSCRIBE_ENABLED=true`（代码管线已实现，只差模板 ID）

### B · C 档上线动作（人工/生产环境）

1. iOS 虚拟服务页提交微信审核人工确认一次（虚拟商品口径严于 Apple）
2. 迁移 043-050 低峰执行（DEPLOY_CHECKLIST 已逐条标注回填/修复 UPDATE；050 为 child 加列无回填）
3. 生产 `REDIS_LOCK_FAIL_OPEN=false`（多实例必须）
4. 商户后台核对退款通知地址 = `WECHAT_REFUND_NOTIFY_URL`
5. 生产前跑 `backend.seeds.seed_rbac`（含 migrate_admin_roles，F34 双保险）
6. 开发机装 pango/cairo 后跑 789 单环境全绿存档

### C · P3 待排期（多数需前端/内容/运营）

F15 tiers 文案 30 天 / F16 升级时长双算 / F17 前端展示价脱节 / F18 iOS 价格残留 /
F19 亲子课开课后报名 / F20 独孩兜底 / F23 毕业提醒防重依赖 365 天消息 / F24 缓冲期口径 /
F27 电子凭证 / F29 EXITED 不清 member 时间 / F30 订阅消息渠道（已部分闭环）/ user 列表
child_status 筛选（**已修复**）/ 存量 RETURNED 罚款回补（运营决策）/ 孤儿借阅人工盘点 /
words_read 防刷（观察）/ 9:00 任务错峰（观察）/ PENDING 缴款单残留（**已修复**）

## 三、审查结论史（完整链）

| 轮次 | 结果 | 报告 |
|---|---|---|
| 早期审计（7 月，Step/安全/合规） | 数十项 P0/P1（越权/锁/事务/XSS/隐私） | Step1-4、token、transaction、XSS、privacy 等 |
| 20 关终审 | 3 P0 + 2 P1 + 10 P2 | 终审意见-K3执行单 |
| 超级大审查 | 9 修 + 1 可接受 + 3 不成立 | 超级大审查 |
| FINAL3.0 深度 | 4 P1 + 8 P2 + 6 勘误 → 清零 | 专家审查意见-FINAL3.0 |
| 终极全量审查第一批 D1/D2 | 5 P0 + 9 P1 + 10 P2 + 6 P3 → 全修 | 终极全量审查-第一批 |
| 终极全量审查第二批 D3/D4/D5 | 5 P0 + 17 P1 + 12 P2 + 7 P3 → P0/P1 全修 | 终极全量审查-第二批 |
| 第三批 D6/D7/D8 + 专项 | streak P2 + D5 600 回调 P1 + 订阅消息 P1 → 全修 | 终极全量审查-第三批 |
| D5 专项复审 | 600 奖励回调 409 P1（PaymentCallbackData 缺 out_refund_no）→ 已修 | 第三批意见书附 |
| 外部专家终审（20260807） | **认可放行** + 保留意见（二/三批系自审） | 第三批意见书附 |

## 四、关键机制定案（勿重开，除非有实测反证）

- **H5 计时**：child.exited_at（唯一写入/只写一次/revive 清空）；purge 豁免 user_id=0；
  consent_record 永不删
- **F5 快照**：user.paid_member_ever 四支付入口全覆盖；P3-④ 裁定（管理端标已付不激活会员，
  会员开通走 PUT /child/{id}/status，F13 起收归超管 + 二次确认 + from→to 审计）
- **F7**：保留支付 + 每日 5:00 对账（PAID 未激活）告警 + 人工队列
- **F8**：缓冲期 9 折含 day-0（0<=days<=grace + member_expire_time<=now 守卫）
- **F35/F36**：fine_in_outstanding 标记列 + 差额增量（不抹损坏/丢失罚款）
- **F37** total=原单额；**F38/F76** out_refund_no/partial_refund_no 分列持久化；
  **F39** DepositPaidEvent 仅即时路径 + PENDING 150min 复位；**F54** original_amount 快照
- **F60** 音频锁自然日口径；**F62** 申诉期 reviewed_at 起算（3 入口统一）；
  **F67** 核销 min(抵扣, outstanding)；**F74** 回调幂等；**F55** 回调按单号分发
- **订阅消息**：标题→模板映射 + after_insert 钩子 + WECHAT_SUBSCRIBE_ENABLED 开关
  （默认 false，模板 ID 未配降级只落库）
- **并发写入铁律**：child 统计 with_for_update；先查后插唯一约束 + SAVEPOINT；
  金额禁 float；LIKE 必须 escape_like；时区应用侧 datetime.now（JWT 例外）
- **配置治理**：66 项，改 DEFAULTS 必跑 gen_config_doc --check；新键登记 RANGES/levels
- **测试纪律**：behave steps 真实 API（禁 ORM 伪造/自带公式/恒真断言/空断言）；
  MySQL 并发用 scripts/verify_mysql_concurrency.py（独立测试库实测全 PASS）

## 五、错误记忆库与防复发（20260807 起强制）

- **error_list/**（项目根目录）：
  - README.md：周期性重做六步（收集→提取→聚类→更新→自测→广播）
  - 错误记忆库-全项目-20260807.md：9 类根因模式 + 防复发动作 + F1-F80 全量映射 +
    早期轮次全量 + 正反案例 + 引用锚点
- **项目级 AGENTS.md**：完整继承全局 + librio 附加（开工必读 error_list + 8 条防复发红线 +
  验证环境口径）——新会话进项目目录自动注入
- **最核心三条**：① 修一类 bug 先 rg 全库枚举同类点 ② 测试禁假绿（真实路径 + RED→GREEN）
  ③ 门禁不缩范围 + 报告贴原始输出

## 六、陷阱清单（累计，最新在前）

- pytest 通过数随环境，全绿限定开发机；SQLite 行锁 no-op（并发用 MySQL 脚本）
- BDD feature 硬编码种子数（rbac.feature 29）；RBAC 种子无启动钩子（新增权限手动重跑）
- 给 User/Child 加列四同步（迁移回填 + schema + 表结构 + PRD）；配置四同步
- SQLite 测试老化时间：create_time 构造传参、update_time 需 text SQL 绕过
- 管理端新页面四件套 + data-action 须被 wiring 7 种正则捕获
- 小程序 wxss 禁非注释 hex；编辑文档 old_string 含下一节标题时 new_string 保留原标题
- timedelta.days 截断；check_model_consistency import 强制 DATABASE_URL=sqlite
- with_for_update SQLite no-op；事件 handler 不自行 commit（调用方统一）
- apply_patch 大文件先看目标结构；monkeypatch 注意函数内 import

## 七、接手第一步

1. `git log --oneline -3` 确认 HEAD，`git status` 确认干净
2. 读 `error_list/README.md` + 最新记忆库 §三（开工必读）
3. 若做新功能：按 CLAUDE.md 铁律 + AGENTS.md 红线执行；门禁全量跑
4. 若甲方带来输入：按 §二 逐项执行（数值类改配置即时生效；流程类评估返工量）
5. 每轮大审查后：按 error_list README 六步周期性重做记忆库
