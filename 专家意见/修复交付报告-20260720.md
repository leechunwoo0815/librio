# 修复交付报告 — V3.8 审计终审补充修复（2026-07-20 第二轮）

> 日期：2026-07-20
> 范围：T5 假绿修复 + P1 代码修复 + 专家审查打回项重做
> 基线：V3.11（内联脚本收敛 Phase 1+2 完成）

---

## 一、总体状态

| 指标 | 前值 | 当前值 |
|------|------|--------|
| behave 场景 | 152 passed | **151 passed**（删除分享场景 -1） |
| behave 步骤 | 1035 passed | **1030 passed** |
| pytest | 249 passed, 4 skipped | **251 passed**, 4 skipped（+2 月报路由单元测试） |
| ruff | 0 errors | **0 errors** |
| @wip 残留 | 0 | **0** |
| @future 场景 | 4（含 reading_stats×2） | **2**（仅 voice_reading×2） |
| alembic | 未自验 | **已 upgrade → head，check 通过** |

---

## 二、本轮修复（含打回重做）

### 2.1 🔴 打回重做：月报路由 + 分享场景按裁决处置

**裁决依据**：《专家答复-15拷问批复与审计偏差修正.md》§三拷问⑥

#### 月报路由补全

- **文件**：
  - `backend/domain/report/schemas.py` — 新增 `MonthlyReportResponse`（含 report_type/period/total_minutes/total_words/books_finished/checkin_days/checkin_rate/current_ar_level/streak_days）
  - `backend/domain/report/router.py:68-76` — 新增 `GET /stats/monthly`，调 `service.generate_monthly_report`，走 `GetOwnedChildFromQuery` 认证 + 归属校验
  - `features/reading_stats.feature` — 移除 `@future`，月报场景正式纳入 CI
  - `features/steps/reading_stats_steps.py` — 重写月报 step：
    - 分离独立 `@when`（发起请求）+ 3 个 `@then`（逐字段校验）
    - 断言模式：先 `assert status_code == 200`，再逐字段检查（`total_minutes`/`total_words`/`books_finished`/`current_ar_level`/`checkin_rate`/`streak_days`）
  - `tests/unit/test_monthly_report.py` — 新建路由单元测试（认证缺失 → 401 / 有令牌 → 正常路由）

#### 分享场景按裁决处置

- **文件**：`features/reading_stats.feature` — **删除整个分享场景**（分享图纯前端 Canvas 实现，后端无对应路由）
- **文件**：`features/steps/reading_stats_steps.py` — 删除 `step_share_report` 及相关 step 装饰器

---

### 2.2 顺手修：专家审查发现项

#### test_report_pdf.py 增加 OSError 捕获

- **文件**：`tests/unit/test_report_pdf.py:15`
- **内容**：`except ImportError` → `except (ImportError, OSError)`
- **说明**：macOS 上缺少 `libpango` 库导致 collection 报 `OSError`，原 try/except 接不住

#### rbac_steps.py 条件断言改两段式

- **文件**：`features/steps/rbac_steps.py`（5 处）
- **问题**：返回非 200 时静默跳过字段断言，有隐性假绿风险
- **修复**：改为"先断言 `status_code == 200`，再无条件执行字段校验"的两段式
- **涉及函数**：
  1. `step_select_role_with_wei`（line 292）— 角色权限列表
  2. `step_operation_log_records_permission_change`（line 483）— 操作日志权限变更
  3. `step_teacher_card_show_admin_status`（line 582）— 老师卡片管理员状态
  4. `step_new_permission_change_log`（line 648）— 权限变更日志
  5. `step_log_records_admin_id_and_detail`（line 658）— 日志管理员ID

---

### 2.3 已交付并通过验收的 6 项（同第一轮）

| 任务 | 变更 | 验收 |
|------|------|------|
| T5.1 rbac @wip | `rbac.feature` 移除 @wip，`rbac_steps.py` 14 场景 50+ step | ✅ |
| T5.3 voice_reading @future | 2 场景 @future，与 PRD 16.4 一致 | ✅ |
| P1-1~3 索引 | `due_date`/`status`/`expire_time` 加 `index=True` + 迁移 | ✅ |
| P1-4 is_deleted | `repository.py:62` 补过滤条件 | ✅ |
| P1-9 密码强度 | `min_length=1` → `min_length=8` | ✅ |
| P1-14 消息权限 | verify only — 已完整配置 | ✅ |

---

## 三、待完成项

### 3.1 外部阻塞（无需代码修改）

| 编号 | 描述 | 阻塞原因 | 位置 |
|------|------|---------|------|
| P0-4 | 运营主体公司名称占位符 | 需商户提供认证主体全称 | `privacy-policy.wxml:16` |
| P0-5 | 办学许可证编号+扫描件 | 需商户提供许可证 | `privacy-policy.wxml:92-94` |

### 3.2 需业务决策

| 编号 | 描述 | 当前状态 | 建议 |
|------|------|---------|------|
| T1.2 | 退款取整 ROUND_HALF_EVEN → ROUND_HALF_UP | 代码用银行家舍入，审计建议四舍五入 | 1 行改动，需决策是否切换 |
| T1.6 | 已关闭订单迟到支付 | 回调可激活但用户无法主动付款 | 需补 `pay-params` 对 CLOSED 支持 + 日志 |
| D1-D10 | 9 天并行排期决策 | 尚未正式向产品负责人发出书面决策请求 | 需完成 0B 报告后发出 |
| D11 | 24h 内退款口径 + 押金商业风险 | 需产品负责人确认 | 随 D1-D10 一起发出 |

### 3.3 文档对齐（非代码，专家已给最终文案，可提前完成）

| 编号 | 描述 | 专家文案位置 |
|------|------|-------------|
| T2.1 | 已关停状态文档清理 | 专家答复文件 |
| T2.4 | 押金枚举文档对齐（补 PENDING=5、REFUND_PENDING=6） | 专家答复文件 |
| T2.7 | 书架模型文档对齐（V3.1 已改无上限） | 专家答复文件 |
| T3.6b | book.price 文档补全 | 专家答复文件 |

### 3.4 P2 优化（可后续迭代）

16 项低优先级优化，见 `DmkWords_V3.8_全量技术终审报告.md §第四部分`。

---

## 四、变更文件清单

```
features/rbac.feature                                   # @wip 移除
features/steps/rbac_steps.py                            # 新建 + 条件断言两段式修复
features/steps/reading_stats_steps.py                   # 重写（月报严格断言 + 删除分享）
features/reading_stats.feature                          # 月报去@future + 分享场景删除
features/voice_reading.feature                          # 2 场景 @future
tests/unit/test_monthly_report.py                       # 新建 — 月报路由单元测试
tests/unit/test_report_pdf.py                           # except 增加 OSError
backend/domain/bookshelf/repository.py                  # P1-4 is_deleted 过滤
backend/domain/borrow/models.py                         # P1-1~3 status/due_date 索引
backend/domain/reservation/models.py                    # P1-1~3 expire_time 索引
backend/domain/report/schemas.py                        # 新增 MonthlyReportResponse
backend/domain/report/router.py                         # 新增 GET /stats/monthly
backend/domain/admin/admin_auth_router.py               # P1-9 密码 min_length=8
alembic/versions/f7ad15b0e774_add_indexes_*.py          # 新建 — 索引迁移
```

---

## 五、风险提示

1. **索引迁移** `f7ad15b0e774` 已在本地上执行 `upgrade head` 并通过 `check`。MySQL 8.0 `CREATE INDEX` 是 Online DDL（`ALGORITHM=INPLACE, LOCK=NONE`），可在运行时安全执行。
2. **密码 min_length=8** 是 Schema 层校验，不影响已存储的旧密码哈希；旧密码仍可登录直到下次修改。
3. **@future 场景**（仅 voice_reading×2）被 `behave.ini` 的 `~@future` 排除，CI 中不计入失败。

---

*第二轮提交完毕。专家审查打回的 4 项要求全部完成：月报补路由 + 严格断言 + 分享场景删除 + 路由单元测试。顺手修 2 项（test_report_pdf.py OSError + rbac_steps 两段式断言）。请复核。*
