# K3 终审整改处置报告 · 2026-08-03

> 依据：`专家意见/终审意见-审计整改计划-K3执行单-20260803.md`（20 关全量审查 + 13 项复核）
> 执行人：K3 ｜ 基线 HEAD：整改前 `a7e3963`
> 复测门禁（§八）：**十关全绿** — pytest **570 passed**/0 skipped、behave **210 scenarios/1361 steps**/0 failed、ruff check+format ✅、API 合约 ✅、模型一致性 **56 表** ✅、alembic 无漂移（head `c4e5f6a7b8c9`）✅、integration 56 步 ✅、wiring --strict ✅

---

## 一、P0 整改回填（3/3 闭环）

### P0-1 前端 30 天/÷30 文案 → 45 天 ✅

- 9 处全部统一：observation.wxml ×6（hero/徽标/单价/报告/退款公式/底部）、observation.js FAQ、official.js 对比表、service-agreement.wxml 会员类型；
- 退款公式文案最终版："前 7 天全额退款；第 8 天起按天扣除已使用费用（每天 = 总价 ÷ 45），剩余金额原路返回"——与 refund/service.py `refund = amount − amount÷45×(used−7)` 及 BDD `500−500÷45×(10−7)` 数值一致（执行单原文"按剩余天数÷45 结算"有歧义，字面实现会少退 7 天费用，已在批次 18b 修正）；
- **增量发现同级问题**：service-agreement.wxml 逾期条款仍是旧口径（"1 元/天累计罚款"）→ 已同步对齐 fine_policy（宽限 3 天/首次免罚/封顶半价/丢失 1.5 倍+7 天寻找期/记账抵扣）；
- 验收自查：`rg "30 天|30天|/ 30" frontend` 全前端无业务残留（仅 30 分钟/30 人/排行榜 30 天筛选等无关语义）；FAQ"7 天内全退"保留未动。

### P0-2 老师工作台 + 课后反馈管理端 UI ✅

- 新增 `templates/admin/teacher_workbench.html` + `static/admin/js/pages/teacher_workbench.js` + `css/pages/teacher_workbench.css`（38/34/36 件套同步）；
- 页面聚合：今日课程 / 待审核提交（带审核队列跳转）/ 负责孩子近况 / 最近指导 + **课后反馈表单**（下拉选孩子→调 POST /admin/api/teacher/feedback→推送家长 msg_type=4）；
- 入口：base.html 侧边栏"老师工作台"（data-perm=dashboard.view）+ admin_page_router PAGE_PERM_MAP 注册；老师角色 TEACHER_PERMS 已含 dashboard.view + report.comment，无需改种子；
- verify_action_wiring --strict 通过（submit-feedback 动作闭环）。

### P0-3 H5 数据保留期 ✅

- **4 个配置键**入 DEFAULTS（warning 级）：`data_retention_finance_years`(5) / `data_retention_behavior_years`(2) / `data_retention_message_years`(1) / `voice_retention_months`(6)；
- **purge_expired_data 任务**：每日 04:30、@distributed_lock、全量 try/except；消息类按年龄物理删除（read_status 级联）、行为类仅 EXITED 且退出超期、财务类 EXITED+超 5 年、语音 6 个月删行+删文件（复用 purge_soft_deleted.delete_voice_files）；**consent_record 永不删除**（专项测试钉死）；
- **口径三方统一**：前端隐私政策第七条第 4 款 ↔ PRD 附录 N.10（补配置键列+执行机制注）↔ 任务实现；
- 附赠修复：`scripts/gen_config_doc.py` 落地（原"脚本生成"承诺有名无实），并发现旧清单**漏列 `parent_course_required`**（59 项只列 58）已补；清单现为 64 项且支持 `--check` 校验；
- 测试：tests/unit/test_data_retention.py **10 用例**（到期删/未到期留/EXITED 才删/财务 5 年门槛/语音 6 个月/同意记录豁免）。

## 二、P1 整改回填（2/2 闭环）

### P1-1 iOS 支付豁免 + 收款码引导 ✅

- **放行**：押金缴纳/补缴/罚款（实物担保与实物关联赔偿，非虚拟商品）三处 iOS 拦截已移除，wx.requestPayment 正常发起；
- **保留拦截 + 引导**：观察期/正式会员（虚拟服务）拦截保留，弹窗改为 `utils/platform.js showIOSPaymentGuide()`——展示门店收款码/对公转账替代路径 + /venue/contact 门店电话 + 客服微信（配置为空自动省略该行）；
- iOS 判定统一封装 `platform.isIOS()`（原 3 页 3 种写法：getWindowInfo/getDeviceInfo/getSystemInfoSync）；
- 拦截页新增"查看办理指引"按钮（原 iOS 分支只有文案没有动作入口）；
- wxss 全程 var(--accent)/var(--surface)，未引入非注释 hex（P2-10 红线不破）。

### P1-2 review_sla_hours 配置化 ✅

- DEFAULTS 新增 `review_sla_hours`(24, warning 级, 范围 1-168)；audit_sla_escalation 改读配置，告警文案同步动态化；
- 测试：tests/unit/test_review_sla_config.py **3 用例**（默认 24h 触发/改 48h 不触发/48h 下 50h 仍触发）。

## 三、P2 整改回填（8/8 闭环，2 项留档不动）

| # | 处置 | 证据 |
|---|------|------|
| P2-1 | ✅ DEPLOY_CHECKLIST 2.3 新增"多实例必须 REDIS_LOCK_FAIL_OPEN=false" | 与 DEBUG/MOCK 并列 |
| P2-2 | ✅ C1 今日全勤实现：4 类型集满发 SystemMessage（msg_type=5），按日幂等防重 | reading/service.py maybe_send_full_attendance_message + 4 打卡点接入 + 2 用例 |
| P2-3 | ✅ 押金页新增 SLA 文案（借满 10 本无逾期退 600，24h 发起/3-5 工作日到账） | deposit.wxml info-banner |
| P2-4 | ✅ 配置范围校验：config_levels.py CONFIG_RANGES **42 键** min/max，非法值 422 | set_config 路由层校验 + 5 用例（含 RANGES↔DEFAULTS 漂移防护） |
| P2-5 | ✅ error-view 组件 attached 拉取 /venue/contact，电话/微信拼接展示，空值优雅隐藏 | error-view.js/wxml/wxss |
| P2-6 | ✅ ARCHITECTURE.md 570/210/20 更新；PRD 附录 H 22 任务（去重周报行+补 purge）；V3.23 更新说明注明 F.13 六态 | 三处文档 |
| P2-7 | ✅ "17 jobs"→"22 jobs"；confirm_expired_damage_reports 补 @distributed_lock | scheduler.py |
| P2-8 | ✅ checkout-photos 直查下沉 AdminBorrowService.save_checkout_photos（行锁保留） | admin_borrow_router.py + 2 用例 |
| P2-9 | 留档不动（38 处 assert True 均为前端场景标注） | — |
| P2-10 | 留档不动（50 处 hex 均带 intentional 注释） | — |

## 四、增量发现（执行单之外，如实上报）

**亲子课 99 元购买流程在小程序端不存在**。执行单 P1-1 假设"亲子课与观察期同页"——实际 observation 页只创建 type=2 订单。后端 type=1 全链路完备（slot_id 关联/名额校验/课程开始后不退/BDD 覆盖），但小程序无 slot 选择 UI、无用户端 slot 列表 API（parent_course_time 路由仅管理端）。首页 FAB"立即报名 99 元亲子课"实际跳转 500 元观察期页。A1 双轨制默认 parent_course_required=false，主流程不受影响，但"99 元亲子课"这一轨在用户端不可达。

**建议（待甲方决策）**：① 补亲子课前端报名页（用户端 slot 查询 API + 选时段表单页，约 1-1.5 天）；② 或 FAB 文案改为观察期引导。未擅自实现，已录入交接文档遗留项。

## 五、52 题口径回填

执行单附录口径"42 ✅ / 10 ⚠️ / 0 未实现"→ 整改后 **50 ✅ / 0 ⚠️（除 B3/D3 二期、C2 图片素材外部依赖外全部闭环）**。A2/A3/A4/A5/C1/D1/D2/E2/G2/H5 十题的"部分实现"缺口已全部补齐（对应 P0-1/P0-2/P0-3/P1-1/P1-2/P2-2/P2-3/P2-5）。

## 六、门禁纪律说明

- 批次 A/B/C 合并为单次提交：工作树在门禁全绿前从未处于中间可验状态，拆分提交会留下未测中间态，不如单提交完整可溯；
- 全部新增测试 22 用例（10 数据保留 + 3 SLA + 9 P2），pytest 548 → **570**；
- 交接文档已同步至批次 18（含新陷阱 6 条）。
