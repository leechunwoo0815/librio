# K3 全量修复报告 · 终版（R1-R7 + 自主批 + B0-B5）

> 日期：2026-07-26 ｜ 执行人：K3 ｜ 全部已提交并 push 至 origin/main
> 最终 HEAD：`56fff33`（本日共 17 个 commit：`62b5634`→`56fff33`）

---

## 一、最终十关基线（全部实测）

| 关 | 结果 |
|---|---|
| ruff check backend/ tests/ + features/ scripts/ | ✅ All checks passed |
| ruff format --check | ✅ 374 files |
| pytest | ✅ **418 passed, 0 skipped**（pango 后 5 个 PDF 测试激活） |
| behave | ✅ **189 scenarios / 1240 steps**（18 features） |
| verify_api_contract | ✅ OK |
| check_model_consistency | ✅ 54 tables PASSED |
| integration_test | ✅ 全部通过 |
| alembic check | ✅ 无漂移（head=18d299533269） |
| verify_action_wiring --strict | ✅ 通过 |

## 二、B0-B5 批次明细（用户确认计划执行）

| 批 | Commit | 交付 |
|---|---|---|
| B0 | `9498c39` | UML-ER favorites 清除 5 处；表结构合计 55→54；damage reviewed_at String→DateTime+迁移 53490c8cb715；service 写入 .isoformat()→datetime.now() |
| B1a | `307ddb1` | **扫码枪取书（P0）**：fulfill 条码驱动（仅条码自动匹配最早 PENDING 预约）；事件链 book_copy_id 穿透→借阅精确到副本；预约页扫码框（自动聚焦/回车/500ms 防误扫）；手动取书保留备用 |
| B1b | `0e307ab` | PRD/扫码枪对接规范.md V1.0（选型/条码规则/SOP/降级/契约/8 用例） |
| B2 | `7d5b442` | 弱断言专项：实测 16 场景（非 147）；12 场景升级「拦截提示」强断言；2 假 step 根治（转让 PUT 403 假绿/deposit 无 book 静默跳过）；3 功能缺口挂 B3 |
| B3 | `b3227bf` | 观察期漏斗校验（需先完成亲子课）；Order+parent_course_time_id（迁移 18d299533269）+ 名额校验（PAID 计数口径）；开始后不退（精确 slot，删旧粗暴校验）；亲子课时段 CRUD 页面+路由+侧边栏；代客预约弹窗；票码签到错配修正；删 3 僵尸端点 |
| B4 | `c297cdf` | brew install pango → WeasyPrint 69.0 可用（中文 PDF 38KB 实测）；5 个 PDF 测试 skip→实跑；重复支付回调幂等场景（有效期不重复延长） |
| B5 | `56fff33` | 删 test_results*.json + page_template；审查令入库 ×2；5 文档 29 处数字校准；PRD V3.21 + 表结构 V3.15 |

## 三、关键数字（全部实测）

| 项 | 值 | 变化 |
|---|---|---|
| pytest | 418 passed / 0 skipped | +21（本日累计） |
| behave | 189 scenarios / 1240 steps | +10 scenarios |
| 业务表 | 54 | favorites 删除 |
| API 端点 | 311（含 38 页面路由） | 删 7 僵尸 |
| 管理端页面 | 35 业务页（37 模板含 login/403） | +亲子课时段，-page_template |
| 配置项 | 40 | +3（quiz_cooldown/original_price×2） |
| 定时任务 | 18（17 带锁+1 无锁） | — |
| 权限码 | 141 grep 口径（135 权限+3 role+3 模板） | — |
| 小程序页面 | 34 | — |

## 四、本日修复的真 bug 精选（16 项）

1. msg_type 2 越界(6/7)+6 分类错位（R2）
2. 副本状态借书拦截缺失——已借出/维修/报废/损坏/丢失可借（P1，§7.3 落地）
3. 预约取书不分配副本——丢书/损坏无法追溯到本（B1a，P0）
4. 票码签到错配——票码传给 enrollment_id 端点（B3）
5. 押金退款活跃借阅校验从未被测试真实触发（B2 假 step）
6. 亲子课退款旧校验全表扫描不过滤用户（B3 删旧换新）
7. 观察期购买无亲子课漏斗校验（B3，PRD 三步漏斗落地）
8. 亲子课报名无名额校验（B3，D9 30 人决策落地）
9. dead_letter resolved_at / damage reviewed_at String 时间列（R4/B0）
10. damage reviewed_at 写入 .isoformat()（MySQL 严格模式报错隐患）
11. favorites 双表并行（D5 落地删除）
12. 4 处测试假绿（assert True/绕 API 插库/403 弱断言/None 静默）
13. 转让 step 用管理员端点 PUT status（403 假绿）→ POST /child/transfer
14. 退款 7 天告警/会员/观察期 msg_type 防回归断言
15. 僵尸端点 7 个（report generate 旧版/ar-evaluation×3/book×2/logout）
16. 配置 DEFAULTS 3 项缺失（管理端配置页不可见）

## 五、业务规则固化（文档+测试双写）

1. **扫码枪流程**：借书扫会员码+书条码；还书直扫；取书扫书条码自动匹配预约（规范 V1.0 交供应商）
2. **预约不验押金**：锁库存免费，取书转借阅时验（全流程 §9.3）
3. **押金独立**：不随会员转让（D6，behave 固化）
4. **三步漏斗**：亲子课（已支付）→ 观察期 → 正式会员（订单校验落地）
5. **亲子课名额**：时段 PAID 订单计数 ≥ max_participants 拦截（D9）
6. **亲子课退款**：课程日开始即不可退（slot 精确判断）

## 六、遗留（仅外部阻塞 + 1 项架构观察）

| 项 | 状态 |
|---|---|
| 商户材料/微信测试号/域名 SSL | 🔴 用户手里（不催） |
| 会员 QR 码扫枪直扫解析 | 预留增强项（当前搜索孩子定位，不影响流程） |
| 小程序亲子课报名页时段选择 UI | slot_id 后端已就绪，前端报名页接入为后续增强（当前线下登记时段） |
| AdminService 2121 行上帝类 | 冻结不拆（无测试网覆盖时强拆=造 bug，已记录） |

## 七、结论

除外部阻塞项（商户/微信/域名，在甲方手里）外，**所有已知遗留问题已全部解决**：
- 7 轮审查（R1-R7）+ 睡前自主批（P1/僵尸/P3）+ 6 批次计划（B0-B5）全部收口
- 17 个 commit 全部 push，十关全绿
- 代码/PRD/表结构/UML-ER/配置清单/部署文档五方对齐
- 扫码枪接口已按甲方采购计划预留完毕（规范可交供应商）
