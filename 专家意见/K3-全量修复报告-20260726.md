# K3 全量修复报告（R1-R7 + 自主执行批）

> 日期：2026-07-26 ｜ 执行人：K3 ｜ 全部已提交并 push 至 origin/main
> 最终 HEAD：`4318405`（共 10 个 commit：`62b5634`→`4318405`）

---

## 一、最终十关基线（全部实测）

| 关 | 结果 |
|---|---|
| ruff check backend/ tests/ | ✅ All checks passed |
| ruff check features/ scripts/ | ✅ All checks passed |
| ruff format --check | ✅ 371 files |
| pytest | ✅ **406 passed**, 5 skipped |
| behave | ✅ **188 scenarios / 1235 steps**（18 features） |
| verify_api_contract | ✅ OK |
| check_model_consistency | ✅ 54 tables PASSED |
| integration_test | ✅ 全部通过 |
| alembic check | ✅ No new upgrade operations（head=b9f196875c6a） |
| verify_action_wiring --strict | ✅ 通过 |

## 二、Commit 清单（10 个）

| Commit | 内容 |
|---|---|
| 62b5634 | R2 msg_type 语义归位：2 越界(6/7)+6 分类错位 |
| eff0e17 | R1 枚举四方对齐（PRD V3.19/表结构 V3.13） |
| 2a8bf7a | R3 反向审查：假绿修复+配置 37→40+端点 sweep |
| 0cb3478 | R4 resolved_at 类型+迁移+FK/UK 63+19 零漂移 |
| 9ece6ee | R5 favorites 合并删除（D5 落地，19 文件） |
| 02e55be | R6 异常场景补缺+3 假绿根治（behave 186s） |
| 42b31e9 | R7 部署核验+交接备忘 |
| cc14c9d | **P1 副本状态借书拦截**（§7.3 五态，pytest +6） |
| 4213b30 | 僵尸端点删除 4 个（report generate 旧版 + ar-evaluation×3） |
| 4318405 | P3 转让驳回重提/押金独立 + 退款 7 天告警测试 |

## 三、重大修复明细

### 真 bug（代码级）
1. **msg_type 越界/错位 8 处**：观察期提醒 6→1、退款失败 7→1、借阅逾期 2→3（原错挂活动 tab）、活动通知×2 5→2、借阅到期 5→3、会员续费/退款告警 5→1
2. **P1 副本状态借书拦截缺失**：已借出/维修/报废/损坏/丢失副本原可被借出；已在 borrow_book 汇聚点统一拦截，文案对齐 §7.3（含预计归还日期）
3. **dead_letter_event.resolved_at** String(30)→DateTime（迁移 db9ee05cf8bf）
4. **order.type 注释**补季度/半年（同迁移）

### 测试假绿（4 处，全部根治）
1. rbac_steps `assert True`（数据范围从未验证）
2. 「工作人员扫描条码尝试借书」无条码时静默跳过 + 家长 token 调管理员端点 403 被弱断言吞掉
3. 「用户点击预约借书」绕过 API 直接插库，service 校验全失效
4. reservation「库存为0」书不存在时 context.book=None

### 架构/文档
- favorites 表删除（用户裁决贯彻 D5）：全链路 19 文件，迁移 b9f196875c6a
- 僵尸端点删 4；保留 certificates alias（文档兼容层）与票码签到（语义独立）
- PRD V3.20 / 表结构 V3.14 / 动态配置清单 40 项 / 全流程文档 §9.3 押金说明 §10.2 损坏流程
- 配置 DEFAULTS 补 quiz_cooldown_minutes + original_price×2（管理端配置页可见性）

### 测试新增
- pytest +12：msg_type 断言 2、副本状态 6、转让兼容修复 0、退款告警 2、其他 2
- behave +9 场景：借阅上限/同书重复/库存不足/重复预约/转让重复提交/转让源状态/题库为空/驳回重提/押金独立
- 新增强断言 step「拦截提示」（4xx+detail 含消息），替代弱断言「显示提示」用于新场景

## 四、业务规则澄清（已写进文档）

1. **预约不验押金**：锁库存免费，取书转借阅时才验（PRD 全流程 §9.3 补写）
2. **押金独立不随会员转让**（D6）：behave 场景固化验证
3. **驳回后可重新提交转让申请**：仅审核中(0)/已通过(1) 拦截

## 五、遗留清单（未修，按优先级）

| 级别 | 项 | 说明 |
|---|---|---|
| P1 架构债 | 预约取书不分配具体副本 | fulfill 为 book 级，借阅记录 book_copy_id=None，副本标记缺失。需产品决策（取书改扫码制？） |
| P2 | 弱断言「显示提示」147 处 | 新场景已用强断言；存量强化需逐场景核对真实消息，大工程 |
| P2 | 僵尸端点保留 11 个 | 7 无前端入口（代客预约/押金扣除/亲子课时段 CRUD 等已实现功能待补 UI）+ 4 观察保留 |
| P2 | 本机 weasyprint 缺 pango | PDF 功能本机不可用；生产按 DEPLOY_CHECKLIST §2.4 装 libpango（函数内懒加载，启动无影响） |
| P3 | 重复支付订单幂等 BDD | 代码已实现（order/service.py:234 already paid 跳过），仅缺场景 |
| 外部 | 商户材料/微信测试号/域名 SSL | 在用户手里 |

## 六、本轮新陷阱（已记入交接文件）

- behave step 模板冲突 3 次（参数化 vs 固定文案）：命名避让原则——新 step 加业务前缀
- POST /borrow/ 全系管理员端点；behave 需 _staff_borrow_headers（borrow.create）
- Book.offline_available=1 才可预约，造书必带
- 删 router 文件后模型注册链断裂 → deletion_service 显式 import models
- check_model_consistency.py 被 import 会强制 DATABASE_URL=sqlite（模块顶层副作用）
