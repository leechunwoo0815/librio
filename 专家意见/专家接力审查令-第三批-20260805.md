# 专家接力审查令 — DmkWords (librio) 第三批：预约/借阅链修复复审 + T1 测试整改验收

> 写给下一棒审查专家。你接手时项目刚完成第二批审查（D3/D4/D5）的大部分修复，本令覆盖：① 即将交付的 F40-F47 整改批复审；② 该批交付后的剩余队列。

## 一、你的角色与铁律

你是 DmkWords（儿童英语阅读 OMO 系统）的独立审查专家。工作对象是开发模型已推送的代码。铁律：

1. **不轻信报告，逐项独立实测**：开发模型给的任何"已完成/全绿"数字都要你自己复跑，代码精读 + 运行时验证双轨。
2. **只验证和出意见，禁止修改任何代码**（RED→GREEN 撤行复验除外——临时撤一行实现看对应测试是否转红，验后必须 git checkout 还原并确认工作区干净）。
3. **结论分级明确**：P0/P1/P2/P3 + 证据 file:line + 你的独立复测数据；定性用"属实/不成立/部分属实/可接受风险"，不粉饰不硬顶。
4. **复审记录原样入库**：追加进《专家意见/终极全量审查-第二批-D3借阅库存+D4预约清单+D5退款押金-20260805.md》（不删改历史）；推翻自己上一轮说法必须回写更正。
5. 中文回复；不确定就说不确定，禁止编造验证数据。

## 二、项目上手包（零先验可跑）

- 项目路径：/Users/litianyu/cc-projects/librio（macOS，本机有 MySQL 3306 dmkwords，可跑全量验证；venv/bin/python）
- 按序先读：
  1. 《专家意见/K3-执行中任务交接-20260726.md》——恢复卡/基线/队列/纪律/陷阱清单
  2. 《专家意见/终极全量审查-第二批-D3借阅库存+D4预约清单+D5退款押金-20260805.md》——41 个发现（F35-F75）+ 两轮复审记录（含 F76-F80）
  3. CLAUDE.md §六——十一关门禁命令（另有第十二关 tests/unit/test_payment_gateway_contract.py 随 pytest 跑）
- 业务：线下实体书借阅 + 线上伴读；微信小程序 34 页（家长端）+ Jinja 管理端 39 模板；FastAPI + SQLAlchemy 2 + Pydantic V2 + MySQL，测试 SQLite :memory:。
- 基线（20260805 交接时）：pytest 661 collected · behave 211/1368 · 56 表 · 66 配置 · 24 定时任务 · 332 API · alembic head=b4c5d6e7f8a9（迁移 047 最新）。
- 环境注意：ruff format --check 必须全仓 `.`；alembic check 必须连本机 MySQL（先 upgrade head 再 check）；behave 需 MOCK 环境变量的只有 integration；SQLite 行锁 no-op——并发类问题单测测不出，要靠代码精读判定。

## 三、待审对象 1：F40-F47 整改批（预约/借阅链，开发模型交付后复审）

原始发现与证据在第二批意见书 §三，修复口径即意见书的"修复"栏。逐项验收要点：

| # | 原问题 | 验收要点（你自己扩，别只信这份） |
|---|---|---|
| F40 | 取消预约不拦 EXPIRED/FULFILLED → 库存双重释放 | cancel 是否只允许 PENDING→CANCELLED（条件 UPDATE 或等价）；魔法数字 3 是否换枚举；双重释放反例用例是否真实（先过期再取消不再 +1） |
| F42 | 手动取书产生 book_copy_id=None 孤儿借阅 | 手动取书是否强制绑定 AVAILABLE 副本；test_fulfill_barcode.py:138-147 固化的旧错误预期是否改写；孤儿记录的存量数据如何处理（是否有说明/回填） |
| F43 | 管理端扫码取书必 422 | FulfillReservationRequest 是否增 barcode 可选字段并透传；前端 reservation.js:134 的 {barcode} 请求是否真的能 200；child_id 必填不用的死参数是否清理 |
| F44 | 还书事件不带 book_copy_id（已修，抽验） | 归还后副本回 AVAILABLE 是否有端点级测试；与 F41 对账的互相掩盖效应是否随 F41 定口径一并处理 |
| F45 | 预约取消/过期/取书三方竞态 | 三处是否统一条件 UPDATE（WHERE status=PENDING）按 affected rows 判定；对照 close_expired_orders 的 F5 模式 |
| F46 | 预约不查同书未还借阅 | create 是否加同书活跃借阅校验（状态流转图 9.3）；与取书端现有拦截（borrow/service.py:389-400）是否重复/一致 |
| F47 | 首次扫码建档 500（author NOT NULL） | schema 补 author 或模型默认值；补真实 API 建档测试（不得 ORM 伪造）；MySQL strict mode 下同样验证 |

**横向攻击面**：
1. 修 F40/F45 后，ReservationCancelledEvent/ExpiredEvent 的库存 +1 与 waitlist 通知顺序是否仍正确（borrow_handlers.py:55-108）；
2. F42 绑定副本后，取书事件 → borrow_from_reservation → 副本 BORROWED 的链路是否闭环，与 F44 归还回 AVAILABLE 形成完整生命周期；
3. 新测试是否调真实 service/API（T1 义务，见下）；
4. 门禁数字你本人复跑：pytest 全量、behave 211+、十一关 + 迁移（如有 048）upgrade+check。

## 四、待审对象 2：T1 测试整改验收（随各域修复同步）

第二批 §六 的整改要求：修一个域重写一个域的 behave steps。**验收红线**：
- `features/steps/borrow_record_steps.py` / `reservation_steps.py` / `deposit_steps.py` / `refund_application_steps.py` 不得再出现 ORM 直接造数替代业务调用（grep `db.add(BorrowRecord` / `db.query` 在 steps 里的残留逐一判断合理性）；
- steps 不得自带业务公式（原 :391-414 自写逾期逻辑且公式错误）；
- 恒真断言（status ∈ {400,403,422,200}、只断言对象非 None）必须清零；
- Gate 12 已有部分退款用例（total=原额），确认未被改回全退场景。

## 五、剩余队列（复审通过后按序推进）

1. **F48-F50 损坏定责批**：待复核期间找回冲销其他罚款 / 改判丢失→重度库存口径错+罚款清零 / 重度损坏不标记 DAMAGED。注意前向指引：必须处理 fine_in_outstanding 标记列交互（丢失覆盖 fine_amount 后标记残留会使后续 sync delta 失真）。
2. **五项待甲方口径（勿自行推进）**：F41 对账 available 口径（专家建议：在馆副本数 − PENDING 预约数）/ F57 退款中借书白名单 / F63 非会员预约门槛 / F71-① 预约是否校验押金（PRD 自相矛盾）/ F73 押金余额展示口径。
3. **P2 排期**：F58-F68；**P3 排期**：F31-F34、F69-F75（明细见第二批意见书与第一批 F13 复审记录）。
4. 第一批遗留：F6/F9/F10 等甲方口径；F3 出生日期方案已送审待拍板。

## 六、勿重开清单（已定案，除非有实测反证）

- 第一批评审全部定案（H5 exited_at / F5 快照 / P3-④ 标已付不激活 / F7 对账路线 / F8 窗口含 day-0 / iOS 分层 / 技术铁律）；
- 第二批已闭环：F35-F39（罚款账差额增量/退款 total=原额/out_refund_no 持久化/押金事件即时块限定）、F76（partial_refund_no 分列）、F54（original_amount 快照）、F77（迁移 047 回填）、F78（150 分钟+按需复位）、F44/F51-F56/F72、F79/F80；
- F12 回调守卫、F2 元分转换已两轮验证成立；
- 金额禁 float、金额传网关一律分；child 统计字段读改写 with_for_update；先查后插必须唯一约束+SAVEPOINT；LIKE 必须 escape_like；时区一律应用侧 datetime.now()（JWT 例外）；新增配置键四同步；新增迁移交付前必须开发机 alembic upgrade+check。

## 七、已知残留观察项（记录在案，非新发现）

- 存量 RETURNED 带罚款记录：旧逻辑下这些罚款从未入 outstanding（F36 数据损失），迁移未回补——需运营决策是否补记，不是代码缺陷；
- WECHAT_REFUND_NOTIFY_URL 为环境变量配置，生产部署必须设置（DEPLOY_CHECKLIST 是否已登记需核对）；
- 微信退款通知地址同时受商户平台后台配置影响，代码层无法完全确认，上线前需人工核对商户后台；
-押金 PENDING 复位窗口 150 分钟依赖"微信支付单有效期约 2h"的经验值，若微信侧口径有变需同步调整。

## 八、输出规范

- 结论结构：总判定（放行/有条件放行/不通过）→ 逐项复核（属实/不成立/部分 + file:line + 你的复测数据）→ 新发现问题（P0-P3）→ 给开发模型的下一步指令；
- 门禁数字必须你本人跑出来的，不允许引用开发模型报告里的数字；
- 复审记录追加进第二批意见书（追加，不删改）；
- 一切以证据说话；发现报告与代码不符，宁可多花时间复测也不要放行。
