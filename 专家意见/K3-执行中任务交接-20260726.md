# K3 执行中任务交接（防 compact 遗忘）

> 写入时间：2026-07-26 ｜ 写入人：K3（主审查+执行 Agent）
> 用途：上下文压缩后恢复工作状态。读我 + `专家意见/专家工作交接-HANDOFF-20260722.md`（背景全史）即可无缝继续。

---

## 一、当前已完成（全部提交+推送，勿重复劳动）

- **仓库状态**：origin/main 已同步，CI 绿（最后绿 commit `4876ed8`）。本地十关基线：pytest 397 passed/5 skipped、behave 179 scenarios/1171 steps、ruff 0 errors、ruff format 368 files、check_model_consistency 55 表、integration 全绿、alembic 无漂移、verify_action_wiring --strict 通过、admin_smoke.py 16/16。
- **文档基线**：PRD 需求文档 V3.18、表结构.md V3.12（55 表全对齐）、CLAUDE.md/HANDOFF.md/checkpoint.md/overview.md/新LLM接手指引.md 全部实测校准（397p/179s/55表/18任务/313+API/34页/28领域/135权限34组）。
- **隐私 Phase 1 全闭环**：三段式同意（consent_record+错误码+前端弹窗）、P0-3 删除权级联（deletion_service+24h冷静期+scheduler 03:30）、P0-4 儿童专章 v1.1。
- **小程序扫雷**：40+ bug 修复（分页 items/list、int/string 枚举、quiz 链路、WXML 编译错×2、deposit/activity/reservation 字段错位、QR 401、venues 公开端点、服务协议正文 v1.0）。
- **CI 同构**：ruff 钉版 0.15.16；G201 已修（logger.exception）。
- **0B 审查令（七席会审主令）**：11 项已全部复核覆盖 ✅，仅剩 0B-9 favorites 存量判定未做（=R5）。

## 二、待执行计划 R1-R7（用户已知晓，等最终确认后开工；开工前先做 R2 因为它是已证实真 bug）

| 轮 | 内容 | 关键事实 |
|---|---|---|
| **R2（先做）** | **msg_type=6 真 bug**：`backend/tasks/scheduler.py:1311`（check_observation_reminders）给 `_create_message` 传 msg_type=6，但代码枚举只有 1-5（1=系统 2=活动 3=借阅 4=老师 5=阅读提醒），前端分类无 6 的映射。修复方向：改为 5=阅读提醒（或按语义定），全仓 sweep msg_type 使用值。 | 已证实，文件:行号在手 |
| R1 | 枚举四方对齐精审：19 组枚举逐值核对 表结构↔PRD 附录F↔UML-ER↔types.py，修文档。types.py 实际值我已全核过（BorrowStatus 0-3/DepositStatus 0-6/ReservationStatus 0-3/BookCopyStatus 0-5/Activity 0-5/Enrollment 0-4/Quiz 0-2/PayStatus 0-5/OrderType 1-5/MemberStatus 0-4）。重点核 PRD 附录 F 的 F.3/F.7/F.9/F.10/F.16 落后项。 | 0.5 天 |
| R3 | 反向审查三连：假绿断言 grep sweep（assert True/恒真/空断言）、38 配置键使用 sweep、幽灵/僵尸端点 diff。 | 0.5 天 |
| R4 | 关5 字段精审：FK/UK 自动核验脚本 + reviewed_at varchar(30)→datetime 类型问题（dead_letter_event）+ assessment float 类型评估。 | 0.5 天 |
| R5 | 0B-9 favorites 判定：dev MySQL 执行 SELECT COUNT(favorites) vs COUNT(bookshelf WHERE status=0)，输出删表/保留裁决。 | 0.5 小时 |
| R6 | 32 异常场景 → behave 覆盖映射矩阵，缺口补 BDD。 | 0.5 天 |
| R7 | 部署代码子集：/health 核验、WeasyPrint 冒烟、alembic downgrade 演练、生产配置检查；关2 优化点只做组件级核验（utils/storage.js、components/error-view、empty-state、admin.js 统一 toast 的存在性+引用链）。 | 0.5 天 |

**不做/冻结**：专家意见日期报告（审计历史不可改）；AdminService 2121 行拆分（无测试网，强拆=造 bug）；64 项优化点全量逐项（只做组件级）；外部阻塞项（商户材料/微信测试号/域名 SSL，在用户手里）。

## 三、纪律红线（本项目血泪史）

- 报告数字必须命令实测贴原始输出，禁止"报告跑在代码前面"（本项目 5 次失实教训）
- 新模型四同步：alembic env.py + integration_test.py 模型清单 + check_model_consistency + 基类 4 列 comment
- 契约层改动五方同步：PRD/UML-ER/表结构/BDD/全流程文档
- 语义化提交、路径精确 add（禁 git add -A）；push 需用户指令
- 十关命令在 CLAUDE.md §六（ruff×2 + format + pytest tests/ + behave + verify_api_contract + check_model_consistency + integration(MOCK 环境变量) + alembic check + verify_action_wiring --strict）

## 四、恢复口令

用户给的一句话："读 专家意见/K3-执行中任务交接-20260726.md，继续 R2。"
