# K3 执行中任务交接（防 compact 遗忘）

> 更新时间：2026-07-26（R1-R7 全部完成后）｜ 写入人：K3（主审查+执行 Agent）
> 用途：上下文压缩后恢复工作状态。

---

## 一、R1-R7 全部完成（2026-07-26，全部本地提交，未 push）

| 轮 | 提交 | 成果 |
|---|---|---|
| R2 | `62b5634` | msg_type 语义归位：2 越界(6/7)+6 分类错位修复；新增强断言测试 test_scheduler_msg_types.py；十关 399p/179s |
| R1 | `eff0e17` | 枚举四方对齐：PRD V3.19（附录F 十处修正+F.19 新增）、表结构 V3.13（activity 六态/order.type 补季度半年/reservation 清"已备"） |
| R3 | `2a8bf7a` | 反向审查：rbac assert True 假绿修复；DEFAULTS 补 3 key(37→40)；端点 sweep 幽灵 0；B-1 分层扫描通过 |
| R4 | `0cb3478` | resolved_at String→DateTime+迁移 db9ee05cf8bf；order.type comment；FK/UK 三侧核验 63FK+19UK 双向零漂移 |
| R5 | `9ece6ee` | D5 落地：favorites 合并删除（用户裁决），19 文件 +108/-355，迁移 b9f196875c6a DROP TABLE，业务表 55→54，PRD V3.20/表结构 V3.14 |
| R6 | `02e55be` | 30 异常场景映射（17→24 覆盖）+7 新 BDD 场景；3 假绿根治（借书 step 403 弱断言/预约 step 插库绕过 API/库存为0 None）；behave 186s/1215 steps |
| R7 | 纯核验 | /health ✅；alembic downgrade↔upgrade 演练 ✅；生产配置 S-08 ✅；组件级 storage/error-view/empty-state/admin.js ✅ |

**当前十关基线**：pytest 398 passed/5 skipped、behave 186 scenarios/1215 steps、ruff 0 errors、format 371 files、check_model_consistency 54 表、integration 全绿、alembic head=b9f196875c6a 无漂移、wiring --strict 通过。

## 二、遗留清单（按优先级）

### P1 功能缺口
1. **预约取书副本状态校验缺失**：`_create_borrow_record`（borrow/service.py:419-431）预约转借阅时不校验 BookCopy 状态（维修/报废副本可被取走）。文档 §9.3 承诺此拦截。
2. **扫码借书副本维修/报废拦截未确认**：scan_borrow 路径大概率同缺。

### P2 待裁决（需用户拍板）
3. **R3 僵尸端点 15 个**：3 重复（GET /admin/api/certificates、POST /report/observation/generate 用户端、PUT enrollments/sign-in）/ 7 无前端入口（POST /admin/api/reservations 代客预约、POST /deposit/deduct、parent-course-time/* 6 个）/ 5 疑似死代码（POST /ar-evaluation + GET×2、POST /book、POST /admin/logout、GET /admin/api/export/{param}）。
4. **pdf_service 本机 pango 缺失**：weasyprint import 失败，PDF 功能本机不可用；生产按 DEPLOY_CHECKLIST §2.4 装 libpango。pdf_service 已是函数内懒加载，启动不受影响。

### P3 质量债
5. **common_steps:31「显示提示」弱断言**（status 2xx/4xx 全过，不验消息文本）：147 处使用。新场景已用强断言「拦截提示」。全面强化是大工程（逐个场景核对真实消息）。
6. **R6 未补场景**：重复支付订单幂等/退款 7 天告警（scheduler 层，宜 pytest）；转让驳回后可重提/押金不随转让（逻辑在 _validate_transfer，场景未写）。

### 外部阻塞（用户手里）
商户材料/微信测试号/域名 SSL——不催。

## 三、纪律红线（不变）

- 数字必命令实测贴原始输出；新模型四同步（alembic env.py+integration 清单+check_model_consistency+基类 4 列 comment）；契约五方同步；语义化提交精确 add；**push 需用户指令**；十关命令在 CLAUDE.md §六。

## 四、已知陷阱（本轮新教训）

- macOS grep 无 -P，用 python 正则替代
- check_model_consistency.py 被 import 会强制 DATABASE_URL=sqlite（模块顶层副作用）
- behave「显示提示」是弱断言（R3 漏网）；「拦截提示」是强断言（R6 新增）
- POST /borrow/ 全系管理员端点（require_perm borrow.create），behave 需 _staff_borrow_headers
- 预约不验押金（取书才验）；Book.offline_available=1 才可预约（造书必须带）
- alembic autogenerate 的 drop_index 在 MySQL 多余（索引随表删），需手工删行
