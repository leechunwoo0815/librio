# K3 执行中任务交接（防 compact 遗忘）

> 更新时间：2026-08-03（超级大审查第三轮 + 20 关终审整改全闭环后）｜ 写入人：K3
> 恢复口令："读 专家意见/K3-执行中任务交接-20260726.md，继续。"

---

## 一、当前状态

- 版本：PRD **V3.23**、表结构 **V3.17**（check_in 唯一约束已补注）、动态配置 **64 项**（scripts/gen_config_doc.py 生成+--check 校验）
- 终审整改（批次18）：3 P0 + 2 P1 + 8 P2 全部闭环，处置报告 `专家意见/K3-终审整改处置报告-20260803.md`
- **新发现遗留（待甲方决策，非阻塞）**：亲子课 99 元购买流程后端完备（type=1+slot_id+名额校验+BDD），但小程序端无购买页（无 slot 选择 UI、无用户端 slot 列表 API）；首页 FAB"立即报名 99 元亲子课"实际跳到 500 元观察期页。A1 双轨制默认 parent_course_required=false 故主流程不受影响。需甲方决策：补亲子课前端报名页（用户端 slot API + 页面）或 FAB 改文案。
- 待甲方输入（不变）：G2 门店电话+客服微信（填 venue.phone + service_wechat）；C2 低龄题配图；扫码枪采购联调；52 题最终确认（改判：数值类改配置，流程类返工顺延）

## 二、批次 commit 链（批1-16 见 git log）

→ a7e3963(批17 超级大审查返修：CheckIn唯一约束043/行锁/Decimal/Redis降级/LIKE转义/suggest/pass_score/token缓存) → 批18(终审整改：45天文案/工作台UI/数据保留purge/iOS分层/review_sla_hours/范围校验/全勤/文档数字，详见处置报告)

## 三、关键机制（后续开发必读，批17/18 新增在前）

- **并发写入铁律（批17 定案）**：child 统计字段读改写必须 `with_for_update()`；"先查后插"必须 DB 唯一约束兜底 + `add_with_unique_fallback()`（SAVEPOINT 静默跳过，sql_utils.py）
- **金额/精度铁律（批17 补强）**：金额禁 float（schema 层也不许）；与 Numeric 列比较的阈值一律 Decimal
- **搜索铁律（批17 定案）**：所有 LIKE/ILIKE 用户输入必须 `escape_like()` 转义 + `escape="\\"`
- **数据保留（批18 H5）**：purge_expired_data 每日 04:30（消息1年/行为 EXITED 后2年/财务5年/语音6个月含文件），consent_record 永不删；配置 4 键 warning 级
- **iOS 支付分层（批18 定案）**：实物担保（押金/补缴/罚款）iOS 放行 wx.requestPayment；虚拟服务（观察期/会员）拦截走 platform.showIOSPaymentGuide（门店收款码/对公转账+/venue/contact 人工兜底）；iOS 判定统一 `utils/platform.js isIOS()`
- **配置治理（批18 补强）**：64 项配置改 DEFAULTS 后必须 `PYTHONPATH=. venv/bin/python scripts/gen_config_doc.py` 重新生成清单；数值范围校验在 config_levels.py CONFIG_RANGES（42 键），新数值键要登记范围+管控级别
- **今日全勤（批18 C1）**：4 类型集满 → maybe_send_full_attendance_message（幂等：按日查重 SystemMessage.title="今日全勤"）
- **时区铁律（批15/16 定案）**：ORM 时间戳与比较一律应用侧 datetime.now()；JWT 例外（jose 内部 UTC 时间戳，批17 论证）
- **读完链路**：save_progress 读完→PENDING 提交+读完打卡+total_books_finished 首读累计；测验通过+≥10min 自动 APPROVED（D4）
- **fine_policy.py**：逾期服务费唯一计算口径（宽限 3 天/上限半价/首次免罚）
- **自动审核链**：refund ≤500、押金、活动退款自动批（权益转让仍人工）；audit_sla_escalation 读 review_sla_hours（默认24）
- **D4 提交流 / ALUMNI=5 / EXITED revive / 押金 String(64) 单号 / iOS pay-code+confirm-transfer**：见 git log 批1-16

## 四、当前无未答复的用户请求

三轮审查（DeepSeek 两轮 11 项 + 超级大审查 13 项）+ 20 关终审（3P0+2P1+10P2）全部闭环。下一轮若用户只说"继续"：提醒亲子课前端页决策 + G2 电话/客服号即可，不擅自开新工作。

## 五、纪律与陷阱（累计，最新在前）

- SQLite 测试里 create_time 用构造传参改老，update_time 有 onupdate 必须用 `db.execute(text("UPDATE ..."))` 绕过 ORM
- ConfigService.set_config 需要 config_audit_log 表：测试文件要 `import backend.common.config_audit_model`
- 语音录音 voice_recording.text_content/duration_seconds 为 NOT NULL，测试造数据必填
- 管理端新页面四件套：模板 html + js/pages/x.js + css/pages/x.css + admin_page_router（PAGE_PERM_MAP + 路由）+ base.html 侧边栏 data-perm；data-action 必须能被 wiring 7 种正则捕获
- 小程序 wxss 禁非注释 hex（P2-10 红线）：颜色一律 var(--accent)/var(--surface) 等 token
- wxml 里 iOS 分支若 wx:if/else 互斥，按钮要放进 iOS 分支内（否则 iOS 用户看不到按钮）
- 编辑文档 old_string 含下一节标题时 new_string 必须保留原标题（防吞标题）
- 后台门禁任务会读到未提交的半批改动：每批先完整再跑门禁；pytest 全量约 5-6 分钟，后台任务 timeout 给 900s
- alembic check 连的是开发 MySQL：新迁移必须先 upgrade head 再 check
- with_for_update() 在 SQLite 下为 no-op，单测只验功能
- 审查类反馈逐条给"属实/不成立/非bug/可接受风险"结论，不粉饰不硬顶
- 迁移改列后 alembic check 会报注释漂移：迁移文件里补 alter_column comment
- BDD 步骤硬编码配置默认值会与服务端新默认脱节——步骤默认值必须与服务端同步
- ConfigService._cache 进程级共享：测试间 set_config 后需 invalidate（fixture 尾部清理）
- behave 步骤名全局唯一；admin.username/role.code 唯一约束需 uuid 后缀
- timedelta.days 截断：剩余天数预期值按截断后整数算
- check_model_consistency.py 被 import 会强制 DATABASE_URL=sqlite
- integration 需 MOCK_PAYMENT=true MOCK_SMS=true DEBUG=true；十关命令在 CLAUDE.md §六
