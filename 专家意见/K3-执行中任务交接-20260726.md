# K3 执行中任务交接（防 compact 遗忘）

> 更新时间：2026-07-26（B0/B1a/B1b 完成后）｜ 写入人：K3
> 恢复口令："读 专家意见/K3-执行中任务交接-20260726.md，继续 B2。"

---

## 一、当前状态（全部已 push）

- HEAD：`0e307ab`。十关基线：pytest **413 passed/5 skipped**、behave **188 scenarios/1235 steps**、ruff/format 372、contract/model 54 表/integration/alembic（head=53490c8cb715）/wiring 全绿
- 用户授权：除外部阻塞项（商户材料/微信测试号/域名 SSL）外全部解决，0 bug；无二期；push 已授权
- 完整计划见下方"三、B2-B5 待办"（用户已确认 B0→B5 总计划）

## 二、已完成批次（B0/B1 共 4 commit，均已 push）

| 批 | Commit | 内容 |
|---|---|---|
| B0 | `9498c39` | UML-ER favorites 清除 5 处；表结构合计 55→54；damage reviewed_at String→DateTime+迁移 53490c8cb715；damage service .isoformat()→datetime.now() |
| B1a | `307ddb1` | 扫码枪取书：fulfill 条码驱动（reservation_id 可选+barcode 可选；仅条码时自动匹配最早 PENDING 预约）；事件链 book_copy_id 穿透（ReservationFulfilledEvent→handler→borrow_from_reservation→记录+BookBorrowedEvent）；**P0：取书精确到副本**；预约页扫码框（自动聚焦/回车/500ms 防误扫）；手动取书保留备用 |
| B1b | `0e307ab` | PRD/扫码枪对接规范.md V1.0（选型/条码规则/SOP/降级/契约/8 用例） |
| — | 前序 | R1-R7（62b5634→42b31e9）+ P1 副本拦截（cc14c9d）+ 僵尸端点（4213b30）+ P3（4318405）+ 报告（85b2243） |

**专家暗伤裁决**（已回应）：msg_type/reservation已备/order.type/activity 4 项前序已修；UML-ER/合计数/reviewed_at 3 项 B0 修；assessment float 维持 R4 裁决不改。

## 三、B2-B5 待办（按序执行，每批十关+提交+push）

### B2 弱断言专项（最大批）
1. 写审计脚本扫「显示提示」（common_steps.py:31 弱断言，status 2xx/4xx 全过）全部使用点 → 输出**分类报告**（A 类：有 HTTP 响应必须升级强断言「拦截提示」；B 类：纯文案保留）**先给用户过目**
2. A 类逐个核对后端真实报错文案升级；后端文案与 PRD 附录 K 不符的同步修代码
3. 验收：behave 全绿 + 升级对照表
- 已知：「拦截提示」强断言 step 在 features/steps/borrow_record_steps.py（4xx+detail 含消息）

### B3 接口处置（无二期全做）
1. 代客预约：管理端预约页补"新建预约"按钮+弹窗（调 POST /admin/api/reservations）
2. 亲子课时段 CRUD 页面：调 parent-course-time 6 接口（GET/POST/PUT/DELETE /parent-course-time/admin + GET /venue/{id}），侧边栏加入口（admin_page_router + base.html）
3. 删除验证：POST /deposit/deduct（损坏定责已覆盖→验证后删）、POST /book、POST /admin/logout、GET /admin/api/export/{param}（确认无调用→删）
4. 票码签到接线：管理端活动签到页调 PUT /admin/api/enrollments/{ticket_code}/sign-in

### B4 环境+补测
1. `brew doctor` → `brew install pango` → 真实生成观察期报告 PDF 验证（backend/common/pdf_service.py，report/service.py:354 懒加载）
2. 重复支付订单幂等 behave 场景（order/service.py:234 already paid 跳过逻辑）

### B5 目录清理+总对齐
1. 先输出《待删/待归档/待保留清单》**给用户过目**：specs/ 13 个重构方案稿、专家意见过程稿、deliverables/audit-2026-07-08/、.ai/context/、scripts/test_results*.json、__pycache__、根目录散落
2. 两份审查令入库（专家意见/ 下两个 untracked md：终极审查令_审查补充.md、超级全量终审 Prompt.md）
3. 全文档数字重测对齐（接口/表/场景/页面/权限/配置数）

## 四、纪律与陷阱（累计）

- 十关命令在 CLAUDE.md §六；integration 需 `MOCK_PAYMENT=true MOCK_SMS=true DEBUG=true`；alembic 在项目根跑
- behave step 命名避让：参数化模板会撞固定文案（「提示」「已缴纳押金」已撞 3 次），新 step 加业务前缀
- POST /borrow/ 全系管理员端点（behave 用 _staff_borrow_headers）
- Book 造书必带 offline_available=1（预约需要）
- 删 router 文件后模型注册链断裂 → deletion_service 已显式 import evaluation.models
- 事件链测试需在 fixture 调 register_event_handlers()
- DateTime 列写入必须传 datetime 对象（.isoformat() 在 MySQL 严格模式报错）
- macOS grep 无 -P；alembic autogenerate 的 drop_index 在 MySQL 多余
