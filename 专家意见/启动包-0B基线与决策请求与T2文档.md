# 启动包：0B 基线报告 + D1-D11 决策请求 + T2 纯文档任务

> 目的：把三项流程债的全部所需信息集中到一个文件，开发大模型拿到本文件即可开工，无需再翻其他报告
> 来源索引：以下内容为指引，引用证据的全文位置均标注出处

---

## 任务一：0B 代码基线调查报告

### 交付格式（固定模板，逐项填写）

```markdown
# 0B 代码基线调查报告
> 日期 / 调查人
> 方法：每项先 grep/读代码，贴关键代码片段 + 文件:行号，再给结论

## 0B-N：<调查项名称>
- 调查命令/方法：<实际执行的 grep 或文件阅读>
- 证据：<代码片段 + 文件:行号>
- 现状结论：<一句话>
- 对计划任务的影响：<该结论改变哪个 T 任务的性质（纯文档/文档+代码/仅代码）>
```

### 已完成 3 项（直接引用，复核签字即可）

| 项 | 结论 | 证据出处 |
|----|------|---------|
| 月报路由缺失 | service 层已有 `generate_monthly_report()`，路由已补（本轮 R 系列已完成） | 《专家复核-打回项重做第二轮》§一 |
| favorites 存量 | favorites 与 bookshelf 关系、废弃三步走方案已定 | 《专家答复-15拷问批复》§三拷问③ |
| 试读拦截位置 | — | **未查到，仍是欠账，并入 0B-9** |

### 待查 6 项（每项给了具体操作方法）

**0B-1 押金枚举**
- 已代查：`backend/common/types.py:92-101` DepositStatus 共 6 值（UNPAID=0/PAID=1/REFUNDED=2/DEDUCTED=3/REFUNDING=4/PENDING=5/REFUND_PENDING=6）
- 你要做的：复核确认 + 检查 `child.deposit_status` 与 `deposit_record.status` 双写位置是否都覆盖 6 值（grep `deposit_status =` 全部赋值点）

**0B-2 书架模型**
- 已代查：`bookshelf/service.py:50` 默认 limit=0 无上限（证据见《专家答复》§二拷问⑬）
- 你要做的：复核确认即可

**0B-3 退款计算现状**
- 查：`backend/domain/order/service.py` 退款金额计算函数（`refund_amount` 相关，`daily_rate` 计算段，约 :390-426 区域）
- 回答：used_days 是自然日还是 24 小时制？取整方式（ROUND_HALF_EVEN 还是 UP）？——这是 T1.2 决策的直接输入

**0B-4 重考冷却现状**
- 已代查：`advancement/service.py:88-119`，冷却 1 小时从 `Quiz.create_time` 起算，硬编码 `timedelta(hours=1)`
- 你要做的：复核确认 + 记录配置化改造点（quiz_cooldown_minutes 应注入的位置）

**0B-5 已关闭订单迟到回调现状**
- 查：`order/service.py` `handle_payment_callback`（:209 起）对 `pay_status == CLOSED` 的处理路径
- 回答：CLOSED 订单收到回调时走哪个分支？报错还是静默？——T1.6 的直接输入

**0B-6 预约枚举现状**
- 已代查：`types.py:109-115` ReservationStatus = PENDING(0)/FULFILLED(1)/EXPIRED(2)/CANCELLED(3)，无"已备"
- 你要做的：grep 数据库迁移与种子数据，确认存量 reservation 行中 status 值的分布（是否曾有 1 被用作"已备"写入的历史数据）

**0B-9（新增）试读拦截位置**
- 查：体验用户 trial_pages 限制的实现位置（grep `trial_pages`）
- 回答：拦截发生在哪个文件哪个函数？体验用户能否访问音频？——开放问题 #4 的输入

---

## 任务二：D1-D11 决策请求书

### 交付格式

写成一页纸发给产品负责人，每条决策按此格式：

```markdown
### D<N>：<议题>
- 背景：<一句话>
- 选项：A. ... B. ...（C. ...）
- 专家建议：<选项>（理由一句话）
- 默认方案（48h 无答复时执行）：<选项>
- 请回复：A / B / C 或其他指示
```

### 11 条议题索引（完整背景与选项见原文件，不再复制）

| # | 议题 | 专家建议 | 完整材料出处 |
|---|------|---------|-------------|
| D1 | iOS 虚拟支付合规：A 商品结构改造为主 + B 线下引导开关备用（默认关） | A 为主 B 兜底 | 《整改计划表》D1；《专家答复》§六 |
| D2 | "已关停"状态：删除概念（代码零存在，证据已附） | A 删除 | 《专家答复》§二拷问⑨ |
| D3 | 订单状态机终态：删"已完成"，退款失败回退已支付+人工重试 | 采纳补丁方案 | 《整改计划表》D3；《专家答复》§六 |
| D4 | 预约"已备"状态：删除（代码无 PREPARED），枚举对齐 PENDING(0)/FULFILLED(1)/EXPIRED(2)/CANCELLED(3) | A 删除 | 《专家答复》§四偏差2 |
| D5 | favorites 表：废弃只读，统一 bookshelf（先查存量再定迁移） | A 废弃 | 《专家答复》§三拷问③ |
| D6 | 多孩押金：按孩子独立 1200/人 + 附商业风险提示（3 孩家庭预付 13000+） | A 独立缴纳 | 《整改计划表》D6；《专家答复》§三拷问④ |
| D7 | 多孩欠费连坐：孩子各自独立，PRD 明文 | A 独立 | 《整改计划表》D7 |
| D8 | 逾期还书：先还书罚款挂账，不阻断还书 | A 挂账 | 《整改计划表》D8 |
| D9 | 亲子课人数：30 人（PRD 为准），表结构注释修正 + 配置化 activity_capacity_default | A 30 人 | 《专家答复》§六 |
| D10 | 重考冷却：60 分钟可配置（quiz_cooldown_minutes），从测验创建时刻起算（代码已如此） | A 60 分钟 | 《专家答复》§四偏差表 |
| D11 | 开通 24h 内退款 used_days=0 全额退；超 24h 自然日口径下限 1 天；同 user 同商品 30 天限 1 次全额退防刷 | 按方案 | 《专家答复》§三拷问⑪ |

**决策请求书的动作要求**：发出后在交付报告中附"已发出"证据（邮件/消息记录或文档链接），48h 无回复按默认方案执行并标注"待业务确认"。

---

## 任务三：T2 纯文档四件套（不依赖决策，可立即做）

| 任务 | 内容 | 最终文案出处（原样替换即可） |
|------|------|------------------------------|
| T2.1 | 删除"已关停"概念，全流程文档 1.1 改写；EXPIRED 规则补全（可还书/不可新借/音频到期锁/续费解锁不重算借期） | 《Step2-批次1-维度一二三》维度二 P0（已关停）补丁全文 |
| T2.4 | 表结构押金枚举对齐代码 6 值（含 PENDING=5、REFUND_PENDING=6），补状态流转边 | 《专家答复-15拷问批复》§四偏差1 修正方案 |
| T2.7 | 全流程文档第四章 V2.0 书架模型整体替换为 V3.5 想读清单模型；10.1 借阅异常表 bookshelf_limit→borrow_limit | 《Step2-批次1-维度一二三》维度二 P0（书架容量）补丁全文 + 维度二 P1（borrow_limit 混用）补丁 |
| T3.6b | 表结构补 book.price 字段（对齐 `book/models.py:60-61`：Numeric(10,2) nullable，"图书定价（元），用于丢书罚款计算"） | 《专家答复》§四"顺带关闭 3 个开放问题"表 |

**五方同步检查**：每项改完 grep 确认 PRD / UML-ER / 表结构 / BDD / 全流程文档无矛盾残留。

---

## 交付与验收

- 三样成果合并为一次交付，附五项回归输出（ruff/pytest/behave/integration/alembic）
- 我 24h 内核验；0B 格式缺项或证据不足的，退回补件不进入 T2 验收
