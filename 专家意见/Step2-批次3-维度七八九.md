# Step 2 批次 3：维度七、八、九 详细审计报告

> 审计时间：2026-07-19
> 审计维度：七（权限与角色）、八（可测试性）、九（配置化设计）
> 本批发现：P0×0、P1×5、P2×5

---

# 维度七：权限与角色

## 【P1 | DmkWords_V3.5需求文档.md：17.1/17.2 | 128 个权限码的完整清单及角色-权限映射未给出 | 权限边界无可审计依据 | 越权无法验证】

**原文引用：** 17.1"super_admin（超级管理员）| 128 | 全部权限"；17.2"种子共 128 个权限，覆盖 31 个功能分组"。

**问题分析：** 全文只有权限总数（128/102/27）与分组数（31），但从未给出：
1. 128 个权限码的完整清单（哪些 group.action）
2. staff 的 102 个权限具体比 super_admin 少哪 26 个
3. teacher 的 27 个权限具体是哪些
4. 每个权限码对应的 API 端点/页面映射表

BDD rbac.feature 中出现了具体数字："创建运营人员管理员…该管理员拥有 102 个权限"、"教师…拥有 27 个权限"，但这只是断言数量，没有断言具体权限项。没有清单意味着：
- 开发无法核对实现是否遗漏权限码
- 审计无法判断"教师能否看到孩子的押金/退款财务数据"这类边界问题
- 新增功能时无标准流程决定该权限归属哪个角色

**影响范围：** 全部管理后台功能、越权审计。

💊 需求补丁：
【替换位置】DmkWords_V3.5需求文档.md 新增"附录 E：RBAC 权限矩阵"
【原文】（未找到明确定义）
【修订后文本】
```
## 附录 E：RBAC 权限矩阵

权限清单以数据库 permission 表种子数据为唯一真源，本文档不维护副本（防止漂移）。
但以下边界规则必须在本文档固化：

### E.1 三角色互斥边界
| 能力 | super_admin | staff | teacher |
|------|------------|-------|---------|
| 退款审核（refund.audit） | ✅ | ❌（仅可发起） | ❌ |
| 押金扣除（deposit.deduct） | ✅ | ❌ | ❌ |
| 全局配置修改（config.update） | ✅ | ❌ | ❌ |
| 管理员账号 CRUD（admin.*） | ✅ | ❌ | ❌ |
| 角色权限管理（role.*） | ✅ | ❌ | ❌ |
| 扫码借还（borrow.scan） | ✅ | ✅ | ❌ |
| 图书录入（book.create） | ✅ | ✅ | ❌ |
| 审核阅读提交（submission.review） | ✅ | ✅ | ✅（仅自己负责的孩子） |
| 活动签到（activity.checkin） | ✅ | ✅ | ✅ |
| 查看财务数据（order/refund/deposit 列表） | ✅ | ✅ | ❌（教师不可见任何金额） |

### E.2 权限新增流程
新增功能时，由产品负责人在本附录登记权限码、归属角色、数据范围，作为开发凭据。
```

---

## 【P1 | DmkWords_V3.5需求文档.md：17.4 | data_scope（all/own/none）的场景映射表未定义 | 数据范围规则不完整 | 数据泄漏风险】

**原文引用：** 17.4"数据隔离：teacher 角色通过 `get_scoped_child_ids()` 仅返回自己负责的孩子数据"；admin_auth 响应中有 `data_scope` 字段。

**问题分析：** data_scope 有三个取值（all/own/none），但全文只在 teacher 角色处举了一个例子（仅返回自己负责的孩子）。未定义：
1. super_admin 与 staff 的 data_scope 是 all 吗？staff 能否看到所有场馆的数据，还是仅本场馆（admin.venue_id）？
2. teacher 的 own 范围在每个业务模块的具体表现：能看自己孩子的订单金额吗？能看自己孩子的押金状态吗？（按 E.1 补丁教师不可见财务，但原文档无此定义）
3. data_scope=none 适用于什么场景？没有任何角色被定义为 none，那为什么要设计这个值？
4. 跨场馆场景：staff 属于人广馆，徐汇馆的孩子来人广馆还书，staff 能否操作？

**影响范围：** 全部管理端列表查询、数据隔离中间件。

💊 需求补丁：
【替换位置】DmkWords_V3.5需求文档.md 17.4 权限检查体系
【原文】"- **数据隔离**：teacher 角色通过 `get_scoped_child_ids()` 仅返回自己负责的孩子数据"
【修订后文本】
```
- **数据隔离**：按角色固化 data_scope 规则：

| 角色 | data_scope | 孩子数据 | 订单/财务数据 | 场馆数据 |
|------|-----------|---------|-------------|---------|
| super_admin | all | 全部 | 全部 | 全部场馆 |
| staff | venue | 全部孩子（借还操作不限场馆） | 全部 | 仅本场馆配置 |
| teacher | own | 仅 teacher_id=自己的孩子 | 不可见 | 不可见 |

- teacher 的 own 范围仅作用于孩子相关模块（孩子列表、阅读提交、观察期报告、活动签到）；
  财务模块（订单、退款、押金）对 teacher 整体不可见（data_scope 不适用，直接无权限码）。
- staff 借还操作不受场馆限制（任何场馆的会员可跨馆借还），但配置类操作（场馆信息、排班）仅本场馆。
- data_scope=none 保留给未来"客服只读"角色，当前三角色均不使用。
```

---

## 【P2 | DmkWords_V3.5需求文档.md：全文 | 同一家长多个孩子之间的数据互可见性未定义 | 多孩数据隔离缺失】

**原文引用：** 3.1"同一用户可以有多个孩子"；全文未定义孩子视角的数据隔离。

**问题分析：** 家长在微信中切换"当前孩子"后，大宝能否看到二宝的阅读记录、证书、生词本？从功能设计看家长账号下数据全通，但存在真实场景：15 岁的大宝不希望 5 岁二宝（操作家长手机时）看到自己的阅读记录。需求层未定义孩子间数据是否互相隔离。

**影响范围：** C 端全部孩子数据页面。

---

## 【P2 | DmkWords_V3.5需求文档.md：17.5 | 管理员操作审批流（二次确认/上级审批）的规则未定义 | 高危操作无审批】

**原文引用：** 17.5"角色权限变更通过 OperationLog 记录"。

**问题分析：** 仅定义了"记录日志"，未定义哪些高危操作需要审批流：
1. 押金扣除超过某金额（如 500 元）是否需要第二个管理员复核？
2. 退款审核是否允许同一人既发起又审批？
3. 全局配置修改（如价格从 5400 改为 54）是否需要二次确认？（优化点表 PC-024 有"重要配置强提醒"，但 PRD 正文无）

**影响范围：** 财务操作、配置安全。

---

# 维度八：可测试性

## 【P1 | features/rbac.feature：第4行 | RBAC 需求无有效验收用例 | BDD 覆盖缺口 | RBAC 无法回归验证】

**原文引用：** rbac.feature 第 4 行功能描述前标注 `@wip`（work in progress，behave 默认跳过）。

**问题分析：** PRD 第 17 章 RBAC 是唯一一个有完整 BDD 用例文件但被整体标记 @wip 跳过的模块。14 个 RBAC 场景（角色 CRUD、权限变更、数据隔离、教师/运营视图差异）全部不参与测试执行。这意味着：
1. RBAC 需求没有可执行的验收标准，权限回归只能靠人肉测试
2. 权限变更（如给 staff 增加一个权限码）后无法自动验证是否影响其他角色
3. 与维度七 P1-1 叠加：既无权限清单，又无可执行用例，RBAC 处于完全不可验证状态

**影响范围：** 全部管理后台权限控制。

💊 需求补丁：
【替换位置】features/rbac.feature 第 4 行
【原文】"@wip"
【修订后文本】（删除 @wip 标记，并补充环境依赖说明）
```
功能: RBAC 管理后台权限控制
  # 执行依赖：需预置三角色种子数据（super_admin/staff/teacher）及 128 权限码
  # 若 CI 环境无法预置，则在 environment.py 中为本 feature 单独建种子，不允许整体跳过
```

同时将"RBAC 用例必须参与每次回归"写入 PRD 17.5：
```
### 17.6 验收要求
- rbac.feature 全部场景必须从 @wip 移除，纳入每次发布前的强制回归
- 权限码新增/删除时，必须同步更新 rbac.feature 中的权限数断言（102/27 等）
```

---

## 【P1 | DmkWords_V3.5需求文档.md：全文 | 性能指标完全未定义 | 验收标准缺失 | 性能劣化无判定依据】

**原文引用：** 全文搜索"响应时间/并发/QPS/毫秒"，仅 4.3 查词处有"毫秒级响应"（338 万词条 ECDICT 查询）。

**问题分析：** 以下高频/重操作均无性能验收标准：
1. 图书搜索（AR 筛选+主题筛选+关键词）的响应时间要求
2. 扫码借书从扫码到反馈的时长要求（门店排队场景，超过 3 秒即影响运营）
3. 定时任务处理量（逾期检测 1 万条借阅记录的完成时限）
4. PDF 报告生成（weasyprint）的单次时长与并发上限
5. 小程序首屏加载时长

**影响范围：** 全部接口与定时任务。

💊 需求补丁：
【替换位置】DmkWords_V3.5需求文档.md 新增"附录 F：非功能性需求"
【原文】（未找到明确定义）
【修订后文本】
```
## 附录 F：非功能性需求

### F.1 性能指标
| 场景 | 指标 | 测量方式 |
|------|------|---------|
| 扫码借书/还书接口 | P95 ≤ 1.5s | 服务端日志 |
| 图书搜索（筛选+关键词） | P95 ≤ 800ms | 服务端日志 |
| 查词接口（ECDICT 命中） | P95 ≤ 50ms | 服务端日志 |
| 逾期检测定时任务 | 1 万条记录 ≤ 5 分钟 | 任务日志 |
| 观察期报告 PDF 生成 | 单次 ≤ 10s，并发上限 4 | 任务日志 |
| 小程序首屏 | ≤ 2s（4G 网络） | 前端埋点 |

### F.2 容量假设
- 同时在线用户 ≤ 500；借阅记录总量 ≤ 10 万；图书总量 ≤ 2 万册。
```

---

## 【P2 | features/reading_stats.feature：65-69 行 vs 实测 | 月报场景 BDD 与 API 契约不一致 | 用例与实现脱节】

**原文引用：** reading_stats.feature："场景： 查看月报…当 用户查看上月阅读月报，那么 显示月度阅读数据汇总"。

**问题分析：** 集成测试执行日志显示 `GET /report/stats/monthly` 返回 404（4 次）且 `POST /report/share` 返回 404（3 次），但该场景在 behave 执行中显示"通过"。这说明 BDD 断言的对象（step 实现）与真实 API 契约脱节——step 可能在 mock 层断言，或 API 路径已变更但 feature 未同步。用例"通过"但功能不存在，属于假绿。

**影响范围：** 学习报告模块、BDD 可信度。

---

## 【P2 | DmkWords_V3.5需求文档.md：12.1 | "精美的证书"等主观验收标准不可量化 | 验收标准无效】

**原文引用：** level_certificate.feature："我希望在晋级时获得一张精美的证书"；PRD 12.1"证书通过 HTML 模板渲染，可导出为 PDF"。

**问题分析：** "精美"不可测试。证书的可量化验收标准（包含哪些字段、编号格式、二维码尺寸、PDF 页边距）在 PRD 中仅有字段清单，无版式验收标准。

**影响范围：** 证书功能验收。

---

## 【P2 | DmkWords_V3.5需求文档.md：15、2.2 | 退款失败重试、支付中断恢复等异常场景无 BDD 用例 | 异常覆盖缺口】

**原文引用：** 无。BDD 退款用例仅覆盖"审核通过/拒绝"两个正常路径。

**问题分析：** 以下已在维度三/六确认的异常场景均无 BDD 用例：微信退款回调失败重试、已关闭订单收到迟到回调、并发预约库存、罚款超过押金余额。异常路径零用例覆盖。

**影响范围：** 资金安全场景的回归保障。

---

# 维度九：配置化设计

## 【P1 | 动态配置清单.md：全文 | 配置变更的生效方式与多实例一致性未定义 | 生效机制缺失 | 配置不一致事故】

**原文引用：** 动态配置清单开头："所有配置存储在 `system_config` 表，管理员可在后台直接修改，无需重启"。

**问题分析：** "无需重启"暗示有内存缓存，但以下机制全部未定义：
1. 缓存时长是多少？配置修改后多久对全部请求生效（实时？最长 N 秒？）
2. 多 worker 部署时，worker A 修改配置后，worker B 的本地缓存何时失效？
3. 价格类配置（price_official_member）修改后，对"已创建未支付"的存量订单是否追溯？
4. 配置读取失败（数据库连接中断）时的降级值是什么？

**影响范围：** 全部 37 个配置项的线上行为。

💊 需求补丁：
【替换位置】动态配置清单.md 开头说明段
【原文】"> 所有配置存储在 `system_config` 表，管理员可在后台直接修改，无需重启"
【修订后文本】
```
> 所有配置存储在 `system_config` 表，管理员可在后台直接修改，无需重启。
>
> 生效机制：
> - 应用内缓存 TTL 60 秒：配置修改后最长 60 秒内对全部请求生效
> - 多 worker 一致性：各 worker 独立缓存，均以 TTL 过期为准，不做主动推送；
>   修改配置后页面提示"配置已保存，最长 60 秒后全量生效"
> - 读取失败降级：数据库读取失败时使用 SystemConfig.DEFAULTS 内置默认值，并告警
>
> 价格类配置特殊规则：
> - price_* 系列仅影响修改后新创建的订单；已创建未支付的订单仍按原价格收款
> - borrow_limit / borrow_period_days 等规则类配置仅影响修改后新的借阅，存量借阅不变
> - overdue_fine_per_day 修改不影响已产生的罚款记录
```

---

## 【P1 | 动态配置清单.md：全文 | 配置值的校验规则（类型/范围/非法值处理）未定义 | 配置注入风险 | 系统异常】

**原文引用：** 全文无任何配置校验规则。例如"`borrow_limit` | 20 | 每个孩子最大同时借阅数"。

**问题分析：** 管理员在后台把 borrow_limit 改成 -1、0、999999，或把 renewal_discount 改成 1.5（150%，续费比原价还贵）、把 member_days 改成 0，系统行为完全未定义：
1. 各配置项的合法值范围是多少？
2. 非法值是拒绝保存还是保存后按默认值处理？
3. 枚举型配置（如 due_remind_days = "5,3,1,0"）输入乱序"3,5,1"或非数字"abc"如何处理？

**影响范围：** 配置中心、全部依赖配置的业务规则。

💊 需求补丁：
【替换位置】动态配置清单.md 新增"配置校验规则"章节
【原文】（未找到明确定义）
【修订后文本】
```
## 配置校验规则

| 配置键 | 类型 | 合法范围 | 非法值处理 |
|--------|------|---------|-----------|
| borrow_limit | int | 1-50 | 拒绝保存并提示 |
| borrow_period_days | int | 1-90 | 拒绝保存并提示 |
| overdue_fine_per_day | decimal | 0-100 | 拒绝保存并提示 |
| lost_book_fine_multiplier | decimal | 1.0-3.0 | 拒绝保存并提示 |
| renewal_discount / multi_child_discount | decimal | 0.5-1.0 | 拒绝保存并提示 |
| member_days / observation_days | int | 1-730 | 拒绝保存并提示 |
| member_grace_days | int | 0-90 | 拒绝保存并提示 |
| *_remind_days / due_remind_days | 逗号分隔 int 列表 | 每项 0-30，自动去重排序 | 格式错误拒绝保存 |
| price_* | decimal | 0.01-100000 | 拒绝保存并提示 |
| deposit_amount | decimal | 0-10000 | 拒绝保存并提示 |
| reservation_expire_hours | int | 1-168 | 拒绝保存并提示 |
| order_expire_minutes | int | 5-120 | 拒绝保存并提示 |
| vocab_lookup_limit / trial_pages | int | 0-1000 | 拒绝保存并提示 |
| quiz_pass_rate | decimal | 0.5-1.0 | 拒绝保存并提示 |
| quiz_total_questions / quiz_pass_count | int | 1-20 | 拒绝保存并提示 |
| bookshelf_limit | int | 0-500 | 拒绝保存并提示 |
| activity_cancel_hours | int | 1-72 | 拒绝保存并提示 |
| checkin_min_minutes / checkin_min_vocab | int | 1-120 | 拒绝保存并提示 |
| daily_checkin_limit | int | 1-10 | 拒绝保存并提示 |
| enable_* | bool | true/false | 其他值拒绝保存 |

未列范围的配置键按上述同类规则执行；所有校验失败在管理端实时提示，不写入数据库。
```

---

## 【P2 | 动态配置清单.md：六 | quiz_pass_count 与 required_books 的联动关系未定义 | 配置依赖缺失】

**原文引用：** "quiz_pass_count | 5 | 每级需通过测验的最少书数（默认=required_books，全部通过）"。

**问题分析：** "默认=required_books"意味着存在联动，但：管理员把某级别的 required_books 从 5 改为 8 后，quiz_pass_count 是否自动跟随变为 8？还是保持 5（造成"读 8 本书但只需过 5 本测验"的规则漏洞）？配置依赖方向未定义。

**影响范围：** 晋级规则正确性。

---

## 【P2 | 动态配置清单.md：十二 | 已废弃配置（venue_name/venue_address/default_required_books）仍在清单中且无清理机制 | 配置腐化】

**原文引用：** "venue_name | 人广馆 | 场馆显示名称（已废弃，代码读取 Venue 表）"。

**问题分析：** 3 个已废弃配置仍展示在配置清单与管理后台（推测），未定义废弃配置的处置规则：隐藏？只读？删除？管理员误改废弃配置后的行为未定义。

**影响范围：** 配置中心可用性。

---

## 【P3 | 全文 | 应配置化但写死的参数】

1. PRD 11.3"每本书至少 5 道选择题"——题库下限写死在文字中，未配置化。
2. PRD 6.2"每日凌晨 2 点执行"逾期检测——执行时间写死，未配置化（其他任务如观察期到期 9:30 同样写死）。
3. 附录 B 价格体系中"丢书罚款 定价 × 1.5"虽有 lost_book_fine_multiplier 配置，但附录 B 的展示未标注"可配置"。

---

## 本批次小结

| 维度 | P0 | P1 | P2 | P3 |
|------|----|----|----|----|
| 七：权限与角色 | 0 | 2 | 2 | 0 |
| 八：可测试性 | 0 | 2 | 3 | 0 |
| 九：配置化设计 | 0 | 2 | 2 | 1 |

下一批次：维度十（用户体验需求）、十一（术语与概念体系）、十二（OMO 账实一致性）。
