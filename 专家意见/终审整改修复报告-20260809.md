# librio 终审整改修复报告（2026-08-09）

> 依据：《专家意见/全部修复报告终审-20260809.md》§四 问题清单。
> **现行基线指针**：本文为历史审查档案（当时基线见文内），当前权威基线以《专家意见/项目交接-20260809.md》为准（HEAD=3ff4ee3 / pytest 925 / ruff 496 / MySQL A-K 全 PASS）。
> 方法：每项先亲验复现 → 修复 → RED→GREEN（撤行实证）→ 单主题 commit → 全量门禁。
> 当前 HEAD=`c091fcb` 前序整改批（P1/P2/P3 已提交，见 git log），工作区仅剩本报告与
> 证据未提交。

## 一、P1×3（修复前必须处理）

| 项 | 修复 | commit | RED→GREEN |
|---|---|---|---|
| P1-1 F-057 前端判分断裂 | 后端提交响应新增 `question_review`（每题对错/正确答案/解析，仅作答提交后返回）；前端 quiz.js 删除本地判分（`_correctAnswers`），选中仅记录，错题回顾改由服务端提交响应生成；onUnload 销毁 TTS（F-011 顺手） | `ec133d4` | 撤后端 review → 2 failed；恢复 3 passed |
| P1-2 F-116 管理端看不到下架书 | `BookRepository.search` 增加 `published_only` 参数；admin /books 列表传 `False`（含下架书可重新上架）；用户侧默认不变 | `3861159` | 撤 router 传参 → 1 failed；恢复 7 passed |
| P1-3 F-005 cancel_refund 处方未交付 | deposit cancel_refund 仅允许 REFUND_PENDING→PAID；REFUNDING（钱在途）抛 ConflictError，状态与记录保持不变 | `355961c` | 撤守卫 → 1 failed；恢复 9 passed |

## 二、P2×9

| 项 | 修复 | commit | RED→GREEN |
|---|---|---|---|
| P2-1 F-077 撤行不红 | 测试屏蔽 DB unique 兜底（monkeypatch IntegrityError 类型），证明应用层占用检查是独立必需第一道闸 | `926ba26` | 撤应用层检查 → 1 failed；恢复 3 passed |
| P2-2 F-022 logger.error 无 exc_info | except 块内 9 处补 `exc_info=True`（request_log/main 本已有）；业务失败分支（无活动异常）不加堆栈 | `9b0f47d` | AST 全库扫描守护测试（新增） |
| P2-3 F-020 明文 phone | tencent/aliyun 通知与 dev 模式日志统一 `_mask_phone` | `9b0f47d` | 全库 rg 无明文 phone 日志 |
| P2-4 F-016 回退路径漏 is_deleted | `_rollback_refund_failure`（订单/退款单）、`mark_refunded`、`handle_refund_failed` 查询补 `is_deleted==0`；订单缺失时整链不动作 | `276792f` | 撤过滤 → 4 failed；恢复 4 passed |
| P2-5 F-035 月末边界 | 月报 top_books/quiz_pass_rate/refund_rate 改 `< next_month_start`（5 处） | `65a8617` | 撤修复 → 1 failed；恢复 1 passed |
| P2-6 F-015 修复错位 | check_observation_reminders 改一次范围查询 + 按到期日分组（func.date 逐日循环废除） | `65a8617` | 撤修复 → 1 failed；恢复 4 passed |
| P2-7 F-069 重叠校验缺失 | 亲子课时段 create/update 同日同 venue 区间重叠校验（半开区间相交） | `18fe7c7` | 撤校验 → 2 failed；恢复 9 passed |
| P2-8 F-049 两回归 | `_check_page_perm` 区分"未登记拒绝"与"显式空权限白名单"；403 页登记白名单（消除无限重定向）；profile 放行 | `e72be2c` | 撤修复 → 2 failed；恢复 9 passed |
| P2-9 B3-evidence 覆盖夸大 | 补 F-022 AST 全库守护、F-011 quiz TTS/前端不判分静态断言；F-021/F-012/F-034 测试随 P3 修复补齐 | `82ba3dd` 等 | 新测试全绿 |

## 三、P3 顺手消化（代码类全部落地）

| 项 | 修复 | commit |
|---|---|---|
| F-011 quiz TTS 未销毁 | quiz.js onUnload destroy `_ttsCtx` | `ec133d4` |
| F-012/034 data-perm 错位 | edit-page→`book.edit`、delete-audio→`content.delete`（content.js/audio.js 三处）+ 静态断言 | `dea6b40` |
| F-021 任务无 trace_id | `distributed_lock` 包装器 TASK_START/END 携带 trace_id | `9a24662` |
| F-078 approve/reject 并发 | 复核：reject 走 `_get_report_or_raise`（F-080 已带行锁），当前代码已安全，无需再改 | — |
| F-097 update_activity 不重校验 | 合并后 start<end 校验 | `624bc8d` |
| F-098 手动提醒并发双发 | `get_overdue_records` 加 `with_for_update`（与标记同事务） | `55f44aa` |
| F-119 音频查重无 DB 兜底 | 生成列 `active_audio_key` 唯一索引（迁移 056）+ update 查重前置（防 autoflush 撞索引） | `55f44aa` |
| F-010 english_name/has_next | 报名列表 english_name 空串兜底；get_user_refunds 补 has_next | `624bc8d` |
| F-073 isbn 无格式校验 | 4 个 schema 加 ISBN pattern（13 位/10 位/带连字符） | `624bc8d` |
| F-084 手册笔误 | 手册 `[STATUS_REFUNDING]` → `[PENDING,APPROVED,COMPLETED]` + 勘误注 | `c091fcb` |
| 退款魔法数字 2/3 | 统一 `Order.REFUND_PROCESSING/DONE/FAILED` 常量 | `624bc8d` |
| evidence 数量 40 vs 41 | 交接文档/终审令修正为 41（实测） | `c091fcb` |

## 四、全量门禁（本机实测，2026-08-09）

```text
pytest:  910 passed, 69 warnings in 429.99s（0 失败；较 883 +27）
behave:  20 features / 211 scenarios / 1369 steps，0 failed（Took 2min 16s）
ruff:    check backend+tests / features+scripts 全过；format --check . 494 files
contract: OK · model 56 tables · wiring ✅ · config 67 ✅ · integration 全部通过
alembic: upgrade head（含 056）+ check 零漂移（head=e4f506172839）
MySQL 并发: A-I 全 PASS（含 F-119 唯一索引兜底语义：C 场景同款机制）
```

基线变更：pytest 883→**910**、ruff 486→**494**、alembic head→**e4f506172839**（迁移 056）。
已同步 checkpoint/CLAUDE/HANDOFF/ARCHITECTURE/AGENTS/DEPLOY_CHECKLIST/交接/恢复卡/决策表。

## 五、剩余事项（全部外部阻塞，非代码）

- 甲方：决策表 I/J 组（I1 隐私占位符、I5 标已付书面确认、I6 birth_date、I7 音频锁范围、
  I8 库存对账口径、I9 亲子课购买页、I2/C6/F-045、I3/F-051、I4/F-068、J1-J28）
- 上线动作：iOS 微信审核人工确认、迁移 043-056 低峰、生产 REDIS_LOCK_FAIL_OPEN=false、
  商户后台退款通知地址核对、seed_rbac、pango/cairo 后跑 910 存档
- 前端真机验证：F-057 判分改造（quiz 页交互变化）、F-011/012/034 前端改动

## 六、证据

`audit_loop/fix-evidence/T-final-20260809-evidence.txt`（P1/P2/P3 撤行 RED→GREEN 摘要 +
全量门禁原始输出）。
