# 终极全量审查 · 第二批报告 — D3 借阅库存 + D4 预约清单 + D5 退款押金

> 审查日期：2026-08-05 ｜ 基线：HEAD=3e4d91d（代码同 a48d08a，仅 docs 提交）｜ 审查人：接力审查专家
> 方法：三路子代理分域深查（D3/D4/D5）→ 全部 P0/P1 由本人逐条亲验（file:line 可复核）+ 关键项运行时复现 → P2/P3 抽查定去留
> 范围：PRD §六 借阅、§七 押金、§八 预约、§十五 退款、附录 B/D/F、状态流转图 §七/八/九/十一
> 编号规则：沿用第一批全局序列（第一批 F1-F32、F13 复审 F33/F34），本批自 F35 起

---

## 〇、结论先行

- **P0 五项（F35-F39）**：两条罚款账断裂 + 三条退款/押金资金链断裂。其中 F37/F38/F39 与第一批 F1/F2 同属"mock/DEBUG 掩盖下的生产资金路径"盲区——**Gate 12 契约测试本身把 F37 的错误语义钉成了"契约"**，测试体系问题是本批最大的系统性发现（见 §六）。
- **P1 十七项（F40-F56）**：库存占用语义被对账任务 nightly 抹平、还书副本永不回 AVAILABLE、管理端扫码取书 422 断路、多个"置了状态不动钱"的退款路径。
- **P2 十二项（F57-F68）、P3 七项（F69-F75）**。
- **F2/F12 修复验证：当前代码仍然成立**（逐调用点核对，见 §五），但 D5 域在修复之外另有 3 个 P0——第一批"元分"只是押金/退款链问题的第一层。
- **处置判定：P0 五项均为上线阻断级；F41（对账口径）需先由 PRD 定口径再动代码。**

---

## 一、问题总览

| 级别 | 数量 | 编号 |
|---|---|---|
| P0（资金/核心机制断裂） | 5 | F35 F36 F37 F38 F39 |
| P1（流程断裂/PRD 明确条款未实现） | 17 | F40–F56 |
| P2（体验/口径/并发隐患） | 12 | F57–F68 |
| P3（加固/打磨/文档） | 7 | F69–F75 |

---

## 二、P0 问题（上线阻断级，全部本人亲验）

### F35【资金/账】每日逾期任务整体覆写 `outstanding_fines`，抹掉丢失/损坏/手工罚款

- **证据（本人亲验）**：`backend/tasks/scheduler.py:1544-1567`——按孩子汇总时只统计 `status==OVERDUE` 记录的 fine_amount，然后 `child.outstanding_fines = fine_map.get(child.id, ...)` **直接赋值**；对照全库其他写入点（`deposit/service.py:430/479`、`damage_admin_service.py:84/160`）均为增量 `+=` 语义。
- **后果**：丢失赔偿 150 元经定责计入 outstanding 后，次日孩子任意一本书逾期被任务处理 → outstanding 被覆写为"当前 OVERDUE 罚款之和"（宽限期内甚至为 0），150 元静默蒸发。资金内控断裂。
- **修复方向**：任务只维护"逾期服务费"部分（增量差额调整），或 outstanding 的权威口径改为"全部未缴来源重算"而非仅 OVERDUE 借阅。

### F36【资金/账】还书时的逾期服务费从不计入 `outstanding_fines`（PRD §7.3 B11 未闭环）

- **证据（本人亲验）**：`backend/domain/borrow/service.py:215-258` `return_book` 只 `apply_fine` 写 record，无任何 outstanding_fines 写入（全库 grep 佐证，borrow 域零写入点）；F35 的覆写逻辑又只统计 OVERDUE 状态记录——已归还书的罚款两头都不进账。
- **后果**：逾期书在凌晨任务跑批前归还，服务费只留在 `record.fine_amount` 上，退押金/退款抵扣（E7/B11）全部抵扣不到——罚款体系对"按时还书的逾期者"完全失效。
- **修复方向**：`return_book` 中 fine_amount>0 时 `child.outstanding_fines += fine`（行锁），与 F35 同批整改（同写字段，必须统一口径设计）。

### F37【资金】订单退款 `total_amount` 传退款额而非原单额 + 网关拒绝被静默吞掉——生产部分退款 100% 失败且无人发现

- **证据（本人亲验）**：`backend/domain/refund/service.py:238-247`——`total_amount` 与 `refund_amount` 都传 `yuan_to_cents(amount)`（amount=退款额，函数签名 :206-207）；微信 V3 要求 `amount.total`=原订单实付，否则报错 268892183「订单或退款金额不一致」。对照 600 奖励退款的正确写法（`deposit/service.py:756-758` 用 `record.amount + refund_amt` 还原原单 1200）。且 `pay_v3.refund` 对被拒**返回 success=False 不抛异常**（`pay_v3.py:301-306`），而 `_execute_wechat_refund` **不检查 result.success**（对比押金侧有检查 `deposit/service.py:601-604`）→ 拒绝被当成"已提交"，退款单永挂 APPROVED。
- **后果**：凡按天扣费的退款（观察期第 8 天起 466.67 类、会员 5059.73 类、E7 抵扣后）在生产**笔笔被微信拒绝且静默**，7 天后才由 alert_stale_refunds 告警（还发错对象，见 F72）。
- **掩盖链**：DEBUG 分支直接 return（:223-226）+ Mock 恒成功 + **Gate 12 反向钉死错误语义**（`test_payment_gateway_contract.py:159-161` 断言 total==refund==50000，恰好全退时两值相等，测不出部分退款）。
- **修复方向**：`total_amount` 改用订单原额（order.amount 传入）；检查 `result.success`，False 走现有失败回退分支；Gate 12 补部分退款用例（total=原额、refund=部分）。

### F38【资金】`out_refund_no` 不落库、每次重试新生成——重复退款（钱多出）

- **证据（本人亲验）**：`refund/service.py:236` 每次执行 `RF{uuid}` 新生成；押金两处退款调用根本不传 out_refund_no（`deposit/service.py:592-599`、`:752-761`），由 `pay_v3.py:281` fallback 生成一次性 uuid。微信按 out_refund_no 幂等，官方明确要求"失败重试使用原商户退款单号"。而失败回退路径（`refund/service.py:264-265` 等）恰恰允许重审重试。
- **后果**：首次请求已到达微信但响应超时 → 重试产生**第二笔真实退款**（钱多出）；且退款单号不留存无法对账；已提交微信但回退 PENDING 后微信推 SUCCESS 回调时 `mark_refunded` 只认 APPROVED/COMPLETED → 409 → 微信重试风暴，钱已退系统显示未退。
- **修复方向**：out_refund_no 持久化到 RefundApplication/DepositRecord（申请时生成、重试复用）；补 query_refund 对账任务。

### F39【资金/机制】`pay_deposit` 无条件发布 DepositPaidEvent——生产环境未付款即获得借书资格

- **证据（本人亲验）**：`backend/domain/deposit/service.py:152-164`——`event_bus.publish(DepositPaidEvent)` 在 `if is_instant:` 块**之外**；事件同步执行，handler 无条件 `child.deposit_status = PAID`（`events/order_handlers.py:154-169`）；生产网关 `supports_instant_payment=False`（`pay_v3.py:67-69` 本人核读），即创建 prepay 单成功后、用户一分钱未付，孩子押金状态已 PAID，借书校验直接通过（`borrow/service.py:73-78`）。若用户放弃支付，无任何任务复位该状态（scheduler 只巡检 REFUND_PENDING，:1322-1332）→ 永久无押金借书。对照 `repay_deposit` 正确地把发布放在块内。
- **掩盖**：全部 deposit 单测 mock `supports_instant_payment=True`（`tests/unit/test_deposit_service.py:44`），非即时路径从未被执行。
- **修复方向**：发布移入 `if is_instant:` 块内；补 PENDING 超时清理任务（或押金状态对账）。

---

## 三、P1 问题（流程断裂 / PRD 明确条款未实现，全部本人亲验）

### F40 取消预约不拦 EXPIRED/FULFILLED → 库存双重释放/幻影库存

- 证据：`backend/domain/reservation/service.py:453` 只拦 `status == 3`（且是魔法数字）；EXPIRED(2)/FULFILLED(1) 放行置 CANCELLED 并发 ReservationCancelledEvent → `events/borrow_handlers.py:71-76` 无条件 +1。已过期预约过期时已 +1（:63-68），再取消又 +1；已取书的取消后书在孩子手里库存 +1，还触发等候名单通知（幻影库存）。
- 修复：cancel 只允许 PENDING→CANCELLED（条件 UPDATE `WHERE status=PENDING`），魔法数换枚举。

### F41 每日库存对账抹掉预约占用（PRD 口径矛盾，须先定口径再改码）

- 证据：`scheduler.py:289-318` reconcile_stock 把 available_stock 重置为 `count(copies AVAILABLE)`；而预约锁只体现在计数 -1（副本全程 AVAILABLE）→ 每晚 3:00 所有 PENDING 预约的锁被静默回补，次日可超卖。**PRD 附录 D.4 伪代码与 D.2 #4 自相矛盾，代码忠实实现了 D.4**。
- 专家裁定建议：available 口径 = 在馆副本数 − PENDING 预约数（对账同步扣除），请甲方/产品确认后改码。注意与 F44 的互相掩盖效应（见下）。

### F42 手动取书产生 `book_copy_id=None` 孤儿借阅：副本不动 + 无法扫码归还

- 证据：`reservation/service.py:160-164`（无 barcode 放行，book_copy_id=None）；`admin_borrow_router.py:419`（管理端"完成预约"固定无条码）；`borrow/service.py:200-211`（scan_and_return 按 book_copy_id 匹配，孤儿记录永远扫不到）；该行为被测试固化（`test_fulfill_barcode.py:138-147`）。
- 后果：书物理借出但无副本转 BORROWED，架上副本可被再次借出（物理同一本借两次）；归还只能手工按记录 ID。
- 修复：手动取书强制绑定一本 AVAILABLE 副本。

### F43 管理端扫码取书（PRD §8.2 主路径）必然 422，完全断路

- 证据：`backend/static/admin/js/pages/reservation.js:134` 只 POST `{barcode}`；`admin_schemas.py:594-600` FulfillReservationRequest 要求 reservation_id+child_id 且 `extra="forbid"` → 422；路由 `admin_borrow_router.py:411-420` 也只透传 reservation_id，barcode 丢弃；child_id 必填却从不使用。
- 修复：schema 增 barcode 可选字段并透传 ReservationFulfillRequest。

### F44 还书事件不带 `book_copy_id`——副本永不回 AVAILABLE，且借过一次的副本永远无法再借

- 证据（本人亲验）：`borrow/service.py:247-254` return_book 发 BookReturnedEvent **不传 book_copy_id**（事件类有该字段 `common/events.py:112`）；scan_and_return 也委托 return_book（:213）；唯一把副本置回 AVAILABLE 的 handler 依赖 `event.book_copy_id`（`borrow_handlers.py:31-40`），而库存 +1 走 `event.book_id` 照常发生（:42-46）。
- 后果：凡绑定副本的借阅，归还后副本永远 BORROWED——该物理副本扫码再借被五态校验拦截（`borrow/service.py:112-141`），可借副本随借阅次数单调递减；库存计数与副本状态持续背离，每晚被 F41 的对账"抹平"，两个 bug 互相掩盖。扫码 SOP 下这是上线即炸的链路。
- 修复：return_book 发事件带 `record.book_copy_id`。

### F45 预约取消/过期/取书三方竞态：check-then-act 无锁无条件更新

- 证据：fulfill 的预约读取无行锁（`reservation/service.py:136-149`，base_repo 普通 get）；expire_reservation 同样无锁（:202-218）；cancel 有锁但只拦 status==3（F40）。对照组 close_expired_orders 已用条件 UPDATE（F5 修复模式）。
- 后果（MySQL 下真实，SQLite 掩盖）：cancel×expire 双 +1；fulfill×expire 借阅已建库存又释放。
- 修复：三处统一条件 UPDATE `WHERE id=? AND status=PENDING`，按 affected rows 判定。

### F46 预约创建缺"已有同书未还借阅"拦截（PRD 明确要求）

- 证据：`reservation/service.py:48-111` 只查重 PENDING 预约不查 BorrowRecord；状态流转图 9.3 与 §十一 10.4 均要求"已有相同书未归还→提示先归还"。取书端有拦（`borrow/service.py:389-400`）但库存已白锁最长 72h。
- 修复：create 时加同书活跃借阅校验。

### F47 首次扫码建档借书必 500：`Book.author` NOT NULL 但未传（已运行时复现）

- 证据：`book/models.py:33`（author nullable=False）；`borrow/service.py:315-324` 创建 Book 未传 author；schema 无 author 字段（`borrow/schemas.py:20-32`）。**本人复现**：内存库调 `scan_and_borrow`（全参数）→ `IntegrityError: NOT NULL constraint failed: book.author`。
- 后果：PRD 6.1"首次扫码自动建档"链路门店必报错；无任何测试覆盖该路径（behave"首次扫码"场景 ORM 伪造）。
- 修复：请求模型补 author（或 Book.author 允许空/默认值），补真实 API 建档测试。

### F48 丢失定级"待复核"期间登记找回/换新，冲销孩子其他合法罚款

- 证据（本人亲验）：创建时 needs_review 不计 outstanding（`damage_admin_service.py:81-84`）；但 mark_book_found 的报告查询**包含 STATUS_PENDING_REVIEW**（:251-258），且 `:243-246` 无条件 `waived = min(fine, outstanding); outstanding -= waived`（replace_with_new_copy :335-338 同构）。
- 后果：150 元丢失罚款还在复核（未入账）时登记找回，孩子另有 50 元合法罚款被错误冲销。
- 修复：PENDING_REVIEW 报告跳过 outstanding 扣减，仅物理回滚 + 置 OVERRIDDEN。

### F49 改判丢失→重度：副本置 DAMAGED 却 available+1、还书后再 +1；未填金额时罚款清零

- 证据（本人亲验）：`damage_admin_service.py:503-527` override 3→2 副本置 DAMAGED 但 `available_stock +1`（DAMAGED 计入可借，违反附录 D 口径），record 恢复 BORROWING 后该书归还时事件再 +1 → 累计虚增 2；`:469-470` override_level 填了但金额未填时 `override_fine = 0`——丢失（1.5×）改重度（应 0.5×）未填金额则罚款直接清零。
- 修复：override→2 时 available 不加；override_level=2 未填金额按 0.5×定价计算默认值。

### F50 重度损坏（level 2）从不标记副本 DAMAGED，三级定级名存实亡

- 证据（本人亲验）：`damage_admin_service.py:85-113`——仅 level 3 联动副本（LOST），level 1/2 只加罚款不动副本；对照状态流转图 §7.1"损坏(4)：定责登记产生"。
- 后果：重度损坏的书归还后照常流通，"DAMAGED"状态实际只能由 F49 的改判路径产生。
- 修复：level 2 定级时（书在馆/归还时）副本置 DAMAGED。

### F51 365 天 1 次退款限制只统计 APPROVED——COMPLETED 不计，防滥用失效

- 证据（本人亲验）：`refund/service.py:72-83` 只查 `STATUS_APPROVED`；退款完成置 COMPLETED（:319）后即不计数。买→全退→再买→再全退可无限循环免费试用（注释明写"防滥用循环退款"）。现有测试种子就是 APPROVED，把破规则固化。
- 修复：统计条件改 `status.in_([APPROVED, COMPLETED])`，测试种子同步改。

### F52 管理员代发起退款：金额公式死代码 + 不动钱就置已退款

- 证据（本人亲验）：`admin/services/refund_service.py:138-141` 调 `refund_service.calculate_refund`，但 `calculate_refund` 在 **OrderService**（`order/service.py:663`）→ hasattr 恒 False → 永远退回 order.amount 全额，无视已用天数；超管路径直接 APPROVED + `order.pay_status=REFUNDED`，路由（`admin_system_router.py:511-529`）**没有任何 BackgroundTasks 调网关** → 钱未退、订单显示已退款。
- 修复：改调 OrderService.calculate_refund；复用统一审核链路（含网关执行）；死代码 approve_refund 同清理。

### F53 E5 活动取消"自动退款"只写状态不动钱

- 证据（本人亲验）：`activity/service.py:392-405` 创建 `RefundApplication(order_id=None, status=APPROVED)`，全库无任何代码对 order_id=None 的退款单发起网关退款；家长通知却写"退款将自动处理"（:417）。
- 修复：活动收费接入真实支付单走统一退款链，或改人工处理队列+运营告警+文案改口径。

### F54 600 元奖励退款后再退押金，total 与原单不符 → 生产必失败死循环

- 证据（本人亲验）：partial_refund 把 `record.amount` 减为 600（`deposit/service.py:743`）；之后全额退款 `total_amount=yuan_to_cents(record.amount)`=60000 分 vs 原支付单 120000 分 → 微信 268892183 必拒 → 回退重试同样失败，押金退不出去。（partial 自身正确：:756-758 还原 1200。）
- 修复：DepositRecord 增 `original_amount` 快照（迁移），退款 total 一律用原额。

### F55 押金退款到账链路断裂：回调 404 + REFUNDING 无巡检 + 纯人工点按钮

- 证据（本人亲验）：微信退款回调统一进 `/refund/callback` → `mark_refunded` 只查 Order 表（`refund/service.py:290-297`）→ 押金 DP 单号回调 404 → 微信重试风暴；押金到账确认唯一入口是管理员手点 mark-refunded（`admin_borrow_router.py:349-370`）；`pay_v3.refund` 未传 notify_url；REFUNDING 状态无任何超时巡检（audit_sla_escalation 只查 REFUND_PENDING，:1322-1332；alert_stale_refunds 只查 RefundApplication，:1056-1063）。
- 修复：回调按单号前缀分发押金退款；REFUNDING 纳入 SLA 巡检；补人工完成入口。（注：微信退款通知是否可达还取决于商户平台配置，代码无法确认，上线前需核对商户后台。）

### F56 管理员代缴押金接口 500

- 证据（本人亲验）：`admin_borrow_router.py:302-312` 调 `service.pay_deposit(req, payment_gateway)` 未传 current_user → `deposit/service.py:82` `current_user.id` AttributeError（签名 :58-63 current_user=None 默认）。即使传了 admin 也是用 admin 的 openid 支付（语义错误，正确模式见 repay_deposit 用 child.user_id 的 openid）。
- 修复：代缴改走 repay_deposit 的取 openid 逻辑，或线下确认模式不经网关。

---

## 四、P2 / P3 问题（子代理发现 + 本人抽查，证据已核）

### P2

| # | 问题 | 证据 | 处置建议 |
|---|---|---|---|
| F57 | 押金退款中（REFUNDING/REFUND_PENDING）仍可借新书——与 PRD 6.1"必须已缴纳押金(PAID)"冲突；退款到账后书在外、担保落空 | `borrow/service.py:73-77`、`:412-416`（本人核读） | 可借白名单仅 PAID，或到账时复查在借并告警；若有意放宽需 PRD 写明 |
| F58 | mark_overdue_books 与还书无锁竞态：快照查 BORROWING 无行锁逐条赋值，可把 RETURNED 覆盖回 OVERDUE（lost update，凌晨窗口小但无防护） | `scheduler.py:1506-1519` vs `borrow/service.py:217-232`（带锁） | 任务内逐条 with_for_update 重取或条件 UPDATE |
| F59 | 损坏报告无幂等：同一借阅可重复登记轻度/重度（record 仍 BORROWING），重复计费；先重度再丢失 outstanding 累计 2× 而 record.fine 仅显示 1.5× | `damage_admin_service.py:59-60`；`damage_model.py` 无唯一约束 | 存在未终结报告时禁止重复登记 |
| F60 | 音频锁定口径不一致：锁判断用 datetime 精确比较（`reading/service.py:141-148`）vs 罚款按自然日（fine_policy），宽限第 3 天傍晚即锁，比"第 4 天起锁（B8）"提前数小时；且只认 OVERDUE 状态（任务未跑不锁）；图书详情接口整书 audio_url 无锁定检查（`book/service.py:107-155`，是否属锁定范围存疑） | 见左 | 锁定改用 calc_overdue_days 自然日口径；明确整书音频范围 |
| F61 | 丢失登记覆盖已有逾期费无留痕（先逾期 5 元后丢失 150 元，逾期费被覆盖） | `deposit/service.py:466`、`damage_admin_service.py:106` | 丢失罚款与逾期费分列或写 fine_original |
| F62 | 7 天申诉期从报告创建日起算，双人复核耗时吞噬申诉期（复核拖 ≥7 天，一进待申诉即被当晚任务自动确认） | `damage_admin_service.py:438-440`、`:561-573`；PENDING 从复核通过才开始（:162） | 申诉期改从进入 PENDING（reviewed_at）起算 |
| F63 | EXPIRED/EXITED/TRIAL/ALUMNI 均可创建预约锁库存 72h（无任何会员状态校验，文档无豁免）；join_waitlist 同 | `reservation/service.py:48-111`、`:248-301` | create/join_waitlist 加会员状态门槛或 PRD 写明豁免 |
| F64 | 事件处理器内自行 commit：borrow_from_reservation（:484）违反事件总线同事务契约（`common/events.py:29,36`），中途落库后失败无法整体回滚 | 本人核读函数归属（:360 起） | 删 handler 链路内 commit，统一调用方提交 |
| F65 | 管理端预约列表无 expire_time（前端"24h 内到期"统计恒 0）、路由丢弃 service 层支持的 status 筛选、客户端只过滤第 1 页 | `admin/services/borrow_service.py:253-263`、`admin_borrow_router.py:397-408`、`reservation.js:11-45` | 列表补字段、路由补筛选、统计改服务端 |
| F66 | 条码取书匹配"该书最早待取预约"不核对到店人（A 到店 B 的预约被核销，借阅挂 B）；venue_id 不校验 | `reservation/service.py:137-149` | 扫码后展示预约人信息供店员确认 |
| F67 | E7/B11 罚款抵扣与 B12 线上缴费双重收取窗口：申请时扣 fine_deducted、到账时核销（max(0,·) 只是不负），中间 3-5 天家长线上缴清则被罚两遍 | `refund/service.py:135`、`:326-339`；`deposit/service.py:387`、`:677-682`、`:890-910` | 退款申请后冻结 pay_fines，或核销按 min(抵扣额, 当前 outstanding) 并回补差额 |
| F68 | 押金缴纳并发双单：无记录时无锁可谈，两并发各建 PENDING 各生成支付单 → 双扣 1200；deposit 表无唯一约束 | `deposit/service.py:74-96`、`:243-267`；`deposit/models.py` | child_id 活跃状态部分唯一索引兜底 |

### P3

| # | 问题 | 证据 |
|---|---|---|
| F69 | 副本级双借只靠 Book 计数兜底（副本状态检查无行锁），F40/F41/F42 任一虚增计数后同副本可双借 | `borrow/service.py:112-114`、`:291-295` |
| F70 | mark_book_lost 库存扣减用 `func.greatest`——SQLite 不可运行（本人实测 `no such function: GREATEST`），端点全路径零测试 | `deposit/service.py:491-492`；端点 `admin_borrow_router.py:174-194` |
| F71 | 文档/文案打包：① PRD §8.1"预约前置：已缴纳押金"与状态流转图 9.3"不校验押金"及 reservation.feature 直接冲突（需甲方定稿）；② 表结构.md:745 预约状态流转"已取(2)"笔误（2=已过期）；③ 小程序预约页"保留 48 小时"vs 系统 72h；④ reservation repository get_expired_pending 死代码；⑤ frontend/utils/api.js:129 fulfillReservation 死/错代码（无入口、需 admin、多传 child_id 必 422）；⑥ join_waitlist 查重无锁无唯一约束可重复入队；⑦ 预约过期对用户本人无通知 | 各见左 |
| F72 | alert_stale_refunds 告警发给家长而非运营（E2 语义是升级超管），且每日重复发 | `scheduler.py:1070-1077`（本人核读） |
| F73 | 押金扣除后余额语义混乱：deduct_deposit 置 DEDUCTED 不减 amount，get_deposit_status 展示 record.amount；deposit.feature"余额更新为 1080"与实现与 B7 三方不一致，steps 空断言 | `deposit/service.py:413-419`、`:937`；`deposit_steps.py:419-427` |
| F74 | 押金支付回调不幂等：重复回调只查 PENDING → 404 → 微信重试风暴（订单链路已幂等） | `deposit/service.py:176-187` vs `order/service.py:411-413` |
| F75 | 数据质量杂项：① RefundCreate.damage_amount 死字段；② refund_application.amount（订单原额）用户申请路径从不写入，对账缺原额；③ 支付/押金回调 trade_state 解析了但无处消费（纵深防御）；④ 押金退款申请后、审核前新增罚款不补扣（快照口径，仅自动审核路径无此窗口） | `refund/schemas.py:16`、`refund/service.py:138-146`、`pay_v3.py:251`、`deposit/service.py:579-581` |

---

## 五、F2/F12 修复验证（D5 专项，逐调用点本人核对）

**F2（元分）——单位转换全部成立**（问题在别处）：小程序支付参数（`order/router.py:310-312`）、A5 收款码（`order/service.py:482-485`）、押金缴纳/补缴/罚款缴纳（`deposit/service.py:99-106`、`:270-277`、`:838-845`）、600 奖励退款（total 正确还原 1200）均为分。例外即 P0：F37（订单退款 total 语义）与 F54（押金全额退 total 语义）。

**F12（回调守卫）——完全成立**：非 SUCCESS 不标记（`refund/router.py:123-136`）、CLOSED/ABNORMAL 回退 PENDING+告警（`refund/service.py:344-392`）、PROCESSING 等待、mark_refunded 幂等（:315-317）、4 分支测试真实。

**财务公式验算（本人复算）**：观察期 500 用 10 天退 466.67 ✓；会员 5400 用 30 天退 5059.73 ✓；多孩 4860 用 36 天退 4473.86 ✓；E7 抵扣 4959.73 ✓；B11 押金 1200−50=1150 ✓；≤500 自动批边界 ✓；亲子课开课即不退 ✓。**公式本身无误，问题全部在执行层（total 语义/单号幂等/事件提前）。**

---

## 六、测试体系专项评价（本批最大的系统性发现）

**T1：behave 借阅/预约/押金域大面积假绿，且测试把错误行为固化成"契约"。**

- `features/steps/borrow_record_steps.py:46-118`：扫码借/还直接 ORM 操作不调任何 service；`:207-239`"确认创建"直接 ORM 造数（所以 F47 的 500 永远测不到）；`:391-414` 定时任务步骤**自己重写逾期逻辑**（1 元/天、无宽限/上限/首免，且公式写错成"定价×1.5"）——"逾期还书"场景是带着错误公式的假绿。
- 断言空转：`:616-620`"库存释放"只断言 book is not None；`:686-690`"发送提醒"只断言 child 非空；`:633-641`"音频锁定"从未请求音频接口。feature 文本"从押金中扣除120元"与 B11 记账决策矛盾（文本与断言两张皮）。
- `reservation_steps.py:301-310` 事件没触发时**手动扣库存再断言**；`:69-79` 押金/无库存提示接受 200 通过。
- `deposit_steps.py:373-382`/"退款申请不创建"断言 status ∈ {400,403,422,200} 恒真；`refund_application_steps.py:149` 金额容差 ±1 元，466.67 断言实际分辨力只有整数位。
- **Gate 12 缺陷**：`test_payment_gateway_contract.py:159-161` 用全退场景断言 total==refund，把 F37 的错误语义钉成"契约"；CapturingGateway 恒 success=True，测不出 F37 的"拒绝被吞"。
- 无真实断言的 P0/P1 路径：F35（任务连单测都没有）、F36、F40（全 tests 无一处调 cancel_reservation）、F41、F42、F44、F45（SQLite 测不出）、F47、F48、F52、F53、F56。
- `test_concurrency.py:82-193` 的并发用例 100 线程同一 child 被查重拦截/依赖 SQLite 写串行化，并未证明 MySQL 下的并发控制。

**整改要求**：修一个域、重写一个域的 behave steps（调真实 API/service，禁 ORM 伪造）；Gate 12 补部分退款用例（total=原额）与 success=False 分支；删除恒真断言。此项与 P0 同级对待——它解释了为什么十一关全绿还能藏着 5 个 P0。

---

## 七、门禁基线（本日实测，代码自 a48d08a 起未变）

pytest 632 passed / 0 failed / 0 skipped（283s，开发机）· behave 211 scenarios / 1368 steps / 0 failed · ruff×3（425 files）· contract · 56 表 · config 65 项 · wiring · alembic check 无漂移（head=e9f8a7b6c5d4）· integration 56/56。**上述全绿与 41 个新发现并存——门禁测的是"代码按设计跑"，本批抓的是"设计与真实世界/PRD 脱节"以及"测试假绿"。**

---

## 八、处置建议（给开发模型与甲方）

1. **立即（上线阻断，均为小改动）**：F37、F38、F39（退款/押金资金三件套）；F35、F36（罚款账，同字段统一设计一并改）。每项必须带真实测试（含 Gate 12 部分退款用例）。
2. **紧随**：F44（一行字段）、F40、F42、F43、F45、F46、F47（预约/借阅链）；F51-F56（退款押金其余断裂）。
3. **需先定口径**：F41（对账 available = 在馆 − PENDING 预约，专家建议口径，甲方确认）；F57（退款中借书白名单）；F63（非会员预约门槛）；F71-①（预约是否校验押金，PRD 自相矛盾）；F73（押金余额展示口径）。
4. **测试体系**：T1 随各域修复同步重写；完成前"behave 全绿"不作为借阅/预约/押金域的放行依据。
5. **排期**：F48-F50（损坏定责批次）、F58-F68、F69-F75。

> 第三批（如有）与 F6/F9/F10（等甲方口径）不受本批阻塞。本批 P0 修复完成后，建议对退款/押金链做一次生产邻近环境（DEBUG=false + 录制回放网关）端到端复验——这是前两批共同暴露的盲区根源。

---

## 附：P0 五修复复审记录（ec50e56，20260805）——五项修复方向全部正确，附 2 个 P1（1 个引入性）+ 1 个 P2 + 2 个 P3

**复审人**：接力审查专家（全程只读 + 运行时复现 + 撤行复验，验后还原，工作区干净）。
**对象**：ec50e56（F35/F36 罚款账 + F37/F38 退款网关 + F39 押金事件，迁移 046）。
**结论**：**有条件放行**——五项修复的设计与实现全部核实正确、测试真实；但本批引入 1 个新 P1（押金退款单号复用）且迁移 046 缺存量回填（已复现双计），两项必须随下一整改批闭合，不阻塞后续 F44 等"紧随"批次开工。

### 逐项复核

| 修复项 | 结论 | 证据（本人亲验） |
|---|---|---|
| F35/F36 罚款账 | 属实，设计正确 | `fine_policy.py:94-117` sync_outstanding_fine 差额增量（delta=fine−已入账标记，增长/归零/管理员清欠后不重加 语义均正确）；`scheduler.py:1548-1564` 任务改按记录增量（覆写逻辑已删除）；`borrow/service.py:244-255` 还书入账带 child 行锁；4 用例抽查断言精确到金额（150+逾期费、增量差额） |
| F37 total 语义 + 拒绝处理 | 属实 | `refund/service.py:251` total=order.amount、`:257-265` success=False 走 _rollback_refund_failure（PENDING 回退+FAILED+user_id=0 告警）；Gate 12 补部分退款与拒绝分支。**撤行复验**：把 total 改回退款额后恰好 `test_partial_refund_total_is_original_amount` + `test_partial_refund_total_is_order_amount` 两例转红，还原后全绿 |
| F38 订单侧单号持久化 | 属实 | 申请时生成（`refund/service.py:147`）、执行复用（:243-251）、审核兜底（:193-196）、带索引；每退款申请一行一号，订单侧无复用冲突 |
| F38 押金侧单号持久化 | **部分属实——引入新 P1（见下 F76）** | 单号确已持久化携带（`deposit/service.py:582-584`、`:750-753`），但单列复用破坏多单号场景 |
| F39 事件移位 + 复位任务 | 属实 | publish 已移入 is_instant 块内（`deposit/service.py:153-164` 缩进核实）；repay_deposit 原已正确未动；reset_stale_pending_deposits 任务注册（`scheduler.py:214-220`）、配置 deposit_pending_expire_minutes 四同步（gen_config_doc --check 本人实测 66 项一致）；即时路径行为不变有用例守护 |
| 迁移 046 | **有缺口（见下 F77）** | upgrade/downgrade 齐全、nullable/comment/索引规范；alembic upgrade head + check 本人实测 "No new upgrade operations detected"（head=f3a2b1c0d9e8）；但无存量回填 |

### 新发现问题

- **F76【P1，本批引入】押金 `out_refund_no` 单列被多次退款复用——第二笔退款被微信幂等吞掉或报错**。证据：`deposit/service.py:582-584`（audit_refund）与 `:750-753`（partial_refund）均为 `if not record.out_refund_no: 生成`——600 奖励退款生成 DPRF-A 后，后续全额退剩余押金**复用同一 DPRF-A**。微信幂等规则：同 out_refund_no 视为同一退款请求——参数一致返回原退款结果（钱没出去系统以为出去了），不一致报错。这正是 F54（partial→full 流程）修复后的下一道 blocker，两 bug 在同一条链上前后接力。修复：每次退款"操作"生成独立单号（重试同一操作才复用），列语义改"最近一次退款单号"并在发起新退款操作时重新生成；与 F54 同批修。
- **F77【P1】迁移 046 缺存量回填——老逾期记录罚款双计（已运行时复现）**。证据：迁移文件全文无 UPDATE 回填；本人复现：构造存量场景（历史已还记录 2 元已入账 + 逾期记录 3 元旧逻辑已入 outstanding=5.00、标记列迁移后=0），跑 mark_overdue_books 后 outstanding=**8.00**（3 元被计两遍）。修复：迁移补 `UPDATE borrow_record SET fine_in_outstanding = fine_amount WHERE status = OVERDUE AND is_deleted = 0`（已按旧逻辑每晚覆写入账的存量记录）。若上线前确认清库无生产数据可降 P2，但违反项目"加列含存量回填"纪律，一行 SQL 建议补上。
- **F78【P2】F39 复位窗口与重缴阻塞**：① `deposit_pending_expire_minutes` 默认 30 分钟 < 微信支付单有效期（约 2 小时）——每日 1:30 任务可能把"支付单仍有效"的 PENDING 复位，回调随后抵达 → 404（F74 叠加）+ 已付款但 UNPAID 的资金悬挂；② 任务每日一次，白天弃付的用户最长 ~24h 无法再缴（get_active_by_child 视 PENDING 为活跃）。建议：超时窗口 ≥ 支付单有效期（120 分钟）；pay_deposit 对 stale PENDING 按需复位而非等任务；回调命中 UNPAID 但 pay_order_id 匹配的记录时告警人工而非裸 404。
- **F79【P3】scheduler 日志硬编码**：`scheduler.py:223` "Scheduler started with 23 jobs"，实际 add_job 24 个（本人 grep 计数）。
- **F80【P3】scheduler 路径 sync_outstanding_fine 未持 child 行锁**：docstring 明写"调用方须已对 child 加行锁"，但 `scheduler.py:1554-1564` 批量查 Child 无 with_for_update——与还书路径并发时存在 lost update 窗口（夜间单任务，窗口小，SQLite 测不出）。建议任务内儿童行加锁或注释说明取舍。

### 前向指引（给后续修复批次）

- **F48/F49/F61 批次必须处理 fine_in_outstanding 交互**：丢失登记覆盖 fine_amount（F61）、找回/换新冲正（F48）、改判（F49）都会使标记列与 fine_amount 脱节，后续 sync delta 会失真——这些路径需同步重置/调整标记列。
- F76 与 F54 同链同修；F77 回填随下一迁移并入。

### 门禁（本人独立复测，开发机）

pytest **647 passed / 0 failed / 0 skipped**（300s）· behave **211 scenarios / 1368 steps / 0 failed** · ruff check×2 + format 全仓（427 files）· verify_api_contract OK · 56 表 PASSED · wiring --strict 通过 · gen_config_doc --check OK（66 项）· alembic upgrade head + check 无漂移（head=f3a2b1c0d9e8）· integration 56/56 全部通过。与开发模型报告数字逐项一致。

### 下一步指令

放行"紧随"批次开工（F44 一行 → F40/F42/F43/F45/F46/F47 → F51-F56），但 **F76/F77 必须并入下一个整改批**（F76 与 F54 同批、F77 随下一迁移）；T1 测试假绿重写义务不变；F41/F57/F63/F71-①/F73 仍待甲方/产品口径。

---

## 附：F76-F80 + F44/F51-F56/F72 复审记录（26ecdce + f27d0a9，20260805）——全部闭环，放行

**复审人**：接力审查专家（只读核验 + 专项测试抽跑）。
**对象**：26ecdce（F76/F54/F77/F78 + F79/F80，迁移 047）、f27d0a9（F44/F51-F53/F55/F56/F72）。
**结论**：**放行**。上轮 2 个 P1（F76/F77）与 1 个 P2（F78）全部闭合，紧随批七项修复方向正确、测试真实；无新 P0/P1。剩余：F40-F47 预约/借阅链（下一整改批）、F48-F50、五项甲方口径、P2/P3 排期。

### 逐项复核（本人亲验）

| 项 | 结论 | 证据 |
|---|---|---|
| F76 单号分列 | 属实 | 迁移 047 加 `partial_refund_no` 独立列；600 奖励用 partial_refund_no（`deposit/service.py:760-763`）、全额用 out_refund_no（:609-611），两笔退款幂等键不再冲突；partial_refunded=1 防二次 partial，单列复用安全 |
| F54 原额快照 | 属实 | original_amount 缴纳时写入（:100）+ 迁移回填（partial_refunded=1 按 amount+600 还原）；两处退款 total 均 `original_amount or ...`（:609-611、:772-778） |
| F77 存量回填 | 属实 | 迁移 047 `UPDATE ... SET fine_in_outstanding = fine_amount WHERE status=2 AND fine_in_outstanding=0 AND is_deleted=0`（status=2=OVERDUE 本人核枚举 types.py:70-76）；专项测试 `test_backfilled_marker_prevents_double_count` 重放回填 SQL 后断言不双计——正是我上轮的复现场景，27 个相关用例本人抽跑全过 |
| F78 复位窗口+按需复位 | 属实 | 默认 150 分钟（> 支付单 2h 有效期，config_levels (1,1440) 覆盖）；pay_deposit 遇超窗 PENDING 行锁下复位 UNPAID 并续缴（:76-84），未超窗仍拦截（有用例） |
| F79/F80 | 属实 | 任务数日志动态化 `len(scheduler.get_jobs())`；mark_overdue_books 儿童查询补 with_for_update（:1559） |
| F44 | 属实 | return_book 事件补 book_copy_id（`borrow/service.py:264`） |
| F51 | 属实 | 365 天统计改 APPROVED+COMPLETED（`refund/service.py:73-79`），有用例 test_completed_refund_blocks_new_application |
| F52 | 属实 | 公式改调 OrderService.calculate_refund；创建时只置 refund_status=1 不再置 REFUNDED；路由补 BackgroundTasks 网关执行（admin_system_router）；死代码 approve_refund 已删 |
| F53 | 属实 | 活动取消退款置 PENDING 人工队列 + user_id=0 运营告警 + 家长文案"门店人工处理"（`activity/service.py`） |
| F55 | 属实 | 回调按 DP 前缀分发（`refund/router.py:147-152`，DP 前缀本人核实 `deposit/service.py:55-56`）；mark_refunded_by_order_no 带行锁；REFUNDING 超 7 天巡检回退 REFUND_PENDING+运营告警（scheduler alert_stale_refunds）；pay_v3 携带 notify_url（空值不加，:288-289）；WECHAT_REFUND_NOTIFY_URL 环境配置（生产部署需设置，已写入接力审查令残留观察项） |
| F56 | 属实 | 代缴取孩子监护人 openid（admin_borrow_router），pay_deposit(current_user=user) |
| F72 | 属实 | alert_stale_refunds 改发 user_id=0 运营 |
| 迁移 047 | 属实 | upgrade+check 本人实测 "No new upgrade operations detected"（head=b4c5d6e7f8a9）；downgrade 齐全 |

### 门禁（本人独立复测，开发机）

pytest **661 passed / 0 failed / 0 skipped**（317s）· behave **211 scenarios / 1368 steps / 0 failed** · ruff×3（429 files）· contract OK · 56 表 PASSED · wiring 通过 · gen_config_doc --check OK（66 项）· alembic check 无漂移 · integration 56/56。与开发模型报告数字逐项一致。

### 观察项（不阻塞，记录在案）

- F55 回调 DP 前缀分发依赖"押金单号永远 DP 开头"的约定，暂无测试钉死单号生成格式（建议后续补一条前缀格式断言，P3）。
- 存量 RETURNED 带罚款记录不回补（F36 数据损失）仍需运营决策，已列入接力审查令残留观察项。

### 下一步

开发模型：F40-F47 预约/借阅链整改批（二批 P1 清零）+ T1 steps 重写义务。审查接力：《专家意见/专家接力审查令-第三批-20260805.md》已备好，含逐项验收要点、横向攻击面、T1 红线、勿重开清单与残留观察项。

---

## 附：第三批复审记录（e9bb284 + d7909c2，20260805）——F40-F47 整改批 + T1 借阅/预约/书架域整改，放行

**对象**：e9bb284（F40-F47 预约/借阅链 + T1 真实 API steps）+ d7909c2（T1 收尾）。**结论：放行，无新 P0/P1。**

### 逐项复核（本人独立复测，含撤行复验）

- **F40 ✅**：`cancel_reservation` 仅 PENDING→CANCELLED（`backend/domain/reservation/service.py:455`），魔法数字 3 换 `ReservationStatus` 枚举；EXPIRED/FULFILLED 取消抛 ConflictError。**撤行复验**：临时把守卫改回 `if record.status == 3`，`test_cancel_expired/fulfilled_rejected` 两用例转红，还原后全绿（工作区干净）。
- **F42 ✅**：手动取书（无条码）强制绑定 AVAILABLE 副本（:160-172 附近），无可用副本抛 ValidationError；`test_fulfill_barcode.py:138-147` 旧错误预期（book_copy_id=None）已改写为断言绑定副本 1；新增无副本拦截用例。**孤儿存量**：历史 `book_copy_id=None` 借阅无法可靠回填（物理副本不可判定），登记为运营人工盘点项，非代码缺陷。
- **F43 ✅**：`FulfillReservationRequest` 增 `barcode` 可选、删除不用的 `child_id`（`admin_schemas.py:594-600`）；路由透传 barcode；HTTP 级用例 `{barcode}` → 200 且借阅绑定副本（此前 422 断路）。
- **F45 ✅**：fulfill/expire/cancel 三处统一条件 UPDATE（`WHERE status=PENDING`，fulfill 另加 `expire_time > now`）按 affected rows 判定；`expire_after_fulfilled/cancelled` 反例确认不再重复释放库存。
- **F46 ✅**：`create_reservation` 拦截同书 BORROWING/OVERDUE 借阅（:66-80），与取书端校验一致不重复。
- **F47 ✅**：`ScanAndBorrowRequest` 增 `author` 必填 + `scan_and_borrow` 校验；behave"首次扫码"场景改走真实 `/borrow/scan`（此前 ORM 伪造，F47 的 500 测不到）。
- **F44（抽验）✅**：还书事件携带 book_copy_id，归还后副本回 AVAILABLE——取书→BORROWED→归还→AVAILABLE 生命周期闭环（borrow_from_reservation 发 BookBorrowedEvent → handler 置 BORROWED）。
- **F70（T1 前置顺带）✅**：`mark_book_lost` 弃 `func.greatest`（SQLite 不存在）改 Python max，丢失端点从此有真实路径测试。

### T1 红线核查（grep + 运行）

- `borrow_record_steps.py`：扫描借/还/取书/丢失改真实端点分发；自写逾期公式（`overdue_days * 1`）已删，定时任务步骤调真实 `mark_overdue_books(db=...)` + `check_due_date_reminders(db=...)`；空断言（库存释放/发送提醒/音频锁定）全部改为真实断言（+1 实值、SystemMessage 内容、真实 ReadingService 锁定校验）。
- `reservation_steps.py`：恒真断言 `status in (400,403,200)` 收窄为 4xx + detail 内容断言；手动扣库存 hack 删除，改 before-1 实断言。
- `bookshelf_steps.py`：库存锁定/预约成功两处空断言清零。
- Gate 12 部分退款用例（total=原额）未被改回。

### 顺带修复的真 bug（被假绿掩盖，均为本批真实测试暴露）

1. **check_due_date_reminders 上界漏当天到期**（`scheduler.py:1150`）：`due_date <= today+N 零点` 漏掉当天晚些时候到期的记录——生产同款问题，此前提醒场景空断言掩盖。改为开区间次日零点，5 天/3 天/1 天/当天四个提醒场景全部真实通过。
2. **behave context 跨场景复用**导致 admin token 泄漏（新场景 401）→ `_staff_borrow_headers` 改为按库幂等查找角色/管理员。
3. **逾期场景 `.first()` 取到 prior 记录** → 罚款断言按 `borrow_record.id` 精确定位。

### 门禁（本人实测，开发机）

pytest **671 passed / 0 failed / 0 skipped**（+10 新用例：F40×3/F42×2/F43×1/F45×2/F46×1/F47×2）· behave **211 场景 / 1369 步**（+1 步）· ruff×3（430 files）· contract · 56 表 · config 66 · wiring · integration 56/56 · alembic check 无漂移（head=b4c5d6e7f8a9，本批无新迁移）。

### 下一步

1. **F48-F50 损坏定责批**（含 fine_in_outstanding 交互前向指引：丢失覆盖 fine_amount 后标记残留会使 sync delta 失真，须同步置标记）；T1 剩余 deposit/refund steps 待相应域修复时重写。
2. 五项甲方口径（F41/F57/F63/F71-①/F73）勿自行推进。
3. 残留观察项维持：孤儿借阅人工盘点、存量 RETURNED 罚款回补待运营决策、WECHAT_REFUND_NOTIFY_URL 生产配置、商户后台人工核对。

---

## 附：精装批复审记录（20260805，开发模型自兼专家独立复验）——F48-F50/P2/P3/T1 剩余批 + F62 补刀

**对象**：5501685（F48-F50 损坏定责 + F62 申诉期起点 + fine_in_outstanding 丢失交互）→ 0e113b3（T1 deposit/refund steps 真实化）→ af224e5（P2 批 F58/F59/F60/F64/F67/F68，迁移 048）→ f9668ed（P3 批 F31/F32/F69/F74/F75）→ 1484bb9（F65/F66/F25/F21/F28）→ caf6de2（F71 部分/F22/F26，迁移 049）→ a944181/75ab114/08a68ab（文档收口与交接对齐）。
**结论：逐批核验通过；新发现 1 个 P1（F62 漏改家长申诉入口，已同批修复 + 2 测试）与 1 个文档 P2（HANDOFF §二 历史快照未标注，已修）。**

### 逐项复核（本人独立复验，含运行时验证）

- **F48 ✅**：`mark_book_found`/`replace_with_new_copy` 的财务冲正加 `charged` 守卫（`report.status != STATUS_PENDING_REVIEW` 才冲销）——待复核报告从未入账，找回/替代不再冲销孩子其他合法罚款；仅物理回滚 + 置 OVERRIDDEN + 清 `fine_in_outstanding`。对照用例（PENDING 已入账报告正常冲正）设计正确。
- **F49 ✅**：`review(override 3→2)` 未填金额时按 `record_price × LEVEL_MULTIPLIERS[2]` 计算默认值（0.5×定价，R 路径 book.price 双源回退）；副本置 DAMAGED 时 `available_stock` 不 +1（附录 D 口径），对照用例 3→1 恢复 +1。金额 quantization 正确。
- **F50 ✅**：`create_report(level 2)` 行锁重取副本置 DAMAGED；`fine_amount=0.5×定价` 用例精确断言。
- **F62 ⚠️→✅（本批发现 P1 漏改，见下）**。
- **F61 交互 ✅**：`mark_book_lost` 与 `DamageAdminService.create_report(level 3)` 两处统一为"delta = fine − marker，outstanding 只补差额，marker 同步为丢失罚款额"；`mark_overdue_books` 的 `sync_outstanding_fine` 按 marker 增量不再双计。方向与专家前向指引逐字一致。
- **T1 deposit/refund steps ✅**：自写公式 `days*1` 删除（逾期走真实 `mark_overdue_books`、丢失走真实 `deduct_deposit`）；恒真断言 `(400,403,422,200)` 收窄为 4xx；空断言实化（fine==outstanding、有效余额=amount−deduct_amount）；退款金额改精确 Decimal（466.67 与 467 不再不可区分）。首次免罚场景补历史逾期记录使罚款真实生效。
- **F58 ✅**：`mark_overdue_books` 两处循环均先 `with_for_update` 重取 + 状态守卫（新逾期须 BORROWING、存量须 OVERDUE），防还书并发把 RETURNED 覆盖回 OVERDUE；`apply_fine` 幂等标记（`fine_in_outstanding` 已入账额度不重加）。
- **F60 ✅**：音频锁定改 `calc_overdue_days > grace_days` 自然日口径（与 fine_policy 一致），宽限第 3 天傍晚不再提前锁；测试双侧（宽限内不锁/超宽限锁）真实。
- **F64 ✅**：`borrow_from_reservation` 移除自提交，`fulfill_reservation`（调用方）统一 `self.db.commit()`——中途失败可整体回滚，链路核实无其他漏提交分支。
- **F67 ✅**：到账核销改 `min(fine_deducted, current outstanding)`，窗口内已缴清则差额回补 `actual_refund_amount`——"罚两遍"关闭。
- **F68 ✅**：迁移 048 生成列 `active_child_id`（status IN (5,1,4,6) 时=child_id，否则 NULL）+ 唯一索引；状态码与 `DepositStatus` 枚举逐项核对一致（PENDING=5/PAID=1/REFUNDING=4/REFUND_PENDING=6）；存量重复活跃押金修复 UPDATE（保留 MAX(id)）；测试 DB 拦截用例真实。
- **F31 ✅**：`generate_due_reports` per-child 失败捕获后写 user_id=0 告警（7 天去重，F7 模式），随外层 commit 落库。
- **F69/F74/F75 ✅**：`borrow_book` 副本行锁；押金回调幂等（PAID 直接返回、非 PENDING 抛 ConflictError）；`trade_state != SUCCESS` 不标记已支付。`RefundCreate.damage_amount` 死字段已删；退款申请落订单原额。
- **F22/F26 ✅**：同孩子同类型 PENDING 订单拦截（亲子课走既有行锁分支）；`trade_no` 服务层前置检查 + 迁移 049 DB 唯一索引兜底。
- **F25 ✅**：365 天窗口 `timedelta(365)`（闰年 2/29 不再 replace 崩）。
- **F21/F28 ✅**：转让目标补 ALUMNI 拒绝；`update_order_status` 全状态转移矩阵（REFUNDED 终态禁出、CLOSED→PAID 保留 F5 迟到支付）。
- **F65/F66 ✅**：预约列表补 `expire_time`、路由 status 服务端筛选（前端不再只滤第 1 页）；取书响应带孩子名、前端成功提示显示。
- **F71 ✅**：表结构.md 状态流转笔误修正；死代码/死前端 API 删除；`book_waitlist` 活跃唯一生成列（WAITING/NOTIFIED 时 child×book）；预约过期通知用户本人（`expire_reservations` 成功过期后发 SystemMessage）。
- **迁移 048/049 ✅**：alembic upgrade head + check 实测 "No new upgrade operations detected"（head=d8e9f0a1b2c3）；迁移文件含列 comment 防漂移。
- **文档收口批 ✅**：a944181 的 backend 改动均为 ruff format 纯格式化（逐 diff 核对无逻辑变化）；DEPLOY_CHECKLIST 已覆盖迁移 043-049 + 环境变量 + 上线动作；HANDOFF/checkpoint/CLAUDE/ARCHITECTURE/K3 五件套基线一致。

### 本批新发现并已修复

**P1 — F62 漏改家长申诉入口（`damage_admin_service.py:489`）**：`confirm_expired`/`batch_confirm_expired` 已按 `reviewed_at or create_time` 计算 7 天申诉期，但 `appeal()`（家长申诉）仍用 `report.create_time.date()`。运行时复现：报告创建 10 天、双人复核通过仅 2 天 → `confirm_expired` 正确拒绝自动确认，但家长 `appeal` 被错误拒绝"已超过7天申诉期（10天）"——需双人复核的报告家长申诉通道实际不可用。修复：`appeal()` 改用 `reviewed_at or create_time`（与其余两处同口径）；补 2 用例（复核 2 天可申诉 / 复核 8 天拒绝）；既有 `test_appeal_after_7_days_rejected` 仅前移 create_time 未前移 reviewed_at（复核时间仍是现在），已按 F62 语义修正模拟方式（前移 reviewed_at=8 天前），非绕过测试。

**P2 — HANDOFF.md §二 历史快照未标注**：标题"当前状态（2026-07-23 终端验证）"下是 391/5、189/1240、54 tables、十关等旧数字，准绳文档内易误读为现行基线。已改标题为"历史快照（非现行基线）"并加指向 K3 交接 §一 的标注；命令块注明"当时为十关，现行十一关见 §八"。

**P3 — `refund_application_steps.py` 审核拒绝断言含 403 死分支**：审核端点正常返回 201（管理员有权限），403 无实际路径；收窄为 `(200, 201)`，退款 feature 10 场景复跑全绿。

### 门禁（本人实测，开发机）

pytest **702 passed / 0 failed**（+2：F62 appeal 双侧用例）· behave **211 场景 / 1369 步**（含 403 收窄后复跑）· ruff check×2 + format --check . **435 files** 全过 · contract OK · 模型一致 **56 表** PASSED · wiring --strict 通过 · gen_config_doc --check OK（**66 项**）· integration **56/56** · alembic upgrade head + check **无漂移**（head=d8e9f0a1b2c3）。基线五件套（HANDOFF/checkpoint/CLAUDE/ARCHITECTURE/K3）已同步 **702**。

### 下一步

1. 甲方阻塞项维持：F41/F57/F63/F71-①/F73/F6/F9/F10/F3 + D 档五项 + C 档上线动作（iOS 微信审核人工确认、迁移 043-049 低峰、REDIS_LOCK_FAIL_OPEN=false、商户后台退款通知地址核对）。
2. P3 待排期维持：F15-F20/F23/F24/F27/F29/F30/F33/F34、user 列表 child_status 筛选摆设、存量 RETURNED 罚款回补（运营决策）、孤儿借阅人工盘点。
3. 精装收口完成：除外部阻塞项外，代码侧与文档侧均达"拎包入住"状态。
