# librio 审查修复批次 3 报告（2026-08-08 补写）

> 说明：批次 3 由前任开发大模型完成（tag `audit-fix-batch-3`），当时未产出独立汇总报告；
> 本报告依据 git 提交历史 + `audit_loop/fix-evidence/` 证据文件补写，逐项可复核。
> 存档说明：本文为 2026-08-08 补写记录（含当时基线）。现行基线以
> 《专家意见/项目交接-20260809.md》为准。

---

## 一、交付总览（P3 安全/权限 15 项）

按《修复方案-按图施工手册》§3 批次 3 执行，15 项全部闭环。
批次完成基线：pytest **783 passed** / ruff **461 files**（commit `1c51da1`，当时实测）。

| F 编号 | 内容 | commit |
|---|---|---|
| F-011 | 音频实例 onUnload 销毁（纯前端，需前端验证） | `c456d2b` |
| F-012/F-034 | 前端按钮 data-perm 与后端权限对齐 | `c456d2b` |
| F-020/F-041 | SMS phone + openid 日志脱敏 | `c456d2b` |
| F-021 | trace_id contextvar + TraceIdFilter 全链路透传 | `c456d2b` |
| F-022 | 21 处 logger.error(f) 补 exc_info=True | `c456d2b` |
| F-042 | mark_observation_viewed 归属校验（IDOR） | `c456d2b` |
| F-049 | admin_page 未登记页面 fail-closed | `c456d2b` |
| F-057 | 取题答案剥离（用户路径去答案、admin 出卷保留） | `ec1c472` |
| F-062 | create_admin 角色层级校验（非超管不能创建同级/上级） | `c456d2b` |
| F-070 | _USER_GROUP_MAP 补 EXPIRED/EXITED/ALUMNI | `c456d2b` |
| F-082 | users 导出去 openid + CSV 公式注入前缀 | `c456d2b` |
| F-043/F-106 | 随 F-023 同批修复 | `c456d2b` |

另：`ca86b0b` 批内 ruff format（9 文件）。

---

## 二、关键修复细节

### F-057 取题答案剥离（本批核心）
用户取题路径改为 `QuestionPublicResponse`（无 correct_answer/explanation）；
管理端出卷保留完整字段。F-057 的 response_model `extra="forbid"` 约束：service 返回字段
必须精确匹配 schema，否则 500——此后所有 response_model 改动均需逐字段核对。

### F-049 fail-closed
admin_page 未登记页面拒绝渲染（原未登记路径可能静默开放），消除管理端页面级后门。

### F-021 trace_id 全链路
contextvar + logging Filter 透传，请求日志/异常日志可跨中间件与任务关联。

---

## 三、证据

- `audit_loop/fix-evidence/B3-evidence.txt`（15 项逐条 + 测试/回归结果）

验收：批末全量门禁 + `git tag audit-fix-batch-3`；前端项（F-011/012/034）标注
"需前端验证"（无浏览器环境）。
