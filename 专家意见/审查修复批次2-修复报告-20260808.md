# librio 审查修复批次 2 报告（2026-08-08 补写）

> 说明：批次 2 由前任开发大模型完成（tag `audit-fix-batch-2`），当时未产出独立汇总报告；
> 本报告依据 git 提交历史 + `audit_loop/fix-evidence/` 证据文件补写，逐项可复核。

---

## 一、交付总览（P3 资金/并发 19 项）

按《修复方案-按图施工手册》§2 批次 2 执行，19 项全部闭环。
批次完成基线：pytest **776 passed** / ruff **460 files**（commit `2be6b67`，当时实测）。

| 组 | 内容 | commit |
|---|---|---|
| F-005/006/007/009 | 退款/押金/回调状态转移守卫 | `258bf1d` |
| F-030/031/032/033 | 网关失败窗口/乱序回调/舍入（ROUND_HALF_UP）/int 截断 | `18426f1` |
| F-047/054/055 | 首次免罚并发（当前读）/重缴拦截/宽限期不消耗免罚额度 | `1eff529` |
| F-058/066/067/075/076/078/107 | 先查后改行锁族（7 入口）+ 迁移 051 | `82c2cc0` |
| F-119 | 音频查重（同书同页/全文唯一，应用层定稿） | `04f12f6` |

另：`d50c61b` 批内 ruff format。

---

## 二、关键修复细节

### F-030/031/032/033 资金守卫与舍入
- F-030：pay_deposit/repay_deposit prepay 失败保持 PENDING（原置 UNPAID 会丢网络超时
  窗口的回调匹配；超时由 F78 复位任务兜底）
- F-031：handle_refund_failed 对已 REFUNDED 订单忽略乱序回调（不覆盖终态）
- F-032：订单金额 7 处 quantize 统一 ROUND_HALF_UP（332/383/638/658/763 + damage fine）
- F-033：deposit 三处 int(x*100) 截断改 yuan_to_cents 四舍五入

### F-047 首次免罚并发（本批最重要教训）
`apply_fine` 免罚判定加 child 行锁 + `is_first_overdue` 的 count 改当前读
（`with_for_update`）——MySQL REPEATABLE READ 下"先锁后查"仍需当前读才能看到并发已提交行。
**MySQL 实证：修复前双免孩子 10/10，修复后 0/10**（RED 10/10 → GREEN 0/10）。

### F-058/066/067/075/076/078/107 行锁族（模式族 A/B）
先查后改无锁 ×7 统一加 `with_for_update`：review_submission/pay_fines（+迁移 051
active_fine 活跃唯一 DB 兜底）/亲子课 slot 名额/活动报名（MySQL 实证并发 8 条→恰 1 条）/
save_book_page_admin/benefit 审核/create_report 查重。

### F-119 音频查重（专家 B4 定稿）
应用层查重为主（覆盖全文 NULL 场景，DB 唯一索引对 NULL 不生效），复杂场景只做应用层。

---

## 三、证据

- `audit_loop/fix-evidence/B2-F005-009-evidence.txt`
- `audit_loop/fix-evidence/B2-F030-033-evidence.txt`
- `audit_loop/fix-evidence/B2-F047-054-055-evidence.txt`
- `audit_loop/fix-evidence/B2-locks-evidence.txt`（含 MySQL 场景 I 实证）
- `audit_loop/fix-evidence/B2-F119-evidence.txt`

验收：批末全量门禁 + MySQL 并发实证，`git tag audit-fix-batch-2`。
