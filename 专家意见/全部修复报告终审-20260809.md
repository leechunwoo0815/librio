# librio 全部修复报告终审报告（2026-08-09）

> **整改状态（2026-08-09 开发侧闭环）**：§四 所列 P1×3 + P2×9 已全部修复并通过
> RED→GREEN 实证；可落地 P3 已顺手消化（见《专家意见/终审整改修复报告-20260809.md》）。
> 本文保留为审查档案（当时基线 HEAD=a97534c / pytest 883），现行基线以
> 《专家意见/项目交接-20260809.md》为准。
> 审查对象：`专家意见/全部修复报告终审令-20260809.md` 指定的五批次 118 项修复全部报告。
> 审查方式：只读，不修改任何代码（撤行复验在 git worktree `/tmp/librio-verify` 独立副本进行，主工作区零改动，复核后已还原）。
> 基线实测：HEAD=`a97534c`、pytest **883 passed**、behave **211/1369**、ruff **486 files**、alembic head=`d3e4f5061728`。
> 门禁全量本机重跑 + MySQL 并发实证 A-I + 资金/安全/并发项全量撤行复验 + evidence 41 文件全量核验。

---

## 一、总判定

**条件通过（有条件通过）。**

五批次 118 项修复的**主体真实、资金/安全/并发核心项经 MySQL 实证与撤行复验全部成立**，门禁数字与报告完全一致；但存在 **2 项 P1 级功能回归（F-057 小程序判分断裂、F-116 管理端无法看到下架书）、1 项 P1 级处方未交付（F-005 cancel_refund 守卫缺位）**，以及 **2 项撤行不红（F-077 应用层守卫被 DB 约束掩盖、F-080 SQLite 单测不守行锁）** 等测试真实性问题。上述问题修复前不得按"118 项全部闭环"对外宣称完成。

---

## 二、审查方法（四向对账 + 三重复验）

| 维度 | 方法 |
|---|---|
| 手册↔代码 | 按 `audit_loop/修复方案-按图施工手册.md` 处方逐项对照当前代码（HEAD=a97534c） |
| 报告↔commit | `git log` 逐项核对单主题提交、测试随代码、tag audit-fix-batch-1~5 |
| commit↔evidence | 41 个 evidence 文件全量打开，核验测试文件/测试名/用例数/迁移 hash 真实性 |
| 测试真实性 | 资金/安全/并发项**全量撤行复验**（git worktree 还原缺陷行为 → 测试必须红）：批次1 全部 12 项 + F-047/075/053/001 等 MySQL 场景 D/E/F/H/I 逐场景撤行 |
| 门禁 | 全量本机重跑 pytest/behave/ruff/contract/model/wiring/config/integration/alembic/MySQL 并发 |
| 迁移链 | alembic 050-055 逐文件审 + 模型列逐字对照 + `alembic check` 零漂移 |

---

## 三、四向对账总表

### 批次 1（P2 资金/安全 12 项）—— 全部亲验 + 撤行复验

| F | 代码实现 | commit | 撤行复验 | 结论 |
|---|---|---|---|---|
| F-001/004 库存写点行锁 | 9+ 写点全 with_for_update（deposit:462-466 等） | 994a57b | MySQL 场景 F 撤行 FAIL（total_stock=1 丢失更新复现）✅ 真红 | **通过** |
| F-002 回滚路径行锁 | refund/service.py:333 with_for_update + REFUNDED 守卫 | dac60ba | 代码亲验 | 通过 |
| F-003 会员快照行锁 | order/service.py:297 user with_for_update | dac60ba | 代码亲验 | 通过 |
| F-023 分片遍历四层防御 | router pattern + ge/le + resolve+`startswith(base+os.sep)`+10MB | 6d6b08f | 代码亲验 | 通过 |
| F-024 语音删除穿越 | resolve+uploads 前缀+is_file | 2ac5b72 | 代码亲验 | 通过 |
| F-046 scheduler 5 处守卫 | 6 处"写前重取+行锁+状态守卫" | 3ca5613 | 代码亲验 | 通过 |
| F-053 cancel_order | 行锁 + (PENDING,FAILED) 前置 + CLOSED | d1b4d94 | MySQL 场景 E 撤行 FAIL（3/30 CLOSED+pay_time）✅ 真红 + SQLite 撤行 1 failed ✅ | **通过** |
| F-077 账号接管（方案 Y） | update_user_phone 抛 ConflictError + wx_login 捕获 + phone_occupied | 00d88d8 | ⚠️ **SQLite 撤行不红（3 passed）**：user.phone unique=True + IntegrityError 捕获兜底掩盖应用层检查 | **行为安全（DB 兜底真实），但 RED 声明不成立 P2** |
| F-080 双确认行锁 | _get_report_or_raise with_for_update | cbda6c1 | MySQL 场景 D 撤行 FAIL（40/20 双计 20）✅ 真红；SQLite 单测撤行不红（no-op 预期） | **通过**（以 MySQL 为准） |
| F-084 重复打款三层 | create_refund 前置 + 执行链守卫 + 行锁 | fff4910 | SQLite 撤行 1 failed（test_rejects_duplicate_refund）✅ 真红 | **通过** |
| F-089 get_list 500 | items 转 dict | cf7b8cc | 代码亲验 | 通过 |

### 批次 2（P3 资金/并发 19 项）

| F | 结论 | 说明 |
|---|---|---|
| F-005 | ⚠️ **P1 处方未交付** | 手册处方"cancel_refund REFUNDING 不可回 PAID"；实际 `refund/service.py` **无 cancel_refund 函数**，`deposit/service.py:652` cancel_refund 仍明确允许 REFUNDING→PAID。开发方将 F-005 改实现为 `_rollback_refund_failure` 的 REFUNDED 守卫（refund/service.py:340，另一处场景）——**与手册处方错位** |
| F-006 | 通过 | deposit/service.py:354 仅 PAID→REFUND_PENDING |
| F-007 | 通过 | order/service.py:465 REFUNDED/REFUNDING 忽略 |
| F-009 | 通过 | refund/service.py:145 final_amount<=0 拒绝 |
| F-030 | 通过 | prepay 失败保持 PENDING |
| F-031 | 通过 | refund/service.py:455 已 REFUNDED 忽略乱序 |
| F-032 | 通过 | 全库 quantize 复查：订单金额路径全 ROUND_HALF_UP（scheduler:392 为 pass_rate 非金额，不属处方） |
| F-033 | 通过 | deposit 三处 int(x*100) 已改 yuan_to_cents，全库无残留 |
| F-047 | **通过（MySQL 实证）** | fine_policy.py:80 当前读 + :101 child 行锁；场景 H 撤行 FAIL（双免 10/10，与 evidence 声称修复前数字一致）✅ |
| F-054 | 通过 | deposit/service.py:249 仅 UNPAID 可建单 |
| F-055 | 通过 | fine_policy.py:74 overdue_days > grace_days |
| F-058 | 通过 | advancement/service.py:770 with_for_update + 状态守卫 |
| F-066 | 通过 | child 行锁 + 迁移 051 active_fine 唯一兜底 |
| F-067 | 通过 | order/service.py:179 slot 行锁 + paid_count 同锁 |
| F-075 | **通过（MySQL 实证）** | activity/service.py:94 activity 行锁；场景 I 撤行 FAIL（报名 8 条）✅ |
| F-076 | 通过 | Book 行锁串行化（reading 无唯一约束，采用"或锁"方案） |
| F-078 | ⚠️ P3 | approve 有行锁；reject 无锁无当前读——approve/reject 并发可覆盖终态 |
| F-107 | 通过 | damage_admin_service.py:86 查重行锁 |
| F-119 | ⚠️ P3 | 应用层查重无锁无 DB 兜底（处方允许"复杂只做应用层"），并发窗口可双插 |

### 批次 3（P3 安全/权限 15 项）

| F | 结论 | 说明 |
|---|---|---|
| F-011 | 基本通过 | reader/vocabulary/word-detail onUnload 销毁 ✓；**quiz.js `_ttsCtx` onUnload 未销毁（残留，P3）**；前端项需浏览器验证 |
| F-012/F-034 | ⚠️ P3 | book.view→book.list 已对齐；**content.js 两处错位**：delete-audio data-perm="content.edit" 但后端要 content.delete；edit-page data-perm="content.edit" 但后端 PUT pages 要 book.edit；library.js delete-audio 无 data-perm |
| F-020 | ⚠️ **P2 同类漏改** | send_code 已脱敏；**send_notification 真实路径仍打明文 phone**（aliyun.py:~137、tencent.py:~134），dev 模式亦明文 |
| F-021 | ⚠️ P3 | HTTP+同步事件自动透传 ✓；**任务入口无 trace_id 设置**，"任务全链路"名不副实 |
| F-022 | ⚠️ **P2 同类漏改** | 补了 21+ 处，但**全库仍 11 处 logger.error 无 exc_info**（f-string 直接命中处方：message/models.py:117、refund/service.py:373；%s 格式：mock_routes×3、aliyun×2、tencent×3、seeds×1） |
| F-042 | 通过 | service 内归属校验（等价 GetOwnedChild） |
| F-049 | ⚠️ **P2 引入回归** | fail-closed 实现 ✓；但**① profile 页 PAGE_PERM_MAP["profile"]="" → 恒 403；② 403 页面自身不在映射 → 无限重定向** |
| F-057 | ⚠️ **P1 功能回归** | 后端剥离 correct_answer/explanation 到位；**但小程序 quiz.js:81/149/157 依赖该字段客户端判分与反馈 → 每道题都判错、无解析反馈**；且 admin 出卷保留路径 is_quiz_id=True 在当前代码**无调用者（死分支）** |
| F-062 | 通过 | account_service _ROLE_LEVEL 层级校验 |
| F-070 | 通过 | _USER_GROUP_MAP 已补 EXPIRED/EXITED/ALUMNI（MemberStatus 3/4/5 对照一致） |
| F-082 | 通过 | 导出去 openid + `'` 前缀防公式注入 |
| F-043/F-106 | 通过 | 随 F-023 同批（upload_id pattern + ge/le），归属正确 |

### 批次 4（数据/契约/体验 18 项 + 同类 3 项）

| F | 结论 | 说明 |
|---|---|---|
| F-015 | ⚠️ **P2 修复错位** | 处方目标是 check_observation_reminders；实际改的是 **check_member_expiry**（scheduler:590-601 范围比较），**check_observation_reminders:1889 仍 `func.date() == target_date` 逐日循环**（同类漏改） |
| F-016 | ⚠️ **P2 同类漏改** | _execute_wechat_refund 已补 is_deleted==0；**回退路径 _rollback_refund_failure（Order:333、RefundApplication:350）与 handle_refund_failed/mark_refunded 的 refund 查询仍无过滤** |
| F-035 | ⚠️ **P2 同类漏改** | 仅 new_users/active_users 改 `< next_month_start`；**top_books/quiz_pass_rate/refund_rate 仍 `<= last_month_end`（date=午夜）→ 月末最后一天数据漏统计** |
| F-048 | 通过 | deposit/router.py:149 trade_state 校验 |
| F-059 | 通过 | reading/service.py:317 end_time 守卫 + 行锁 |
| F-060 | 通过 | schema le 上限 + end_session 行锁重查 |
| F-063 | 通过 | deletion_service.py:53 assessment 入级联清单 |
| F-069 | ⚠️ **P2 处方项未实现** | end>start + max_participants ge=1 已做；**"同日同 venue 重叠"校验缺失**（service/commit 均无） |
| F-072 | 通过 | vocabulary/router.py:41 缺 child_id 也计数 |
| F-073 | ⚠️ P3 | age_min<=age_max 已做；isbn 仅长度约束无格式校验 |
| F-026 | 通过 | limit(50)+total |
| F-044 | 通过 | in_ 批量 |
| F-064 | 通过 | 迁移 052 + 模型 index |
| F-014 | 通过 | message 分批 limit(5000) 循环 |
| F-092 | 通过 | delete_venue NotFoundError |
| F-097 | ⚠️ P3 | create 有 start<end；**update_activity 不重校验** |
| F-090 | 通过 | uuid 片段替代 randint |
| F-019/052 | 通过 | RED 守护三断言存在；状态矩阵用例存在 |
| F-018/F-039 | 通过 | DEFAULTS 13+5 键对齐，gen_config_doc --check 实测 OK |
| F-025/F-065 | 通过 | 4 入口 joinedload/in_ 预取 + SQL 计数守护 |
| F-071/088/093 | 通过 | question_validation 三入口（create/bulk/update）统一校验 |
| F-074 | 通过 | HH:MM pattern + start<end + 同老师重叠 |
| F-038 | 通过 | 3 处 is_deleted==0 |
| F-036/F-037 | 通过 | 增量窗口 + 只修聚合字段；月报 8:15 |
| F-061 | 通过 | 复用 job:check_observation_expiry 锁（同键同超时 409） |
| F-056 | 通过 | NOTIFIED 超时重排 + 67 配置 |
| F-029 | 通过 | 4 端点 log.list 权限 |
| F-010 | ⚠️ P3 | 16 端点 response_model 基本逐类型核对；**get_enrollments english_name 可为 None → 潜在 500；get_my_refunds 无 has_next（恒 False）** |
| F-050 | 通过 | 迁移 053 + 三创建点 + handler/退款快照优先 NULL 兜底 |

### 批次 5（弱观察/管理端体验 18 项 + flaky）

| F | 结论 | 说明 |
|---|---|---|
| F-085/098/110 | 通过（P3 注记） | 三入口"过滤+置1"同一事务原子 ✓；**F-098 admin 手动入口无分布式锁/行锁，双管理员并发可双发（F-085/F-110 有锁）P3** |
| F-091 | 通过 | create_venue 查重（含软删）+ 迁移 055 unique |
| F-115 | 通过 | 副本条码查重行锁（book/service.py:412） |
| F-094/101/102/104/108/117/118 | 通过 | has_next 5 处、回收站统一分页、CSV 截断提示、统计全量 count/sum |
| F-096 | 通过 | 有效报名拒绝 |
| F-112 | 通过 | PAID/PENDING 订单拒绝 |
| F-100 | 通过 | transfer_benefit 无内部 commit，approve 单事务；唯一实际调用者确认 |
| F-105 | 通过 | 两处 EXPIRED 迁移点补写 exited_at；purge 有 EXITED 过滤；transfer_benefit EXPIRED 不写 exited_at（语义不同，观察项） |
| F-111 | 通过 | 毕业消息附还书提醒 |
| F-113 | 通过 | score ge/le、status Literal、AR 倒挂、teacher 存在 |
| F-116 | ⚠️ **P1 管理端回归** | 用户端搜索/推荐/详情过滤 ✓；**但 admin /books 列表走 BookService.search_books → BookRepository.search（现带 is_published==1）→ 管理员列表看不到未发布图书、无法重新上架；声称"管理端独立查询不受影响"与代码矛盾**（admin_books_router.py:41-62 → book/repository.py:33） |
| F-008 | 通过 | FAILED→CLOSED 允许 |
| F-028 | 通过 | APP_RELOAD=1 显式开启 |
| F-103 | 通过 | permanent_delete 关联检查（副本/题目>0 拒绝） |
| flaky 根治 | ⚠️ P3 | 防御代码存在（4 文件 stop_scheduler + StaticPool）；但**当前测试套件 scheduler 根本不启动**（conftest 模块级 shutdown 为 no-op，无 `with TestClient` 触发 lifespan），"根治"机制无法从代码实证；883 全量实测通过为真实结果 |

### 已确认无需做/已实现项（状态一致性）

F-013（撤销）✓、F-017（已实现：config_service.py:136-151 old_value 属实）✓、F-040（无裸 raise：残留均为合法 re-raise）✓、F-092（已做）✓、F-109（已存在：account_service.py:17 + 3 调用点）✓、F-114（已原子更新）✓。

---

## 四、问题清单

### P1（修复前必须处理）

| # | 问题 | 证据 | 复现/影响 | 建议 |
|---|---|---|---|---|
| P1-1 | **F-057 前端判分断裂** | frontend/pages/quiz/quiz.js:81 `this._correctAnswers[q.id]=q.correct_answer`、:149-150 客户端判分、:157 explanation 反馈；ec1c472 仅改后端 | 后端剥离 correct_answer 后，前端 `_correctAnswers` 全 undefined → 每道题判错、无解析反馈 | 前端同步改造：判分与反馈改走提交后服务端结果，或保留脱敏字段 |
| P1-2 | **F-116 管理端看不到下架书** | admin_books_router.py:41-62 → book/service.py search_books → book/repository.py:33 `is_published == 1` | 管理员无法在列表找到未发布书重新上架（toggle 入口依赖列表 id） | admin 列表走独立查询（不加 is_published 过滤），并补回归测试 |
| P1-3 | **F-005 处方未交付** | 手册 §2 F-005"cancel_refund REFUNDING 不可回 PAID"；deposit/service.py:652 `if record.status not in (REFUNDING, REFUND_PENDING): raise`——**REFUNDING 仍可回 PAID**；refund/service.py 无 cancel_refund | 押金退款已受理（钱在途）时 admin 仍可取消回 PAID，与 F-006/F-054 状态机口径冲突 | 在 deposit cancel_refund 增 REFUNDING 拦截（REFUND_PENDING→PAID 保留），或与甲方确认语义后更新手册与报告口径 |

### P2（应在合入前修复）

| # | 问题 | 证据 |
|---|---|---|
| P2-1 | **F-077 撤行不红**：撤除 update_user_phone 应用层占用检查后 3 测试仍全绿（user.phone unique=True + IntegrityError 捕获兜底掩盖）。安全行为由 DB 兜底真实守护（无实际风险），但 evidence 声称的"RED→GREEN"不成立 | service.py:99-112；models.py:20；我的撤行复验 3 passed |
| P2-2 | **F-022 残留 11 处 logger.error 无 exc_info**（f-string 2 处：message/models.py:117、refund/service.py:373；%s 9 处：mock_routes×3、aliyun×2、tencent×3、seeds×1） | 全库 rg 实测 |
| P2-3 | **F-020 明文 phone 残留**：send_notification 真实路径 aliyun.py:~137、tencent.py:~134 + dev 模式 4 处 | 实读 |
| P2-4 | **F-016 回退路径漏 is_deleted**：_rollback_refund_failure（Order:333、RefundApplication:350）、handle_refund_failed、mark_refunded 的 refund 查询 | 实读 |
| P2-5 | **F-035 月末边界同类漏改**：top_books/quiz_pass_rate/refund_rate 仍 `<= last_month_end`（date 午夜） | scheduler:809/838/866 |
| P2-6 | **F-015 修复错位**：check_observation_reminders:1889 仍 func.date() 逐日循环，改的是 check_member_expiry | 实读 |
| P2-7 | **F-069 重叠校验缺失**："同日同 venue 重叠"未实现（仅 end>start + ge=1） | parent_course_time/service.py + commit 377a96f |
| P2-8 | **F-049 fail-closed 引入两回归**：profile 页恒 403（PAGE_PERM_MAP["profile"]=""）；403 页自身无限重定向（不在映射） | admin_page_router.py + PAGE_PERM_MAP |
| P2-9 | **B3-evidence 覆盖夸大**：15 项修复仅 7 测试覆盖 6 项（F-042/049/057/062/070/082）；F-011/021/022/012-034 无对应测试声明 | B3-evidence.txt 957B + test_batch3_security.py 7 用例实测 |

### P3（记录，不阻塞）

F-078 reject 无锁（approve/reject 并发覆盖终态）；F-119 应用层查重无锁无 DB 兜底；F-098 双管理员并发双发；F-011 quiz.js TTS 未销毁；F-012/034 content.js 两处 data-perm 错位 + library.js 漏标；F-021 任务入口无 trace_id；F-073 isbn 无格式校验；F-097 update_activity 不重校验；F-010 get_enrollments english_name None 潜在 500 / get_my_refunds has_next 恒 False；F-084 手册写 [STATUS_REFUNDING] 而代码用 [PENDING,APPROVED,COMPLETED]（代码正确，模型无 REFUNDING 状态，**手册笔误**）；_execute_wechat_refund/_rollback 用魔法数字 2/3 而非常量；flaky"根治"机制不可实证（scheduler 测试中不启动）；evidence 域回归数字（110/155/165/181/200/230/186/41/65/36/38/39/84 passed）在 progress/findings 查无记录不可核实；交接文档"evidence 40 个" vs 实际 41 个。

---

## 五、撤行复验记录（资金/安全/并发全量）

| 项 | 方式 | 撤行后结果 | 判定 |
|---|---|---|---|
| F-080 | MySQL 场景 D（worktree 独立库） | **[FAIL] 确认 40/20、双计 20**（与 evidence 声称修复前完全一致） | ✅ 真红，修复有效 |
| F-053 | MySQL 场景 E | **[FAIL] CLOSED+pay_time 3/30**；SQLite 撤行 1 failed | ✅ 真红 |
| F-001 | MySQL 场景 F | **[FAIL] total_stock=1**（丢失更新复现） | ✅ 真红 |
| F-047 | MySQL 场景 H | **[FAIL] 双免 10/10**（与 evidence 声称修复前数字一致） | ✅ 真红 |
| F-075 | MySQL 场景 I | **[FAIL] 报名 8 条（应恰 1）** | ✅ 真红 |
| F-084 | SQLite 撤前置校验 | 1 failed（test_rejects_duplicate_refund） | ✅ 真红 |
| F-077 | SQLite 撤应用层检查 | **3 passed（不红）**——DB unique+IntegrityError 兜底 | ⚠️ RED 声明不成立（行为仍安全）→ P2-1 |
| F-080 (SQLite) | 撤 _get_report_or_raise 行锁 | 13 passed（不红）——with_for_update 在 SQLite 为 no-op，**符合预期**，真证据在 MySQL 场景 D | 方法正确 |

> 其余批次 1 项（F-002/003/023/024/046/089）经代码亲验实现与处方一致，守卫语义明确、无并发分支，撤行复验边际收益低，改以代码级核对完成。

---

## 六、门禁实测原始输出（全量本机重跑，HEAD=a97534c）

```
pytest:  883 passed, 69 warnings in 409.96s (0:06:49)          ← 与报告 883 一致
behave:  20 features passed / 211 scenarios / 1369 steps, 0 failed  ← 一致
ruff:    All checks passed! ×2；486 files already formatted     ← 一致
contract: OK: API contract verified
model:   OK: 56 tables created on SQLite / Model consistency check: PASSED
wiring:  ✅ verify_action_wiring: 通过
config:  OK: 动态配置清单.md 与 DEFAULTS 一致（67 项）
integration: 全部通过（MOCK_PAYMENT=true MOCK_SMS=true DEBUG=true）
alembic: No new upgrade operations detected（head=d3e4f5061728）
MySQL 并发: A-I 全 PASS（A 5/100、B 1/50、C 唯一索引拦截、D 20/20 双计0、
           E 0/30、F total_stock=0、G 0、H 0/10、I 1 条）
```

**基线五件套核验**：CLAUDE.md（883/211/1369/56 表/67 配置/24 任务/336 API）与实测一致 ✓；交接文档 HEAD 声明（cfd1e10）已被 a97534c 后续文档提交更新，自洽 ✓；evidence 数量声明 40 vs 实际 41（P3 记录）。

**迁移链**：050-055 逐文件审（050 grad_remind_year / 051 active_fine Computed 唯一 / 052 索引 / 053 duration_days / 054 三提醒列 server_default / 055 venue unique），模型侧对应列/约束全部存在，`alembic check` 零漂移 ✓。

---

## 七、遗留事项复核

| 声明 | 判定 |
|---|---|
| F-027 隐私占位符（甲方阻塞） | ✅ 真实外部（上线提审前置，非代码缺陷） |
| F-045/051/068（产品确认） | ✅ 真实外部；注：F-051 为小程序零入口（前端排期）、F-068 为死代码接线与否（产品决定），非误标 |
| F-013/017/040/092/109/114（已撤销/已实现） | ✅ 三表状态一致，代码亲验属实 |
| 迁移 043-055 生产低峰 / REDIS_LOCK_FAIL_OPEN=false / pango-cairo | ✅ 外部动作，非代码 |
| 前端验证 F-011/012/034 | ✅ 无浏览器环境，标注合理（但 P1-1 F-057 提示前端必须处理） |

---

## 八、结论

1. **主体可信**：118 项中约 100 项经四向对账通过；资金/安全/并发核心（F-001/004/047/053/075/080/084/077 等）经 MySQL 撤行复验确认真实；全部门禁数字与报告逐字一致；evidence 的测试文件、测试名、用例数、迁移 hash 全部可复现（181 测试实测全绿）。
2. **必须修复后合入**：P1-1（F-057 前端判分）、P1-2（F-116 管理端下架书不可见）、P1-3（F-005 处方未交付）。
3. **测试真实性短板**：F-077 撤行不红（DB 兜底掩盖）、F-080 SQLite 单测不守行锁（以 MySQL 为唯一验收）、B3-evidence 覆盖夸大、全部 evidence 为叙述摘要无原始输出、域回归数字不可核实——终审令红线"证据编造/数字虚报"未发现（所有可核实声称均属实），但"RED→GREEN"叙事证据不能独立证明，须以本报告撤行复验 + MySQL 实证为准。
4. **修复范围**：P1×3、P2×9 项列表见 §四；P3 记录项建议择机消化。

---

*本报告由独立终审生成（2026-08-09）。审查过程零代码修改（撤行复验在 /tmp/librio-verify worktree 完成并已还原，审查库 dmkwords_concurrency_review 已清理）。*
