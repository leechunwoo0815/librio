# 专家审查：修复交付报告-20260720 的核验结论与方向纠偏

> 审查时间：2026-07-20
> 审查人：专家审查组
> 审查对象：《修复交付报告-20260720.md》
> 结论：**部分通过。1 项违反既有裁决打回重做；2 项流程违规要求立即纠偏；已交付的 7 项中 6 项验收通过。**

---

## 一、实测复核（我亲自跑的数据）

| 指标 | 报告声称 | 我的实测 | 判定 |
|------|---------|---------|------|
| ruff（backend + features） | 0 errors | All checks passed | ✅ 属实 |
| behave | 152 场景 / 1035 步 / 0 失败 | 17 features / 152 scenarios / 1035 steps 全过 | ✅ 属实 |
| pytest | 249 passed, 4 skipped | 249 passed, 4 skipped（忽略 test_report_pdf.py 后） | ✅ 属实（见问题 5） |
| alembic 迁移 | 未执行 | 本地库未执行（current=7a8b9c0d1e2f），我代执行 upgrade head 成功，check 无漂移 | ⚠️ 迁移有效，但交付前未自验（见问题 5） |
| @wip 残留 | 0 | grep 全 feature 目录，0 命中 | ✅ 属实 |
| @future | 4 处 | reading_stats×2 + voice_reading×2，behave.ini `~@future` 排除生效 | ✅ 属实，但其中 2 处不该是 @future（见问题 1） |

报告数字无注水，态度诚实，先肯定这一点。

---

## 二、逐项验收（7 项已交付）

| 任务 | 验收 | 证据 |
|------|------|------|
| T5.1 rbac @wip 移除 + 14 step | ✅ 通过（附 1 条改进建议） | `rbac.feature` 头部无 @wip；`rbac_steps.py` 664 行、50 个 step 函数、含 403/404 权限断言 |
| T5.2 reading_stats step 重写 | 🟡 一半通过，一半打回 | 趋势/周报等 step 已改严格断言（`== 200` + 字段级校验），这部分合格；**月报/分享的 @future 处置违反裁决，见问题 1** |
| T5.3 voice_reading @future | ✅ 通过 | 2 场景标记正确，与 PRD 16.4"后续版本"定位一致 |
| P1-1~3 三个索引 | ✅ 通过 | 模型 `index=True`、迁移文件内容正确、单 head、upgrade+check 实测通过。`reservation.expire_time` 字段名与模型一致 |
| P1-4 FavoritesRepository is_deleted | ✅ 通过 | `repository.py:62` 过滤条件已补 |
| P1-9 密码 min_length=8 | ✅ 通过 | `admin_auth_router.py:30` 已改 |
| P1-14 消息权限 verify only | ✅ 通过 | 未改动代码，仅确认现有配置完整，符合"verify only"声明 |

**改进建议（不打回）**：`rbac_steps.py` 第 292/483/582/648/658 行存在 `if response.status_code == 200:` 条件断言——返回非 200 时静默跳过后续字段断言，有隐性假绿风险。建议改为"先断言状态码，再断言字段"的两段式。下轮顺手修，不阻塞。

---

## 三、必须打回的问题（1 项）

### 🔴 问题 1：T5.2 月报/分享场景的处置违反既有裁决

我在《专家答复-15拷问批复与审计偏差修正.md》§三拷问⑥ 中白纸黑字裁决：

> **月报**：`report/service.py:544` 已有 `generate_monthly_report()`，只是没注册路由。**补路由（1-2h）+ step 改严格断言**。
> **分享**：`POST /report/share` 根本不该存在——分享图是前端 Canvas 实现。**删除该场景或改写为前端 Canvas 断言**。

你的实际交付：两个场景都标 `@future` 挂起。

这不是"发现审计偏差按黄灯区流程报告"，是直接无视已下达的裁决。且 `@future` 的语义是"功能尚未实现，属于未来版本"——月报的 service 层已实现，只差一个路由注册，把它标 @future 等于把一个 1-2 小时的任务伪装成"未来版本工作"无限期挂起。**这正是 Grill-4 禁止的行为：用标记手段替代修复。**

**打回重做要求**：
1. 补 `GET /report/stats/monthly` 路由（调 `generate_monthly_report`，参照同文件 `/stats/weekly` 的写法），加 response_model + 认证 + child 归属校验
2. 月报 step 去掉 @future，改严格断言（`== 200` + 关键字段校验）
3. 分享场景去掉 @future，按裁决二选一：删除场景（推荐，因分享图纯前端实现），或改写 step 为"验证分享按钮配置存在"的前端逻辑断言
4. 补 1 个路由单元测试（认证缺失 401、正常 200）

---

## 四、流程违规（2 项，立即纠偏）

### 🟠 问题 2：0B 代码基线调查报告未交付

批准的执行基线写明：**"Day 0：开工 0B 代码基线调查 → 产出报告 → 我审查（0.5 天内回复）"**，并且明确"0B 不是可选项，是整个计划成立的地基"。

本轮交付没有 0B 报告。你跳过地基直接做了阶段 5 的部分任务。虽然我已经代查了其中 3 项（favorites 存量、试读拦截位置、月报路由缺失）并在答复文件中给了证据，但 0B 的完整 8 项（押金枚举、书架模型、退款计算、重考冷却、迟到回调、BDD 假绿根因、预约枚举、book.price）需要你自己逐项确认并签字——**这不是形式主义，后续 T2.x 的每个改动都要引用 0B 证据**。

### 🟠 问题 3：未索求 D1-D10 决策就开工

计划红线：**"开发大模型开工前必须拿到书面决策，禁止自行选择。"**

你的报告把 T1.2/T1.6 标为"需业务决策"——说明你知道有决策依赖，但没有向产品负责人发出正式的 D1-D10 决策请求，而是绕开阶段 0/1/2/3，直接做了不依赖决策的阶段 5 任务和另一份技术报告的 P1 项。

**这个行为本身可以理解（不等空转），值得肯定；但流程上欠一个动作：你必须现在就发出决策请求，而不是把"需决策"写在报告里等我来问。**

---

## 五、其他发现（不追责，记录在案）

1. **pytest collection error**：`test_report_pdf.py` 在 macOS 上 collection 阶段报 `OSError: cannot load library 'libpango'`。原因：文件内 `try/except ImportError` 接不住 `OSError`。这是既有问题非本次引入，但顺手修复成本是 1 行（`except (ImportError, OSError)`），建议下轮带上。
2. **alembic 迁移未自验**：交付时连本地开发库都未执行 upgrade。迁移内容本身正确（我已代为验证通过），但交付标准应是"本地库已执行 + check 通过"。风险提示里写"尚未在生产数据库执行"是负责任的，但应该连本地也一起说清楚。
3. **范围说明**：本轮 P1-1~14 系列来自《DmkWords_V3.8_全量技术终审报告.md》，不是我的需求审计整改计划表（T0-T6）编号。这些修复有价值，但**我的计划表主体（阶段 0 决策、阶段 1 资金安全、阶段 2 状态机、阶段 3 规则补全、阶段 4 代码对齐）一项未动**。后续报告请把两套编号体系分开列示，避免混淆进度口径。

---

## 六、后续方向（按此顺序执行）

### 立即（本轮收尾，预计 0.5 天）

1. **打回项重做**：按问题 1 的 4 条要求修复月报路由 + 分享场景
2. **顺手修**：`test_report_pdf.py` 的 `except (ImportError, OSError)`；`rbac_steps.py` 条件断言改两段式
3. 全量回归（含 alembic upgrade + check）后重新提交本轮报告

### 本周内（回到批准的基线，预计 1 天）

4. **补交 0B 报告**：8 项逐项 grep 确认 + 证据 + 签字。其中 3 项（favorites 存量、试读拦截位置、月报路由缺失）可直接引用我答复文件中的证据，但需你复核确认
5. **发出 D1-D10 决策请求**：书面提交产品负责人（附上我为每项准备的技术立场与默认方案），同时发出 D11（开通 24h 内退款口径）和押金商业风险提示

### 决策到达后（按批准的 Week 排期）

6. T1.2/T1.3/T1.5 文档+代码+测试三件套双流任务
7. T2.1/T2.4/T2.7/T3.6b 纯文档任务（这些已不依赖决策，**其实现在就可以做**——它们在我的答复文件里已全部给出最终文案，建议你将其提前到等待决策期间完成）

---

## 七、给开发大模型的一句话

交付质量本身不错，数字诚实，测试修复的断言rewrite 是合格工程。但**裁决就是裁决**——月报标 @future 不是"灵活处理"，是违反已下达的决定；流程上 0B 和决策请求也不是可选项。先补这三样，再继续。

*审查完毕。重做项完成后重新提交，我 24h 内核验。*
