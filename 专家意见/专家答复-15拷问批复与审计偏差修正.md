# 专家答复：开发大模型 15 拷问的批复与审计偏差修正

> 答复时间：2026-07-19
> 答复人：专家审查组
> 针对：《开发大模型-审计理解与苏格拉底拷问.md》
> 本文件性质：执行基线的正式修订，与原计划冲突处以本文件为准

---

## 〇、先给结论

1. **你的执行计划调整（0A/0B 并行、文档代码双流、测试提前、9 天排期）——有条件批准**，条件见 §一。
2. **拷问⑨⑬⑫⑥逼我去查了代码，查证结果改变了 3 个任务的性质**（2 个 P0 从"文档+代码"降级为"纯文档"，1 个 P0 修复方向需要修正）。
3. **我认领 2 个审计偏差**：押金枚举漏报 2 个状态；预约状态补丁的枚举值与代码冲突。修正方案见 §四。
4. **3 个开放问题立即关闭**（图书定价字段、月报 API 修复路径、重考冷却起点），新增 1 个决策点（D11 开通即退款）。

---

## 一、执行计划调整的批复

| 你的调整 | 批复 | 附加条件 |
|---------|------|---------|
| 0A 决策 + 0B 代码基线并行 | ✅ 批准 | 0B 产出《代码现状基线调查报告》必须覆盖你列的 8 项 + 我补充的 3 项（见 §二），完成后发我审查再进阶段 1 |
| T1.2/1.3/1.5 文档代码双流同步 | ✅ 批准 | 每个双流任务在同一 PR 中包含：文档 diff + 代码 diff + 新测试 diff，三者缺一不合入 |
| 测试修复提前（T5.1/5.3/5.5） | ✅ 批准 | T5.2 例外，必须等 0B-6 的根因证据（我已提前查了，见 §三拷问⑥，可直接用） |
| 9 天排期 | ✅ 有条件批准 | 条件：0B 结果若把任何"纯文档"任务翻转为"文档+代码"任务，工时重新评估，不许硬扛 9 天死线 |

对你 1.3 节"文档完美主义"批评的回应：接受。原计划"阶段 1-3 纯文档冻结代码"的瀑布节奏确实不适合在研项目，你的双流方案更优。但底线保留一条：**同一任务的文档与代码必须同 PR 落地**，不允许"文档先行合并、代码下周再说"——那才是四方漂移复发的根源。

---

## 二、五个优先拷问的答复（附代码证据）

### 拷问⑤：12.5 天估算是否包含代码反查验证？

**坦白回答：没有显式包含，这是估算缺陷。**

12.5 天的拆分是"修改动作"的成本，没有把每个任务"先 grep 现状、再验证不冲突"的反查成本单列。你的指控成立。修正后的估算模型：

- 0B 代码基线调查：**0.5-1 天**，作为阶段 0 强制前置（你提出，我采纳）
- 每个 T 任务的反查验证：计入任务自身工时，不单独列项——一个 T 任务的标准动作就是"查现状→改文档→改代码→验证"，而不是"改文档"四个字
- 因此你的 9 天并行方案在数学上成立：**前提是 0B 一次性把"文档 vs 代码谁对谁错"的不确定性全部消除**。0B 不是可选项，是整个计划成立的地基。

### 拷问⑧："挂起"是否允许不依赖开放问题的子任务先做？

**允许。挂起只锁定"直接依赖该决策的最小任务单元"，不搞连坐。**

规则明确为：
- 任务可拆的，拆开执行。T3.6 拆为：T3.6a 定级流程+拍照登记+申诉期（不依赖定价字段，先做）；T3.6b 定价字段来源与罚款金额联动（挂起等答复）。
- 每个被拆出的挂起子任务，在计划表中单独标"挂起-等决策#N"。
- 产品负责人响应 SLA：开放问题发出后 24 小时无答复，升级一次；48 小时无答复，该子任务按我给的默认方案执行并在 PRD 中标注"待业务确认"。**不允许无限期挂起阻塞交付。**

### 拷问⑨："已关停"在数据库中的实际存在情况？

**我已查证。结论：代码层从未存在，纯文档问题，零迁移成本。**

证据链：
- `backend/common/types.py:54-61` `MemberStatus` 枚举仅 5 个值：TRIAL=0 / OBSERVATION=1 / OFFICIAL=2 / EXPIRED=3 / EXITED=4
- 全后端 grep `已关停`、`status == 5`（会员语境）、`WITHDRAWN = 5`：**零命中**
- 枚举层不存在 status=5，数据库物理上不可能写入该值

**修复性质修正：T2.1 从"文档+数据迁移风险"降级为"纯文档任务"**——全流程文档 1.1 删除"已关停"概念即可，无任何数据迁移。

同时认领：**审计报告提出"删除已关停"时未附存量证据，是执行瑕疵。** 你引用 Grill-7 反将我——"要求开发者自查"而非"审计先给证据"——这一将将得对。规则补充：今后凡 P0 级"删除状态/删除字段"类修复建议，审计方必须先附各层存在性证据，再谈方案。

### 拷问⑬：书架容量矛盾，专家查过代码现状吗？

**现在查了。结论：场景 A 确认——代码已是 V3.5 无上限模型，纯文档问题。**

证据链：
- `backend/domain/bookshelf/service.py:50`：`limit = ConfigService.get_int(self.db, "bookshelf_limit", 0)`——默认 0 即无上限，仅当管理员显式配置正数才拦截
- `backend/domain/bookshelf/models.py:6` 注释："旧代码把 Bookshelf 当成借阅书架（STATUS_BORROWING + 20本上限），这是错误的"
- `backend/domain/admin/models.py:129`：配置默认值 `"bookshelf_limit": ("0", ...)`

**修复性质修正：T2.7 降级为半小时级纯文档任务**——全流程文档第四章整体替换为 V3.5 模型（补丁已在批次 1 给出），代码一行不动。

### 拷问⑮：发现审计偏差时，停等回复还是自行修正？

**选 B 的受控版本：按授权边界分级行动。**

| 区域 | 情形 | 行动 |
|------|------|------|
| 🟢 绿灯区 | 文档漏写代码已存在的字段/枚举；明显笔误；行号漂移 | 直接修正 + 工作报告中列"审计偏差清单"，不等回复 |
| 🟡 黄灯区 | 我的补丁与代码现状冲突，需要对齐方案（如枚举值选哪套） | 按"对齐代码现状"原则修正 + 24h 内报告 + 方案可回滚 |
| 🔴 红灯区 | 状态机增删、公式口径变更、任何资金规则、D1-D10 决策点 | **必须停下等回复**，最长等 24h，超时升级 |

本次对话中已有两个活生生的例子，按此规则处理见 §四。

---

## 三、其余拷问逐条答复

### 拷问①（iOS A+B 双线维护成本）
**采纳你的修正。** A 为主力方案，B 为配置开关备用的退路——开关存在但默认 `online`，不要求双态同时在线运营。拒审后切 B，24h 内完成。维护成本 = 1 个配置项 + 1 处条件渲染，可控。T1.1 补丁按此修订。

### 拷问②（"已关停"的业务动机）
无从考证文档作者动机，但你的工程解法正确且我正式采纳：**运营分层需求（刚过期 vs 过期 3 个月）用"过期天数"派生维度解决（`now() - member_expire_time`），不进入状态机。** 召回 push 按过期天数分段（7 天内/30 天内/30 天+）配置化实现，与会员状态枚举解耦。

### 拷问③（favorites 废弃的技术方案）
明确三步，禁止 UNION 永久双轨：
1. 0B 新增一项：查 favorites 表存量（行数 + 与 bookshelf 的重合度）
2. 若为空或全重合 → 表转只读、前端无入口、写入路径全禁，列入 V3.9 物理删除清单
3. 若有独有数据 → 一次性迁移脚本 `INSERT IGNORE INTO bookshelf SELECT ... FROM favorites`，验证后转只读
**"废弃只读"的完整定义 = 无前端入口 + 无新写入 + 历史数据一次性合并 + 下版本删表。**

### 拷问④（多孩 3600 押金门槛）
接受批评，审计立场升级：这不仅是"PRD 明示"问题，是商业风险。行动：在 D6 决策材料中附加书面风险提示——"3 孩家庭预付超 13000 元（押金 3600 + 会员费），需运营验证该门槛对多孩转化率的影响；需求层只能保证规则透明，商业策略（如押金分期、家庭押金池）超出本文档范围，但若运营反馈负面，D6 需要重开。"

### 拷问⑥（T5.2 假绿修复成本）
**根因已查明，你的"4-6 小时"判断基于错误前提，实际更低。** 证据：`features/steps/reading_stats_steps.py:145` —— `assert context.response.status_code in (200, 404)`；`:161` —— `in (200, 201, 404)`。这就是"把断言改弱让它过"的实锤，且是人为写进去的豁免，不是框架限制。

修复路径决策（已定的，不用再论证）：
- **月报**：`report/service.py:544` 已有 `generate_monthly_report()` 方法，只是没注册路由。**补路由（1-2h）+ step 改严格断言**，而非"补完整 API"。
- **分享**：`POST /report/share` 根本不该存在——分享图是前端 Canvas 实现（learning-report.js 已落地）。**删除该场景或改写为"分享按钮调起前端 Canvas 生成"的断言**，不补后端端点。
2 天估算成立，且 T5.2 可以从 0B 完成后立即开工。

### 拷问⑦（定时任务 BDD 可测试性）
你的担心部分成立，但结论相反：**项目已有成熟的定时任务 step 模式**。证据：`borrow_record.feature` 的"当 定时任务在"2026-06-02"执行逾期检测"场景已在执行集内且通过——step 实现直接调 service/任务函数并注入时间上下文，不需要 freezegun。

但优先级我接受调整，前 6 强制项改为：**#1 季度会员、#2 半年会员、#3 会员升级、#4 缓冲期续费、#7 查词上限、#8 试读限制**（全是 API 层断言，无定时任务依赖）。定时任务提醒场景（#5/#6）降为第二优先级，复用 borrow_record 的 step 模式实现。

### 拷问⑩（冻结范围）
**明确为 B：仅冻结直接修改状态机/支付/退款/押金逻辑的 PR。**
- 线上 bug 修复豁免：PR 标题前缀 `[HOTFIX]`，合入后 24h 内必须补文档同步说明
- 非敏感功能（前端优化、文案、非契约 API）不冻结
- 冻结边界争议的裁决权在我，不在执行方

### 拷问⑪（退款下限 1 天 vs 0 天）
真问题，原补丁确实粗糙。修订为 D11 决策点提交产品负责人：

```
D11：开通后极短期退款的口径
方案：pay_time 起 24 小时内申请退款 → used_days = 0（全额退）；
      超过 24 小时 → 自然日口径，下限 1 天。
风控兜底：同一 user 同一商品类型，30 天内仅享 1 次"24h 全额退"，
      第 2 次起无论时长均按下限 1 天计（防恶意刷退）。
```

### 拷问⑫（并发测试落地方案）
给出本项目技术栈（FastAPI + TestClient + SQLite 测试库）的具体三层方案：

1. **不要求** DB 行锁语义的并发测试——SQLite 与 MySQL 行锁行为不同，测了是假证据，你的质疑完全正确
2. **API 层并发**：复用项目已有模式——`scripts/integration_test.py` Exception 5（并发预约库存，threading + TestClient）已在用，新并发场景照此模式写
3. **并发安全的本质保障在代码层**（唯一约束、SELECT FOR UPDATE、乐观锁），验证方式 = 代码审查 + "唯一约束冲突时返回 409"的单测，而非强行模拟真并发
4. **MySQL 特有行为**列入 DEPLOY_CHECKLIST 的 staging 验证项，不在单元测试层强求

Grill-9 按此修订，原表述"必须有并发测试"修正为"必须有并发安全设计 + API 层冒烟并发用例（照 integration_test 模式）"。

### 拷问⑭（审计基准版本）
基准 = 2026-07-19 工作区文件（PRD 标识 V3.8 / 2026-07-15）。规则：
- 执行期间 PRD 被产品负责人修改 → **以最新版本为准**，与新版本冲突的补丁自动作废，冲突清单报我重新裁定
- 补丁不随版本自动有效；每次阶段开工前先 `git log` 确认文档无他人变更

### 问题 1（四方同步粒度）
认可链式修改。同步标准 = **同一 PR 合并前四方（实际五方：PRD/UML-ER/表结构/BDD/全流程文档）全部改完**，不要求同时改，允许链式传播。我之前说"四方"漏了全流程文档，是口误，修正为五方。

---

## 四、审计偏差认领与修正（2 项）

### 偏差 1：押金枚举漏报 2 个状态 —— T2.4 任务范围扩大

`backend/common/types.py:92-101` `DepositStatus` 实际有 **6 个值**：UNPAID=0、PAID=1、REFUNDED=2、DEDUCTED=3、REFUNDING=4、**PENDING=5（支付中）**、**REFUND_PENDING=6（退款待审核，V3.8 新增）**。

我的 P0-3 只报了"缺 4=退款中"，漏报了 5 和 6。**修正：T2.4 的任务是把表结构文档对齐到代码的全部 6 个状态**（含状态流转图补 PENDING→PAID、REFUND_PENDING→REFUNDING 两条边），不是只补一个 4。按拷问⑮规则，这属 🟢 绿灯区，你直接修正并列入偏差清单。

### 偏差 2：预约状态补丁的枚举值与代码冲突 —— 补丁作废重发

`backend/common/types.py:109-115` `ReservationStatus` = PENDING(0) / **FULFILLED(1)** / **EXPIRED(2)** / **CANCELLED(3)**，无"已备"。

我原补丁拍的"待取(0)/已取(2)/已取消(3)/已过期(4)"是按表结构文档的编号惯性拍的，**与代码枚举值冲突**——表结构文档"0=待取 1=已备 2=已取 3=取消"的 1 是"已备"，代码的 1 是"已取"，若按我的补丁改表结构，2=已取又会撞上代码的 2=已过期。

**修正方案（对齐代码现状，代码不动）**：
```
reservation.status 统一定义（对齐 backend/common/types.py）：
0 = PENDING   待取
1 = FULFILLED 已取
2 = EXPIRED   已过期（超时自动取消）
3 = CANCELLED 已取消（用户主动取消）
删除表结构文档中的"已备"；UML-ER 1.8、PRD 第 8 章、reservation.feature 同步对齐。
```
按拷问⑮规则，这属 🟡 黄灯区——我主动给出修正，你按新方案执行，无需再停等。

### 顺带关闭 3 个开放问题

| 原开放问题 | 查证结果 | 结论 |
|-----------|---------|------|
| #1 图书定价字段来源 | `backend/domain/book/models.py:60-61` 已有 `price = Column(Numeric(10,2), nullable=True, comment="图书定价（元），用于丢书罚款计算")` | **关闭**。表结构文档漏写，补文档即可；T3.6b 解除挂起，罚款=price×倍率 可直接引用 |
| #4 体验用户试读与音频关系 | —（本次未查到直接证据） | **保留开放**，0B 新增一项：查试读拦截的代码位置 |
| D10 冷却起点 | `advancement/service.py:110` `Quiz.create_time > now_utc - timedelta(hours=1)`，从创建时刻算 | **关闭**。你的补充与代码一致，文档按"从测验创建时刻起算 60 分钟，可配置 quiz_cooldown_minutes"写 |

---

## 五、补丁复核的 3 组答复（你的第五部分）

### 5.1 退款公式
- 同日退款：见拷问⑪ 的 D11 方案（24h 内全额退 + 30 天限 1 次）
- 闰年口径白纸黑字确认：**固定天数制**。`member_expire_time = start + N×24h`，年费恒 365 天，闰年不加天；`used_days` = 日历日期相减的自然日差。该组合在闰年区间会让用户单日成本略低（366 天按 365 除），方向有利于用户、不利于平台，可接受，无须修正，写明即可

### 5.2 罚款超押金
- `outstanding_fines` **只记总额**（维持现状字段），分笔明细由 `borrow_record.fine_amount` + `operation_log` 承担，不新建欠款明细表（YAGNI）
- 5 项禁令**非永久**：`outstanding_fines` 缴清归零即自动全部解除，无须人工干预

### 5.3 iOS 开关
- 读取时机：页面渲染时读，配置 TTL 60s 的生效延迟可接受
- Android 不读此配置，始终 online；后端不按 user-agent 区分返回，差异全在前端渲染层
- `offline_guide` 链路：管理员手动后台创建订单（pay_type=对公转账），触发 order.paid 事件的角色是**管理员**；银行自动对账超出本期范围，列 V3.9 backlog

---

## 六、对你 D1-D10 技术立场的裁决

| 决策点 | 你的立场 | 裁决 |
|--------|---------|------|
| D1 | A 为主 B 备用，开关默认关 | ✅ 采纳，替代我原"A+B 同时在线"的表述 |
| D2 | 删除+存量核查 | ✅ 采纳；存量已查（零存在），直接删除 |
| D3 | 补丁方案+重试方式存疑 | ✅ 采纳；**重试方式裁定为人工**：退款失败→回退已支付(1)+死信告警+管理员手动重新发起，自动重试的幂等设计不进本期 |
| D4 | 删除已备 | ✅ 采纳；按 §四偏差 2 的修正枚举执行 |
| D5 | 统一+先查存量 | ✅ 采纳；0B 加查 favorites 存量，按拷问③三步走 |
| D6/D7 | 独立押金/独立信用+商业警示 | ✅ 采纳；按拷问④附加书面风险提示进决策材料 |
| D8 | 先还书挂账 | ✅ 维持 |
| D9 | 30 人+配置化 | ✅ 采纳；`activity_capacity_default` 进配置清单，种子数据修正 |
| D10 | 60 分钟可配置+从提交时刻算 | ✅ 采纳；代码已一致，纯文档+配置化任务 |

---

## 七、修订后的执行基线（以此为准）

1. **Day 0**：你开工 0B 代码基线调查（8 项 + 我补的 3 项：favorites 存量、试读拦截位置、月报路由缺失确认——最后两项我已代查，直接引用本文件证据即可）→ 产出报告 → 我审查（0.5 天内回复）
2. **Day 1 起**：按你的 Week1/Week2 排期执行，双流任务同 PR 三件套（文档+代码+测试）
3. **偏差汇报**：按拷问⑮ 的三区规则行动，本文件已示范一次（§四）
4. **冻结纪律**：仅冻结契约层 PR，HOTFIX 豁免（拷问⑩）
5. **审查节奏**：每阶段末你提交工作报告，我按验收清单 24h 内核验

去干吧。0B 报告出来发我。
