# 首席质量终审官 · 20 关全量审查意见与 K3 整改执行单

> 日期：2026-08-03 ｜ 审查对象：HEAD `a7e3963`（144 commits）｜ 审查令：DmkWords V3.22 上线前终极全量审查令 FINAL-2.0
> 执行人：K3 ｜ 委托方：首席质量终审官（本文件为唯一整改依据，勿改动已判定"属实/不修"条目）
> 方式：全部命令实测，每项附 `文件:行号` 证据；未验证项一律标 ⚠️

---

## 〇、结论先行

1. **十关 CI 全绿属实**（10/10 Exit Code=0；pytest 548 / behave 210+1361 / 56 表 / 59 配置 / alembic 无漂移 / integration 56 步 / wiring 全过）。
2. **超级大审查 13 项处置已逐条复核，全部属实、已闭环**，无需返工（抽查证据见 §五）。
3. **仍有 3 项 P0、2 项 P1、10 项 P2 未闭环**（13 项修复轮未覆盖），本单为 K3 下一轮修复范围。
4. **审查令自身存在若干口径错误**（§七 勘误表），K3 不得据此"修"正确实现；请以本单为准。
5. 门禁：**P0 > 0 → 禁止上线**。P0/P1 全部闭环并复测后再走终审签署。

---

### 20 关逐关审查总览（验证点计数，含两轮全部审查）

| 关 | 名称 | 验证点 | 通过 | 失败 | 对应整改单 |
|----|------|:---:|:---:|:---:|------|
| 1 | CI 十关 | 10 | 10 | 0 | —（全绿） |
| 2 | 52 题决策落地 | 52 | 45 | 7 | P0-1/P0-2/P0-3、P1-1、P2-2/P2-3/P2-5 |
| 3 | 59 项配置 | 59 | 55 | 4 | P0-3、P1-2、P2-4 |
| 4 | 56 表对齐 | 8 | 8 | 0 | —（全过） |
| 5 | 架构合规 | 3 | 2 | 1 | P2-8 |
| 6 | 资金安全与并发 | 3 | 3 | 0 | —（全过） |
| 7 | 安全审计 | 3 | 2 | 1 | P1-1、P0-3（H5 隐私） |
| 8 | 前端全量审查 | 4 | 2 | 2 | P0-1、P0-2、P1-1、P2-5 |
| 9 | 测试全量审查 | 4 | 3 | 1 | P2-9（留档） |
| 10 | 定时任务 | 5 | 3 | 2 | P0-3（purge 缺位）、P2-7 |
| 11 | 文档一致性 | 3 | 1 | 2 | P2-6 |
| 12 | 运营与体验 | 4 | 2 | 2 | P0-1、P0-2、P1-1 |
| 13 | 部署与运维 | 2 | 2 | 0 | P2-1（新增部署检查项） |
| 14 | 遗留项确认 | 2 | 2 | 0 | —（全过） |
| 15 | 交叉验证与反向审查 | 3 | 3 | 0 | —（全过） |
| 16 | 视觉对齐闭环 | 5 | 5 | 0 | P2-10（留档） |
| 17 | 性能与可观测性 | 2 | 2 | 0 | —（全过） |
| 18 | 状态机完整性 | 6 | 6 | 0 | —（全过） |
| 19 | 上线门禁判定 | 2 | 1 | 1 | P0 清零前禁止上线 |
| 20 | 十席签署 | 10 | — | — | P0/P1 闭环后复签 |
| **合计** | | **119** | **103** | **16** | 唯一整改项见 §二/§三/§四 |

> 注：失败验证点与整改单为多对一映射（同一问题在多关被命中，不重复计数）。全部唯一整改项 = 3 P0 + 2 P1 + 10 P2。

---

## 一、K3 声明验证结果（复核基线）

| K3 声明项 | K3 报告原文（08-02） | 本终审实测（08-03 HEAD） | 结论 |
|---|---|---|---|
| pytest | **528 passed** | **548 passed**（528 + 20 新增回归） | ✅ 属实且递增 |
| behave | **210 scenarios / 1361 steps** | **210 / 1361 / 20 features / 0 失败** | ✅ 完全一致 |
| 数据库表 | 56 表 | 56 tables PASSED（新表 book_waitlist、fine_payment） | ✅ 属实 |
| 动态配置 | 59 项 | SystemConfig.DEFAULTS = 59 项 | ✅ 属实 |
| alembic head | `a5d2e8f33c10` | `c4e5f6a7b8c9`（= a5d2e8f33c10 + 043） | ✅ 演进合理，链完整 |
| 52 题落地 | 44 代码 + 6 文档/SOP | 44+6 属实；**但 3 处"部分实现"未披露**（P0-1/P0-2/P0-3） | ⚠️ 有夸大 |
| 十关全绿 | 全绿 | 10/10 全绿 | ✅ 属实 |

> ⚠️ 审查令中写的 "518 / f4c1a7d88eb9" 与 K3 报告原文（528 / a5d2e8f33c10）及当前代码均不符，为审查令基线笔误，不计入 K3 责任。

---

## 二、P0 整改单（3 项，上线阻断，按审查令"52 题部分实现即 P0"口径）

### P0-1 前端观察期报名页仍宣传"30 天 / ÷30"，与后端 45 天不一致

**证据（前端残留 10 处）**：
- `frontend/pages/order-pkg/observation/observation.wxml:18` "30 天沉浸式英语阅读之旅"
- `frontend/pages/order-pkg/observation/observation.wxml:23` "30 天体验期"
- `frontend/pages/order-pkg/observation/observation.wxml:27` "/ 30天"
- `frontend/pages/order-pkg/observation/observation.wxml:71` "30 天后生成个性化阅读能力分析报告"
- `frontend/pages/order-pkg/observation/observation.wxml:80` "计算公式：{{price}} / 30 x 剩余天数"
- `frontend/pages/order-pkg/observation/observation.wxml:108` "30 天观察期 · 不满意随时退款"
- `frontend/pages/order-pkg/observation/observation.js:14` "观察期为30天体验"
- `frontend/pages/order-pkg/official/official.js:38` 对比表 `observation: '30天'`
- `frontend/pages/agreement/service-agreement/service-agreement.wxml` "观察期会员（500 元/30 天）"

**后端正确实现（勿改）**：
- `backend/domain/refund/service.py:337-352`：`observation_days=45`、`refund_free_days=7`，`used = max(0, min(used_days, total_days) - free_days)`，÷observation_days
- `backend/domain/order/service.py:336`：升级抵扣按 observation_days
- `backend/domain/admin/models.py:110`：`observation_days = ("45", "int", ...)`

**影响**：宣传 30 天、实际 45 天；退款公式展示 ÷30、实际 ÷45。执行对家长更优，但属于合同性文案与系统不一致，上线前必须统一，避免客诉与合规争议。

**修复要求**：
1. 上述 10 处文案统一为 **45 天**；退款公式文案改为 "前 7 天全额退款，第 8 天起按剩余天数÷45 结算"（与 A4 一致）。
2. 注意 `observation.js` FAQ "7 天内全退"表述已正确，保留。
3. 全仓自查同类残留：`rg -n "30 天|30天|/ 30|/30" frontend/pages/order-pkg frontend/pages/agreement`

**验收**：
- `rg` 全前端无 "30 天观察期 / ÷30" 业务文案残留（排除与"30 分钟/30 人"等无关语义）；
- 报名页截图核对 45 天 + 退款公式；
- BDD 或手工核对 `500 - 500÷45×(10-7)` 口径。

---

### P0-2 老师工作台（D1）与课后反馈（D2）管理端 UI 缺失，功能不可达

**证据**：
- 后端就绪：`backend/domain/admin/routers/admin_teacher_workbench_router.py:27-33`（GET /workbench）、`:41-48`（POST /feedback）；`backend/domain/admin/services/teacher_workbench_service.py:157`；测试 `tests/unit/test_teacher_d4_d6.py:136/153`
- **缺失**：`backend/templates/admin/` 无工作台/反馈页面；`backend/static/admin/js/pages/` 无对应 JS；`backend/templates/admin/base.html` 侧边栏无入口链接；全仓 `rg "workbench|工作台" backend/templates backend/static/admin` 0 命中

**影响**：老师无法使用工作台、无法发课后反馈；家长端消息分类（msg_type=4 老师消息）虽已就绪（`frontend/pages/order-pkg/messages/messages.js:11/94`），但无发送端，链路断。

**修复要求**：
1. 管理端新增"老师工作台"页面（模板 + JS + 侧边栏菜单 + 权限 `dashboard.view`/`teacher.*`），聚合今日课程/待审核/负责孩子；
2. 工作台内提供"课后反馈"表单，调用现有 POST /admin/api/teacher/feedback；
3. 验收需覆盖：老师账号登录可见入口 → 加载工作台 → 发反馈 → 家长端"老师消息"列表可见。

**验收**：管理端 UI 存在；behave/手工链路全通；如评估确实无 UI 必要，须**明确降级为二期并同步改 PRD 附录 N.4 与 K3 报告**（不推荐，D1/D2 为甲方决策正式项）。

---

### P0-3 H5 数据保留期仅有 PRD 条款，无配置键、无 purge 执行机制

**证据**：
- PRD 附录 N.10（`PRD/DmkWords_V3.5需求文档.md:1772`）写了条款：财务 5 年 / 行为退出后 2 年 / 消息 1 年；
- `SystemConfig.DEFAULTS`（59 项）中**无** `data_retention_finance_years / data_retention_behavior_years / data_retention_message_years`；
- `backend/tasks/scheduler.py` 21 个任务中**无**数据保留 purge 任务（`rg "purge|retention" backend/tasks` 无实现）；
- **前端隐私政策已向用户承诺自动删除**（`frontend/pages/agreement/privacy-policy/privacy-policy.wxml:94`："语音录音保留 6 个月后自动删除…交易凭证…到期自动物理删除"）——文案已承诺、代码未实现，合规风险比纯文档更大；
- 且前端隐私政策口径与 PRD N.10 **不一致**：前端写"订单 5 年、押金结清后 3 年、退款完成后 3 年、借阅与损坏记录 2 年、权益转让 1 年、语音 6 个月"；PRD N.10 写"财务 5 年/行为 2 年/消息 1 年"——两套口径需统一。

**修复要求**：
1. 新增 3 个配置键（默认 finance=5 / behavior=2 / message=1，带范围校验），入 `SystemConfig.DEFAULTS` 与动态配置清单；
2. 新增每日 purge 任务：财务表按保留期归档/保留、行为数据到期匿名化或删除、消息表到期清理；**consent_record 永不物理删除**（与既有删除权级联一致）；
3. 语音录音 6 个月自动删除也需实现或明确由既有 uploads 清理承接；
4. 统一前端隐私政策与 PRD N.10 保留期口径为同一数字；
5. 配置项进 `config_levels.py` 管控级别。

**验收**：DEFAULTS 3 键存在且被任务读取；任务注册于 scheduler；单测覆盖保留期边界（到期前保留/到期删除/consent_record 豁免）；文档口径一致。

---

## 三、P1 整改单（2 项，上线前必修，不阻断可与 P0 同批）

### P1-1 iOS 支付豁免缺失：押金（实物担保）、亲子课（线下服务）被一并拦截

**证据**：
- `frontend/pages/order-pkg/deposit/deposit.js:33-35`：iOS 拦截押金支付；
- `frontend/pages/order-pkg/observation/observation.js:72-75`：iOS 拦截（亲子课与观察期同页，一并被拦）；
- `frontend/pages/order-pkg/official/official.js:110-113`：iOS 拦截会员；
- A5 决策（PRD 附录 N.1）：观察期/会员为虚拟服务 iOS 禁付（正确）；**押金 1200 实物担保、亲子课 99 线下服务应可 iOS 支付**；
- 后端收款码/对公转账端点已就绪：`backend/domain/admin/routers/admin_system_router.py:541/562`、`backend/domain/order/service.py:411/437`，但小程序端无入口。

**修复要求**：
1. `deposit.js` iOS 分支放行 `wx.requestPayment`（实物担保）；亲子课支付路径放行；
2. 观察期/会员页保留拦截，但将"联系客服"改为展示**门店收款码/对公转账引导入口**（可调后端 pay-code/confirm-transfer，或至少给出门店电话+客服微信文案）；
3. iOS 判定建议统一封装（现各页重复 `wx.getWindowInfo().platform`）。

**验收**：iOS 真机/模拟器四路径：押金可付、亲子课可付、观察期拦截并引导、会员拦截并引导。

---

### P1-2 E2 审核 SLA 24h 硬编码，未配置化

**证据**：`backend/tasks/scheduler.py:1279` `cutoff = datetime.now() - timedelta(hours=24)`；审查令要求 `review_sla_hours=24` 配置键（0-168）。

**修复要求**：新增 `review_sla_hours = ("24", "int", ...)` 入 DEFAULTS + config_levels（警告级），`audit_sla_escalation` 改读配置。

**验收**：改配置为 48 后任务按 48h 巡检；单测覆盖。

---

## 四、P2 整改单（10 项，低成本收尾，建议与 P0/P1 同批或下一批）

### P2-1（新增部署风险）REDIS_LOCK_FAIL_OPEN 默认 true，生产多实例需强制 false
- 证据：`backend/config.py:54` 默认 True；`backend/common/distributed_lock.py:39-43` 降级语义。
- 要求：DEPLOY_CHECKLIST 2.3 安全确认列表新增一行"生产多实例必须 `REDIS_LOCK_FAIL_OPEN=false`"（与 DEBUG/MOCK 并列）；不要求改代码默认值。

### P2-2 C1"今日全勤"奖励未实现
- 决策表 C1（`PRD/PRD需求决策表-甲方确认版.html:308`）含"集满 4 次额外'今日全勤'奖励"；PRD 正文与附录 N.3 未写，代码仅实现每类型每日 1 次 + 连续打卡成就。
- 要求：二选一——实现"4 类型集满当日发全勤奖励/提示"；或与甲方确认后从决策表删除该子项（需留痕）。推荐实现，成本低。

### P2-3 A2 押金页未展示"借满 10 本无逾期退 600"SLA 文案
- 证据：`frontend/pages/order-pkg/deposit/deposit.wxml` 全文无 600/10 本文案；后端 `deposit/service.py:686-743` 已实现。
- 要求：押金页补充 SLA 文案（10 本无逾期 → 减半退 600；审核后 24h 发起、3-5 工作日到账）。

### P2-4 配置值无范围校验
- 证据：`backend/domain/admin/routers/admin_system_router.py:141-158` `set_config` 仅做三级管控，无 min/max/枚举校验；`backend/domain/admin/services/system_service.py:32-44` 直接写值。
- 要求：为数值类配置增加范围校验（如 observation_days 1-730、borrow_limit 1-50、refund_free_days 0-30、lost_search_days 1-30、quiz_low_level_questions 1-10、refund_auto_approve_max 0-10000 等，按动态配置清单口径），非法值返回 422。

### P2-5 G2 前端错误页未接入门店电话/客服微信
- 证据：后端 `backend/domain/venue/router.py:36-48`（/venue/contact）就绪；前端 `rg "venue/contact|contact" frontend -g '*.js'` 0 命中；`frontend/components/error-view/` 无电话展示。
- 要求：错误页/支付拦截弹窗接入 /venue/contact 展示门店电话 + service_wechat（配置为空时优雅隐藏）。

### P2-6 文档数字过期（三处）
- `ARCHITECTURE.md:47,136-137`：pytest 397/179 场景/17 features → 更新为 548/210/20；
- `PRD/DmkWords_V3.5需求文档.md` 附录 H：仍写 18 个任务 → 更新为 21（补取书提醒/SLA 巡检/毕业/打卡约束相关任务）；
- PRD V3.19 更新说明 "F.13 损坏报告四态" → 注明 V3.22 起为 6 态（+PENDING_REVIEW/CANCELLED，表结构 V3.17 已对）。

### P2-7 调度器陈旧文案与缺失锁
- `backend/tasks/scheduler.py:199` 日志 "Scheduler started with 17 jobs" → 改 21；
- `confirm_expired_damage_reports`（scheduler.py:1716）无 `@distributed_lock` → 补（与其余任务一致）。

### P2-8 分层违规 1 处
- `backend/domain/admin/routers/admin_borrow_router.py:233` router 层直查 `db.query(BorrowRecord)`（B9 拍照存档，含行锁，风险低）→ 建议下沉到 BorrowService。

### P2-9 弱断言留档（不要求修改，仅登记）
- `features/steps/` 共 38 处 `assert True`，全部标注 "on frontend"（前端交互场景无法 API 断言）；52 题新场景均用"拦截提示"强断言。维持现状，禁止新增。
- 顺带：`behave --dry-run` 汇总显示 "2 failed"（@future 场景计数怪癖），真实运行 210/0 全过，非缺陷。

### P2-10 装饰性 hex 留档（不要求修改）
- PC 后台 50 处 hex（头像/等级色/证书印章）均带 `/* intentional */` 注释；小程序 0 处非注释 hex；旧主色/oklch/token 重定义均 0。维持现状。

---

## 五、已核验通过项明细（勿动——以下均为实测通过的正确实现）

> K3 修复时如遇"这算不算问题"的疑问，先查本表与 §七 勘误表；表中内容不属于整改范围。

### 5.1 第一关 CI（10/10，命令实测）
ruff check backend/tests ✅、ruff check features/scripts ✅、ruff format --check（409 files）✅、pytest **548 passed** ✅、behave **210 scenarios / 1361 steps / 20 features / 0 failed** ✅、verify_api_contract OK ✅、check_model_consistency 56 tables PASSED ✅、verify_action_wiring --strict 全匹配 ✅、integration_test 56/56 ✅、alembic check 无新操作 ✅。

### 5.2 第四关 表与迁移
- 56 表（54→56：新增 book_waitlist、fine_payment），模型一致性脚本通过；
- 枚举六方对齐：Child 0-5（含 ALUMNI=5）、Deposit 0-6、Order 1-5、Activity 0-5、msg_type 1-5、Damage 0-5（与表结构 V3.17 一致）；
- Alembic 迁移链 base→043 线性无分支，54 个迁移文件全部含 downgrade；head=c4e5f6a7b8c9；alembic check 无漂移；040 删 max_borrow_count、035 建 book_waitlist、041 建 fine_payment、043 建打卡唯一约束均存在。

### 5.3 第六关 资金安全
- 金额零 float（全 Decimal/Numeric）；资金操作全覆盖 `with_for_update`（refund/borrow/order/deposit/damage）；
- 退款公式与 PRD/BDD 一致：`refund = amount − amount/总天数 × max(0, used − free_days)`；BDD 断言 `500−500÷45×(10−7)`、`5400−5400÷365×(30−7)` 等与代码完全吻合；
- 支付回调幂等（integration 实测：重复回调不重复处理）；押金/罚款/退款链路集成测试 56 步全过。

### 5.4 第七关 隐私合规（除 P0-3 外）
- 三段式同意（privacy_policy / child_data / voice_recording，含文案版本 + SHA-256 哈希）✅；
- 删除权 24h 冷静期 + 级联物理删除（18 表 + quiz_answer + message_read_status，财务表法定保留）✅；
- `consent_record` 永不物理删除 ✅（不在任何级联删除清单中）。

### 5.5 第九关 测试
- 548 例 pytest 实测全绿（含 20 个 08-03 回归用例）；behave 210/1361 实测全绿；
- 弱断言"显示提示"存量仅 2 处（审查令所称 147 为旧口径）；38 处 `assert True` 均为标注 "on frontend" 的前端交互场景，非新用例；
- 52 题新场景全部使用强断言（"拦截提示"20 处）。

### 5.6 第十关 定时任务（除 P2-7 两项外）
- 21 个任务全部注册（B4 取书提醒、E2 SLA 巡检、F2 毕业任务在列）；20/21 带 @distributed_lock 与 try/except；
- B10 丢失寻找期无独立到期任务属可接受：罚款在标记丢失时即时生效，lost_search_deadline 仅影响找回免赔口径，`mark_book_found` 对逾期找回同样冲正。

### 5.7 第十五关 交叉验证
- 前端 33 个 API 路径全部有后端路由（共 275 条），无"前端调了后端没接"；
- 代码读取的配置键全部有定义（反向扫描 NONE）；仅 default_required_books / venue_address 两个"预留"键未读（注释已声明）；
- verify_api_contract 无孤儿 action。

### 5.8 第十六关 视觉
- 小程序非注释 hex = 0；PC 后台 50 处 hex 全部带 `/* intentional */`（装饰色）；oklch 残留 = 0；旧主色 = 0；Token 重定义 = 0；31 个管理端页面 CSS 全存在。

### 5.9 第十七关 可观测性
- trace_id 中间件（X-Trace-ID 透传）✅；JSON 结构化日志（JSONFormatter，兼容 ELK/Loki）✅；
- 换绑/迁移/监护人/复活/损坏复核/反馈等新操作均有 operation_log 审计 ✅；
- 6 处裸 `except Exception` 全部有 logger.exception/error 或注明降级语义，无真吞异常。

### 5.10 第十八关 状态机
- Child 复活仅 EXITED 可（guardian_service.py:151-152）；Deposit 仅 REFUND_PENDING 可审核、仅 REFUNDING/REFUND_PENDING 可取消；BookCopy 流转有白名单（book_service.py:312-316）；LOST 找回 → RETURNED + 库存回滚全链正确；Reservation 过期释放联动候补；test_state_machines.py 覆盖非法转换。

### 5.11 其余
- 第十三关：DEPLOY_CHECKLIST 覆盖外部依赖/环境变量/WeasyPrint/HTTPS+回调/健康检查（/health）/回滚/备份；
- 第十四关：K3 遗留项自述透明（C2 图片素材、B3/D3 二期、G2 待填值、扫码枪硬件、A6 跨日 ±1 天留档）；64 项优化点核心组件（storage.js / error-view / empty-state / submit-lock.js）全部存在。

---

## 六、超级大审查 13 项处置抽查结论（已闭环，勿返工）

| # | 声明 | 终审抽查证据 | 结论 |
|---|---|---|---|
| P0-1 | 迁移 043 + 4 处 SAVEPOINT | `add_with_unique_fallback`：reading/service.py:336/468/506 + **vocabulary/service.py:229**（第 4 处，声明准确） | ✅ |
| P0-2/3 | 统计字段行锁 | with_for_update 已补（batch15/16 相关） | ✅ |
| P1-2 | total_revenue float→Decimal | `admin/services/dashboard_service.py:210` + `admin_schemas.py:69` 注释 | ✅ |
| P1-3 | RedisError 全族 + FAIL_OPEN | `common/distributed_lock.py:37` 捕 redis.RedisError；`config.py:54` 开关 | ✅ |
| P1-4 | escape_like 8 文件 17 处 | 全仓 18 命中 = 定义 1 + 调用 17 | ✅ |
| P2-1 | msg_sec_check 补 suggest | `domain/security/router.py:54-61` errcode=0 后再查 result.suggest | ✅（原代码确实等于没检） |
| P2-2 | pass_score Decimal + 边界回归 | `domain/advancement/service.py:248` Decimal；float(0.80)*100 边界已钉死 | ✅ |
| P2-3 | access_token Redis + 降级 | 已实现（token 安全修复链） | ✅ |
| P2-5 | 030→030b 重命名 | `a3f5c8d91e02_030b_add_fine_waiver_columns.py` 无重号 | ✅ |
| P1-1/P2-6/P2-4 | 3 项不成立 | 结论采信（JWT 时区论证自洽；teacher 域文档如实；退款窗口为可接受风险） | ✅ |

---

## 七、审查令口径勘误（防误修清单——以下"问题"不存在，K3 不得修改）

| # | 审查令写法 | 实际正确状态 | 说明 |
|---|---|---|---|
| 1 | pytest 518 | 实测 548（K3 报告 528） | 只增不减，非虚报 |
| 2 | alembic head=f4c1a7d88eb9 | f4c1… 是 042 修订号；head=c4e5f6a7b8c9 | 链完整，无漂移 |
| 3 | 第 8 天退款 = 500−500÷45×8 = 411.11 | 正确值 488.89（= 500−500÷45×1，前 7 天全退） | 审查令示例与"前 7 天全退"自相矛盾；代码与 PRD/BDD（`500−500÷45×(10−7)`）一致 |
| 4 | 配置键 require_parent_course | 实际 `parent_course_required`（默认 false） | 命名差异，功能已实现 |
| 5 | 配置键 fine_cap_ratio / low_age_quiz_* / auto_approve_threshold | 实际 `overdue_fine_cap_ratio` / `quiz_low_level_*` / `refund_auto_approve_max` | 命名映射，功能已实现 |
| 6 | DamageReport.status 0-4 五态 | 代码与表结构 V3.17 均为 0-5 六态（B9 新增 PENDING_REVIEW/CANCELLED） | 审查令旧口径 |
| 7 | 模板 teacher_workbench/feedback/fine_payment/waitlist 应存在 | fine_payment 在小程序 deposit 页；waitlist 在小程序 books 页；teacher_workbench/feedback 为**真实缺失**（见 P0-2） | 审查令假设了错误路径 |
| 8 | 弱断言存量 147 | 实测仅 2 处"显示提示"，38 处 assert True 均为前端场景 | 存量已远低于 147 |
| 9 | B10 丢失寻找期到期任务缺失 | 语义可接受：罚款在标记丢失时已即时生效，deadline 仅影响找回免赔口径；`mark_book_found` 对逾期找回同样冲正 | 无需新增任务 |
| 10 | 27 域 / teacher 域宣称 | ARCHITECTURE.md 如实描述 teacher 服务位于 admin/services 下 | 不成立项已确认 |

---

## 八、建议修复顺序与验收门禁

1. **批次 A（P0）**：P0-1 前端文案统一 → P0-2 工作台/反馈管理端两页 → P0-3 H5 配置键 + purge 任务 + 隐私政策口径统一；
2. **批次 B（P1）**：P1-1 iOS 支付豁免与收款码入口 → P1-2 review_sla_hours 配置化；
3. **批次 C（P2）**：P2-1 部署清单 → P2-2~P2-8 实现类 → P2-9/P2-10 留档；
4. **复测门禁**：十关 CI 全绿（pytest ≥ 548、behave 210/1361、56 表、59 配置、alembic 无漂移、integration、wiring）+ 本单 P0/P1 逐项验收通过 → 十席复签。

---

## 九、附：本单之外的审查观察（可选，不阻塞）

- 批量连扫 `doBatchBorrow`（`backend/static/admin/js/pages/borrow.js:248`）为纯前端逻辑，无自动化测试——建议后续补前端 e2e 或至少手工用例脚本；
- 押金/罚款链路的并发压测覆盖偏单线程（现有 `test_stock_reconciliation` 等已覆盖库存并发）——上线前可补一轮多线程压测（非阻塞）。

---

*本执行单由首席质量终审官出具，全部条目均可按证据复核。K3 修复完成后按 §八 复测并回填结论，再交终审签署。*

---

## 附录：52 题逐题核验明细表（第二关全量结果，供 K3 溯源）

> ✅ = 代码+配置+测试+文档四步核验通过；⚠️ = 部分实现（对应整改单编号）；— = 二期/文档项（PRD 已标记）。

| 题 | 决策 | 核验结果 | 关键证据 | 整改单 |
|----|------|:---:|----------|:---:|
| A1 | 双轨制（可配置恢复强制） | ✅ | order/service.py:161-172；parent_course_required=false；test_enroll_dual_track.py | — |
| A2 | 押金 1200 + 退款 SLA + 10 本无逾期退 600 | ⚠️ 后端✅ | deposit/service.py:686-743（deposit_partial_refund_books=10 / amount=600）；**押金页无 SLA 文案** | P2-3 |
| A3 | 观察期 45 天 | ⚠️ 后端✅ 前端❌ | refund/service.py:337；observation_days=45；**前端 10 处"30 天"残留** | P0-1 |
| A4 | 前 7 天全退 + 第 8 天起按天扣 | ⚠️ 后端✅ 前端❌ | refund/service.py:333-370（refund_free_days=7）；**报名页公式 ÷30** | P0-1 |
| A5 | iOS 门店收款码 + 对公转账 | ⚠️ 后端✅ 用户端❌ | admin_system_router.py:541/562；order/service.py:411/437；**押金/亲子课 iOS 被拦、无收款码入口** | P1-1 |
| A6 | 中途升级按剩余天数抵扣 | ✅ | order/service.py:309；upgrade_deduct_enabled；test_upgrade_deduct_transfer.py | — |
| A7 | 删"评估通过"前置 | ✅ | order/service.py 无评估校验；状态门槛制 | — |
| B1 | 批量连扫 + 一键确认 | ✅ | borrow.html 队列 + borrow.js:158-260（doBatchBorrow） | — |
| B2 | 扫码枪故障降级（手动+纸质登记） | ✅ | borrow.js:276-313（manualSearchBooks）；SOP 文档 | — |
| B3 | 自助借书（二期） | — | PRD N.2 标记 | — |
| B4 | 预约 72h + 24h 提醒 + 超时释放候补 | ✅ | reservation/service.py:85；scheduler remind_reservation_pickup；waitlist 联动 | — |
| B7 | 宽限 3 天 / 上限半价 / 首次免罚 / 改称谓 | ✅ | fine_policy.py 全配置化；overdue_grace_days=3 / overdue_fine_cap_ratio=0.5 / first_overdue_free=true | — |
| B8 | 逾期音频第 4 天起锁 | ✅ | reading/service.py:85-102（宽限期 cutoff） | — |
| B9 | 借出拍照 + 量化标准 + 双人确认 | ✅ | damage_admin_service.py:40-169；checkout_photos；damage_dual_review；N.8 量化标准 | — |
| B10 | 丢失 7 天寻找期 + 买新还书 + 回滚 | ✅ | damage_admin_service.py mark_book_found / replace_with_new_copy；lost_search_days=7 | — |
| B11 | 罚款记账制 + 退押金抵扣 | ✅ | deposit/service.py:350-403；refund fine_deducted | — |
| B12 | 线上缴罚款（全链路） | ✅ | deposit/router.py:95-104 + service.py:784-857 + fine_payment 表 + 前端按钮 | — |
| B13 | 副本维修/报废入口 | ✅ | book_service.py:312-349；admin_books_router.py:244 | — |
| B14 | 借阅上限 10 本 | ✅ | borrow_limit=10；BDD 拦截用例 | — |
| C1 | 每类型各 1 次 + 全勤奖 | ⚠️ 打卡✅ 全勤奖❌ | uq_checkin_child_date_type（迁移 043）+ daily_checkin_limit=4；**"今日全勤"专项奖励未实现** | P2-2 |
| C2 | 低龄 3 题对 2 + 听题 + 零负面 | ✅ | advancement/service.py:111-125/256；quiz.wxml TTS；quiz-result "再读一遍就能答对啦！"；图片素材内容团队待制（非阻塞） | — |
| C3 | 查词 30 次 + 自动记录生词本 | ✅ | vocab_lookup_limit=30；vocabulary/service.py:51/257 | — |
| C4 | 想读 100 本 + 无库存标灰 | ✅ | bookshelf_limit=100；books.wxml inv-empty 样式 | — |
| C5 | 一键听读简化模式 | ✅ | reader.js:58-76；reader.wxml 大按钮 | — |
| C6 | A-F 每级 3 本 + 排行榜年龄分组 | ✅ | init_levels.py（A-F=3）；leaderboard_service.py:46-77 | — |
| C7 | 试读 20 页 | ✅ | trial_pages=20；reading/service.py:224-238 | — |
| D1 | 老师工作台 | ⚠️ 后端✅ UI❌ | admin_teacher_workbench_router.py:27-33；**管理端无页面/菜单/JS** | P0-2 |
| D2 | 老师→家长消息通道 | ⚠️ 后端✅ 家长端✅ 发送端❌ | teacher_workbench_service.py:157-207（msg_type=4）；messages.js:94；**管理端无发送 UI** | P0-2 |
| D3 | 建议晋级（二期） | — | PRD N.4 标记 | — |
| D4 | 达标自动审核 | ✅ | quiz_handlers.py:74（submission_auto_approve） | — |
| D5 | 系统自动出卷口径 | ✅ | PRD:696/1718 口径修正 | — |
| D6 | 报告转化导向 | ✅ | report models/schemas peer_avg_books / cta_text | — |
| E1 | ≤500/押金/活动自动审核（转让人工） | ✅ | refund/service.py:148-152；deposit_refund_auto_approve；benefit_transfer 人工 | — |
| E2 | 审核 SLA 24h + 超时升级 | ⚠️ 任务✅ 配置❌ | scheduler audit_sla_escalation；**timedelta(hours=24) 硬编码** | P1-2 |
| E3 | 配置三级管控 | ✅ | config_levels.py（locked/warning/free）；BDD 验证 | — |
| E4 | 活动轻量/正式双模式 | ✅ | activity.is_light；admin_schemas.py:441；BDD 自动通过 | — |
| E5 | 活动退款自动通过 | ✅ | activity/service.py:383 | — |
| E6 | Dashboard 运营报表 | ✅ | admin_system_router.py:104（/dashboard/ops） | — |
| E7 | 会员费退款校验罚款 + 自动抵扣 | ✅ | refund/service.py:123-145/314-325 | — |
| F1 | 换绑/迁移/监护人变更 | ✅ | guardian_service.py:29/94；user/router.py:75 | — |
| F2 | 毕业 ALUMNI + 押金引导 | ✅ | scheduler graduate_children；MemberStatus.ALUMNI=5 | — |
| F3 | 门店关闭 venue.status + SOP | ✅ | venue.status 字段；PRD N.9 SOP | — |
| F4 | 等候名单 + 同类推荐 | ✅ | book_waitlist 表；reservation/router.py:59-85；notify_next_waiter | — |
| F5 | EXITED 复活 + 多孩资格 | ✅ | guardian_service.py:141-152；order/service.py:292 | — |
| G1 | 附录 K 文案全对齐 | ✅ | test_h_group_copywriting.py；抽查 K.1 文案 | — |
| G2 | 错误提示加门店电话 | ⚠️ 后端✅ 前端❌ | venue/router.py:36-48（/venue/contact）；**前端错误页未接入** | P2-5 |
| G3 | 后端对齐 K.1 | ✅ | "该书暂无库存"（book:237 / borrow:154,445 / reservation:61） | — |
| H1 | 支付中禁止切换孩子 | ✅ | order 创建锁 child_id；TestH1PaymentLocksChild；前端支付页无切换入口 | — |
| H2 | 挑战徽标 + max_borrow_count 移除 | ✅ | max_ar_level（"挑战"徽标）；迁移 040 删列 | — |
| H3 | 周报/月报消息触达 | ✅ | scheduler:568/667 写 SystemMessage | — |
| H4 | 创建即锁价（PRD 补写） | ✅ | PRD 附录 B（1753 行） | — |
| H5 | 数据保留期（5/2/1 年） | ⚠️ 仅文档 | PRD N.10 条款；**无配置键、无 purge 任务、隐私政策口径不一** | P0-3 |

**统计**：✅ 42 题（含二期/文档项 4 题按 PRD 标记通过）｜ ⚠️ 部分实现 10 题（A2/A3/A4/A5/C1/D1/D2/E2/G2/H5，全部已进 P0/P1/P2 整改单）｜ 无"未实现"题。
