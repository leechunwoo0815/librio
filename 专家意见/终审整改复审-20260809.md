# librio 终审整改复审报告（2026-08-09）

> 审查对象：终审整改批（a97534c..a3b9c66，19 个提交），依据《终审整改修复报告-20260809.md》
> **现行基线指针**：本文为历史审查档案（当时基线见文内），当前权威基线以《专家意见/项目交接-20260809.md》为准（HEAD=3ff4ee3 / pytest 925 / ruff 496 / MySQL A-K 全 PASS）。
> 声称的 P1×3 + P2×9 + P3×14 全部整改项。
> 方法：只读复审（撤行复验在 git worktree `/tmp/librio-verify2` 独立副本完成并已还原，主工作区零改动）；
> 门禁全量本机重跑 + P1 四项亲自撤行复验 + 子代理全量代码核查 + T-final evidence 逐条核验。
> 实测基线：HEAD=`a3b9c66`、pytest **910 passed**（432.13s 实测）、behave **211/1369**、
> ruff **494 files**、alembic head=`e4f506172839`、MySQL 并发 A-I 全 PASS。

---

## 一、总判定

**不通过（含 1 项新 P1 + 1 项 P2 未闭环）。**

整改批主体质量高：P1 三项（F-057/F-116/F-005）**经撤行复验全部真红**、门禁数字与报告逐字一致、新增测试全部真实可复现。但本次复审发现 **1 项 P1 级整改声明不实（F-078 跨域误判，权益并发风险原状保留）+ 1 项 P2 级同类漏改（tencent.py dev 模式明文 phone）**，均须修复后重新验收。

---

## 二、P1 整改复审（亲自撤行复验，全部真红）

| 项 | 整改 | 亲验 | 撤行复验（worktree 实测） | 结论 |
|---|---|---|---|---|
| P1-1 F-057 | submit 响应新增 question_review（服务端权威判分），前端删本地判分，错题回顾走服务端 | service.py:257-271 review_items 构建 + schemas.py QuestionReviewItem + quiz.js 删 `_correctAnswers`/selectOption 只记录/onUnload 销毁 _ttsCtx | **撤 review 构建 → 2 failed**（test_submit_response_carries_authoritative_review + test_review_answers_match_db_after_submit）；还原 3 passed | **通过**（前端交互变化已标注真机验证，诚实） |
| P1-2 F-116 | BookRepository.search 加 `published_only`（默认 True 用户侧不变）；admin /books 显式 False | repository.py:30-35 条件过滤 + admin_books_router.py:53 传参 + 测试含路由级直接调用 | **撤 router 传参 → 1 failed**（test_admin_router_uses_admin_query）；还原 7 passed | **通过**（admin update/delete/toggle 均按 book_id 直查不受影响，闭环完整） |
| P1-3 F-005 | deposit cancel_refund 仅 REFUND_PENDING→PAID，REFUNDING 抛 ConflictError | service.py:652-655 + 测试断言状态不变 | **撤守卫 → 1 failed**（test_cannot_cancel_refund_while_refunding）；还原 9 passed | **通过**（P3 记录：测试用 `pytest.raises(Exception)` 过宽，应 ConflictError） |

## 三、P2 整改复审

| 项 | 结论 | 依据 |
|---|---|---|
| P2-1 F-077 测试真实性 | **通过** | test_f077:68-72 monkeypatch 将 `user_service.IntegrityError` 替换为不可匹配类型（真实屏蔽 DB 兜底）；亲验：屏蔽后撤应用层检查 → **1 failed**，还原 3 passed |
| P2-2 F-022 exc_info | **通过（有 P3 注记）** | 亲验全库 10 行窗口扫描：残留仅 4 处且全部为"无活动异常的业务失败分支"（refund:307、aliyun:93、tencent:94、subscribe:141，错误字符串返回非异常抛掷，符合声明）；AST 守护测试真实扫描 except 块。**P3**：守护只查 exc_info 存在不查真值（exc_info=False 会漏过） |
| P2-3 F-020 明文 phone | ⚠️ **P2 未闭环（同类漏改）** | aliyun.py 全部脱敏 ✓；**tencent.py:113-117 dev-mode `send_notification` 仍打明文 `request.phone`**（SDK/凭据未配置时触发；同位置 aliyun 已修，tencent 漏改——防复发红线模式 1 复发）。非 dev 分支 L133 已脱敏 |
| P2-4 F-016 回退路径 is_deleted | **通过** | _rollback_refund_failure（Order:333-338/Refund:352-359）、mark_refunded（385-408）、handle_refund_failed（453-482）全补 is_deleted==0；订单缺失整链不动作；4 测试实测全过。P3：mark_refunded 对软删订单抛 NotFoundError 而非静默（行为可接受） |
| P2-5 F-035 月末边界 | **通过** | 实测 7 处 `< next_month_start`（783/795/810/839/851/867/878，比声称 5 处更彻底）；1 测试实测通过 |
| P2-6 F-015 范围查询 | **通过** | check_observation_reminders 一次范围查询（`>=lower AND <upper`）+ Python 按日分组（scheduler.py:1892-1906），func.date 逐日循环废除；与 check_member_expiry 无冲突（不同状态域） |
| P2-7 F-069 重叠校验 | **通过** | `_assert_no_overlap` 半开区间相交（service.py:108-129）+ create/update 双入口（60/95）；3 测试实测全过 |
| P2-8 F-049 白名单 | **通过** | `_check_page_perm` 区分"未登记拒绝"（无 key→False）与"显式空权限放行"（key 存在 value=""→True）；profile/403 均在映射；403 路由实测正常渲染（无限重定向消除） |
| P2-9 B3 覆盖 | **通过** | test_f011_quiz_tts_cleanup / test_f012_f034_perm_attrs / test_f021_task_trace / test_f022_exc_info_guard 全部存在、断言真实非恒真、实测全绿 |

## 四、P3 整改复审

| 项 | 结论 | 依据 |
|---|---|---|
| F-011 TTS 销毁 | 通过 | quiz.js:343-352 onUnload destroy `_ttsCtx`（try/catch） |
| F-012/034 data-perm | 通过 | content.js edit-page→book.edit、delete-audio→content.delete + audio.js；静态断言测试 |
| F-021 任务 trace_id | 通过（P3 注记） | distributed_lock 包装器 TASK_START/END 带 trace_id；**P3**：trace_id 不注入任务内层日志，仅起止边界可关联 |
| F-097 update 校验 | 通过（P3 注记） | update 合并后 start<end 校验；**P3**：无专门回归测试（624bc8d 仅加 ISBN 测试） |
| F-098 手动提醒行锁 | 通过 | get_overdue_records with_for_update（同事务标记）；**P3**：SQLite no-op，需 MySQL 实证（脚本 A-I 已含同类机制） |
| F-119 DB 唯一兜底 | 通过 | 迁移 056（e4f506172839，active_audio_key 生成列唯一）与模型一致；update 查重前置防 autoflush；alembic 实测无漂移 |
| F-010 序列化/分页 | 通过 | english_name 空串兜底；get_user_refunds has_next |
| F-073 ISBN 格式 | 通过（P3 注记） | 4 输入 schema 加 pattern（13/10 位带连字符）；**P3**：BulkImportQuestionItem.isbn、ScanAndBorrowRequest.isbn 两个输入路径仍无格式校验（同类漏改候选） |
| F-084 手册勘误 | 通过 | 手册改 [PENDING,APPROVED,COMPLETED] + 勘误注，与 admin create_refund 代码（107-111）一致 |
| 退款魔法数字 | 通过（P3 注记） | refund/service.py 全改 Order.REFUND_* 常量；**P3**：scheduler.py:876 仍 `refund_status.in_([1,2])` 魔法数字未改（同类漏改） |
| evidence 41 | 部分通过 | 交接/终审令修正为 41 ✓；**实际 fix-evidence/ 现有 42 文件**（T-final 由 e092346 添加，未计入声明） |

## 五、本次复审新发现问题

### P1-新：F-078 整改声明不实（跨域误判）

- **声称**（整改报告 §三）："F-078 approve/reject 并发——复核：reject 走 `_get_report_or_raise`（F-080 已带行锁），当前代码已安全，无需再改"
- **事实**：
  - `_get_report_or_raise` 定义于 `backend/domain/admin/services/damage_admin_service.py:712`，属 **DamageAdminService**（BookDamageReport 损坏定责域），与权益转移**完全无关**
  - `benefit_transfer_service.py`（BenefitTransferAdminService）：**approve L92 有 `.with_for_update()`**，**reject L113-137 无任何行锁**——`if app.status != 0` 检查后在无锁窗口下写 `status=2`
  - 并发 approve+reject 竞态：approve 锁行转移权益并置 status=1 → reject 无锁读到旧快照 status=0 → 置 status=2 → **权益已实际转移（child 表已变更）但申请状态为 REJECTED**，资金/权益口径不一致，需人工对账
- **证据**：benefit_transfer_service.py:92（approve 锁）vs 113-137（reject 无锁）；damage_admin_service.py:19/712
- **影响**：终审原 P3 问题**原状保留**且被"已覆盖"声明掩盖 → 报告不实，升级为 P1
- **修复建议**：reject 查询加 `.with_for_update()`（与 approve 对称），或状态更新改条件 UPDATE（`WHERE status=0`）并检查行数；补并发 approve+reject 回归测试（MySQL 实证）

### P2-新：tencent.py dev 模式明文 phone（同类漏改，模式 1 复发）

- tencent.py:113-117 `send_notification` dev 分支（SDK/凭据未配置时触发）日志 `request.phone` 明文
- 同位置 aliyun.py 已脱敏——tencent 漏改，防复发红线模式 1 复发
- **证据**：tencent.py:113-117 vs aliyun.py:112-117
- **修复建议**：`_mask_phone(request.phone)`（一行）

### P3 记录（不阻塞，择机消化）

1. T-final evidence 数字与实测不符：F-119 声称"test_batch2 恢复 9 passed"但 test_batch2_f119_audio_dedup.py 仅 6 测试（B2 原始声称 4）；F-069 声称"恢复 9 passed"但 test_batch4_data.py 现 10 测试（624bc8d 加 ISBN 测试后未更新）；F-049 声称"+39 行"实为测试文件 +34（39 为跨 2 文件总插入）；F-069 声称"+115 行"实为 +87
2. F-005 测试 `pytest.raises(Exception)` 过宽
3. F-022 AST 守护不查 exc_info 真值
4. F-021 trace_id 不注入任务内层日志
5. F-097 无专门回归测试
6. scheduler.py:876 `refund_status.in_([1,2])` 魔法数字未统一
7. evidence 目录 42 文件 vs 文档声称 41
8. F-073 两个额外 isbn 输入路径无格式校验

---

## 六、门禁实测（全量本机重跑，HEAD=a3b9c66）

```
pytest:  910 passed, 69 warnings in 432.13s (0:07:12)          ← 与声称一致
behave:  20 features / 211 scenarios / 1369 steps, 0 failed     ← 一致
ruff:    All checks passed! ×2；494 files already formatted     ← 一致
contract: OK · model 56 tables PASSED · wiring ✅ · config 67 OK · integration 全部通过
alembic: upgrade head 成功（含 056）；check: No new upgrade operations（head=e4f506172839）← 一致
MySQL 并发: A 5/100 · B 1/50 · C 唯一拦截 · D 双计0 · E 0/30 · F total_stock=0 · G 0 · H 0/10 · I 1 —— 全 PASS ← 一致
```

**基线同步核验**：e092346 提交信息（910/494/head=e4f506172839/迁移056）与 T-final evidence、整改报告 §四 完全一致；checkpoint/CLAUDE 等五件套同步声明与实测一致。迁移 056 已登记 DEPLOY_CHECKLIST（L67/121）。

**evidence 真实性**：10 个声称测试文件全部存在，实测 56 测试全绿（F-057×3/F-016×4/F-077×3 等）；alembic head 与证据一致；撤行声称与文件结构自洽（F-057 撤 review→2 用例 KeyError、F-116 撤传参→路由级测试红、F-005 撤守卫→状态机测试红）。数字小出入见 §五 P3-1。

---

## 七、结论

1. **整改主体通过**：P1 三项亲验 + 撤行全真红；P2 七项修复到位；P3 大部分落地；门禁全量实测与报告逐字一致；新增 27 测试（883→910）真实可复现。
2. **必须修复**：新 P1（F-078 reject 行锁——整改声称不实，权益并发风险保留）+ 新 P2（tencent.py dev 明文 phone）。两项均为**同类漏改/跨域误判**类问题，修复成本低（各一行+补测试）。
3. **验收口径**：T-final evidence 数字需按 §五 P3-1 修正；修复后补跑 F-078 的 MySQL 并发撤行实证。

*本报告由独立复审生成（2026-08-09）。审查零代码修改（worktree 撤行已还原，主工作区仅新增本报告）。*
