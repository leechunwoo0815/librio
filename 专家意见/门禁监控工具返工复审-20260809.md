# 门禁监控工具返工复审（d4c5c23）· 2026-08-09

> 复审对象：开发大模型返工报告（对应本人《门禁监控工具专家审查-20260809.md》§五 七项返工要求）
> 复审口径：全部结论以本人亲验为准——ruff 三关全量重跑、24 用例重跑、原 18 断言比对、
> 对抗脚本 A-H 全场景重跑、文档逐项抽查。
> **结论：通过。P1×1 + P2×3 + P3×5 全部实证闭环，未发现新缺陷，返工质量合格。**

---

## 一、逐项亲验结果

| 审查项 | 本人验证方式 | 结果 |
|---|---|---|
| P1-1 ruff format 红 | 全量重跑 `ruff check backend/ tests/` + `ruff check features/ scripts/` + `ruff format --check .` | 三关 rc=0 全绿 ✓ |
| P2-1 半行缓冲 | 源码审读（二进制读 + `read_available()` 残行 `seek(tell()-len)` 回退，scripts/monitor_gates.py:263-278）+ 对抗 C1/C2/C3 重跑 | C1 passed=910（原 0）/ C2 DONE 拼接退出 0（原误超时 3）/ C3 PYTEST✓（原丢失）✓ |
| P2-2 同 inode 截断 | 源码（:315 `st_size < f.tell()` 重开）+ 对抗 E 重跑 | E 读到 77 passed 退出 0（原超时 3）✓ |
| P2-3 段名污染 + 假绿 | 源码（:41 正则收紧 `[^=]+?`、:43-58 KNOWN_SEGMENTS 白名单、:63/:132-135 ERRORS_RE + Interrupted 规则）+ 对抗 H 重跑 | H 两行全小写 error 均 [FAIL] 标记，段清单无垃圾段 ✓ |
| P3-1 HEAD 漂移机制化 | 交接卡 §二 改指引式（"以 git log -1 为准"，:18） | ✓ |
| P3-2 13+5 失实 | 报告/checkpoint 更正（checkpoint v16 §二十七） | ✓ |
| P3-3 stats 窗口 | 源码 :296/:327-341 stats_window 跨轮累计 | ✓ |
| P3-4 文件消失语义 | 源码 :310-314 专用提示 + 退出码 2 | ✓ |
| P3-5 测试证据存活 | `audit_loop/fix-evidence/monitor-gates-test-evidence.txt` 与 /tmp 运行版逐字节一致（diff 空）；对抗脚本副本同目录 | ✓ |
| 24 用例 | 本人重跑 | 24/24 OK（15.1s）；原 18 用例名全保留，抽查断言逐字未放宽（910/20+211+1369/failed=2/passed=2510 等关键值原样） |
| 旧行为退化 | 对抗 A/B/D/F/F2/G 重跑 | 超时码 3 / 增量不重复 / inode 轮转 / SIGINT 码 0 与 1 / exit-on-error 无错码 0——全部无退化 ✓ |
| 文档同步 | checkpoint v16、交接卡 :21/:127、历史报告指针 grep | ruff 495 口径全库一致（bb03e3d=494 + monitor_gates.py=495，推导与本人独立计数吻合）✓ |

## 二、规格补强裁决的复核（小写 error 规则）

返工方未按审查 §五.4 字面"先请示"，而是以三重授权自行实施并在实现报告 §十 显式声明。
本人复核：**授权链成立**——①用户返工令要求对抗 H 转绿，该缺陷真实（假绿退出 0）；
②不补规则则工具在失败场景失守，与工具存在目的相悖；③声明透明可追。
一点精确化备注：仅 `"Interrupted"` 规则已足以令 H 场景转绿；`ERRORS_RE`（`N errors?` 计数）
属小幅额外补强。本人认可保留（pytest/behave 小写 error 汇总行是真实失败信号），
如规格作者反对，删除 ERRORS_RE 一条即可，H 场景仍绿。

## 三、台账处置

L2-001..L2-005（本人 20260809 审查种入的返工项）经本复审确认全部闭环，
台账状态转 VERIFIED，evidence 指向本文件与 d4c5c23。

## 四、遗留观察（非缺陷，不阻塞）

1. KNOWN_SEGMENTS 白名单对未来新增门禁段名是"静默不入清单"（错误检测不受影响）；
   若日后 CI 加段，需同步白名单。已属规格 §一 枚举约定的自然结果。
2. LOOP-2 门禁运行器 `scripts/loop_gate.sh` 的段名（如 `===== RUFF-CHECK-1 =====`）
   不在白名单内——用 monitor_gates 盯 loop_gate 日志时错误检测仍有效
   （`[GATE-ITEM] ... FAIL` 命中 FAIL 规则），仅段名✓清单不展示。可接受。
