# R37 第三十七轮 身份链同类模式枚举（F-077 后续）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-130 起（本轮零发现）。

## 范围

R37 对 F-077（wx_login 账号接管）做**同类模式枚举**（模式 1 纪律）：全库"service 方法返回用户对象 +
调用方重新赋值 user + create_access_token"模式，确认除 wx_login 外无第二处身份替换缺陷。

## 结果

- **发现 0 项**
- **clean 1 项**：C-130 身份链同类模式枚举完成（仅 F-077 一处，phone_login 路径安全）

---

## [C-20260808-130] 身份链同类模式枚举（user 赋值/token 来源） — clean

- **方法**: R37 定向纵深。① 全库 grep "user = *service 方法调用"（5 处）；② 全库 grep create_access_token（2 处）核对 token 身份来源；③ 逐一分析每处的身份替换风险；④ 与 F-077 对比
- **证据**:
  - **wx_login L45/L51（F-077 已报）**：find_or_create_by_openid 创建 → update_user_phone **可能返回他人** → user 替换 → token 他人——唯一缺陷处（R34 已报）
  - **phone_login L154/L157**：`user = get_by_phone(phone)`（查已有）→ `user = link_openid(user.id, openid)`——link_openid（service.py:117-123）**仅绑定 openid 到已存在 user，不替换身份**（无 return existing 分支）✓ → token 自己 ✓ 安全
  - **phone_login L163/L169**：`user = get_by_id(created.id)`（新建用户）→ token 自己 ✓
  - **create_access_token 两处**（L53 wx_login/L169 phone_login）：均 `{"sub": str(user.id), "gen": user.token_generation}`——token 身份 = user.id；wx_login 的 user 可能被替换（F-077），phone_login 不会 ✓
  - **update_user_phone 调用点**（2 处）：wx_login L51（危险，F-077）+ change_phone L92（前置防占用，安全）——仅 wx_login 一处缺陷
- **排重**: R37 本轮身份链 clean 侧（零新缺陷）；F-077 已报为唯一身份替换点；F-041（openid 日志）/C-030（凭据零日志）不同面

---

## R37 完结汇总

- **范围**: 身份链同类模式枚举（user 赋值/token 来源/link_openid）
- **结果**: 发现 0 项 + clean 1 项（C-130）
- **关键结论**:
  - F-077 为身份链**唯一**身份替换缺陷——phone_login 的 link_openid（绑定不替换）与 change_phone（防占用）均安全
  - 模式 1 枚举确认：update_user_phone 两调用点中仅 wx_login 一处有漏洞
  - 本轮为合法零发现（铁律 3），但确认 F-077 是孤立点（修复只改一处）
- **累计**: 76 发现（P0:0 / P1:0 / P2:11 / P3:65）+ 127 clean 记录
- **提交**: 见 git log（本轮 rounds/R37 文件 + progress 索引同步更新）
- **R37 收尾结论**: 三十七轮共 76 项发现无 P0/P1；11 项 P2（含 F-077）。R38 候选：继续轮转新面。
