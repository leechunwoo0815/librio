# R16 第十六轮 事件链接缝（重放幂等性）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-109 起（本轮零发现）。

## 范围

R16 事件链接缝换面。C-003/C-043（handler 无自 commit）、C-094（21 handler 金额/状态一致性）、C-101
（quiz 链）、C-102（streak 链）已审。本轮换面（深度递增：重放/异常视角）：① 事件总线 publish 机制
（同步/事务/异常回滚）；② 事件**重复投递**的 handler 幂等性（order.paid / book.borrowed/returned /
reservation / quiz / streak 各链）；③ 发布点防重守卫（回调幂等）；④ handler 注册顺序依赖守护。

## 结果

- **发现 0 项**
- **clean 1 项**：C-109 事件链重放幂等性 + 发布点守卫 + 注册顺序整体健康

---

## [C-20260808-109] 事件链重放面（发布点守卫 + handler 幂等 + 注册顺序） — clean

- **方法**: R16 定向纵深。① 读 events.py publish（L339-372：同步调用 + handler 异常 re-raise 触发事务回滚 + 共享 session 模式）；② 逐链核查重复投递场景——order.paid（order/service.py:420-490 + order_handlers.py:39-115）、book.borrowed/returned（borrow_handlers.py:14-108）、reservation.created/cancelled/expired、quiz.passed/failed、reading.checkin/book_finished、book.overdue；③ 发布点防重守卫核对；④ registry.py 注册顺序（F4 候补放行依赖）
- **证据**:
  - **发布点守卫完整（order.paid 重放安全）**：支付回调幂等链（order/service.py:431-434 `pay_status==PAID → already paid 直接返回` + L437-446 trade_no 重复检查 + with_for_update 行锁）→ 重复回调不重复 publish → handle_order_paid_for_child（order_handlers.py:39-115）不会重复叠加 member_expire_time（其叠加逻辑依赖发布点守卫，无独立幂等但当前架构无绕过路径）✓
  - **handler 状态守卫**：handle_order_paid_for_child 带 with_for_update + EXITED 拦截 + 合法迁移校验（ALLOWED_TO_OFFICIAL/OBSERVATION）+ activation_issue 标记 ✓
  - **copy 状态同值幂等**：handle_book_borrowed/returned_for_copy_status with_for_update + 无条件覆盖（同值覆盖幂等无害；交错需事件重复投递+借还并发，极弱场景）✓
  - **F4 候补放行**：handle_stock_released_for_waitlist → notify_next_waiter（F-056 已报候补链，不重报）✓
  - **book.overdue**：仅日志（罚款由 scheduler 统一计算，C-094 已确认）✓
  - **quiz/streak 链**：C-101（submit 守卫 + 计数锁）/C-102（自然日幂等重算）已审，重复投递幂等 ✓
  - **注册顺序正确**：registry.py book.returned 链 `handle_book_returned_for_copy_status`（copy 置 AVAILABLE + 库存+1）先注册 → `handle_stock_released_for_waitlist` 后注册（同步调用顺序=注册顺序，docstring 依赖成立）；reservation.cancelled/expired 同构 ✓（无测试守护顺序，但当前正确，风格观察不构成发现）
  - **事件总线机制**：同步 + 异常 re-raise（单 handler 失败 → 主事务回滚，无部分提交）✓（C-003/C-043 已确认无自 commit）
- **排重**: R16 本轮事件链 clean 侧（零新缺陷）；C-094（21 handler 金额/状态）、C-101（quiz 链）、C-102（streak 链）、F-056（候补链）已报不重

---

## R16 完结汇总

- **范围**: 事件链接缝（重放幂等性：发布点守卫 + handler 幂等 + 注册顺序）
- **结果**: 发现 0 项 + clean 1 项（C-109）
- **关键结论**:
  - 事件链工程化质量高：发布点防重守卫（回调幂等链）、handler 状态守卫、同值覆盖幂等、注册顺序正确
  - 事件总线同步 + 异常回滚设计（无部分提交）消除了"handler 失败后数据不一致"类问题
  - 本轮为合法零发现（铁律 3），事件链经 C-003/C-043/C-094/C-101/C-102/C-109 六轮核查彻底
- **累计**: 64 发现（P0:0 / P1:0 / P2:10 / P3:54）+ 106 clean 记录
- **提交**: 见 git log（本轮 rounds/R16 文件 + progress 索引同步更新）
- **R16 收尾结论**: 十六轮共 64 项发现无 P0/P1；10 项 P2 全部未修。R17 候选：deposit 资金域补面（R6 完结时 deposit 1045 行标注未深挖——押金链 R1 审状态机/F-005/006、R4 审并发 F-053/054、R5 审退款公式，本轮换面：押金扣款/扣除/转账链路）。
