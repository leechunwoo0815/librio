# R148 第一百四十八轮 副本创建管理端复核 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：F-115 起，C-242 起。

## 范围

R148 副本创建管理端复核（book/service.py create_book_copy_admin L374-414 + barcode 查重链 + F-086/F-095
对照）。R109（F-095 batch_generate_copies 并发条码/F-086 无唯一约束）、R25（C-118 拷贝管理）已审相邻，
单副本创建入口本轮定向纵深。

## 结果

- **发现 1 项**（P3×1）：F-115 create_book_copy_admin barcode 查重无锁（F-095 同类入口，F-086 叠加）
- **clean 1 项**：C-242 副本创建其余面正确（book 校验 + uuid 默认 + 库存 +1）

---

## [F-20260808-115] create_book_copy_admin barcode 查重无锁——并发/重复创建同条码副本（P3）

- **级别**: P3（弱观察；管理端低频 + 并发罕见；重复条码致借还歧义——F-086 无唯一约束叠加）
- **位置**: backend/domain/book/service.py:382-389（barcode 查重段）
- **类别**: 先查后插无锁（模式⑦ 家族；F-095 同根另一入口——模式 1 同类漏改）
- **事实**: 手动 barcode 查重 `get_by_barcode`（L383-387）**无 with_for_update**——并发两个 create_book_copy_admin
  同 barcode：双方都查不到 → 双副本同条码；且 BookCopy.barcode **无唯一约束**（F-086 已报，DB 不挡）
- **证据**:
  - service.py:383-389 普通 query 查重（无行锁/无唯一约束兜底）
  - F-086：BookCopy.barcode 列无 unique → 重复条码可落库
  - **同类对照**：F-095（R109 batch_generate_copies 并发生成重复条码）——同根（先查后插 + 无唯一约束）
    **不同入口**（批量生成已报，单副本创建漏改）——防复发红线 1
  - R25 C-118（L45"拷贝管理：状态初始化 ✓"）未覆盖 barcode 查重无锁面
- **触发**: 管理端并发/重复创建同手动 barcode 副本（双端操作/双击重试）
- **影响**: 同条码多副本 → 借还扫描歧义（F-086/F-095 影响链一致）；库存重复 +1（L402-403 双加）。
  低频 + 罕见，P3
- **建议**: ① barcode 加 unique=True（F-086 建议，DB 兜底）；② 查重加 with_for_update 或条件插入
  （F-095 修复同模式——统一处理）
- **排重**: F-095（R109 batch 入口）已报；本条单副本入口首次（同类漏改）；F-086（无唯一约束）为根因面
  已报——本条为操作面补充

---

## [C-20260808-242] 副本创建其余面（book 校验/uuid/库存） — clean

- **方法**: R148 定向纵深。读 book/service.py:374-414（create_book_copy_admin 全）+ R25 对照 + 排重
- **证据**:
  - **book 存在校验**：get_by_id + is_deleted 拦截（L379-381，NotFoundError）✓
  - **默认 barcode**：无手动条码时 uuid 生成（L390-391，唯一性由 uuid 保证）✓
  - **库存同步**：total/available_stock +1（L402-403）✓（F-001/004 无锁面已报排重）
  - **状态初始化**：status=0 可借（R25 C-118 已核）✓
  - **权限**：bookcopy.create require_perm（router）✓
- **排重**: R25（C-118）、R109（F-095/F-086）互补；本条为 barcode 查重无锁 + 其余面 clean

---

## R148 完结汇总

- **范围**: 副本创建管理端（barcode 查重面）
- **结果**: 发现 1 项（F-115 P3）+ clean 1 项（C-242）
- **关键结论**:
  - 单副本创建 barcode 查重无锁——F-095 同类另一入口（F-086 无唯一约束叠加），修复须统一
  - book 校验/uuid 默认/库存同步工程正确
- **累计**: 114 发现（P0:0 / P1:0 / P2:14 / P3:100）+ 238 clean 记录
- **提交**: 见 git log（本轮 rounds/R148 文件 + progress 索引同步更新）
- **R148 收尾结论**: 一百四十八轮**发现总数破 114**（P3 破百）。R149 候选：继续轮转新面（如 get_related_books
  复核、save_book_page_admin 复核、dashboard 用户侧补面）。
