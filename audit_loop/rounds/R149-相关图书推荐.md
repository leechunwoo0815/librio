# R149 第一百四十九轮 相关图书推荐复核 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：F-116 起，C-243 起。

## 范围

R149 相关图书推荐复核（book/service.py get_related_books L331-351 + **is_published 全库消费面核查**）。
R147（F-114 toggle 竞态）、R25（C-118 书域）、R66（BookPage 渲染）已审相邻，发布状态消费面本轮定向纵深。

## 结果

- **发现 1 项**（P3×1）：F-116 is_published 写而不消费——下架功能用户端无效（死字段断链，F-068 同类）
- **clean 1 项**：C-243 get_related_books 其余面正确（theme 匹配/limit/NotFound）

---

## [F-20260808-116] is_published 全库写而不消费——toggle_publish 下架后用户端搜索/推荐/详情均不过滤（P3）

- **级别**: P3（低-中：功能失效——"下架"操作无实际效果；数据可见性，无资金/安全）
- **位置**: backend/domain/book/service.py:352-373（toggle_publish 写）/331-351（get_related_books 不过滤）/
  backend/domain/book/repository.py:32（search 不过滤）
- **类别**: 死字段断链（F-068 同类模式——字段写路径无消费方）
- **事实**: is_published 全库消费点仅 3 处：toggle_publish 写入（service.py:365）+ admin BookUpdate schema
  可选字段（admin_schemas.py:508）+ toggle 返回——**用户端所有查询（search_books/get_related_books/
  get_book_detail/list 等）均无 is_published 过滤**
- **证据**:
  - grep is_published 全库：仅 3 处（admin_schemas 可选、toggle 写、toggle 返回）——**无任何查询过滤消费**
  - get_related_books（L339-347）：filter 仅 theme/id!=/is_deleted——无 is_published
  - repo.search（L32）：filter 仅 is_deleted——无 is_published（R136 C-230 复核时确认）
  - **影响链**：管理端 toggle_publish 下架（写 is_published=0）→ 用户端搜索/推荐/详情**仍显示该书**——
    "下架"操作无实际效果（F-114 竞态是写面，本条是**消费缺失**面——更本质）
  - **同类先例**：F-068（R19 evaluation ar_level 字段断链——唯一写路径无消费方）——is_published 为同模式
    第二例
- **触发**: 管理端下架违规/下架书目 → 用户端仍可见可借
- **影响**: ① 下架功能失效（管理操作无效果，书目管控缺手段）；② 已下架书仍可被用户搜索/预约/借阅——
  内容管控面缺口。P3（无资金/安全，但功能语义断裂）
- **建议**: ① 用户端查询统一加 is_published == 1 过滤（search/recommend/detail/list）；② 或确认产品是否
  仍需下架功能——不需要则删除 is_published 字段（F-068 修复同模式：接线或去字段二选一）
- **排重**: F-114（R147 toggle 写面竞态）为写面，本条为**消费缺失面**（不同）；F-068（ar_level 断链）为
  同类先例；grep 无"is_published 无过滤/下架失效"历史

---

## [C-20260808-243] get_related_books 其余面（theme 匹配/limit/NotFound） — clean

- **方法**: R149 定向纵深。读 book/service.py:331-351 + 全库 is_published grep + R25/R66 对照 + 排重
- **证据**:
  - **theme 匹配**：同主题推荐 + 排除自身（L339-344）✓
  - **limit 有界**：limit 6 默认（L331）✓
  - **NotFound**：book 不存在/软删拦截（L334-336）✓
  - **空主题**：theme 为空返回 []（L337-338）✓
- **排重**: R25（C-118）、R66（C-159 BookPage）、R147（F-114）互补；本条为消费面 + 其余 clean

---

## R149 完结汇总

- **范围**: 相关图书推荐 + is_published 消费面
- **结果**: 发现 1 项（F-116 P3）+ clean 1 项（C-243）
- **关键结论**:
  - **is_published 写而不消费**——下架功能用户端无效（F-068 同类断链第二例，比 F-114 竞态更本质）
  - get_related_books 其余面工程正确
- **累计**: 115 发现（P0:0 / P1:0 / P2:14 / P3:101）+ 239 clean 记录
- **提交**: 见 git log（本轮 rounds/R149 文件 + progress 索引同步更新）
- **R149 收尾结论**: 一百四十九轮共 115 项发现无 P0/P1；14 项 P2。R150 候选：继续轮转新面（如
  save_book_page_admin 复核、dashboard 用户侧补面、book 详情页复核）。
