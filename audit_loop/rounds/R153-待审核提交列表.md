# R153 第一百五十三轮 待审核提交列表复核 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：F-117 起，C-247 起。

## 范围

R153 待审核提交列表复核（admin user_service.py list_pending_submissions L132-153 + 截断模式对照）。
R109（F-094 list_bookcopies 500 截断）、R120（F-108 导出 10000 截断）已报同类，待审核列表本轮定向纵深。

## 结果

- **发现 1 项**（P3×1）：F-117 list_pending_submissions limit(100) 静默截断无分页（F-094/F-108 同类第 3 处）
- **clean 1 项**：C-247 待审核列表其余面正确（过滤/排序/字段）

---

## [F-20260808-117] list_pending_submissions limit(100) 静默截断——超 100 条待审核不可见（P3）

- **级别**: P3（弱观察；管理端审核遗漏；触发依赖待审核提交积压 > 100）
- **位置**: backend/domain/admin/services/user_service.py:132-153（list_pending_submissions）
- **类别**: 数据可见性缺口（F-094/F-108 同模式——截断无提示无分页，第 3 处同类漏改）
- **事实**: 待审核列表硬编码 `.limit(100)`（L143）**无分页参数、无 total/has_next 提示**——提交积压超 100
  条时第 101+ 条待审核提交管理端不可见
- **证据**:
  - user_service.py:143 `.limit(100)` + 返回裸 list（L146-152，无分页元数据）
  - **同类对照**：F-094（R109 list_bookcopies limit 500 截断）+ F-108（R120 导出 limit 10000 截断）——
    同一"截断无提示"模式，本条为第 3 处（模式 1 防复发红线）
  - **影响链**：不可见的待审核提交持续 PENDING → F-085（R55 每日重复提醒）继续骚扰家长 + 审核 SLA
    堆积（R131 SLA 任务已覆盖审计但管理端无法看到全部）
- **触发**: 待审核提交超 100 条（运营积压/审核不及时）
- **影响**: ① 审核遗漏（第 101+ 条不可见无法操作）；② 与 F-085 叠加：被漏审提交持续提醒轰炸。
  积压场景低频 + P3 弱
- **建议**: ① 加 page/page_size 分页 + total/has_next（对齐 list_users_with_children 契约）；② 或至少
  返回 count 提示"共 N 条，已显示前 100"；③ 与 F-094/F-108 一并修复（同类截断统一处理）
- **排重**: F-094（R109 列表）/F-108（R120 导出）已报不同入口；本条待审核列表首次（同类第 3 处）；
  R111（F-098 提醒）/R116（工作台 pending limit 20 为有界展示设计）不同面

---

## [C-20260808-247] 待审核列表其余面（过滤/排序/字段） — clean

- **方法**: R153 定向纵深。读 user_service.py:132-153 + R55/R116 对照 + 排重
- **证据**:
  - **状态过滤**：status==0（PENDING）+ is_deleted==0（L137-139）✓
  - **排序**：create_time desc（最新优先）✓
  - **字段受控**：id/child_id/book_id/submitted_at（L146-152）✓
  - **权限**：submission.list require_perm（router）✓
- **排重**: R55（F-085 提醒链）、R116（C-210 工作台 pending 有界展示）互补；本条为截断面 + 其余 clean

---

## R153 完结汇总

- **范围**: 待审核提交列表（截断面）
- **结果**: 发现 1 项（F-117 P3）+ clean 1 项（C-247）
- **关键结论**:
  - limit(100) 静默截断无分页——F-094/F-108 同类第 3 处（截断家族统一修复）
  - 与 F-085 叠加：漏审提交持续提醒骚扰
  - 过滤/排序/字段工程正确
- **累计**: 116 发现（P0:0 / P1:0 / P2:14 / P3:102）+ 243 clean 记录
- **提交**: 见 git log（本轮 rounds/R153 文件 + progress 索引同步更新）
- **R153 收尾结论**: 一百五十三轮共 116 项发现无 P0/P1；14 项 P2。R154 候选：继续轮转新面（如
  get_user_detail 复核、admin_update_child 复核、audio 域补面）。
