# R12 第十二轮 小程序端补面（前端-后端链路）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-105 起（本轮零发现）。

## 范围

R12 小程序端第三轮攻击面。R1（5.1 data null / 5.2 清理 F-014 / 5.3 网络兜底 / 5.4 iOS 支付）与
R2（5.1 事件绑定 / 5.2 定时器清理 / 5.3 网络兜底第二轮）已审。本轮换面（深度递增：链路/异常/重放视角）：
① 本地存储安全（setStorageSync 内容）；② 登录态恢复与 401 清理链路；③ 支付/下单前端防重锁；
④ request.js 鉴权/超时/错误处理；⑤ 前端敏感数据展示；⑥ 前端调用端点 vs 后端路由存在性。

## 结果

- **发现 0 项**
- **clean 1 项**：C-105 小程序前端面第三轮整体安全

---

## [C-20260808-105] 小程序前端面第三轮（存储/登录/支付/请求/端点契约） — clean

- **方法**: R12 定向纵深。① 全库 grep setStorageSync/getStorageSync 枚举存储键；② 读 app.js 全文（登录态恢复/缓存清理/网络检测/隐私授权）；③ 读 utils/request.js 全文（鉴权头/401/403/超时/错误）；④ 读 deposit.js payDeposit/official.js 下单支付链（防重锁）；⑤ grep 前端 WXML 敏感字段展示（phone/openid/password/身份证）；⑥ 脚本提取后端 334 路由 vs 前端 api.js 37 个直接调用端点对比存在性
- **证据**:
  - **存储内容安全**：本地存储仅 token/userInfo/currentChildId/quiz_wrong_*/privacy_agreed（login.js:51-52/158-159、member.js:110、quiz.js:287）——无密码/身份证/支付凭证；token 本地存储为小程序标准做法 ✓
  - **登录态恢复**：onLaunch 读 savedToken 才恢复 userInfo（app.js:21-27），token 缺失则 userInfo 不恢复——无残留 userInfo 冒充登录 ✓
  - **401 清理链路**：request.js:61-70 清 token + globalData 全部清空 + reLaunch 登录页；localStorage 中 userInfo 残留但 onLaunch 因 token 缺失不恢复——安全 ✓；登录页内 401 直接 reject 不重定向（防循环）✓
  - **403 处理**：带 error_code（如 consent_required）交调用方处理 UX，不弹通用提示（request.js:78-89）✓
  - **支付防重锁**：payDeposit/repayDeposit/payFines/official 下单支付均 `loading: true` 前置 + finally 复位（deposit.js:119/167/207、official.js:124）——防双击；refund-apply/benefit-transfer 有独立 submitting 锁 ✓；真正防重在服务端（R4 F-053/F-054 已审后端并发，前端锁为体验层）✓
  - **请求封装**：网络预检（getNetworkType none 拦截）、超时（POST 15s/GET 10s）、统一错误消息（detail/message 提取）、GET 无自动重试（避免重复副作用）✓
  - **敏感数据展示**：WXML 无 phone/openid/password/身份证展示（grep 命中的 phonetic 为音标误报）✓
  - **端点契约**：后端 334 路由（FastAPI app.routes 枚举）vs 前端 api.js 37 个直接 req 调用——零缺失端点 ✓
  - **quiz 缓存清理**：quiz_wrong_*/mw_quiz_progress_* 7 天过期清理（app.js:40-63）✓
  - **网络状态提示**：onNetworkStatusChange 断网 toast（app.js:65-71）✓
- **排重**: R12 本轮前端面 clean 侧（零新缺陷）；R1/R2 的 5.1-5.4 与 C-053/054/055/056 为前两轮面；F-011（音频泄漏，前端展示面）已报不重报；F-020（phone 明文日志，后端日志面）不涉前端展示

---

## R12 完结汇总

- **范围**: 小程序前端第三轮（存储/登录/支付/请求/敏感展示/端点契约六面）
- **结果**: 发现 0 项 + clean 1 项（C-105）
- **关键结论**:
  - 前端面经三轮审查（R1 数据/清理/网络/iOS + R2 事件/定时器 + R3 存储/登录/支付/请求/契约）全部干净——小程序端工程化质量高
  - 前端防重锁仅体验层，服务端并发防护已在 R4（F-053/F-054）与 R9（F-059/F-060）覆盖——前端-后端防护边界清晰
  - 本轮为合法零发现（铁律 3：零发现 = 证据强度高的信号），非遗漏
- **累计**: 61 发现（P0:0 / P1:0 / P2:10 / P3:51）+ 102 clean 记录
- **提交**: 见 git log（本轮 rounds/R12 文件 + progress 索引同步更新）
- **R12 收尾结论**: 十二轮共 61 项发现无 P0/P1。R13 候选：定时任务第三轮（时区/大表/错峰，维度 7 第二轮——R1 7.1-7.4 + R2 换面已审，本轮深度递增：任务内 SQL 与状态机接缝）。
