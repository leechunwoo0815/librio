# R29 第二十九轮 剩余小域综合补面（profile/wechat/security）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-122 起（本轮零发现）。

## 范围

R29 剩余小域综合补面（此前零审查记录）：profile 域（阅读名片聚合）、wechat 域（access_token 缓存/QR 码）、
security 域（内容安全检测）、consent（voice_consent 已审 R9 排重）。检查：token 缓存并发、QR 参数注入、
内容检测完备性、聚合查询 N+1。

## 结果

- **发现 0 项**
- **clean 1 项**：C-122 剩余小域整体安全（token 缓存工程化 + QR check_path 兜底 + check-text 完备）

---

## [C-20260808-122] 剩余小域（profile/wechat/security） — clean

- **方法**: R29 定向纵深。读 wechat/service.py 全（_AccessTokenCache Redis 主+内存降级+双重检查锁/get_access_token/_fetch_access_token/get_unlimited_qr_code）+ wechat/router.py（QR 端点）+ security/router.py（check-text 全）+ profile/service.py（只读聚合）+ consent 排重
- **证据**:
  - **access_token 缓存**（wechat/service.py:30-96）：Redis 共享（多实例不互踢）+ 进程内存降级 + 双重检查锁防击穿（L85-96）+ CACHE_TTL=7000 提前刷新（微信 7200s 有效，留 200s 余量）✓ 工程化质量高（P2-3 审查点已闭环）
  - **QR 码安全**（router.py:30-50）：scene `max_length=32` + page `max_length=256`（Query 校验）+ get_current_user 鉴权 + `check_path=True`（微信侧校验 page 存在，page 路径注入被微信拦截）+ 微信 API 对非法 scene 拒绝 ✓
  - **内容安全检测**（security/router.py）：v2 suggest 检查（pass/review/risky）+ review 一并拦截（儿童产品无人工复核）+ detail 逐项兜底（任一 risky 即拦）+ errcode 87014 违规拦截 + 异常降级提示 ✓（P2-1 审查逻辑已闭环）
  - **check-text 调用面**：前端 utils/security.js:7（L1272 已确认绕过 api.js 直接调用路径）+ child-manage/refund-apply 短文本调用（无超长注入面）✓
  - **profile 聚合**（profile/service.py:23-70）：ChildLevel/Level 单查 + ChildAchievement batch in_ 预取（L43-49，无 N+1）✓ 只读
  - **consent**：voice_consent 同意校验已审（R9 C-102 save_recording）排重 ✓
- **排重**: R29 本轮剩余小域 clean 侧（零新缺陷）；R9（voice_consent）/R12（C-105 ownership）互补

---

## R29 完结汇总

- **范围**: 剩余小域（profile/wechat/security/consent）
- **结果**: 发现 0 项 + clean 1 项（C-122）
- **关键结论**:
  - wechat access_token 缓存是工程化范本（Redis 共享 + 双重检查锁 + TTL 余量）
  - QR 码安全（微信侧 check_path 兜底 + 长度限制）与内容安全检测（suggest+detail 双兜底）完备
  - 本轮为合法零发现（铁律 3）
- **累计**: 74 发现（P0:0 / P1:0 / P2:10 / P3:64）+ 119 clean 记录
- **提交**: 见 git log（本轮 rounds/R29 文件 + progress 索引同步更新）
- **R29 收尾结论**: 二十九轮共 74 项发现无 P0/P1；10 项 P2 全部未修。R30 候选：对 R10-R29 新发现做模式汇总（先查后插无唯一约束 ×3、schema 校验缺失 ×4、业务规则绕过 ×2）或继续轮转新面。
