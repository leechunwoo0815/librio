# R158 第一百五十八轮 微信订阅消息链复核 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-252 起（本轮零发现）。

## 范围

R158 微信订阅消息链复核（integrations/wechat/subscribe.py 全 + message/models.py after_insert 事件钩子 +
R142 密钥面对照）。R142（C-236 微信集成基座）、R22（消息域）、R44（消息管理端）已审相邻，
订阅推送链本轮定向纵深。

## 结果

- **发现 0 项**
- **clean 1 项**：C-252 订阅消息链安全（三级条件 + 异步非阻塞 + after_insert 统一接线）

---

## [C-20260808-252] 微信订阅消息（条件降级/异步/事件接线） — clean

- **方法**: R158 定向纵深。读 subscribe.py 全（~130 行）+ message/models.py after_insert 钩子 + R142/R22
  对照 + 排重
- **证据**:
  - **三级条件**：总开关（WECHAT_SUBSCRIBE_ENABLED）+ openid 有效 + 模板 ID 命中（L47-55）——任一不满足
    降级只落库返回 False ✓
  - **异步非阻塞**：daemon 线程 asyncio.run（L63-68）——不阻塞请求/事件循环；失败静默（L64-66）✓
  - **after_insert 统一接线**：SystemMessage 落库事件钩子（L97-120）——所有创建点零改动接线 + user_id>0
    过滤 + 异常捕获 non-blocking ✓
  - **openid 查询**：同一事务连接查 user.openid + is_deleted 过滤（L106-112）——user 已存在与 message
    插入无依赖 ✓
  - **模板未配置降级**：SubscribeTemplate 模板 ID 为空（R142 C-236 已核）→ get_template_for_title 返回
    None → 只落库 ✓（设计正确，非缺陷）
  - **权限**：无直接用户入口（内部推送链）✓
  - **弱观察 C 记**：每次推送 spawn daemon 线程（无线程池）——消息量小时可接受；量大时线程堆积（低频
    推送场景不构成缺陷）
- **排重**: R142（C-236 集成基座/密钥）、R22（F-070 消息分组）、R44（消息管理端）互补；
  R158 本轮回调订阅消息 clean 侧（零新缺陷）

---

## R158 完结汇总

- **范围**: 微信订阅消息（推送链）
- **结果**: 发现 0 项 + clean 1 项（C-252）
- **关键结论**:
  - 三级条件 + 异步非阻塞 + 失败静默 + 降级只落库——推送链工程正确
  - after_insert 统一接线零改动覆盖全部创建点
  - 本轮为合法零发现（铁律 3）
- **累计**: 118 发现（P0:0 / P1:0 / P2:14 / P3:104）+ 248 clean 记录
- **提交**: 见 git log（本轮 rounds/R158 文件 + progress 索引同步更新）
- **R158 收尾结论**: 一百五十八轮共 118 项发现无 P0/P1；14 项 P2。R159 候选：继续轮转新面（如
  advancement 补面、vocabulary 管理端复核、consent 管理面复核）。
