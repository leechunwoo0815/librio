# R139 第一百三十九轮 库存对账任务 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-233 起（本轮零发现）。

## 范围

R139 库存对账任务（scheduler.py reconcile_stock L257-342：Book 库存字段与 BookCopy 实际计数每日对齐）。
R5（已评 reconcile 任务 clean）、R13（引用 R5 结论）、F-001/004（库存无锁已报）已审相邻，
库存对账任务本轮定向纵深复核。

## 结果

- **发现 0 项**
- **clean 1 项**：C-233 库存对账安全（R5 复核 + 批量高效 + 纠正性幂等）

---

## [C-20260808-233] reconcile_stock（对账口径/效率/纠正性幂等） — clean

- **方法**: R139 定向纵深。读 scheduler.py:257-342（reconcile_stock 全）+ R5 结论对照 + F-001/004 排重
- **证据**:
  - **对账口径正确**：total = AVAILABLE/BORROWED/MAINTENANCE/DAMAGED 计数（L267-276，排除报废/丢失——
    与 R109 set_copy_status 对账口径一致）✓；available = AVAILABLE 计数（L278-287）✓
  - **批量高效**：两次 group_by 一次查询（防 N+1）✓
  - **纠正性幂等**：全量重算写回（L304-315）——基于 BookCopy 当前状态 count，重跑结果一致 ✓
  - **偏差留痕**：OperationLog 记录 expected/actual JSON（L297-306）✓ + 仅偏差才写（fixed 计数）✓
  - **R5 已评排重**：R5 明确"reconcile 任务 clean（对账重算口径一致、非资金）"——本轮复核确认无退化 ✓
  - **F-001/004 排重**：库存无锁（借还链并发面）已报；对账任务是**纠正性全量重算**（写回最新 count），
    非增量累计——与无锁缺陷面不同 ✓
  - **异常处理**：try/except + rollback + 日志 ✓
  - **弱观察 C 记**：对 Book 行无 with_for_update——纠正性写回（基于最新 count）不受并发影响（写回即正确值）
    ，不构成缺陷
- **排重**: R5（已评）、F-001/004（无锁面不同）、R109（set_copy_status 同口径对照）互补；
  R139 本轮回调库存对账 clean 侧（零新缺陷）

---

## R139 完结汇总

- **范围**: 库存对账任务（每日对齐）
- **结果**: 发现 0 项 + clean 1 项（C-233）
- **关键结论**:
  - 对账口径与 R109 set_copy_status 一致 + 批量高效 + 纠正性幂等
  - R5 已评 clean，本轮复核无退化
  - 本轮为合法零发现（铁律 3）
- **累计**: 110 发现（P0:0 / P1:0 / P2:14 / P3:96）+ 229 clean 记录
- **提交**: 见 git log（本轮 rounds/R139 文件 + progress 索引同步更新）
- **R139 收尾结论**: 一百三十九轮共 110 项发现无 P0/P1；14 项 P2。R140 候选：继续轮转新面（如 borrow 用户侧
  复核、parent_course_time 管理端补面、purge_expired_data 数据清理复核）。
