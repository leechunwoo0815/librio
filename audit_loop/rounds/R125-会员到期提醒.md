# R125 第一百二十五轮 会员到期提醒任务 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-219 起（本轮零发现）。

## 范围

R125 会员到期提醒任务（scheduler.py check_member_expiry L535-608 + ConfigService.get_int_list 配置 +
与 F-085 无去重模式对照）。R55（F-085 提醒去重）、R13（删除任务面）、R60（overdue 复核对）已审相邻任务，
到期提醒任务首次定向纵深。

## 结果

- **发现 0 项**
- **clean 1 项**：C-219 会员到期提醒安全（天然去重 + 高效查询 + 配置驱动）

---

## [C-20260808-219] check_member_expiry（天然去重/查询效率/配置驱动） — clean

- **方法**: R125 定向纵深。读 scheduler.py:535-608（check_member_expiry 全）+ ConfigService.get_int_list +
  F-085 对照 + 排重
- **证据**:
  - **天然去重（无 F-085 同类问题）**：notify_days = [30,15,7,3,2,1,0] 按目标日期精确匹配——孩子到期日在
    "7 天后"仅 7 天前那天发（明天变 6 天后不在列表）→ **每日不重复**（与 F-085 remind_pending 的"同一状态
    每日重复匹配"本质不同）✓
  - **查询效率**：一次查询到期日在 [min,max] 范围内孩子 + 按日期分组（L550-565），非 N 次循环 ✓
  - **配置驱动**：notify_days 可配置（get_int_list，L544）+ 空配置早退（L549）✓
  - **消息语义**：days=0 到期当天提醒 + priority 分级（≤3 高优先，L574-575）✓
  - **异常处理**：try/except + rollback + 日志（L579-582）✓ + own_session 关闭（L583-585）✓
  - **续费后不再提醒**：续费更新 member_expire_time → 下次查询不匹配旧日期 ✓
  - **弱观察 C 记**：无 @distributed_lock（对照其他任务有锁）——单实例 scheduler + 同日重跑双发概率极低，
    且消息内容相同（重复无害），不构成新缺陷
- **排重**: F-085（R55 pending 提醒无去重）为不同模式（状态重复 vs 日期精确）；R13（删除面）、R60（overdue）
  互补；R125 本轮回调到期提醒 clean 侧（零新缺陷）

---

## R125 完结汇总

- **范围**: 会员到期提醒任务
- **结果**: 发现 0 项 + clean 1 项（C-219）
- **关键结论**:
  - 到期提醒按日期精确匹配天然去重（与 F-085 状态重复模式本质不同，无同类缺陷）
  - 查询效率/配置驱动/异常处理工程正确
  - 本轮为合法零发现（铁律 3）
- **累计**: 108 发现（P0:0 / P1:0 / P2:14 / P3:94）+ 215 clean 记录
- **提交**: 见 git log（本轮 rounds/R125 文件 + progress 索引同步更新）
- **R125 收尾结论**: 一百二十五轮共 108 项发现无 P0/P1；14 项 P2。R126 候选：继续轮转新面（如 graduate_children
  毕业任务、alert_stale_refunds 退款告警、venue 用户侧补面）。
