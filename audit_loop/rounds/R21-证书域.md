# R21 第二十一轮 证书域（level_certificate 生成链）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-114 起（本轮零发现）。

## 范围

R21 证书域（此前零审查记录）。CertificateService 全（get_child_certificates/generate_certificate/
get_certificate/create_level_certificate/render_certificate_html）+ 模型（唯一约束）+ router（归属）+
level.advanced 事件链（防重闭环）+ 管理端 regenerate/delete（R11 已核权限）。

## 结果

- **发现 0 项**
- **clean 1 项**：C-114 证书域生成链整体安全（幂等 + 归属 + 转义 + 事件闭环）

---

## [C-20260808-114] 证书域（生成幂等/归属校验/HTML 转义/事件链闭环） — clean

- **方法**: R21 定向纵深。读 certificate/service.py 全 151 行 + models.py（唯一约束）+ router.py（归属）+
  misc_handlers.py:12-17（level.advanced 证书 handler）+ advancement/service.py:400（发布点）+ C-101/C-094 对照
- **证据**:
  - **生成幂等**：generate_certificate（L38-85）先查 `get_by_child_and_level`（L41-43）已有则返回 already_granted ✓；create_level_certificate（L104-117）按 level_name 查 Level 后调 generate ✓
  - **事件链闭环**：level.advanced 发布点（advancement/service.py:400）在 check_and_advance 内——C-101 已确认"并发双事件第二个事务重取 is_current 行已晋级则跳过"→ 双投递路径阻断；handle_level_advanced_for_certificate（misc_handlers.py:12-17）调 create_level_certificate（内部再幂等）——双层防重 ✓
  - **归属校验**：GET /certificate/{child_id} 用 GetOwnedChild()（router.py:16）——家长只能看自己孩子证书 ✓
  - **HTML 转义**：render_certificate_html Jinja2 `autoescape=select_autoescape(default=True)`（L136-138）——child_name/level_name/certificate_no 自动转义，无存储型 XSS ✓
  - **cert_no 生成**：`MW-YYYYMMDD-UUID8`（L62）唯一性高 ✓
  - **管理端操作**：regenerate_certificate/delete_certificate 在 admin_advancement_router（R11 已核 require_perm ✓）
  - **删除覆盖**：level_certificate 在 ChildDeletionService DELETE_TABLES_BY_CHILD（R13 C-106 已核）✓
  - **唯一约束缺失**：LevelCertificate 无 (child_id, level_id) UniqueConstraint（models.py:14）——纵深缺失，但双投递路径已被 C-101 阻断 + generate 幂等，当前无实际双证书路径（风格观察，不构成发现）
- **排重**: R21 本轮证书域 clean 侧（零新缺陷）；C-101（quiz 晋级链）/C-094（21 handler）/C-106（删除覆盖）/R11（管理端权限）已报互补

---

## R21 完结汇总

- **范围**: 证书域（生成幂等/归属/转义/事件闭环/删除覆盖）
- **结果**: 发现 0 项 + clean 1 项（C-114）
- **关键结论**:
  - 证书域工程质量高：双层防重（发布点阻断 + handler 幂等）、归属校验、HTML 自动转义、cert_no 唯一
  - 唯一纵深缺失（无 DB 唯一约束）已被双层防重覆盖，无实际双证书路径
  - 本轮为合法零发现（铁律 3）
- **累计**: 68 发现（P0:0 / P1:0 / P2:10 / P3:58）+ 111 clean 记录
- **提交**: 见 git log（本轮 rounds/R21 文件 + progress 索引同步更新）
- **R21 收尾结论**: 二十一轮共 68 项发现无 P0/P1；10 项 P2 全部未修。R22 候选：消息域（SystemMessage 发送/已读链——F-044 已报 N+1，本轮换面：发送权限/目标校验/全勤消息）。
