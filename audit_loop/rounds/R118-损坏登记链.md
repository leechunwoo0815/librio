# R118 第一百一十八轮 损坏登记链（create_report 防重补面） — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：F-107 起，C-212 起。

## 范围

R118 损坏登记链补面（damage_admin_service.py create_report 全 144 行 + 与 R46 F-080/C-139 对照）。
R46 已报 confirm/reject/review 无锁（F-080），R77/R78 已报列表/序列化面——本轮定向纵深 **create_report
防重查重锁面**（F59 防重复登记的并发缺口）。

## 结果

- **发现 1 项**（P3×1）：F-107 create_report 防重查重无锁——并发双登记同一借阅双计罚款
- **clean 1 项**：C-212 create_report 其余面正确（状态守卫/丢失联动/库存保护/F61 差额覆盖）

---

## [F-20260808-107] create_report 防重查重无锁——并发双登记同一借阅双计罚款（P3）

- **级别**: P3（低-中：资金面多收罚款；触发依赖管理端并发双登记，低频；R46 F-080 同模式但定 P3 因入口
  主动操作并发罕见）
- **位置**: backend/domain/admin/services/damage_admin_service.py:62-77（active_report 防重查重）
- **类别**: 先查后改无锁（模式⑦ 家族，F-080 同模式——confirm/reject/review 已报，create 防重入口漏网）
- **事实**: create_report 的 F59 防重查重（L62-77）**无 with_for_update**——并发两个 create_report 对同一
  borrow_record：双方都查到无 active_report（PENDING/PENDING_REVIEW/DISPUTED）→ 都继续 → 各建一个报告 +
  各累计罚款
- **证据**:
  - damage_admin_service.py:62-77 `query(BookDamageReport).filter(...status in (PENDING, PENDING_REVIEW,
    DISPUTED)...).first()` 无 with_for_update（对照 BorrowRecord L40/Child L99/BookCopy L121 均有行锁——防重
    查重是链上**唯一无锁的读**）
  - 并发时序：A/B 同时过防重查重（都 sees 无报告）→ A 建报告 commit（child 行锁 L99 串行罚款）→ B 继续建
    第二报告 + child 罚款再计（child 锁内串行，但**防重已失效**）→ 双报告双罚款
  - F59 注释（L58-60）明确"同一借阅存在未终结报告时禁止重复登记（此前可重复计费）"——防重意图已被并发打破
  - R46 F-080：confirm/reject/review 无锁已报（P2 资金面）；本条为 create 防重入口——同模式漏改点
    （防复发红线 1：修复须 confirm/review/create 同加锁）
- **触发**: 管理端并发双登记同一借阅（双端同时操作/重试双击）
- **影响**: 同一损坏被登记两次 → 双报告 + 罚款 double（资金面多收）；与 F-080 合并则整链双计。管理端低频
  + 双端并发罕见，P3
- **建议**: active_report 查重加 `with_for_update()`（对齐 BorrowRecord/Child 行锁模式）；或唯一约束
  `(borrow_record_id, status)` 部分索引兜底；修复与 F-080 一并（同链同模式）
- **排重**: R46 F-080（confirm/reject/review）已报不同入口；R46 C-139（资金锁 confirm 侧）确认只核 confirm
  行锁，未覆盖 create 防重查重；grep 无"create_report 防重并发"历史

---

## [C-20260808-212] create_report 其余面（状态守卫/丢失联动/库存保护） — clean

- **方法**: R118 定向纵深。读 damage_admin_service.py:36-179（create_report 全）+ R46/R77/R78 对照 + 排重
- **证据**:
  - **借阅状态守卫**：仅 BORROWING/OVERDUE 可登记（L48-51）✓
  - **行锁**：BorrowRecord with_for_update（L40）+ Child with_for_update（L99）+ BookCopy with_for_update
    （L121/L152）——资金/状态写均有锁 ✓
  - **丢失联动（D05）**：level 3 → BookCopy LOST + 库存 max 扣减（L119-129，max(...,0) 防负——F-001/004 无锁
    已报排重）+ 借阅 LOST + 7 天寻找期（B10，L131-138）✓
  - **F61 差额覆盖**：level 3 覆盖已入账逾期费——delta 差额增量 + record.fine_in_outstanding 同步标记
    （L105-110）✓
  - **非丢失 fine 累计**：record.fine_amount += fine（record 有锁）✓；level 2 → 副本 DAMAGED（F50）✓
  - **三级定级 + B9 双人复核**：needs_review 判断（L87-91）+ PENDING_REVIEW/PENDING 分支 ✓
  - **通知/日志**：_send_*_notification + _log_operation ✓
- **排重**: R46（F-080/C-139 资金锁面）、R77（F-089 列表）、R78（C-171 序列化枚举）互补；本条为 create
  其余面 clean 侧

---

## R118 完结汇总

- **范围**: 损坏登记（create_report 防重锁面 + 其余面）
- **结果**: 发现 1 项（F-107 P3）+ clean 1 项（C-212）
- **关键结论**:
  - F59 防重查重无锁——并发双登记双计罚款（F-080 同模式 create 入口漏改，修复须整链同加锁）
  - create_report 其余面工程正确（状态守卫/行锁/丢失联动/差额覆盖齐全）
- **累计**: 106 发现（P0:0 / P1:0 / P2:14 / P3:92）+ 208 clean 记录
- **提交**: 见 git log（本轮 rounds/R118 文件 + progress 索引同步更新）
- **R118 收尾结论**: 一百一十八轮共 106 项发现无 P0/P1；14 项 P2。R119 候选：继续轮转新面（如 export 导出链
  复核、refund 管理端补面、advancement 管理端）。
