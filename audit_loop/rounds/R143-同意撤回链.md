# R143 第一百四十三轮 同意撤回链 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-237 起（本轮零发现）。

## 范围

R143 同意撤回链（consent_service.py withdraw_consent L67-120 + grant_consent/get_consents/has_valid +
ChildDeletionService 级联删除）。R91（get_latest_valid 过滤）、R123（C-217 创建前置 consent）、R141
（C-235 consent 永不物理删除）已审相邻，撤回链首次定向纵深。

## 结果

- **发现 0 项**
- **clean 1 项**：C-237 同意撤回安全（前置全量校验 + 撤回标记 + 级联删除发起）

---

## [C-20260808-237] withdraw_consent（前置校验/级联删除/一致性） — clean

- **方法**: R143 定向纵深。读 consent_service.py 全（121 行）+ ChildDeletionService.check_deletion_blockers/
  request_deletion + R91/R123 对照 + 排重
- **证据**:
  - **child_data 撤回 = 级联删除（P0-3）**：先全量前置校验（check_deletion_blockers 逐 child，L77-82——
    任一阻塞整体拒绝，防半删除）✓
  - **撤回标记**：withdrawn_at 落库（L87/L105）→ get_latest_valid 返回 None（R91 已核过滤）→ 后续创建孩子
    被拒（R123 C-217 consent 前置）——一致性闭环 ✓
  - **级联删除发起**：request_deletion 逐 child 发起（L93-95）——删除队列语义（R13 C-106 已审删除链）✓
  - **其他类型撤回**：标记 withdrawn 幂等 ✓
  - **NotFoundError**：无有效同意记录时拒绝撤回（L74/L101）✓
  - **弱观察 C 记**：撤回 commit 后 request_deletion 循环（事务分割）——若个别删除请求失败，撤回已生效但
    孩子未入删除队列（可重试/人工补发）；删除队列机制（R13 C-106）覆盖重试面，弱不构成缺陷
- **排重**: R91（查询过滤面）、R123（创建前置面）、R141（永不删除面）互补；R143 本轮回调撤回链 clean 侧
  （零新缺陷）

---

## R143 完结汇总

- **范围**: 同意撤回（child_data 级联删除 + 其他类型）
- **结果**: 发现 0 项 + clean 1 项（C-237）
- **关键结论**:
  - child_data 撤回前置全量校验（任一阻塞整体拒绝）——工程正确
  - 撤回标记与创建前置/查询过滤一致性闭环
  - 本轮为合法零发现（铁律 3）
- **累计**: 111 发现（P0:0 / P1:0 / P2:14 / P3:97）+ 233 clean 记录
- **提交**: 见 git log（本轮 rounds/R143 文件 + progress 索引同步更新）
- **R143 收尾结论**: 一百四十三轮共 111 项发现无 P0/P1；14 项 P2。R144 候选：继续轮转新面（如 assessment
  管理端补面、grant_consent 授权面复核、dashboard 用户侧补面）。
