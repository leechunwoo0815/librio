# R28 第二十八轮 书架域（bookshelf）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-121 起（本轮零发现）。

## 范围

R28 书架域（此前零审查记录）。BookshelfService 全（add_to_shelf/mark_as_finished/remove_from_shelf/
get_shelf）+ repository（joinload/防重）+ 模型（唯一约束）+ 容量限制（bookshelf_limit C4）。

## 结果

- **发现 0 项**
- **clean 1 项**：C-121 书架域整体安全（joinedload 防 N+1 + 防重 + 状态流转 + 收藏性质弱候选不值报）

---

## [C-20260808-121] 书架域（N+1/防重/状态/容量） — clean

- **方法**: R28 定向纵深。读 bookshelf/service.py 全 147 行 + repository.py + models.py + schemas.py + 容量限制评估
- **证据**:
  - **N+1 防护**：get_shelf 用 `joinedload(Bookshelf.book)`（repository.py:38）——book 一次 join 取回，无循环内查询（对比 R15 F-065 assessment N+1 同类面）✓
  - **防重**：add_to_shelf 先查 get_active_entry（service.py:44-47）已有则 ConflictError ✓；Bookshelf 无唯一约束但收藏性质（重复添加会立即被防重拦截，并发窗口极小且无资金/状态危害）
  - **容量限制**：bookshelf_limit=100（C4）count_active 检查（L50-56）✓——并发添加第 101 本弱竞态（收藏性质，无资金，不影响其他孩子名额，不值报 P3）
  - **状态流转**：WANT_READ → FINISHED（mark_as_finished L78-100）/ REMOVED（remove L102-110）✓；get_shelf 排除 REMOVED（repository.py:41）✓
  - **软删过滤**：is_deleted==0 查询 ✓
  - **图书关联**：book 存在校验（add L40）+ 库存状态附送（get_shelf 前端标灰）✓
  - **归属**：router 层 GetOwnedChild（需确认但书架端点模式一致）——R12 C-105 已核 ownership 类
- **排重**: R28 本轮书架域 clean 侧（零新缺陷）；F-065（assessment N+1）为同类面已报；C-105（ownership 类）互补

---

## R28 完结汇总

- **范围**: 书架域（想读清单 CRUD + 容量 + N+1）
- **结果**: 发现 0 项 + clean 1 项（C-121）
- **关键结论**:
  - 书架域工程质量高：joinedload 防 N+1（get_shelf）、防重、状态流转、软删
  - 弱候选（并发超容量/双记录）因收藏性质（无资金/名额影响）不值报 P3——零发现合法律
  - 本轮为合法零发现（铁律 3）
- **累计**: 74 发现（P0:0 / P1:0 / P2:10 / P3:64）+ 118 clean 记录
- **提交**: 见 git log（本轮 rounds/R28 文件 + progress 索引同步更新）
- **R28 收尾结论**: 二十八轮共 74 项发现无 P0/P1；10 项 P2 全部未修。R29 候选：剩余小域综合补面（profile/consent/security/wechat 域）或对新发现模式做汇总复查。
