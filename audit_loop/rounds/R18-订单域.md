# R18 第十八轮 订单域补面（价格链 + 亲子课名额）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：F-067 起 / C-111 起。

## 范围

R18 订单域补面。R1（F-002/007/008/009）、R2（F-016/030/031/032/033）、R4（F-053）、R5（退款公式 C-011）
已审。本轮换面（R6 完结标注"order 764 行未深挖"）：订单价格链（create_order/_apply_discount/
_calc_observation_credit/create_upgrade_order/get_upgrade_options）+ 亲子课名额校验（B3 slot）。

## 结果

- **发现 1 项**：F-067（P3）B3 亲子课名额校验竞态（slot 无行锁 + paid_count 无锁 count，并发报名超员）
- **clean 1 项**：C-111 订单价格链整体正确（折扣/抵扣/升级/金额精度）

---

## [F-20260808-067] B3 亲子课名额校验竞态——slot 无行锁 + paid_count 无锁 count，并发报名超员 — P3

- **级别**: P3（观察项；需 MySQL 并发 + 多名家长同时报名同一时段才触发；SQLite 串行下 count 顺序一致）
- **维度**: R4 并发×资金补面（B3 名额面）
- **文件**: `backend/domain/order/service.py:178-199`（create_order 亲子课名额校验）/ `backend/domain/parent_course_time/models.py`（ParentCourseTime）
- **事实**:
  - create_order 亲子课分支（L178-199）：`slot = db.query(ParentCourseTime).filter(id==slot_id).first()`（L186-193，**无 with_for_update**）→ `if slot.status != 1: 拒绝` → `paid_count = db.query(Order).filter(parent_course_time_id==slot.id, type==PARENT_COURSE, pay_status==PAID).count()`（L195-198，**无锁 count**）→ `if paid_count >= slot.max_participants: 拒绝`
  - **竞态**：两名家长同时报名同一 slot（max_participants=10，当前 9 人已 PAID）→ 两事务同时 count()=9 < 10 → 都通过校验 → 都创建亲子课订单 → 都支付 → **11 人报名 10 人班**
  - 对比同文件防重模式：F22（同孩子同类型 PENDING 拦截 L120-128）与亲子课不可重复（L168-176）均带 `with_for_update()`——**名额校验缺同类行锁**（同类漏改）
  - parent_course_time 表无"已占名额"字段（注释明确"已支付订单计数口径，无需占/释放名额字段"——设计依赖 count，但 count 无锁）
- **证据**: ① service.py:178-199 代码（slot 查询无锁 + paid_count 无锁 count）；② 排重 grep：findings 无"名额/超员/max_participants/B3 并发"命中（L1124 为 PRD 字段对照，不同面）；③ 同文件 L120-128/L168-176 防重带锁对照（同类漏改证据）
- **触发**: 运营发布亲子课时段（max_participants=10），9 人已报名；两名家长几乎同时提交报名 → MySQL 下双事务 count 均通过 → 超员
- **影响**: 班级实际人数超员（11/10）——运营需人工处理（退费/加开时段）；无资金损失（报名费正确收取）。低频（亲子课名额少 + 并发报名窗口极窄），P3 观察
- **建议**: ① slot 查询加 `with_for_update()` 行锁（对齐 L120-128 模式——锁住 slot 行后 count 校验串行化）；② 或亲子课订单用条件 UPDATE 占位（如 slot 加已占人数列 + 条件更新按 affected 判定）；③ 或 paid_count 校验移到支付回调（PAID 时再次校验名额，超员则拒绝/排队）
- **排重**: 已 grep 确认不在 F-001~066 / C-001~110 中；F-053（cancel_order 无锁读-改-写）为订单状态面，本项为亲子课名额计数面；C-099（R6 借阅锁分层）不涉亲子课

---

## [C-20260808-111] 订单价格链（create_order/_apply_discount/_calc_observation_credit/升级） — clean

- **方法**: R18 定向纵深。读 order/service.py 全 764 行关键段：create_order（L109-265）/get_price_for_type/_get_price（L66-107）/_apply_discount（L278-381）/_calc_observation_credit（L382-416）/handle_payment_callback（L417-504）/generate_pay_code（L505-534）/get_upgrade_options（L578-650）/create_upgrade_order（L651-687）/calculate_refund（L704-753）+ 排重对照
- **证据**:
  - **金额后端计算**：base_amount 从 ConfigService 读取（L226-229，不信任前端）✓；观察期/会员/季度/半年价格 _get_price（L91-107）✓
  - **多孩优惠**（_apply_discount L278-381）：排除自身 + F5 paid_member_ever 快照兜底 + 续费缓冲期判定（member_expire_time<=now + 0<=days_expired<=grace_days 边界正确）+ 不可叠加取最低价 ✓（quantize 未指定 rounding 为 F-032 已报，排重）
  - **A6 观察期抵扣**（_calc_observation_credit L382-416）：实付 ÷ observation_days × 剩余天数 + ROUND_HALF_UP + min 封顶（不超实付）✓
  - **升级链路**（get_upgrade_options/create_upgrade_order L578-687）：归属校验 + 仅 OFFICIAL + 剩余价值按实付金额（F16）+ max(0, target-remaining) ✓（quantize 为 F-032 已报）
  - **防重完备**：F22 同类型 PENDING 拦截 + 亲子课不可重复（均 with_for_update）✓；观察期仅 TRIAL / 会员仅 OBS/OF/EXPIRED 前置校验 ✓
  - **回调幂等**：already paid + trade_no 重复 + 状态守卫（R16 C-109 已确认）✓
  - **退款公式**：calculate_refund 与 R5 C-011 同构（ROUND_HALF_UP + max(0) + free_days 钳制）✓
- **排重**: R18 本轮订单价格链 clean 侧（F-067 名额竞态为唯一缺口）；F-032（quantize rounding）/F-030/031/033/048 已报不重；C-099/R4 并发面互补

---

## R18 完结汇总

- **范围**: 订单域补面（价格链 + 亲子课名额 + 升级 + 退款）
- **结果**: 发现 1 项（F-067 P3 B3 名额校验竞态）+ clean 1 项（C-111 价格链正确）
- **关键结论**:
  - 订单价格链工程质量高：金额后端计算、折扣不可叠加取最低、A6 抵扣 ROUND_HALF_UP+封顶、升级按实付（F16）、F22 防重带锁
  - 唯一缺口：B3 亲子课名额校验（slot 无锁 + count 无锁）——并发报名超员；修复成本低（slot 加 with_for_update 对齐 F22 模式）
  - quantize rounding 不一致为 F-032 已报（订单域 3 处同类），排重
- **累计**: 66 发现（P0:0 / P1:0 / P2:10 / P3:56）+ 108 clean 记录
- **提交**: 见 git log（本轮 rounds/R18 文件 + progress 索引同步更新）
- **R18 收尾结论**: 十八轮共 66 项发现无 P0/P1；10 项 P2 全部未修。R19 候选：评估域（assessment/ar_evaluation/observation_evaluation 全链）或家长课程域（parent_course_time 排期/名额）。
