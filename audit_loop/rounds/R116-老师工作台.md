# R116 第一百一十六轮 老师工作台链 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-210 起（本轮零发现）。

## 范围

R116 老师工作台链（teacher_workbench_service.py 全 210 行：get_workbench D1 + post_feedback D2 +
TeacherSchedule 模型 + GuidanceRecord/ReadingSubmission 关联）。R30（post_feedback 无唯一约束需求）、
R55（F-038/F-085 scheduler 链）已涉相关面，工作台聚合面首次定向纵深。

## 结果

- **发现 0 项**
- **clean 1 项**：C-210 老师工作台链整体安全（聚合预取 + 归属校验 + 同事务推送）

---

## [C-20260808-210] 老师工作台（D1 聚合 + D2 课后反馈） — clean

- **方法**: R116 定向纵深。读 teacher_workbench_service.py 全（210 行）+ TeacherSchedule 模型 + GuidanceRecord/
  ReadingSubmission 关联 + 排重
- **证据**:
  - **get_workbench**：
    - teacher 存在校验（L28-34，is_deleted 过滤 + NotFoundError）✓
    - 今日课程：weekday 匹配排班 + is_deleted 过滤 + start_time 排序（L38-47）✓
    - 待审核提交：child_ids in_ 批量 + status PENDING + is_deleted==0 过滤 + limit 20 有界（L60-69）✓ ——
      **R55 F-038（scheduler 缺 is_deleted）在此已正确过滤，无同类缺口** ✓
    - 批量预取：book_ids/level_ids in_ 一次查询（L72-80/L95-103，防 N+1——R15 高效模式）✓
    - recent_guidance：limit 5 有界 + guidance_date 倒序（L117-126）✓
  - **post_feedback**：
    - teacher/child 存在校验（L164-178）✓
    - **归属校验**：child.teacher_id != teacher_id → ForbiddenError（L179-180，只能给本人负责的孩子）✓ ——
      防跨老师越权反馈（R3 ownership 面已核 GetOwnedChild 系列，本条独立校验）✓
    - 同事务：GuidanceRecord + SystemMessage 同 commit（L182-198）✓
    - 推送：user_id=child.user_id + msg_type=4（老师消息）✓
    - 操作日志：write_operation_log（L203-208）✓
  - **TeacherSchedule**：无独立状态字段（仅 weekday 匹配），无停用概念——匹配逻辑正确 ✓
  - **权限**：workbench/feedback 路由 require_perm（router）✓
- **排重**: R30（post_feedback 多条语义无唯一约束需求——反馈天然多条，正确）、R55（F-038/F-085 scheduler
  链不同面）、R15（N+1 高效模式）互补；R116 本轮回调工作台 clean 侧（零新缺陷）

---

## R116 完结汇总

- **范围**: 老师工作台（D1 聚合 + D2 课后反馈）
- **结果**: 发现 0 项 + clean 1 项（C-210）
- **关键结论**:
  - get_workbench 工程正确（批量预取/有界 limit/软删过滤齐全）
  - post_feedback 归属校验 + 同事务推送 + 日志完备（R30 已证多条反馈为设计语义）
  - 本轮为合法零发现（铁律 3）
- **累计**: 104 发现（P0:0 / P1:0 / P2:14 / P3:90）+ 206 clean 记录
- **提交**: 见 git log（本轮 rounds/R116 文件 + progress 索引同步更新）
- **R116 收尾结论**: 一百一十六轮共 104 项发现无 P0/P1；14 项 P2。R117 候选：继续轮转新面（如 upload 上传、
  damage_admin 列表面、export 导出链复核）。
