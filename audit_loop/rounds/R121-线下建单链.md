# R121 第一百二十一轮 线下建单链（create_offline_order 资金面） — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-215 起（本轮零发现）。

## 范围

R121 线下建单链（admin order_service.py：create_offline_order L312-393 + _validate_amount L251-260 +
_audit_amount_override L261-311 + User.phone 唯一约束）。R107（admin_create_user 同款 user/child 创建）、
R105（update_order_status F28 矩阵）、R53（F-084 退款）已审相邻面，线下建单资金面首次定向纵深。

## 结果

- **发现 0 项**
- **clean 1 项**：C-215 线下建单链安全（phone unique 兜底 + 金额范围 + F11 审计 + F5 快照）

---

## [C-20260808-215] 线下建单（金额校验/唯一性/审计快照） — clean

- **方法**: R121 定向纵深。读 admin order_service.py:251-393（_validate_amount/_audit_amount_override/
  create_offline_order）+ user/models.py phone 约束 + R107/R105 对照 + 排重
- **证据**:
  - **金额范围前置**：_validate_amount 0 < amount ≤ 100000（L251-260）——越界 422 不落用户/孩子/订单（F11）✓
  - **phone 唯一性**：DB 层 unique=True（user/models.py:20）+ 服务层查重（L325-329）双保险 ✓ ——并发同 phone
    由 DB unique 兜底（IntegrityError 由全局异常处理器接住，R107 同款链路已核）
  - **synthetic_openid**：`admin_created_{phone}`（L330）无独立查重——**但 phone unique 保证派生 openid 唯一**
    （openid 与 phone 一一对应），无重复风险 ✓（R107 C-200 有显式查重为防御纵深，本条依赖派生唯一性成立）
  - **F5 快照**：PAID 线下单 _mark_paid_member_ever 计入多孩资格（L354-355）✓
  - **F11 金额覆盖审计**：OperationLog 明细（L276-287）+ 超阈值 SystemMessage 告警（L296-310，
    amount_override_alert_ratio 配置化，默认 0.5）✓ ——资金面审计链完整
  - **订单状态**：amount+pay_type → PAID + pay_time（L349-353）✓
  - **密码**：secrets.token_urlsafe(12) + bcrypt hash + default_password 明文返回（管理端告知场景，R107
    C-200 同款设计排重）✓
  - **权限**：order.create require_perm（router）✓
- **排重**: R107（admin_create_user user/child 同款）、R105（update_order_status）、R53（F-084 退款）互补；
  R121 本轮回调线下建单 clean 侧（零新缺陷）

---

## R121 完结汇总

- **范围**: 线下建单（create_offline_order 资金面）
- **结果**: 发现 0 项 + clean 1 项（C-215）
- **关键结论**:
  - 线下建单工程正确：金额范围前置校验 + phone unique 双保险 + F11 审计/告警 + F5 资格快照
  - synthetic_openid 依赖 phone 派生唯一性（弱纵深但成立，无实际风险）
  - 本轮为合法零发现（铁律 3）
- **累计**: 107 发现（P0:0 / P1:0 / P2:14 / P3:93）+ 211 clean 记录
- **提交**: 见 git log（本轮 rounds/R121 文件 + progress 索引同步更新）
- **R121 收尾结论**: 一百二十一轮共 107 项发现无 P0/P1；14 项 P2。R122 候选：继续轮转新面（如 account 管理端
  补面、advancement 管理端、borrow 用户侧复核）。
