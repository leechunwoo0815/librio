# R65 第六十五轮 message 已读状态一致性复核对 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-158 起（本轮零发现）。

## 范围

R65 message 已读状态一致性复核对（R22 已审消息域，本轮换面）。is_read 字段（个人）vs MessageReadStatus
表（共享）双轨、unread 计算、mark_as_read 一致性——确认 L1048 评估无退化。

## 结果

- **发现 0 项**
- **clean 1 项**：C-158 消息已读双轨一致性安全（个人字段 + 共享表 + unread 计算正确）

---

## [C-20260808-158] 消息已读双轨（is_read 字段/MessageReadStatus 表/unread） — clean

- **方法**: R65 定向纵深。读 message/service.py（get_user_messages L53-113/mark_as_read L115-145）+
  models.py + 排重对照 L1048
- **证据**:
  - **双轨正确**：个人消息 is_read 字段（L131）+ 共享消息 MessageReadStatus 表（L133-141）——按消息类型分流 ✓
  - **unread 计算**：is_read=0 个人 + 未标记共享（L90-92）+ 批量查已读（_batch_get_read_status in_ 查询）✓
  - **mark_as_read 一致性**：L1048 已确认"单条路径有 is_read 短路 + user_id 校验，无双路径问题"——复核对无退化 ✓
  - **mark_all_read**：个人批量 update + 共享逐条写表（F-044 N+1 已报）✓
  - **归属**：个人消息 user_id 校验（R22 C-115 已核）✓
- **排重**: R65 本轮已读复核对 clean 侧（零新缺陷）；L1048/F-044 已报不重

---

## R65 完结汇总

- **范围**: message 已读双轨（字段/表/unread）
- **结果**: 发现 0 项 + clean 1 项（C-158）
- **关键结论**:
  - 消息已读双轨正确（个人字段 + 共享表分流）+ unread 计算 + mark_as_read 一致性（L1048 已评估）
  - 本轮为合法零发现（铁律 3）
- **累计**: 85 发现（P0:0 / P1:0 / P2:13 / P3:72）+ 155 clean 记录
- **提交**: 见 git log（本轮 rounds/R65 文件 + progress 索引同步更新）
- **R65 收尾结论**: 六十五轮共 85 项发现无 P0/P1；13 项 P2。R66 候选：继续轮转新面。
