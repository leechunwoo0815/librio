# R150 第一百五十轮 图书详情页复核 — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-244 起（本轮零发现）。

## 范围

R150 图书详情页复核（book/service.py get_book_detail L107-156：copy 统计/audio/quiz 聚合 + is_deleted
过滤核查）。R149（F-116 is_published 消费面）、R10（C-103 软删无调用点先例）、R25（C-118）已审相邻，
详情页聚合面本轮定向纵深。

## 结果

- **发现 0 项**
- **clean 1 项**：C-244 图书详情页复核通过（copy 统计缺 is_deleted 但无软删调用点——无实际影响，R10 先例）

---

## [C-20260808-244] get_book_detail（copy/audio 统计核查） — clean

- **方法**: R150 定向纵深。读 book/service.py:107-156（get_book_detail 全）+ 全库 soft_delete 调用点清单
  + R10 C-103 对照 + 排重
- **证据**:
  - **copy 统计缺 is_deleted 过滤**（L117-133 total/available_copies 无 BookCopy.is_deleted==0）——**候选缺口**
  - **但 BookCopy 无软删调用点**：全库 soft_delete 清单（message/teacher/schedule/venue/child/user/book/
    cert/vocab）**不含 BookCopy**——副本只经状态流转（0 在馆/2 维修/3 报废/4 损坏，R148 已核）管理，
    is_deleted 恒 0 → **缺过滤无实际影响**（R10 C-103 先例：ReadingSession 无软删调用点 → 不报）✓
  - **audio 查询无 is_deleted**（L135-138）：AudioFile 同样无软删调用点 → 无实际影响 ✓
  - **quiz 计数带 is_deleted**（L141-145）✓ 正确
  - **F-116 排重**：is_published 不过滤（详情页在 F-116 影响范围内，R149 已报）——本条不重复 ✓
  - **软删防御纵深**：get_by_id_or_raise 过滤 Book 软删（L108）✓
- **排重**: F-116（R149 消费面）、R10（C-103 先例逻辑）、R25（C-118）、R148（F-115）互补；
  R150 本轮回调详情页 clean 侧（零新缺陷）

---

## R150 完结汇总

- **范围**: 图书详情页（copy/audio 聚合）
- **结果**: 发现 0 项 + clean 1 项（C-244）
- **关键结论**:
  - copy/audio 统计缺 is_deleted 过滤为**候选缺口**——经全库 soft_delete 清单核查无对应软删路径，
    is_deleted 恒 0 无实际影响（R10 先例判定），不报
  - quiz 计数过滤正确 + F-116 消费面已报排重
  - 本轮为合法零发现（铁律 3）
- **累计**: 115 发现（P0:0 / P1:0 / P2:14 / P3:101）+ 240 clean 记录
- **提交**: 见 git log（本轮 rounds/R150 文件 + progress 索引同步更新）
- **R150 收尾结论**: 一百五十轮共 115 项发现无 P0/P1；14 项 P2。R151 候选：继续轮转新面（如
  save_book_page_admin 复核、dashboard 用户侧补面、admin 用户管理 list 复核）。
