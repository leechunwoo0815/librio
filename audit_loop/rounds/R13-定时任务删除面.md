# R13 第十三轮 定时任务第三轮（数据删除面）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：F-063 起 / C-106 起。

## 范围

R13 定时任务第三轮换面。R1（7.1 失败/锁/幂等 F-013/014/015）、R2（时刻表重叠 F-037）、R5（资金任务
F-055）、R8（F-056 候补）已审；F-046（R2）已覆盖 scheduler 5 处无守卫状态写；R5 已评估 reconcile 任务
clean（对账重算口径一致、非资金）。本轮换面（数据完整性第三轮）：**删除任务的面**——execute_child_deletions
（ChildDeletionService 级联）与 purge_expired_data（保留期清理）的**表覆盖完整性**：全库 child_id 外键表 vs
删除清单对照，找孤儿残留。

## 结果

- **发现 1 项**：F-063（P3）assessment 表不在儿童删除级联清单（文档+代码双重遗漏）——孤儿残留
- **clean 1 项**：C-106 删除任务面整体覆盖（除 assessment 外全库 child_id 表均有删除路径）

---

## [F-20260808-063] assessment 表不在儿童删除级联清单——删除孩子后 AR 测评历史孤儿残留（文档+代码双重遗漏） — P3

- **级别**: P3（隐私合规缺陷；孤儿行无信息泄露面，但违反儿童数据删除权完整性承诺 P0-3）
- **维度**: 7 定时任务 × 数据合规（交叉维度；模式 1 同类漏改）
- **文件**: `backend/domain/child/deletion_service.py:34-59`（DELETE_TABLES_BY_CHILD 18 表无 assessment）/ `backend/tasks/scheduler.py:1960-2045`（purge_expired_data 无 assessment）/ `专家意见/children_privacy_compliance_20260721.md:245-277`（§4.2 清单 1-28 项无 assessment）/ `backend/domain/assessment/models.py:8-42`（Assessment.child_id FK nullable=False）/ `backend/domain/assessment/service.py:232`（创建路径）
- **事实**:
  - `Assessment`（AR 测评记录）`child_id = Column(BigInteger, ForeignKey("child.id"), nullable=False)`（models.py:13-15），含 ar_level_before/ar_level_after/comprehension_score/notes/recommendation（儿童学习数据）
  - `ChildDeletionService.DELETE_TABLES_BY_CHILD`（deletion_service.py:34-54）18 表：reading_session/reading_progress/check_in/voice_recording/user_vocabulary/quiz/reading_submission/child_level/child_achievement/level_certificate/bookshelf/activity_enrollment/reservation/observation_report/learning_report/ar_evaluation/observation_evaluation/guidance_record——**无 assessment**；DELETE_TABLES_VIA_QUIZ（quiz_answer）与 DELETE_TABLES_BY_USER（message_read_status）也不含
  - `purge_expired_data`（scheduler.py）非财务删除清单（ReadingSession/CheckIn/UserVocabulary/Bookshelf/BookWaitlist/Reservation + Quiz/QuizAnswer）与财务清单（Order/DepositRecord/FinePayment/RefundApplication/BorrowRecord/BookDamageReport/BenefitTransferApplication）**均无 assessment**
  - 合规文档 §4.2 清单（L245-277）1-28 项：有 ar_evaluation（22）/observation_evaluation（23）/guidance_record（24），**无 assessment**——且 deletion_service 注释（L26-33）记录了 quiz/quiz_answer 勘误核对过程，assessment 仍漏
  - 全库 child_id 外键表 25 张（python 脚本枚举）vs 删除路径覆盖：24 张有路径（18 表直接 + order/deposit/fine/borrow/refund 财务保留期 + book_waitlist purge + assessment 例外）
- **证据**: ① python 枚举全库 25 张 child_id FK 表（reading_progress/reading_session/check_in/order/deposit_record/fine_payment/level_certificate/activity_enrollment/bookshelf/reservation/book_waitlist/assessment/voice_recording/borrow_record/ar_evaluation/observation_evaluation/guidance_record/observation_report/learning_report/child_level/reading_submission/quiz/child_achievement/refund_application/user_vocabulary）；② 对照 deletion_service DELETE_TABLES_BY_CHILD 18 表——仅 assessment 缺；③ grep 确认 purge_expired_data 无 Assessment；④ 排重 grep：findings 无 assessment 删除/孤儿命中
- **触发**: 监护人申请删除儿童数据 → 冷静期 24h 后 execute_child_deletions（3:30 每日）级联物理删除 → assessment 表该 child 的 AR 测评记录**残留**
- **影响**: ① 儿童数据删除权（P0-3 隐私合规）覆盖不完整——删除申请明示"所有儿童数据"，AR 测评历史（含测评分数/备注）未删；② 数据库孤儿行（child_id 指向已物理删除的 child）；③ 无独立端点直接展示孤儿（无泄露面），属合规完整性缺口。低频（删除申请少），无资金/安全
- **建议**: ① `DELETE_TABLES_BY_CHILD` 补入 `"assessment"`（对齐 ar_evaluation 模式）；② 同步更新合规文档 §4.2 清单（补 26c 勘误行，模式 1 纪律：改一处必全库核对同类型表）；③ 全量核对一次 deletion_service 清单 vs 全库 FK 表（本次已枚举 25 表，仅 assessment 缺，其余覆盖）
- **排重**: 已 grep 确认不在 F-001~062 / C-001~105 中；F-014（purge 分批）不涉删除面覆盖；C-106 为同轮 clean 侧（其余 24 表覆盖完整）；deletion_service 勘误注释（quiz/quiz_answer）证明此前核对过清单但漏 assessment

---

## [C-20260808-106] 删除任务面整体覆盖（除 assessment 外全库 child_id 表均有删除路径） — clean

- **方法**: R13 定向纵深。① python 脚本枚举全库 25 张 child_id 外键表；② 对照 ChildDeletionService DELETE_TABLES_BY_CHILD/DELETE_TABLES_VIA_QUIZ/DELETE_TABLES_BY_USER 清单；③ 对照 purge_expired_data 非财务/财务/语音三段删除清单；④ 读合规文档 §4.2 清单核对一致性；⑤ 读 execute_child_deletions 调度（3:30 每日 + distributed_lock）与删除前备份（deletion_service.py:218-224）
- **证据**:
  - **删除覆盖**：25 张 child_id 表——18 表在 DELETE_TABLES_BY_CHILD 直接删除 ✓；order/deposit_record/fine_payment/refund_application/borrow_record/book_damage_report/benefit_transfer 按财务保留期（5 年/3 年/2 年）经 purge_expired_data ✓（法定保留合规，C-064 已分类）；book_waitlist 经 purge 非财务段 ✓；quiz_answer 经 quiz_id 特殊级联 ✓；message_read_status 经 user_id ✓；**仅 assessment 例外（F-063）**
  - **级联顺序**：quiz_answer 先于 quiz 删除（scheduler.py:1964-1972 先查 quiz_ids 再删 answer 再删 quiz）✓
  - **删除前备份**：deletion_service.py:218-224 备份目录 → 物理删除 → 提交 → 删文件（voice_files）✓
  - **调度安全**：execute_child_deletions 带 @distributed_lock("job:execute_child_deletions", timeout=600) ✓
  - **软删→物理删分离**：child.soft_delete（软删）+ 冷静期 24h（deletion_requested_at）+ 物理删（is_deleted=1 → execute_due_deletions）✓
  - **purge 财务保留期**：finance_years=5/voice_months=6 配置化 ✓
- **排重**: R13 本轮删除面 clean 侧（F-063 assessment 为唯一缺口）；C-064（资金保留期分类）互补不重；F-014（purge 分批无 LIMIT）为性能面不涉覆盖

---

## R13 完结汇总

- **范围**: 定时任务第三轮（数据删除面：execute_child_deletions + purge_expired_data 表覆盖完整性）
- **结果**: 发现 1 项（F-063 P3 assessment 孤儿残留）+ clean 1 项（C-106 删除覆盖完整）
- **关键结论**:
  - 删除链路工程化质量高：级联顺序正确、删除前备份、软删→冷静期→物理删分离、财务保留期合规、分布式锁齐备
  - 唯一缺口：assessment（AR 测评）漏出删除清单——文档 §4.2 与代码双重遗漏（同域 ar_evaluation/observation_evaluation 都在清单内，唯独 assessment 漏）；修复成本极低（清单补一行 + 文档勘误）
  - 本项与 R10 F-061（双入口并发）类似——均为"结构核对"发现的漏配/遗漏，非代码逻辑缺陷
- **累计**: 62 发现（P0:0 / P1:0 / P2:10 / P3:52）+ 103 clean 记录
- **提交**: 见 git log（本轮 rounds/R13 文件 + progress 索引同步更新）
- **R13 收尾结论**: 十三轮共 62 项发现无 P0/P1；10 项 P2 全部未修。R14 候选：数据库第二轮（慢查询/索引，维度 8 第二轮——R1 8.1-8.4 + R2 换面已审，本轮跑 slow_query_baseline 复查 + 新查询面）。
