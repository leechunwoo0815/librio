# R19 第十九轮 评估域全链（assessment/ar_evaluation 接线）— 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：F-068 起 / C-112 起。

## 范围

R19 评估域全链（此前零审查记录）。assessment 域（管理端 AR 评估记录 CRUD）+ evaluation 域
（AREvaluation/ObservationEvaluation/GuidanceRecord 服务）——检查：CRUD 逻辑、权限、child.ar_level
写路径、服务接线完整性。

## 结果

- **发现 1 项**：F-068（P3）EvaluationService 无调用方——child.ar_level 唯一写路径断裂（死代码/未接线）
- **clean 1 项**：C-112 assessment 管理端 CRUD 正常（权限/软删/查询）

---

## [F-20260808-068] EvaluationService 无调用方——child.ar_level 唯一写路径断裂（未接线服务） — P3

- **级别**: P3（观察项；数据字段永不更新，但前端不展示该字段，无用户可见异常；功能未接线）
- **维度**: 4 API 契约 × 功能完整性（交叉维度；模式：未接线服务）
- **文件**: `backend/domain/evaluation/service.py:33-85`（EvaluationService 全）/ `backend/domain/child/models.py`（Child.ar_level）/ `backend/domain/report/service.py:633/697`（周报/月报读 current_ar_level）
- **事实**:
  - `EvaluationService`（evaluation/service.py）三个方法 `create_ar_evaluation`（L33-56，创建 AREvaluation + 更新 child.ar_level）/`get_ar_evaluations`（L61-72）/`get_latest_ar_evaluation`（L74-85）——**全库无任何调用方**（grep：除定义文件外零引用）
  - `child.ar_level` 的**唯一写路径**是 create_ar_evaluation L53 `child.ar_level = data.ar_level`（grep `\.ar_level =` 全库仅此一处）→ **该路径无调用方 → child.ar_level 永不更新**
  - AREvaluation 表无其他写入点（grep `AREvaluation(` 无调用）
  - 周报/月报（report/service.py:633/697）读 `child.ar_level` → `current_ar_level` 恒为 None
  - 前端不展示 current_ar_level（grep frontend 无 ar_level 绑定；books.js 的 AR级别为图书筛选 Book.ar_level，不同字段）
  - teacher_workbench_service 直接用 GuidanceRecord 模型（不经 EvaluationService）——evaluation 域部分功能被直接模型访问替代
- **证据**: ① `grep -rn "EvaluationService\|create_ar_evaluation\|get_ar_evaluations" backend/` 仅定义文件命中；② `grep -rn "\.ar_level = " backend/` 唯一命中 evaluation/service.py:53；③ 排重 grep：findings 无 ar_level/AR测评 命中；F-012（权限种子未接线）与 L722（advancement get_quiz 死代码"无实际序列化路径，排除风险"）为同类"未接线"先例
- **触发**: 无触发路径——该服务不可达（无 router 挂载、无内部调用）
- **影响**: ① child.ar_level 数据字段恒空（周报/月报 current_ar_level=None，但前端不展示，无用户可见异常）；② AR 测评录入无功能入口（管理端 assessment 表可录 AR 等级但不更新 child.ar_level）；③ 若产品未来启用 AR 测评展示，功能缺失需补接线。无资金/安全影响
- **建议**: ① 确认产品是否仍需要 AR 测评功能：需要则接线（assessment router 或 teacher_workbench 调 create_ar_evaluation，同步更新 child.ar_level）；不需要则删除 EvaluationService 死代码并评估 ar_level 字段去留；② 若保留字段，建议 assessment create/update 时同步 child.ar_level（单一入口）
- **排重**: 已 grep 确认不在 F-001~067 / C-001~111 中；F-012（权限种子未接线）同类模式但为权限面；L722（get_quiz 死代码）为纯死代码排除，本项含**数据字段断链**（ar_level 永不更新）——新面

---

## [C-20260808-112] assessment 管理端 CRUD（权限/软删/查询/N+1） — clean

- **方法**: R19 定向纵深。读 assessment/service.py 全（list_assessments/get_assessment/create_assessment/update_assessment/delete_assessment）+ router.py（权限码）+ models.py + evaluation/service.py 全 + teacher_workbench 引用
- **证据**:
  - **权限**：/admin/api/assessment 全部 require_perm（assessment.list/view/create/edit/delete）✓
  - **CRUD 逻辑**：create 校验 child 存在（L221-235）✓；update 排除未设置字段（L250-272）+ completed 自动补完成时间 ✓；delete 软删（L274-287）✓
  - **查询安全**：get_assessment/list 均 is_deleted==0 过滤 + child/teacher/venue 关联查询（N+1 为 F-065 已报，排重）✓
  - **软删过滤**：全部查询带 is_deleted==0 ✓
  - **字段完整性**：AssessmentResponse 含 ar_level_before/after/change/comprehension_score/status/notes/recommendation ✓
  - **evaluation 域模型**：AREvaluation/ObservationEvaluation/GuidanceRecord 结构完整（child_id/teacher_id 外键）；deletion_service 已含 ar_evaluation/observation_evaluation/guidance_record 删除（R13 C-106 已核）✓
- **排重**: R19 本轮评估域 clean 侧（F-068 未接线为唯一缺口）；F-065（assessment N+1）已报；R13 F-063/C-106（删除覆盖）已核 evaluation 三表在删除清单内

---

## R19 完结汇总

- **范围**: 评估域全链（assessment CRUD + evaluation 服务 + child.ar_level 写路径 + 接线完整性）
- **结果**: 发现 1 项（F-068 P3 EvaluationService 未接线）+ clean 1 项（C-112 assessment CRUD 正常）
- **关键结论**:
  - assessment 管理端 CRUD 工程正常（权限/软删/查询全覆盖）
  - evaluation 域服务（AR 测评）**未接线**——child.ar_level 唯一写路径断裂，字段恒空；当前无用户可见影响（前端不展示），但功能不可用 + 数据字段死
  - 与 F-012（权限未接线）/get_quiz（死代码）同类"未接线"模式——本项目存在未接线遗留的迹象
- **累计**: 67 发现（P0:0 / P1:0 / P2:10 / P3:57）+ 109 clean 记录
- **提交**: 见 git log（本轮 rounds/R19 文件 + progress 索引同步更新）
- **R19 收尾结论**: 十九轮共 67 项发现无 P0/P1；10 项 P2 全部未修。R20 候选：家长课程域（parent_course_time 排期/名额——R18 B3 名额竞态周边）或证书域（level_certificate 生成链）。
