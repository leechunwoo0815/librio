# R124 第一百二十四轮 事件链补面（deposit.paid/level.advanced） — 审查报告（2026-08-08）

> 轮次文件拆分规范（用户指令 2026-08-08）：一轮一个报告文件。编号延续：C-218 起（本轮零发现）。

## 范围

R124 事件链补面（deposit.paid + level.advanced 两事件 handler——R16 C-109 清单未含、R17/R21 部分涉）。
handle_deposit_paid_for_child（order_handlers.py:165-180）+ LevelAdvancedEvent 链（R21 已审证书）+ 发布点
F39 语义（deposit/service.py 三处 publish）+ registry 注册。

## 结果

- **发现 0 项**
- **clean 1 项**：C-218 deposit.paid/level.advanced 事件链安全（幂等 handler + 行锁 + F39 发布点语义）

---

## [C-20260808-218] deposit.paid/level.advanced 事件链（幂等/行锁/F39） — clean

- **方法**: R124 定向纵深。读 order_handlers.py:165-180（handle_deposit_paid_for_child）+ events.py 注册机制 +
  deposit/service.py:160-175/222/350（三处 publish + F39 语义）+ advancement/service.py:400 + R16/R17/R21 对照
- **证据**:
  - **deposit.paid handler 幂等**：handle_deposit_paid_for_child 设 `child.deposit_status = PAID`（L179）——
    重复投递重复设值无副作用（幂等）✓；child with_for_update（L175）防并发双写 ✓
  - **F39 发布点语义**：DepositPaidEvent 仅即时支付路径 publish（deposit/service.py:164-167，prepay 成功≠已付款
    ），生产网关路径不发——借书资格等微信回调（handle_callback）✓（R17 C-110 已核回调链）
  - **level.advanced handler**：handle_level_advanced_for_certificate（misc_handlers）→ create_level_certificate
    内部幂等 + C-101 已核双事件防护——R21 C-114 已审证书链，复核无退化 ✓
  - **注册完整**：registry.py 两事件均 subscribe（L58 deposit.paid / L68 level.advanced）✓
  - **handler 无自 commit**：共享 session 模式（R16 C-109 已核 publish 同步 + 异常 re-raise 回滚）✓
- **排重**: R16（C-109 事件链总体）、R17（C-110 deposit 资金链）、R21（C-114 证书链）互补；R124 本轮
  补审 R16 清单未显含的两事件——确认幂等安全，零新缺陷

---

## R124 完结汇总

- **范围**: 事件链补面（deposit.paid/level.advanced handler）
- **结果**: 发现 0 项 + clean 1 项（C-218）
- **关键结论**:
  - deposit.paid handler 幂等 + 行锁 + F39 发布点语义正确
  - level.advanced 证书链 R21 已审无退化
  - 本轮为合法零发现（铁律 3）
- **累计**: 108 发现（P0:0 / P1:0 / P2:14 / P3:94）+ 214 clean 记录
- **提交**: 见 git log（本轮 rounds/R124 文件 + progress 索引同步更新）
- **R124 收尾结论**: 一百二十四轮共 108 项发现无 P0/P1；14 项 P2。R125 候选：继续轮转新面（如 venue 用户侧
  补面、assessment 管理端、reservation 用户侧复核）。
