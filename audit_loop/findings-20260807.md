# librio 循环审查发现日志 · 2026-08-07

> 指令：专家意见/无限循环审查指令-opencode.md（五铁律：证据/排重/零发现合法/门禁/最小破坏）
> 排重依据：专家意见/项目终态交接-20260807.md（F1-F80 / D1-D8 已结案）+ 本目录历史 findings
> 格式：F-YYYYMMDD-NNN 发现 / C-YYYYMMDD-NNN 审查干净

## 审查轮次：第 1 轮（深度 1：模式匹配 + 命令验证）

### [C-20260807-001] 维度 1.1 commit() 后写操作 — 审查干净
- 方法: 写静态扫描脚本 audit_loop/scan_commit_after_write.py，枚举 backend/domain/ + backend/admin 全部 service 的 `db.commit()` 调用点（150+），扫描 commit 后 40 行窗口内（到首个 return/raise/下一 commit/函数定义为止）的写操作：db.add / db.delete / db.execute / db.bulk_* / 属性赋值。
- 证据: 显式写操作扫描修正前 4 可疑 → 修正 return 截断后 0；属性赋值扫描 3 可疑 → 人工核对全排除。
- 核对明细:
  1. message_service.py L51/L71/L85 → 脚本误报（四互斥分支 add→commit→return，return 紧邻 commit 时未截断）
  2. deposit/service.py L81→L103 → 有意分段提交（F78 stale PENDING 复位 UNPAID 先 commit，再建新 PENDING 再 commit）
  3. advancement/service.py L413 resp.level_name 赋值 → Pydantic 响应对象非 ORM，不触 DB
  4. deposit/service.py L126/L303 record.status=UNPAID → 网关异常恢复路径，紧跟 commit()
- 排重: F78 复位逻辑在终态交接 §四 已结案；无新发现。

### [C-20260807-002] 维度 1.2 先查后改 with_for_update — 3 可疑定级（2×P2 + 1×P3 观察）
- 方法: 写静态扫描脚本 audit_loop/scan_missing_for_update.py：逐 service 函数找 `db.query()...first()/one()`（无 with_for_update）结果对象后续被属性赋值的路径。
- 证据: 3 可疑点全量人工核对，见下方发现条目。

### [F-20260807-001] mark_book_lost 中 Book.total_stock 读-改-写无行锁
- 级别: P2（并发危害需 MySQL 实证；SQLite 行锁 no-op 不得据此下结论）
- 维度: 1.2 并发与事务
- 文件: backend/domain/deposit/service.py:475（查询）→ 509-510（赋值）
- 事实:
  ```python
  book = self.db.query(Book).filter(Book.id == record.book_id).first()  # 无 with_for_update
  ...
  book.total_stock = max((book.total_stock or 0) - 1, 0)
  book.available_stock = max((book.available_stock or 0) - 1, 0)
  ```
- 证据: 同函数内 BorrowRecord(L461-466) 与 Child(L486-490) 均带 with_for_update，唯 Book 无；历史 transaction_lock_audit_20260721.md P0-2/P0-3 只点名 Child（已修，checkpoint.md F2），Book 写点为同类漏网。
- 触发: 两个并发 mark_book_lost 命中同一 book（不同借阅记录/不同副本）→ 两事务均读到 total_stock=N → 各写 N-1，丢一次扣减。
- 影响: total_stock/available_stock 计数偏高 1；由每日 reconcile_stock（scheduler.py:256，03:00，distributed_lock）自动纠正，无持久性危害。
- 建议: book 查询补 `.with_for_update()`，或改用原子 `db.query(Book).filter(...).update({...})`（与 BookCopy L502 一致）。
- 排重: grep 确认 transaction_lock_audit 仅点名 Child；checkpoint F2 只覆盖 Child 补锁；F41 库存对账口径为待甲方项，本发现为行锁缺口，不冲突。

### [F-20260807-002] _rollback_refund_failure 中 order.refund_status 写无行锁（模式 1 同类漏改）
- 级别: P2（并发窗口极窄，无直接资金丢失路径）
- 维度: 1.2 并发与事务
- 文件: backend/domain/refund/service.py:305（查询）→ 307（赋值）
- 事实:
  ```python
  order = db.query(Order).filter(Order.order_no == order_no).first()  # 无 with_for_update
  if order:
      order.refund_status = 3  # FAILED
  ```
- 证据: `order.refund_status` 写点全库共 3 处——mark_refunded(L340 锁)、handle_refund_failed(L410 锁, F12-P2)、_rollback_refund_failure(L305 **无锁**)。两带锁一不带 = 模式 1 同类漏改。
- 触发: 网关拒绝/异常路径同步调用 _rollback_refund_failure（L285/L292），与 mark_refunded 异步回调并发 → FAILED 覆盖 REFUNDED。
- 影响: 极端时序下 order.refund_status 回退 FAILED，管理端人工重试可能对已退款单再发起退款；无资金直接损失，F38/F76 单号分列 + 微信侧幂等兜底。
- 建议: _rollback_refund_failure 内 order/refund 查询补 `.with_for_update()`。
- 排重: F12-P2 终态回退已结案（handle_refund_failed 带锁）；本发现为同概念兄弟写点，未被 F12-P2 覆盖。

### [F-20260807-003] _mark_paid_member_ever 中 user 查询无行锁
- 级别: P3（观察项，布尔幂等写无竞态后果）
- 维度: 1.2 并发与事务
- 文件: backend/domain/order/service.py:274（查询）→ 276（赋值）
- 事实:
  ```python
  user = self.db.query(User).filter(User.id == order.user_id).first()  # 无 with_for_update
  if user and not user.paid_member_ever:
      user.paid_member_ever = 1
  ```
- 证据: paid_member_ever 为布尔置位（0→1 单调幂等），并发下最终值恒为 1，无脏数据。
- 触发: 同一 user 两笔会员订单支付回调并发。
- 影响: 无（幂等写）。
- 建议: 可不改；若统一纪律可补锁或原子 update。
- 排重: F5 快照四入口覆盖已结案（K3-执行中任务交接 L37 补 update_order_status 入口）；本发现不涉及入口缺失，仅行锁纪律。

### [F-20260807-004] Book.total_stock 读-改-写全库枚举（模式 1 聚合）— 至少 6 处无锁
- 级别: P2（管理端低频 + reconcile_stock 每日自愈；并发需 MySQL 实证）
- 维度: 1.2 并发与事务
- 文件: 全库 total_stock/available_stock 写点（grep 枚举）：
  | 位置 | 操作 | 锁 |
  |---|---|---|
  | deposit/service.py:475→509 丢书 -1 | 无锁 ❌ | F-001 已报 |
  | book/service.py:385→404 加副本 +1 | get_by_id 无锁 ❌ |
  | admin/book_service.py:285→298 批量导入 +1 | 无锁 ❌ |
  | admin/book_service.py:350→358 维修/报废重算 | 无锁 ❌（子查询重算） |
  | damage_admin_service.py:62→125-128 丢失 -1 | 无锁 ❌ |
  | damage_admin_service.py:266-269 找回 +1 | 无锁 ❌ |
  | damage_admin_service.py:368/453/612 (+1) | 未逐一展开 |
  | borrow_handlers.py:44-46 还书释放 | **原子 UPDATE ✓**（正确范式） |
- 事实: 多数写点为 `book = query...first()`（无 with_for_update）后 ORM 属性赋值；同概念正面对照为原子 `db.query(Book).update({...})`。
- 证据: grep 全库 `total_stock = ` 共 8+ 写点；逐一核对上述 6 处查询均无 with_for_update；唯一带锁为 damage 文件 BookCopy 行（L118/145 等 21 处 with_for_update 属其他对象）。
- 触发: 两个管理端操作并发命中同一 book（并发加副本/并发丢书不同副本/并发流转）→ 读-改-写丢失更新，计数偏差 1。
- 影响: total_stock/available_stock 计数偏高/偏低；由每日 03:00 reconcile_stock（scheduler.py:256）自动对齐，无持久性危害；非资金路径。
- 建议: 统一改为原子 UPDATE（借 borrow_handlers.py:44 范式）或 book 查询补 with_for_update。
- 排重: transaction_lock_audit 仅点名 Child；F69 副本行锁已结案（BookCopy）；本发现为 Book 行写点，未被覆盖。

### [C-20260807-003] 维度 1.3 事件 handler 自 commit — 审查干净
- 方法: 枚举 backend/events/ 全部 21 个 handler（registry.py 注册表），grep commit/flush；读 events.py 发布机制（共享/独立双模式）。
- 证据: 21 handler 中 0 commit（仅 order_handlers.py:36、borrow_handlers.py:49 两处 flush 配合调用方统一提交）；独立 session 模式（定时任务）自带 commit+失败重试+死信记录（events.py:365-406）；handle_quiz_failed_for_logging(db=None) 纯日志 handler，None 无害。

### [C-20260807-004] 维度 1.4 死信路径 — 审查干净
- 方法: 读 events.py _record_dead_letter（L408-434）异常路径。
- 证据: 死信记录内部 try/except + finally session.close()，自身失败仅 logger.error（L433），不向调用方传播；两次重试失败后才记录死信，不会在异常时二次炸掉。

### [C-20260807-005] 维度 2.1 押金状态机 — 转移矩阵核对（多数合法，2 缺口）
- 方法: 枚举 deposit/service.py + scheduler.py + order_handlers.py 全部状态变更点（36 处），逐点核对前置校验；对照 DepositStatus 枚举与终审意见 L254 定案。
- 证据: 合法转移均有校验——支付回调 PENDING→PAID（F74 幂等+金额校验）、审核 REFUND_PENDING→REFUNDING（L540）、reject→PAID、取消 REFUNDING/REFUND_PENDING→PAID（定案）、mark_refunded REFUNDING→REFUNDED（L682）、扣除仅 PAID（L423）、终态由 repository 过滤保护（test_cannot_refund_already_refunded 覆盖）；scheduler 巡检（F55 REFUNDING 超时→REFUND_PENDING + 告警，L1095-1116）有状态守卫。见下方 2 条缺口。

### [F-20260807-005] cancel_refund 允许 REFUNDING→PAID：网关已提交退款后的取消时序缺口
- 级别: P2（边界时序；无资金直接损失的实锤复现，需 MySQL/网关实证）
- 维度: 2.1 状态机合法性
- 文件: backend/domain/deposit/service.py:653-675（cancel_refund），对照 audit_refund approve Phase 2 调网关（L605-626）
- 事实:
  ```python
  def cancel_refund(self, child_id):
      if record.status not in (DepositStatus.REFUNDING, DepositStatus.REFUND_PENDING):
          raise ConflictError(...)
      record.status = DepositStatus.PAID  # REFUNDING 也允许 → PAID
  ```
- 证据: audit_refund approve 先设 REFUNDING→commit→调网关退款（L590-626）；cancel_refund 允许 REFUNDING 取消（L658）；微信到账回调 mark_refunded 仅 REFUNDING 可（L682-683），PAID 状态将 ConflictError。
- 触发: 管理员 approve（网关已提交退款）→ 微信到账前点击取消退款 → 状态 PAID + 钱已退；用户可再次申请全额退款（新 out_refund_no，F38）→ 重复退款风险。
- 影响: 极端时序下重复退款/状态错乱；微信幂等仅按单号，新单号不拦截。
- 建议: cancel_refund 对 REFUNDING 先调网关撤销退款（或查询退款状态）再置 PAID；或仅 REFUND_PENDING 可取消。
- 排重: 终审意见 L254 定案"仅 REFUNDING/REFUND_PENDING 可取消"——**本发现不重报该定案本身**，仅针对定案未覆盖的"网关已提交后取消"时序缺口；F74 幂等/ F38 单号分列均已结案，本点未被覆盖。

### [F-20260807-006] refund_deposit 申请退款无状态前置校验：PENDING/REFUNDING 可被置 REFUND_PENDING
- 级别: P2（非法转移 + 测试盲区；无资金直接损失）
- 维度: 2.1 状态机合法性
- 文件: backend/domain/deposit/service.py:364-413（refund_deposit）
- 事实:
  ```python
  record = self.deposit_repo.get_active_by_child_for_update(data.child_id)  # 活跃= PENDING/PAID/REFUNDING/REFUND_PENDING
  if not record: raise NotFoundError(...)
  ... # 无 record.status 前置校验
  record.status = DepositStatus.REFUND_PENDING
  ```
- 证据: repository 活跃过滤含 PENDING/REFUNDING（repository.py:44-50）；service 无 status 校验；router（deposit/router.py:39-60）无兜底。PENDING（未支付）→REFUND_PENDING、REFUNDING（退款中）→REFUND_PENDING 均为非法转移。
- 触发: ① 用户发起押金支付（PENDING 未到账）即申请退款 → REFUND_PENDING → E1 自动审核（默认开）→ REFUNDING → 网关拒绝未支付单 → 回退 REFUND_PENDING 脏状态滞留；② 退款进行中（REFUNDING）重复申请 → 状态覆盖，进行中的退款回调 mark_refunded 失败。
- 影响: 脏状态滞留（管理端需人工清理）+ 退款流程被打断；无直接资金损失（网关拒绝未支付单/微信幂等兜底）。
- 建议: refund_deposit 增加前置校验——仅 PAID 可申请退款；测试补 PENDING/REFUNDING 状态用例（test_state_machines.py 现有 test_cannot_refund_unpaid 仅覆盖"无记录"，未覆盖 PENDING/REFUNDING）。
- 排重: 终审意见 L254"仅 REFUND_PENDING 可审核"指审核环节（audit_refund L540 有校验 ✓），申请环节前置未被覆盖；F62 申诉期/其他状态机修复均不涉本点。

### [C-20260807-006] 维度 2.2 五状态机转移矩阵 — 核心转移均合法，2 个 P3 观察项
- 方法: 枚举退款（RefundApplication 4 态）/订单（PayStatus 6 态）/借阅（BorrowStatus 4 态）/预约（ReservationStatus 4 态）/会员（MemberStatus 6 态）全部状态写点 + 前置校验。
- 证据: 退款申请前置完整（PAID 才可/防重复/365 天上限 F51/亲子课拦截/无未还书）；审核仅 PENDING（L190）；APPROVED→PENDING 回退有状态守卫（L316/L430）；COMPLETED 幂等（L365）。订单管理端有完整白名单矩阵（admin/order_service.py:414-421，F28）+ F5 快照；回调幂等 + trade_no 唯一（F26）+ trade_state 消费（F75）+ 金额校验；关单条件更新带前置（scheduler L918，F5）。预约取书/取消仅 PENDING（F40 已修，reservation L170/L531）；还书仅 BORROWING/OVERDUE（borrow L235）。押金见 F-005/F-006。见 2 个 P3 观察。

### [F-20260807-007] 订单支付回调对 REFUNDED/REFUNDING 状态无拦截
- 级别: P3（观察项；触发条件极罕见）
- 维度: 2.2 状态机合法性
- 文件: backend/domain/order/service.py:417-463（handle_payment_callback）
- 事实:
  ```python
  if order.pay_status == PayStatus.PAID:
      return ...  # 仅拦截 PAID
  ...  # REFUNDED/REFUNDING 订单不拦截，L463 直接置 PAID
  ```
- 证据: L431 只拦 PAID；REFUNDED（=4）/REFUNDING（=3）会继续执行到 L463 `order.pay_status = PayStatus.PAID`。
- 触发: 支付成功回调延迟到退款完成后才到达（微信重复通知/极端时序）。
- 影响: 已退款订单被改回 PAID，资金状态错乱；实际概率极低（微信支付回调仅在支付成功时发出，退款后重复通知需严重延迟）。
- 建议: 回调开头对 REFUNDING/REFUNDED 状态直接 return 或报错。
- 排重: F12-P2 终态回退覆盖 CLOSED/ABNORMAL 退款回调，不涉支付成功回调；F75-③ 消费 trade_state 已结案，本点为其未覆盖的终态分支。

### [F-20260807-008] FAILED 订单用户侧无取消/重试出路
- 级别: P3（观察项；管理端有出路）
- 维度: 2.2 状态机合法性
- 文件: backend/domain/order/service.py:754-764（cancel_order 仅 `pay_status != 0` 可取消）
- 事实: `if order.pay_status != 0: raise ValidationError("仅可取消未支付的订单")`——FAILED(=2) 订单被拒；scheduler 仅自动关闭 PENDING 超时（L911）。
- 证据: FAILED 订单用户侧无法取消/重新支付（重试=新建订单），永久滞留 FAILED；管理端白名单 FAILED→PAID/CLOSED（admin/order_service.py:417）可人工处理。
- 影响: 用户体验缺陷（支付失败的单无法自行关闭，列表滞留）；无资金影响。
- 建议: cancel_order 允许 FAILED 订单取消（FAILED→CLOSED）；或前端隐藏/自动清理 FAILED 单。
- 排重: F5/F28 订单状态修复已结案；FAILED 出路未被历史覆盖。

### [C-20260807-007] 维度 2.3 状态变更点前置校验 — 覆盖于 2.1/2.2 逐点核对中
- 方法: 对押金/退款/订单/预约/借阅全部状态写点核对前置校验（见 2.1/2.2 证据）。
- 证据: 除 F-005/F-006（押金）与 F-007/F-008（P3 观察）外，其余写点均有状态前置或白名单矩阵。

### [F-20260807-009] 服务期用满后可创建 0 元退款单并自动审核通过
- 级别: P3（观察项；无资金影响）
- 维度: 3.4 退款公式
- 文件: backend/domain/refund/service.py:126-168（apply_refund）
- 事实: used_days ≥ total_days - free_days 时 `_calculate` 返回 0，但申请仍创建且 L163 `final_amount(0) <= auto_max(500)` 自动 APPROVED → 执行 0 元微信退款。
- 证据: `used = max(0, min(used_days, total_days) - free_days)`；used_days=total_days → refund=max(amount - amount, 0)=0；L163 自动审核无下限校验；_execute_wechat_refund 对 amount=0 仍调 gateway.refund。
- 影响: 无资金损失（退 0 元）；产生 0 元退款单走完流程，微信侧可能拒绝 0 元退款导致失败回退日志噪声。
- 建议: 申请前置 `if final_amount <= 0: raise ValidationError("已超过退款期限，无法退款")`。
- 排重: F2/F37/F38 覆盖金额口径与单号，不涉 0 元边界；F12 覆盖回调状态。

### [C-20260807-008] 维度 3.1 金额路径 float 扫描 — 无 float
- 证据: `rg "float\(" backend/` 共 27 处，全部非金额用途（AR 级别/测评分数/时间戳 expires_at/配置解析 config_levels.py:133）。金额字段（amount/price/fine/refund/deposit）零命中。

### [C-20260807-009] 维度 3.2 元/分转换点 — 无双重转换
- 证据: 转换点全部集中 order/refund/deposit 三处：order/service.py:525（F1）、refund/service.py:273-275（F2）、deposit/service.py:616/618/798/802（yuan_to_cents）。deposit L107/284/882 用 `int(Decimal*100)` 替代 yuan_to_cents——Decimal 输入下精确，风格不一致但无精度风险。其余 `*100` 为 pass_rate 百分比/时间戳，非金额。历史双重转换（回调 router /100）已修。

### [C-20260807-010] 维度 3.3 支付/退款回调链路 — 全覆盖
- 证据: 订单回调（order/service.py:417-463 幂等 L431/trade_no 唯一 L436/金额校验 L455/trade_state 消费 L449）；退款回调（refund/router.py:95-164 签名验签 L110/解密/F12 状态消费 L127/CLOSED·ABNORMAL 回退 L133/F76-P2 按 out_refund_no 分发 L148）；押金回调（签名 L132-136/解密/金额校验/F74 幂等/F55 按单号分发）；罚款回调（deposit/service.py:909-932 幂等 STATUS_PAID 返回 True P2-4、_settle_fine_payment 带 child 行锁）。无遗漏回调入口。

### [C-20260807-011] 维度 3.4 退款公式 — 预览/执行两处实现口径一致
- 证据: order/service.py:704-752 calculate_refund 与 refund/service.py:449-472 _calculate 逐行同构（同 total_days 分派、同 used=max(0,min- free_days) 钳制、同 ROUND_HALF_UP、同 max(0) 下限、else 全额退）；执行侧 F37 原额 + F2 元分 + F38 单号复用。边界矩阵 [0 天→全额退; 8 天(超 free 1)→按日扣; 用满→退 free 天数比例; 超期→钳 total_days; free>total→全额退] 逐场景手算一致。0 元边界见 F-009。

### [F-20260807-010] 16 个返回结构化数据的端点缺 response_model（OpenAPI 契约缺口）
- 级别: P3（观察项；无功能性缺陷）
- 维度: 4.3 response_model 漏网排查
- 文件: activity/router.py（enroll/checkin/enrollments/cancel/sign-in）、deposit/router.py（status/pay-fines）、order/router.py:261（pay-params）、refund/router.py:49（GET /）、reservation/router.py（cancel/waitlist join/cancel）、message/router.py（read/read-all）、vocabulary/router.py（master）
- 证据: rg 多行匹配 132 个路由装饰器中 16 个无 response_model 且返回结构化 dict（其余 16 个为回调固定字符串/二进制 png·pdf·html/DELETE 简单响应，无需契约）。逐端点确认返回均为 service 构造 dict，无 ORM 对象直接序列化、无内部字段泄露。
- 影响: OpenAPI 文档缺 16 个端点的响应契约；FastAPI 不强制，客户端未依赖 schema 生成类型则无运行期影响。
- 建议: 对返回结构稳定的端点补 response_model；回调/文件端点保持现状。
- 排重: D1-D8 未覆盖 response_model 覆盖度专项；verify_api_contract.py 仅核对路径不核对 schema。

### [C-20260807-012] 维度 4.1 API 契约脚本 — 路径级 clean，脚本有盲区
- 证据: `venv/bin/python scripts/verify_api_contract.py` 输出 "OK: API contract verified"。读脚本确认仅核对前端 URL 路径 ↔ 后端路由（单方向），不覆盖字段名（字段级见 4.2）。

### [C-20260807-013] 维度 4.2 api.js 字段名 vs 后端 Schema — 抽查 12 个高频 payload 函数零 mismatch
- 证据: reading（current_page/total_pages/pages_read/words_read/reading_minutes schemas.py:31-83）、refund（order_id/used_days/reason schemas.py:13-15）、message（msg_type router.py:16）、transfer（source_child_id/target_child_id child/schemas.py:77-78）、quiz submit（list[SubmitAnswerRequest]）、refund-preview（used_days Query）、order create（child_id/type）、checkin（year/month）、leaderboard（period/level_id/limit）全部与 api.js 参数名一致。api.js 其余函数为路径参数或无 body。

### [C-20260807-014] 维度 4.3 response_model 漏网排查 — 无 ORM/敏感泄露，16 端点缺契约（F-010）
- 证据: 132 个路由装饰器；rg 多行匹配修正跨行误报后 16 个无 response_model 且返回结构化 dict；逐端点抽查（send-sms 固定 message、qr-code 二进制 png、wx-login/phone-login 实际有 schema、report html/pdf 文件响应）确认无泄露。

### [F-20260807-011] reader/vocabulary 页查词发音局部音频实例无 destroy（累积泄漏模式）
- 级别: P3（观察项；内存累积，非崩溃）
- 维度: 5.2 生命周期清理
- 文件: frontend/pages/reading-pkg/reader/reader.js:424-430（onPlayLookupAudio）、frontend/pages/reading-pkg/vocabulary/vocabulary.js:166-172（onPlayAudio）
- 事实: 两处均为 `const audio = wx.createInnerAudioContext()` 局部变量 + `audio.play()`，无实例引用，无法在 onUnload 调 destroy。
- 证据: reader.js:427-429 `const audio = wx.createInnerAudioContext(); audio.src = audioUrl; audio.play()`；vocabulary.js:169-171 同构。对比 word-detail.js:102/113 正确模式（`this._audio` 引用 + onUnload stop+destroy）。
- 影响: 每次点击发音创建一个音频实例，离开页面时实例不销毁，长时间使用累积内存；阅读器/生词表为高频点击场景。
- 建议: 两处改为 `this._audio` 引用，onUnload/onHide 统一 `stop() + destroy()`；或播放结束 `onEnded` 后 destroy。
- 排重: 维度 5.2 全量定时器（13 处 11 处有清理）与音频（word-detail/quiz 有 destroy）核对后，此两处为唯一无销毁音频点。

### [C-20260807-015] 维度 5.1 data 初始化 vs WXML 绑定 — clean
- 证据: 37 处 `this.data.a.b` 链式访问逐处核对：数组操作（.find/.map/.concat）均有 `[]` 初始化（activities/messages/learning/mastered/achievements/leaderboardData/faqs）；对象属性访问有守卫（profile-card `child && child.id`）或空对象初始化（reader `book: {}`、vocabulary `flashcard: {}`）。两处 null 访问疑点（deposit `depositInfo.fine`、shelf `child.id`）经核实有 UI 层可达性隔离（deposit.wxml L20 `wx:if="{{depositInfo}}"` 包裹全部按钮；shelf child 为 null 时 loadTabData L48 return → 列表空 → 无操作入口）。WXML 深层绑定对 undefined 渲染为空不报错。

### [C-20260807-016] 维度 5.3 网络失败兜底 — clean
- 证据: 28 处 loading:true ↔ 28 处 loading:false 数量平衡；11 处脚本候选卡死路径全部人工核实有兜底（reservation/borrow-history/order-history 用 finally、official/deposit/word-detail/activity-detail 在 catch 内 setData、deposit L105 用 wx.hideLoading、observation loadChild 无 loading 状态设计）。两版正则脚本候选均为解析器截断误报。

### [C-20260807-017] 维度 5.4 iOS 虚拟支付规则 — clean
- 证据: 全库仅 5 处 wx.requestPayment：observation.js:91（L72 isIOS 拦截→showIOSContactGuide）、official.js:129（L110 拦截）、deposit.js:129/168/208（押金/补缴/罚款，P1-1 实物担保金豁免注释明确）。activity 报名无价格/支付逻辑。无订单列表重付绕过路径（全库无第 6 个 requestPayment）。

### [F-20260807-012] RBAC 种子 11 个权限点已授权但后端无 require_perm 端点接线
- 级别: P3（观察项；无权限绕过/功能不可用，属种子超集漂移）
- 维度: 6.4 权限码 vs 种子
- 文件: backend/seeds/seed_rbac.py:24-260（PERMISSIONS + 角色授权）
- 事实: borrow.overdue / evaluation.create|list|view / order.export / order.mark_paid / quiz.view / report.view / submission.reject|view / user.delete 共 11 个权限码在种子定义且已写入角色授权清单，但全库无任何 `require_perm("该码")` 端点、无 data-perm 引用。
- 证据: python 脚本提取 backend/domain 全部 require_perm 共 124 个唯一码 vs 种子 136 → 差集 11 个；反向（后端用但种子无）为 0。前端 48 个 data-perm 全部命中种子。抽查 order.mark_paid 仅 2 处引用（种子定义 seed_rbac.py:46 + 角色授权 :239），无端点。
- 影响: 管理端角色勾选这些权限不产生实际效果（功能未接线）；不构成安全缺口。可能是已移除功能遗留或未来功能预留。
- 建议: 核对 11 个权限点对应的管理端功能是否存在但未用 require_perm 保护（若是则需补接线）；若无对应功能，从种子清除或标注预留。
- 排重: D1-D8 未做权限码×种子双向差集专项。

### [C-20260807-018] 维度 6.1 inline script 全局泄漏 — clean
- 证据: base.html 纯 src 引入（admin.js/admin-pages.js/base-init.js），macros.html 无 script，模板无 inline script。三公共 JS 顶层声明 81 个 vs 页面 JS 顶层声明比对：仅 profile.js/quiz.js 重定义 `formatDateTime`，为有意覆盖（零填充 YYYY-MM-DD HH:mm vs admin.js toLocaleString），页面自洽使用（profile 4 处/quiz 2 处），无泄漏污染。

### [C-20260807-019] 维度 6.2 innerHTML 注入 — clean
- 证据: 204 处 innerHTML 赋值点；用户可控字符串字段（child_name/name/username/phone/teacher_comment/summary 等）拼接点全部经 escapeHtml（admin.js:285-289 textContent 法，转义 <>&" 完整）。未 escape 的 4 处（content.js page_number×2、message_manage.js msg_type/priority）均为后端 int 枚举字段，无注入面。

### [C-20260807-020] 维度 6.3 data-action wiring — clean
- 证据: `venv/bin/python scripts/verify_action_wiring.py` 输出全绿——36 模板 data-action 全部匹配对应 JS handler，无孤儿 handler，data-pg 模式检查通过。

### [C-20260807-021] 维度 6.4 权限码 vs 种子 — 双向零漏配
- 证据: 后端 require_perm 唯一码 124 个全命中种子 136（差集 0）；前端 data-perm 48 个全命中种子（差集 0）；仅 11 个种子超集未接线（见 F-012）。

### [F-20260807-013] reset_stale_pending_deposits 任务无 @distributed_lock（24 任务中唯一）
- 级别: P3（观察项；service 层幂等，多实例冗余执行无害）
- 维度: 7.1 任务失败路径/锁/幂等
- 文件: backend/tasks/scheduler.py:2163-2185 + backend/domain/deposit/service.py:969-995
- 事实: scheduler 24 个任务中 23 个有 @distributed_lock（Redis SET NX EX），仅 reset_stale_pending_deposits 无锁（L2163 无装饰器）。
- 证据: `rg -n "distributed_lock" backend/tasks/scheduler.py` 命中 23 处装饰器；reset_stale_pending_deposits 定义处无。service 层幂等：查询条件 `status == PENDING` + 超时过滤（service.py:969-976），双实例并发时第二个查询不到已处理记录，重复赋值 UNPAID=UNPAID 无副作用。
- 影响: 多实例部署时该任务可能重复执行（无害）；单实例无影响。与其他任务防御一致性缺口。
- 建议: 补 `@distributed_lock("job:reset_stale_pending_deposits", timeout=300)`。
- 排重: F39/F78 覆盖该任务复位逻辑，未涉锁覆盖；D1-D8 未做任务级锁覆盖度专项。

### [F-20260807-014] purge_expired_data 全量过期数据一次性删除，无分批上限
- 级别: P3（观察项；数据规模未达阈值时低风险）
- 维度: 7.3 大表风险
- 文件: backend/tasks/scheduler.py:1855-2054（purge_expired_data）
- 事实: 消息/行为/财务/语音四类过期数据清理均先 `.all()` 收集 id 列表再批量 `.delete(synchronize_session=False)`，无 chunk/batch/LIMIT 分批。
- 证据: L1898-1902 `old_msg_ids = [r[0] for r in db.query(SystemMessage.id).filter(...).all()]` → L1904-1918 `.delete()`；exited_ids（L1941-1947）同理。单条 DELETE 无行数上限。
- 影响: 数据积累量大时（消息 1 年/行为 2 年/财务 5 年），一次性收集+删除导致长事务、大内存、主从延迟放大。每日 4:30 低峰执行缓解。
- 建议: 按 id 分批（如每批 1000）+ 每批 commit；或流式处理。
- 排重: H5 覆盖保留期口径（已结案），未涉分批执行策略。

### [F-20260807-015] check_observation_reminders 日期列函数包裹致索引失效
- 级别: P3（观察项；观察期用户量小，实际负载低）
- 维度: 7.3 大表风险
- 文件: backend/tasks/scheduler.py:1751-1762
- 事实: `sql_func.date(Child.member_expire_time) == target_date` 对列做函数转换，member_expire_time 索引无法使用。
- 证据: L1758 `sql_func.date(Child.member_expire_time) == target_date`；remind_days 7 次循环（L1751），每天执行 7 次全表扫描（带 status/软删过滤）。
- 影响: Child 表大时查询退化；观察期用户量小（小馆几十~几百），每日 7 次低负载查询可接受。
- 建议: 改为 `member_expire_time >= target_date AND member_expire_time < target_date + 1天` 区间查询以利用索引。
- 排重: 维度 7.3 全任务大表扫描核对中唯一函数包裹列点；generate_monthly_reports 的 limit(10) 为 TOP 排行非分批。

### [C-20260807-022] 维度 7.1 任务失败路径/锁/幂等 — 23/24 有锁，全 try/except
- 证据: 24 个业务任务全部 `try/except Exception → rollback → logger.exception`，无裸 except 无 re-raise；23 个 @distributed_lock（Redis SET NX EX + REDIS_LOCK_FAIL_OPEN 配置，审查 P1-3 已结案）；状态条件更新保证幂等（close_expired_orders L918 F5 前置、reset 状态过滤）。唯一无锁见 F-013。

### [C-20260807-023] 维度 7.2 时区 + 7.4 错峰 — clean
- 证据: 7.2 全部任务用 datetime.now()/date.today()（应用侧 Asia/Shanghai），跨月任务 generate_monthly_reports（day=1）用 last_month_start/end 应用侧边界（L760-765），无 utcnow 混用。7.4 9:00 撞车任务 check_member_expiry（单次范围查询，L535-570）+ check_observation_reminders（7 次精确日期查询，带 status 过滤），均轻量小结果集，不构成集中负载问题。

### [F-20260807-016] refund 执行/回退路径对软删订单查询无 is_deleted 过滤（同文件不一致）
- 级别: P3（观察项；需软删订单+已审核退款同时存在才触发）
- 维度: 8.3 软删一致性
- 文件: backend/domain/refund/service.py:245 / :305 vs :342 / :409
- 事实: `_execute_wechat_refund`（L245）与 `_rollback_refund_failure`（L305）查订单用 `db.query(Order).filter(Order.order_no == order_no).first()` 无 is_deleted 过滤；同文件 `mark_refunded`（L342）与另一处（L409）均带 `Order.is_deleted == 0` + with_for_update。
- 证据: L245 `order = db.query(Order).filter(Order.order_no == order_no).first()`；L305 同构；L342/L409 含 `Order.is_deleted == 0`。四种方法同文件两套风格。
- 影响: 订单被软删后，已审核通过的退款任务仍会对其发起微信退款/失败回退改写状态。需软删与退款审核重叠的极端时序；软删订单退款钱仍可正确退回（资金面无损失），但操作软删数据不符合统一软删语义。
- 建议: L245/L305 补 `Order.is_deleted == 0`，与同文件其他查询对齐。
- 排重: 维度 8.3 全库 122 处直接 db.query 候选核查（批量 in_ 映射/seeds/scheduler 物理删除均合理），唯一同文件不一致点。

### [C-20260807-024] 维度 8.1 慢查询基线 — 0 条（用户端 2 端点未覆盖）
- 证据: `PYTHONPATH=. venv/bin/python scripts/slow_query_baseline.py --threshold 0.3` 输出"慢查询 0 条"并写入 专家意见/slow_query_baseline.md。管理端 8 端点全 200（耗时 0.006-0.048s）；用户端 /child/ /user/info 因测试 token 401 未真正执行查询（覆盖缺口如实记录）。

### [C-20260807-025] 维度 8.2 索引覆盖 — clean
- 证据: 全库 Index()/index=True 共 61 处；高频查询列全部有索引：child_id（10+ 表）、order_no（unique+index）、trade_no（uq_index）、pay_order_no（unique+index）、due_date（borrow L46）、user_id（message 两表跨行定义均有 index=True）、active_child_id（uq_deposit_active_child）、isbn/title/ar_value。无索引的 status/pay_status/refund_status 均为低基数枚举列（MySQL 低选择性索引无收益，小馆数据量全扫可接受）。

### [C-20260807-026] 维度 8.4 迁移与模型漂移 — clean
- 证据: `venv/bin/alembic check` 输出 "No new upgrade operations detected."；`PYTHONPATH=. venv/bin/python scripts/check_model_consistency.py` 输出 "OK: 56 tables created on SQLite / Model consistency check: PASSED"。

### [F-20260807-017] 配置变更审计缺口：ConfigAuditLog 生产零写入，旧值不落库
- 级别: P3（观察项；审计完整性缺失，无资金/功能故障）
- 维度: 9.3 TTL 缓存与审计
- 文件: backend/domain/admin/services/system_service.py:28-38（生产写入路径）vs backend/common/config_service.py:115-159（唯一写 ConfigAuditLog 的路径）
- 事实: 生产配置写入全走 `AdminSystemService.set_config`（router→service，L28-38），只写 OperationLog（content=f"更新配置: {key}={value}（级别:{level}）" 含新值、无旧值），**不写 ConfigAuditLog**。唯一写 ConfigAuditLog（old_value/new_value/changed_by）的 `ConfigService.set_config` 生产无调用方（仅 5 个测试引用：test_f15_f16_f23.py:85 / test_enroll_dual_track.py:84 / test_damage_flow.py:130 / test_review_sla_config.py:84,95 / test_upgrade_deduct_transfer.py:87）。
- 证据: `rg "ConfigAuditLog" backend -g "*.py" | rg -v "config_audit_model.py|config_service.py|test_"` 输出空（无生产读写方）；config_audit_log 表在 metadata 存在（Base.metadata 实查 True）；router L165 确认 OperationLog content 无旧值。
- 影响: 配置变更的旧值审计不可追溯（OperationLog 仅存新值）。ConfigAuditLog 表为死表（设计存在、生产永不写入）。管理员误改配置后无法查证原值。
- 建议: 生产 set_config 路径补写 ConfigAuditLog（或 OperationLog content 追加 old_value）。
- 排重: 维度 9 首轮核查；无历史项。

### [F-20260807-018] DEFAULTS 类型元数据与实际消费类型漂移（4 键）
- 级别: P3（观察项；运行无故障，文档/元数据失真）
- 维度: 9.2 fallback vs DEFAULTS
- 文件: backend/domain/admin/models.py DEFAULTS + 消费端
- 事实: 脚本比对 23 个调用点默认值与 DEFAULTS 类型，7 处候选剔除 3 处误报（service_wechat 为 get_str vs string 标签写法差异、overdue_fine_cap_ratio 值一致、member_grace_days 值一致）后实漂移 4 键：
  1. `due_remind_days`: DEFAULTS 声明 `('5,3,1,0', 'string')`，消费端 `ConfigService.get_int_list`（tasks/scheduler.py:1146,1747）
  2. `overdue_fine_per_day`: DEFAULTS 声明 `('1', 'int')`，消费端 `get_decimal`（common/fine_policy.py:31 + config_service.py 示例注释）
  3. `overdue_fine_cap_ratio`: DEFAULTS 声明 `('0.5', 'string')`，消费端 `get_decimal`（fine_policy.py:32）
  4. `quiz_pass_rate`: DEFAULTS 声明 `('0.80', 'string')`，消费端 `get_decimal`（scheduler.py:391 + config_service.py:14 示例注释）
- 证据: 上表出自 PYTHONPATH 脚本打印 DEFAULTS 声明 vs `rg "ConfigService.get_"` 消费类型；gen_config_doc --check 通过（文档与 DEFAULTS 一致，但文档类型列沿用错误元数据）。
- 影响: ① gen_config_doc 生成的《动态配置清单.md》类型列失真（string/int 标错）；② admin get_all_configs 返回的 type 字段失真（settings.js 现仅读 value 不读 type，未触发渲染故障——已核实 L231 仅发 value）；③ 类型标注与真实转换语义（get_int_list/get_decimal）脱节，未来按 type 渲染或校验会踩坑。
- 建议: DEFAULTS 四键类型改 align：due_remind_days→'list'、overdue_fine_per_day/cap_ratio/quiz_pass_rate→'decimal'，并同步 gen_config_doc SECTIONS（如适用）。
- 排重: 维度 9 首轮核查；无历史项。

### [F-20260807-019] F12 幂等测试无 RED 守护：移除幂等守卫后测试仍绿
- 级别: P3（观察项；测试质量缺陷，非生产 bug——生产守卫完好）
- 维度: 10.4 RED→GREEN 抽样实证
- 文件: tests/unit/test_f12_refund_callback_guard.py:109-124（TestMarkRefundedIdempotent）vs backend/domain/refund/service.py:365-367
- 事实: 临时将 `refund/service.py:365-367` 幂等守卫（`if refund.status == STATUS_COMPLETED: return`）替换为 `pass` 后，`pytest tests/unit/test_f12_refund_callback_guard.py` **仍 5 passed 未转红**。
- 证据: 移除前 5 passed；移除后 `5 passed, 1 warning in 0.16s`（完整命令输出记录于本轮）；恢复后 5 passed。测试 `test_second_callback_does_not_error` 仅断言 `refund.status == STATUS_COMPLETED`（终态），未断言"不重复置位"的副作用（如 refund_time 未被覆盖、actual_refund_amount 未被重复计算）。
- 根因: 幂等守卫的唯一可观察副作用差异（重复回调不覆盖 refund_time / 不重复核销罚款）未被测试断言。移除守卫后第二次调用会重复执行 `refund.refund_time = datetime.now()` 与罚款核销逻辑，但测试只查终态 status，故不转红。
- 影响: 若未来有人误删幂等守卫，该测试不会拦截回归。对照实验证明同类守卫的反假绿纪律在 F12 回调状态门处有效（router.py:127 状态门 `refund_status != "SUCCESS"` 临时禁用后同一测试文件 **3 failed 转红**），说明本次发现为孤立缺口而非系统性失败。
- 建议: 在 test_second_callback_does_not_error 中补充断言（如记录首次 refund_time，二次调用后断言 refund_time 未变；或断言罚款核销仅执行一次）。
- 排重: 维度 10.4 首轮实证；无历史项。

### [C-20260807-027] 维度 10.1 反假绿脚本 — clean
- 证据: `PYTHONPATH=. venv/bin/python scripts/check_fake_assertions.py` 输出 "OK: all assert True have explanations"。

### [C-20260807-028] 维度 10.2 测试断言业务含义抽样（10 文件）— clean
- 证据: 抽查 test_p0_batch3_refund_fine_deposit / test_f12_refund_callback_guard / test_refund_auto_approve / test_f13_status_change_guard / test_fine_payment_low_quiz / test_state_machines / test_partial_refund_callback / test_vocabulary_service / test_waitlist / test_token_security。4 处 `pass` 全为合法上下文（fixture finally / no-op stub：test_f13:58、test_p0_batch3:139、test_f12:65、test_partial_refund_callback:69 均在 override_get_db finally 或 _NoCloseSession.close）。断言验证业务含义：test_f12 断言 refund.status/order.pay_status/refund_status 四态矩阵（L154/187/218-219）；test_state_machines 断言 overdue_days>0/fine_amount>0（L214-215）；test_token_security 断言 token_generation 递增与禁用联动（L50/99-100）。

### [C-20260807-029] 维度 10.3 behave steps 真实性 — clean（弱断言均有严格兄弟步骤兜底）
- 证据: 20 个 feature + 16 个 steps 文件。Given 造数走 ORM（合理模式：certificate_steps.py:12-31 / borrow_record_steps.py:15-35），When/Then 走真实 TestClient（common_steps.py:102-143 client.post/put）。弱断言步骤 `step_cannot_submit`（common_steps.py:49-53 允许 200/201 通过）与 `step_refund_blocked`（L334-340）在全部实际使用场景（benefit_transfer.feature:39,46 / refund_application.feature:92）均有严格兄弟步骤 `拦截提示"{message}"`（borrow_record_steps.py:596-604，断言 4xx + detail 含消息）兜底——grep 确认该严格步骤为唯一定义。未发现仅靠弱断言而无严格兜底的场景。

## 累计统计
- 发现: 19（P0: 0 / P1: 0 / P2: 5 / P3: 14 观察）
- clean 记录: 32
- 维度 1-10 全部完成 → 下一维度 11 日志与可观测（11.1 敏感信息打印 / 11.2 trace_id 贯穿 / 11.3 异常路径 exc_info）。
